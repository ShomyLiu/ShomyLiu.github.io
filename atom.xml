<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>天空的城</title>
  
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://shomy.top/"/>
  <updated>2018-03-06T02:34:08.863Z</updated>
  <id>http://shomy.top/</id>
  
  <author>
    <name>ShomyLiu</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>2017</title>
    <link href="http://shomy.top/2018/03/01/2017-review/"/>
    <id>http://shomy.top/2018/03/01/2017-review/</id>
    <published>2018-03-01T03:15:09.000Z</published>
    <updated>2018-03-06T02:34:08.863Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>今年的总结因为赶deadline来的稍晚一些，不过应该不会缺席。</p>
</blockquote>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/2017-review/2017-1.jpg">

</div>
<a id="more"></a>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;今年的总结因为赶deadline来的稍晚一些，不过应该不会缺席。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;div class=&quot;figure&quot;&gt;
&lt;img src=&quot;http://7xotye.com1.z0.glb.clouddn.com/blog/2017-review/2017-1.jpg&quot;&gt;

&lt;/div&gt;
    
    </summary>
    
      <category term="个人心得" scheme="http://shomy.top/categories/%E4%B8%AA%E4%BA%BA%E5%BF%83%E5%BE%97/"/>
    
    
      <category term="2017" scheme="http://shomy.top/tags/2017/"/>
    
  </entry>
  
  <entry>
    <title>关系抽取(分类)总结</title>
    <link href="http://shomy.top/2018/02/28/relation-extraction/"/>
    <id>http://shomy.top/2018/02/28/relation-extraction/</id>
    <published>2018-02-28T03:58:30.000Z</published>
    <updated>2018-03-06T02:33:44.867Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>对近几年(到2017)一些关系抽取/分类(Relation Extraction)的部分文献的一个简单总结。</p>
</blockquote>
<a id="more"></a>
<h2 id="基本介绍">基本介绍</h2>
<p><strong>基本定义</strong></p>
<ul>
<li>关系抽取: 从一个句子中判断两个entity是否有关系，一般是一个二分类问题，指定某种关系</li>
<li>关系分类: 一般是判断一个句子中 两个entity是哪种关系，属于多分类问题。</li>
</ul>
<p><strong>常用数据集</strong></p>
<ul>
<li>ACE 2005: 599 docs. 7 types;</li>
<li>SemiEval 2010 Task8 Dataset:
<ul>
<li>19 types</li>
<li>train data: 8000</li>
<li>test data: 2717</li>
</ul></li>
<li>NYT+FreeBase 通过Distant Supervised method 提取，里面会有噪音数据:
<ul>
<li>53 types</li>
<li>train data: 522611 sentences; 需要注意的是，这里面有近80%的句子的标签为NA</li>
<li>test data: 172448 sentences;</li>
</ul></li>
</ul>
<p>下面以学习方法的不同来对这些文章进行分类：</p>
<ul>
<li>Fully Supervised Learning</li>
<li>Distant Supervised Learning</li>
<li>Joint Learning with entity and relation</li>
<li>Tree Based Methods</li>
</ul>
<p>其中Fully Supervised 一般评测使用label完全准确的SemEval 2010 Task 8 数据集。 Distant Supervised 使用NYT+FreeBase数据集。 SemEval 2010 Task 8 训练数据样例: <figure class="highlight dts"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"> The <span class="params">&lt;e1&gt;</span>microphone<span class="params">&lt;/e1&gt;</span> converts sound into an electrical <span class="params">&lt;e2&gt;</span>signal<span class="params">&lt;/e2&gt;</span>.</div><div class="line">Cause-Effect(e1,e2)</div><div class="line"><span class="symbol">Comment:</span></div></pre></td></tr></table></figure></p>
<p>其中第一行为sentence，第二行为两个entity的relation，第三行为备注。 NYT + FreeBase 训练数据集: <figure class="highlight clean"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">m<span class="number">.0</span>ccvx    m<span class="number">.05</span>gf08    queens    belle_harbor    /location/location/contains    .....officials yesterday to reopen their investigation into the fatal crash <span class="keyword">of</span> a passenger jet <span class="keyword">in</span> belle_harbor , queens...... ###END###</div></pre></td></tr></table></figure></p>
<p>一共6列，前两列为两个entity的Freebase mid, 第三四列为两个entity在句子中的string。第五列为relation，最后一列为sentence（有省略），以###END###结尾</p>
<p>这两个数据集相对来说用的最广泛。 此外关于Joint Learning with entity and relation和Linguistics(NER, POS Tagging, Dependency Tree) Methods 部分暂时还没有关注，仅仅列在这里。</p>
<h2 id="fully-supervised-learning">Fully Supervised Learning</h2>
<h3 id="相关文献">相关文献</h3>
<p>这一部分基本是在SemEval 2010 Task 8 数据集上做关系分类的工作，是一个完全监督的任务。</p>
<p><strong>1. Simple CNN Model (Liu 2013)</strong></p>
<blockquote>
<p>Liu,(2013). Convolution neural network for relation extraction. 8347 LNAI(PART 2), 231–242.</p>
</blockquote>
<p><strong>基本介绍</strong> 这篇文章应该是第一次使用CNN来做关系分类任务的，使用的CNN结构也十分简单，甚至没有Pooling层，输入层也没有用word embedding, 而是对synonym的embedding表示作为输入。 具体的输入层的结构如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/1.jpg">

</div>
<p>具体步骤如下:</p>
<ul>
<li>首先构建synonym list，使用Wordnet数据集，来找那些词属于同义词，相当于利用同义词关系，聚成若干类，当然有可能会有某个词没有同义词，那他自己就是一个类别</li>
<li><p>假设Synonym list 一共有<span class="math inline">\(D_s\)</span>个类别，每个词都属于其中一类，这样就可以来对词做one-hot表示了, 如上图所示: <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/2.png"></p></li>
<li><p>使用一个Look-Up Table 转换为一个低维vector</p></li>
</ul>
<p>输入层之后是卷积网络+一个普通的全连接层以及softmax分类，如下: <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/3.jpg"></p>
<p><strong>实验</strong></p>
<p>数据集： ACE 2005数据集 这里需要说明的是， 这篇文章除了使用前面说的Synonym的特征之外，还用了该数据集提供的entity type/subtype 以及后续做了POS tagger 的特征，都是先使用one-hot表示，然后用LookUP Table 再转换为低维向量，然后所有的feature vector concat成最终的向量，作为CNN层的输入。 在大类Type上的实验结果，相对于几个Basedline 提升还是比较明显的：</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/4.png">

</div>
<p><strong>总结</strong></p>
<p>优点:</p>
<ul>
<li>引入CNN 结构来做关系抽取，虽然本质上属于文本分类，但是属于一次尝试，效果还可以。</li>
<li>使用Synonym Embedding作为词的特征，这一点后续一部分工作也在使用，相当于引入额外的信息。</li>
</ul>
<p>缺点:</p>
<ul>
<li>CNN的结构比较简单, 没有Pooling层，可能受噪音比较明显，因为一个句子里面影响两个词的relation的可能只有几个词，一些可能会产生噪音。</li>
<li>仍然使用了一些Linguistic Feature比如POS Tagger, NER 等，并没有完全做到end-to-end的关系抽取</li>
<li>使用Synonym Embedding(随机初始化的LookUp Table) 可以引入一部分额外信息，但是却完全忽略了word embedding的语义信息，这一块在后续工作中都会加入pre-train的word embedding.</li>
</ul>
<p><strong>2. CNN with Max Pooling and word embedding(Zeng 2014)</strong></p>
<blockquote>
<p>Zeng, . (2014). Relation Classification via Convolutional Deep Neural Network. Coling, 2335–2344</p>
</blockquote>
<p>这篇是中科院刘康老师的一篇文章，这篇文章使用了比较经典的CNN的结构，包含了Pooling层，以及设计了Position Features，后面会具体介绍。 先看整体架构:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/5.jpg">

</div>
<ul>
<li>第一步同样是Embedding Layer, 这篇文章用了Pre-Train的词向量，而不是像上一篇的随机初始化，不过用的并不是word2vec，而是(Turian 2010ACL Word representations: a simple and general method for semi-supervised learning.)提供的word embedding.</li>
<li>之后文中设计了两种Features: Lexical Level Feature 就是词法级别，以及sentence-level(CNN) 级别的特征，后面详细介绍。</li>
<li>将两种Feature 直接 concat 输入到一个全连接层+Softmax 来对关系做分类</li>
</ul>
<p>先介绍相对简单的Lexical-Feature，一共有5部分:</p>
<ul>
<li>L1: entity1</li>
<li>L2: entity2</li>
<li>L3: entity1的左右两个tokens</li>
<li>L4: entity2的左右两个tokens</li>
<li>L5: WordNet中两个entity的上位词</li>
</ul>
<p>举个例子，比如说:</p>
<p>The [haft] of the [axe] is made of yew wood.</p>
<ul>
<li>L1: entity1: <em>haft</em></li>
<li>L2: entity2: <em>axe</em></li>
<li>L3: entity1’s context: <em>the</em>, <em>of</em></li>
<li>L4: entity2’s context: <em>the</em>, <em>is</em></li>
<li>L5: 在wordNet中找到两个entity的上位词。上位词就是包含关系，比如<em>parrot</em>这个词的上位词就是<em>bird</em>. 这种包含关系其实类似于指明了实体的类型特征。</li>
</ul>
<p>这5个特征都是lexical-level的，之后在使用它们的word embedding串联起来 作为最后的lexical-feature.</p>
<p>下面介绍使用CNN的sentence-level feature, 跟其他文章中使用CNN对句子建模的结构很类似, 先直接放框架图:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/6.jpg">

</div>
<p>在输入层每个word 有两种特征:</p>
<ul>
<li>WF(word feature): 很简单就是词向量 word embedding</li>
<li>PF(position feature): 位置特征， 每个word与两个entity的相对距离，举个例子如下 <been> 这个单词距离entity1 <people>为2，距离entity2 <downtown>为-4: <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/7.jpg"> 这样每个word 就有了两个位置特征[d1,d2]，这两个特征也需要再额外使用Lookup-Table 做Embedding，投影成低维向量，也就是每个相对距离，都对应到一个低维实数向量。 这个Position Feature 的引入其实是突出了两个entity的作用，实验证明PF对效果的提升很明显。</downtown></people></been></li>
</ul>
<p>这样每个word的feature维度为: <span class="math inline">\(d_w + d_p * 2\)</span>，其中<span class="math inline">\(d_w\)</span> 为word embedding的维度，<span class="math inline">\(d_p\)</span>为 position embedding的维度。</p>
<p>介绍完输入之后，接下来就是卷积操作，这里不多介绍。与上一篇不一样的地方是，这里在卷积之后使用了Max Pooling操作，这样可以提取每一个卷积核的最有用的特征，在之后又接了一个全连接层最终得到了sentence-level feature。有一点需要说明的是，MAXPooling直接对卷积操作之后的向量进行的，并没有经过激活函数。</p>
<p>最终将上面的lexical-feature 与 sentence-level feature 直接串起来，作为整个句子的特征去做分类，如结构图中所示，加一个全连接层和softmax 即可。</p>
<p>模型需要学习参数有: - word embedding - position embedding - 卷积核权重矩阵 - sentence-level 最后的全连接层参数矩阵 - 用于softmax的全连接层参数矩阵</p>
<p><strong>实验</strong></p>
<p>数据集: 使用SemEval 2010 Task 8 Dataset，考虑方向共19种关系。 整体的实验结果如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/8.png">

</div>
<p>从结果看，没有使用linguistic的一些特征，基本end-to-end, 取得了state-of-art的效果。 再看看单独特征的影响:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/9.png">

</div>
<p>可以看出来，L5(word net)和PF两个特征比较关键。</p>
<p><strong>总结</strong></p>
<p>优点:</p>
<ul>
<li>使用Pooling 操作，提取了每一个卷积核对应的最优特征</li>
<li>引入Position Feature: 相当于在一定程度上引入了位置信息，因为CNN更多是局部N-Gram特征; PF突出了entity的影响，包括后续的PieceWise Max Pooling，同样也是对entity与其他词分开考虑。</li>
</ul>
<p>缺点:</p>
<ul>
<li>虽然使用了多个卷积核，但是仅仅使用同一个window-size, 提取特征比较单一。</li>
<li>结构还是相对简单，更多的是类似于CNN 文本分类的结构。 而且还是加入了人为构造的Lexical Feature。</li>
</ul>
<p>有一点疑问的是 Lexical中的两个entity 的embedding 其实也属于对entity的强调，作用跟Position Feature 感觉有点类似，都是为了突出是对句子中的那两个词做关系分类，或者说都是为了强调距离target entity 越近，影响越大。但是PF的效果要比L1-L4好。</p>
<p><strong>3. CNN with multi-sized window kernels(Nguyen 2015)</strong></p>
<blockquote>
<p>Nguyen, (2015). Relation Extraction: Perspective from Convolutional Neural Networks.</p>
</blockquote>
<p>这一篇文章，在去年zeng的基础上加入了多尺寸卷积核，这样可以提取更多的N-Gram特征；同时完全不再使用zeng的lexical-level feature，完全使用sentence的特征，完整的结构如下: <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/10.png"></p>
<p>从结构可以看出来，基本与上一篇zeng的sentence-level 部分的CNN很类似，不同的地方有： - 卷积阶段使用<strong>多个尺寸的卷积核</strong> - 卷积之后使用了激活函数</p>
<p><strong>实验</strong></p>
<p>数据集：文中使用了ACE 2005 和 SemEval2010 Task 8 两个数据集。 对ACE 2005数据集，做了窗口尺寸不同以及word embedding是否固定的实验分析，结果如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/11.png">

</div>
<p>从实验结果F1-Score值可以看出来，多窗口尺寸的确有效果提升，而且最好的效果则是在word embedding 不固定，需要参与训练的情况下，卷积核窗口为2,3,4,5。</p>
<p>在SemEval2010 数据集的结果达到了state-of-art，比zeng的效果好0.1% 但是是不加任何的lexical-feature的情况下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/12.png">

</div>
<p><strong>总结 </strong></p>
<p>这篇文章改进相对较少， 不过完全不再使用lexical-feature，起到了不错的效果。 优点:</p>
<ul>
<li>使用多个卷积核，提取文本中更多的卷积特征</li>
<li>完全不再使用词法特征，包括wordnet, 词的上下文等。</li>
</ul>
<p>缺点:</p>
<ul>
<li>仅仅使用CNN的传统结构，因此效果提升不是特别明显</li>
</ul>
<p><strong>4. CNN with Rank Loss(Santos 2015)</strong></p>
<blockquote>
<p>Santos(2015). Classifying Relations by Ranking with Convolutional Neural Networks. ACL</p>
</blockquote>
<p>这篇文章同样是在Zeng 2014的CNN基础上做的改进，最大的变化是损失函数，不再使用softmax+cross-entropy的方式，而是margin based的ranking-loss。下面介绍一下细节。 先看整体架构，跟之前的区别很小:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/13.jpg">

</div>
<ul>
<li>输入层: 利用word embedding + position embedding,同zeng 2014</li>
<li>卷积层: 固定尺寸的卷积核(window-size=3)</li>
<li>Pooling层: 直接Max Pooling得到 <span class="math inline">\(r_x\)</span></li>
<li>全连接层: 得到每个类别的score，其中<span class="math inline">\(W^{classes}\)</span>的每一列可以看成 label 的embedding。因此某个label为c的score即为: <span class="math inline">\(s_{\theta}(x)_c=r_x^{T}[W]_c\)</span></li>
</ul>
<p>图中pooling之后得到的<span class="math inline">\(r_x\)</span> 即作为sentence representation。这一部分跟之前的工作都一样，下面开始引入了margin-based ranking loss, 先看loss function:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/14.png">

</div>
<p>在训练过程中的输入有三部分: 当前的句子 x , 该sen的正确标签<span class="math inline">\(y^{+}\)</span>, 以及采样得到的错误的标签<span class="math inline">\(c^{-}\)</span>。<span class="math inline">\(m^{+}, m^{-}\)</span> 对应两个大于0的margin, <span class="math inline">\(\gamma\)</span>起到了一个缩放的作用。 这样最小化<span class="math inline">\(L\)</span>就要求正样本对应的label得分<span class="math inline">\(s_{\theta}(x)_{ {y}_{+} }\)</span>越大越好，负样本对应的得分<span class="math inline">\(s_{\theta}(x)_{ {c}_{-} }\)</span>越小越好。同时margin的存在使得模型尽可能让正样本的得分大于<span class="math inline">\(m^{+}\)</span> ， 以及负样本的得分小于<span class="math inline">\(-m^{-}\)</span>。这样可以讲正负样本分的更加清楚。</p>
<p>几个Tips:</p>
<ul>
<li>负样本的选择上， 并不是随机选择一个负标签，而是选择score最大的那个负标签，这样可以更好地将比较类似的两种label分开。</li>
<li>关于NA label的特殊处理，NA表示两个entity没有任何关系，属于噪音数据，因此如果将这个噪音类别与其他有意义的类别同等看待的话，会影响模型的性能. 因此文中对NA类做了特殊处理。在train的时候，不再考虑NA这一类别， 对于NA的训练数据，直接让(1)式的第一部分为0即可。 在predict的部分， 如果其他类别的score都是负数，那么就分类为NA。实验证明这个效果对整体的performance有提升。</li>
</ul>
<p><strong>实验</strong></p>
<p>数据集: SemEval2010 Task 8 首先看主要实验结果, 再不使用任何lexical-feature的情况下，达到了start-of-art的效果, F1-Score: 84.1%:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/15.png">

</div>
<p>再看三组对比实验: - 损失函数: Ranking Loss 以及 Softmax-Entropy. <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/16.png"> 上面的CNN+Softmax 是文中复现的Zeng 2014的代码，比原文中要好，可能原因是本文使用了300维的word embedding， 而Zeng使用50维. 从前两行可以看出来，使用Ranking的损失函数，Precision, Recall, F1 都有提升。 - 对NA关系的特殊处理的效果: <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/17.png"></p>
<pre><code>从结果中可以看出来，训练过程中不考虑NA的话，整体的performance有提升。 但是对于NA这一个单独的类的Precision有所下降，也就是说有更多的其他的类分到了NA，导致P降低，不过反过来看其他类的P,R,F1的值都提高了，说明增加的这部分分到NA类的那些case即使在考虑NA的情况下，也是Bad Case.</code></pre>
<ul>
<li><p>使用两个entity之间的word embedding 来代替position feature: <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/18.png"></p>
<p>其中Full sentence 表示 使用完整的句子， Word Position表示使用Zeng 2014提出的Positon Embedding。从第一列和第四列的对比可以看出来，在使用Full Sentence的情况下，Postion 的作用很明显，可以差出10%。不过从第二列，第三列对比可以看出来，如果仅仅使用两个entity之间的words，position的影响就会小很多，因为这种情况下，就可以一定程度上指代两个entity的位置。有一个比较有趣的现象，第三列仅仅使用两个entity之间的word, 就可以达到82.8%的F1，已经可以跟Zeng2014 中，使用各种lexical-feature+position feature达到相同的效果。可能的原因是长文本的噪音比较大。</p></li>
</ul>
<p><strong>总结</strong></p>
<p>这篇文章相对Zeng 2014的文章，改进的地方或者一些尝试很值得思考。而且效果很不错，很多地方可以借鉴，尤其ranking loss这一块。</p>
<p>优点:</p>
<ul>
<li>使用Ranking loss，效果提升2%多，而且在没有使用lexical-feature以及单窗口尺寸的情况下。 有提升的原因可能是使用ranking loss可以更容易区分开一些易于分错的类别，而softmax却没有这样的功能，只可以增强正确类别的概率。</li>
<li>使用仅仅两个entity之间的words 可以在一定程度来替代position的作用，而且实现更简单。</li>
</ul>
<p>缺点:</p>
<ul>
<li>同样仍然在简单CNN的基础上做的修改. 后续相关文章加了各种attention.</li>
</ul>
<p><strong>5. RNN (Zhang 2015)</strong></p>
<blockquote>
<p>Zhang (2015). Relation classification via recurrent neural network.</p>
</blockquote>
<p>这篇文章不再使用CNN作为基本结构，而是开始尝试RNN，可以达到Zeng 2014 的效果，基本结构如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/rnn-1.jpg">

</div>
<p>大部分结构相同，有以下几点改进:</p>
<ul>
<li>使用双向RNN来处理，捕获更多的信息。</li>
<li>使用更加简单的Position Indicators(简称PI)，而非Zeng 2014的Position Feature. PI很简单，直接使用<e>标签来表示两个entity的位置. 比如在数据中例子: <em>“<e1> people </e1> have been mov- ing back into <e2> downtown </e2>”</em> 这样就将 <code>&lt;e1&gt;, &lt;\e1&gt;, &lt;e2&gt;, &lt;\e2&gt;</code> 作为四个Indicators. 在训练的时候，直接将这四个标签作为普通的word即可，无需特殊处理. 通过这样的方式来突出两个entity.</e></li>
</ul>
<p><strong>实验</strong></p>
<p>数据集: SemEval 2010 Task 8 首先是一个简单的特征对比实验:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/rnn-2.png">

</div>
<p>可以看出来PI这个特征也可以取得不错的效果。 下面是模型对比实验:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/rnn-3.png">

</div>
<p>可以看出来，再不使用任何Lexical-Feature的情况下， RNN + PI 与使用多窗口的Zguyen 2015 以及使用 WordNet信息的Zeng 2014达到类似的效果。此外从这几篇文章实验可以看出来，word embedding的size也可以很明显提高F值。</p>
<p><strong>总结</strong></p>
<p>这篇属于使用RNN的尝试，效果与CNN类似。 文中后续还有一些小实验，来说明对长文本建模的时候，RNN的记忆优势就可以体现出来。SemEval 2010 Task 8数据集的句子长度都比较小，因此使用CNN便可以很好的建模。</p>
<p><strong>6. BiLSTM Attention (Zhou 2016)</strong></p>
<blockquote>
<p>Zhou. (2016). Attention-Based Bidirectional Long Short-Term Memory Networks for Relation Classification. ACL</p>
</blockquote>
<p>这篇文章同样是基于RNN对句子建模，在上一篇RNN的基础上做了一点改进，使用标准的的Attention + BiLSTM，效果与Ranking Loss 类似 84%，基本结构:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/bilstm-1.png">

</div>
<p>主要介绍一下Attention Layer, 其实就是一个对LSTM的每一个step的输出做一个加权的过程，而非仅仅只取最后一个step. 设 <span class="math inline">\(H=[h_1, h_2, h_3,..h_T]\)</span> 为BiLSTM的所有step的输出矩阵，T为句子长度. 然后经过下面的操作来计算每个step的权重:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/bilstm-2.png">

</div>
<p>这样<span class="math inline">\(\alpha\)</span> 就是weight vector, <span class="math inline">\(r\)</span> 就是最终的Attention-BiLSTM的输出，也即sentence的embedding, 之后使用了softmax来分类，损失函数使用对数似然。</p>
<p><strong>实验</strong></p>
<p>数据集: SemEval 2010 Task 8 dataset 结果:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/bilstm-3.png">

</div>
<p>在没有使用词法/语法特征的情况下，达到84%的F1 Score，使用Attention 可以起到作用，能够减弱句子中的噪音词语影响，增强关键词的影响。</p>
<p><strong>总结</strong></p>
<p>Attention + BiLSTM 作为NLP Task的标配，在Relation Classification上也取得了不错的效果。文中不足是仅仅使用标准的Attention+LSTM直接简单用到这个Task， 创新点几乎没有，并没有针对关系分类这个任务的改进。</p>
<p><strong>7. Multi-Level Attention CNN (Wang 2016)</strong></p>
<blockquote>
<p>Wang. (2016). Relation Classification via Multi-Level Attention CNNs. ACL</p>
</blockquote>
<p>这一篇仍然是基于CNN来做的，按照文中实验效果，这一篇目前是最高的F1：88%。设计了相对复杂的两层Attention机制来尽可能突出句子中哪些部分对relation label有更大的贡献。使用了word similarity来定义句子中word与target entity的相似度，从而根据相似度来引入权重，这是第一层的Attention. 第二层Attention则是对卷积之后Pooling阶段，采用Attention Pooling 而不是Max Pooling. 通过这些操作来减弱噪音，加强相关性强的词的权重。此外也改进了前面Santos提出的Ranking Loss. 下面对文中详细介绍.</p>
<p>首先是Input Layer, 同Zeng 2014, 特征仍然是 word embedding + position embedding. 当然word embedding使用的word2vec 进行 pre-train的。因此当前每个词表示为:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/matt-1.png">

</div>
<p>其中<span class="math inline">\(w_i^d\)</span>为word embedding, 两个<span class="math inline">\(w_i^{p}\)</span>是Position Embedding。在之后，将trigram信息融合进去，设置一个滑动窗口k，以每个word 为中心，左右k/2个词作为上下文，然后直接串起来，这样每个词的embedding size变为:<span class="math inline">\((d_w + 2d_p)*k\)</span>，如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/matt-2.png">

</div>
<p>这里需要额外说一句，其实这个n-gram过程现在完成，然后卷积的时候，卷积核的size设置为1就可以了。或者现在不做，卷积核设置为k，可以达到同样的效果。但是后面有Attention，因此在这篇文章中，先做了n-gram。 输入层到这里为止，与其他文章完全一样。 下面就是加入 Input Attention 过程。 首先引入两个对角矩阵<span class="math inline">\(A_1, A_2\)</span> 对应每个句子的两个entity. 然后使用word embedding的向量内积运算来衡量某个词<span class="math inline">\(w_i\)</span> 与 entity <span class="math inline">\(e_j,j =1,2\)</span>的相关性，这样对角矩阵A的元素就是: <span class="math inline">\(A_{i,i}^{j}= f(e_j, w_i)\)</span>，其中f就内积运算。最后使用softamx归一化，来定义Attention的权重:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/matt-3.png">

</div>
<p>额外补充一句，这里的两个对角矩阵完全可以用两个向量来表示。</p>
<p>每个词都有对两个entity的权重:<span class="math inline">\(\alpha^{1}, \alpha^{2}\)</span>. 这样把权重融合到已经得到的<span class="math inline">\(z_i\)</span>中。融合的方法文中给了三个:</p>
<ul>
<li>average: <span class="math inline">\(r_i = z_i \frac{\alpha^1_i+\alpha^2_i}{2}\)</span>，这种方式直接简单，不过从经验感觉，直接求和比平均要好。</li>
<li>concat: <span class="math inline">\(r_i = [(z_i \alpha_i^1)^T, (z_i, \alpha_i^2)T]^T\)</span> 这种方式理论上保留的信息量最大。</li>
<li>substract: <span class="math inline">\(r_i = z_i \frac{\alpha^1_i-\alpha^2_i}{2}\)</span> 这种方式其实有点类似于TranE，将relation视为两个权重的差。</li>
</ul>
<p>这样整个句子表示为: <span class="math inline">\(R=[r_1, r_2, ..., r_n]\)</span>, 至此包含第一层的Attention的Input Layer 完成.</p>
<p>之后是卷积层，这里跟其他文章中卷积相同:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/matt-4.png">

</div>
<p>其中 <span class="math inline">\(W_f\)</span>是卷积核，size为<span class="math inline">\(d^{c} \times k(d^w+2d^p)\)</span>, 前面已经说过了，由于已经在input处做过了tri-gram操作，这里的<span class="math inline">\(d_c\)</span>一般为1.</p>
<p>后面是第二层Attention Based Pooling; 大部分文章直接使用Max Pooling来处理，这样可能会有一些过于简单，有时候并不能抽出跟后续的relation labels 更相关的部分，这也是这篇文章提出Attention Based Pooling的动机。 定义一个关联矩阵表示卷积之后的结果<span class="math inline">\(R^{*}\)</span>的每一个元素与relation labels的相关性，如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/matt-5.png">

</div>
<p>其中U是Attention中的权重矩阵，<span class="math inline">\(W^L\)</span> 表示relation labels的embedding，跟word/position embedding一样，需要在训练过程更新。这样的G就是一个相关矩阵，每一个元素<span class="math inline">\(G_{i,j}\)</span>就表示<span class="math inline">\(R^{*}\)</span>的第i个元素与第j个label的相关性。然后在对G做列归一化:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/matt-6.png">

</div>
<p>有了Attention Pooling矩阵，在做max pooling：</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/matt-7.png">

</div>
<p>这样得到的输出直接与relation embedding 最相关，<span class="math inline">\(w_O\)</span>也是模型最后的输出。完整的结构如下, 看起来很复杂，但是每一部分其实都比较简单:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/matt-8.png">

</div>
<p>最后介绍模型改进的损失函数，根据Santos 2015提出的Ranking Loss, 这篇文章同样使用margin based ranking loss function. 一般的margin function都需要定义一个distance/score function, 从而来区分正负样例. 在santos文中，直接使用了网络最后的输出score。这篇文章利用relation embedding <span class="math inline">\(W_{y}^{L}\)</span>, 定义了如下的score distance function:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/matt-9.png">

</div>
<p>来衡量模型的输出<span class="math inline">\(w^{O}\)</span>与正确label对应的vector <span class="math inline">\(W_{y}^{L}\)</span>的相关度，然后就是margin function:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/matt-10.png">

</div>
<p>这里直接设置了1为margin，<span class="math inline">\(y^{-}\)</span>是错误标签，最后用SGD等方法去优化。</p>
<p><strong>实验</strong></p>
<p>数据集同样是: SemEval 2010 Task 8 先看主体实验结果, 最好的达到了88%，比BaseLine 提升非常大:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/matt-11.png">

</div>
<p>其中仅仅使用Input-Attention可以达到87.5%的效果， 再加上Pooling Attention的效果则达到了88%。从这里可以看出了第一层Attention的作用要更加重要。第二层Pooling复杂度提升很多，performance提升很少。</p>
<p>再看几个对比辅助实验:</p>
<ul>
<li><p>Input Attention的三种融合方式, 结果如下: <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/matt-12.png"></p></li>
<li><p>去掉Pooling Attention, 去掉所有的Attention, 不使用自定义的distance function, 三种结果如下: <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/matt-13.png"> 这个结果也可以看出来，第一层的Input Attention 作用很明显。 其中最后一行去掉Attention, 去掉distance function 基本跟Santos 2015那篇文章相同，因此效果也类似。</p></li>
</ul>
<p><strong>总结</strong></p>
<p>优点:</p>
<ul>
<li>两层Attention: 在文章中的两处Attention 均是基于embedding 的内积来运算的。 其中Input Attention 很直观，利用embedding的内积来衡量相关。</li>
<li>Distance Function：从margin based ranking loss 出发，类似TransE等模型，使用embedding的逼近来作为距离函数。</li>
</ul>
<p>缺点:</p>
<ul>
<li>Input Layer的attention 使用word 与 entity的 embedding similarity 来作为attention的权重，这里有些问题。比如“<e1> caused by <e2>” 这里的 caused 很关键，但是与e1, e2的相似度应该比较小。而且不同的词在不同的relation中的重要程度是不一样的。使用统一的embedding 可能会有噪音影响。在一个可能原因是Out Of Vocab 的词语也会降低performance。</e2></e1></li>
<li>结构复杂 复杂度高，收敛困难，尤其是第二个Attention</li>
</ul>
<p><strong>8. Attention CNNs (Zhu 2017)</strong></p>
<blockquote>
<p>Zhu, (2017). Relation Classification via Target-Concentrated Attention CNNs. ICONIP</p>
</blockquote>
<p>这篇文章基本上是以上一篇为模板，稍微改动了一些， 动机则是每个词在不同的relation有不同的权重Attention 矩阵。 几点不同: - Input Layer 引入一个权重矩阵<span class="math inline">\(M\)</span>，直接计算word 与 relation的相关度， 而不是计算word 与 entity的相关性: <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/zhuatt-1.png"> <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/zhuatt-2.png"></p>
<ul>
<li>去掉了Pool Attention 部分，直接max pooling</li>
<li>目标函数: 没有使用上一篇的自定义的距离函数，而是直接使用类似Santos 2015的那种向量内积计算score的方式. 最终仍然使用ranking loss</li>
</ul>
<p>实验结果, 并没有对比88%的结果: <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/zhuatt-3.png"></p>
<p>这篇文章在Input Layer的Attention的改进比较符合直观。 在没有使用二级Attention 以及改进的损失函数的情况下， 可以达到87.5%，相对于上一篇的86.1%还是有提升。不过整体来看，文章的创新点比较少。</p>
<h3 id="总结1">总结1</h3>
<p>目前从论文结果来看，在SemEval 2010 Task 8 数据集上最高的F1-Score为88%，不过貌似还未有人可以复现。 总结一下已有的模型: - word embedding + position embedding 成为输入层的标配。 - 损失函数Ranking Loss 比 softmax 效果好。 - Attention的作用明显。 - Input Layer加入Attention给每个word赋予weight，可以是word 与 relation的相关性，也可以是word 与 entity的相关性. - RNN/LSTM的所有step 的输出加一个Attention，本质也属于每个word与relation的相关性。</p>
<p>可能改进的方向: 单纯从神经网络的结构出发，改进的余地很小了，因为这个数据集很封闭， 可以利用的信息仅仅是这些sentence 以及 标注的word entity。 在上述的基础上可能可以考虑的方向，从两个target word与relation的关系上加约束。</p>
<h2 id="distant-supervised-learning">Distant Supervised Learning</h2>
<p>上一章的全监督方法都是在完全正确的标注数据集上来做的，因此数据量很小，SemEval 2010 一共是10000左右条文本。因此开始考虑研究如何在大数据集上做关系抽取。2010年提出Distant Supervision可以自动标注训练样本，原理很简单。利用知识图谱中的两个entity以及对应的某个relation，在corpus中进行回标，如果某个句子中同时包含了两个entity，那么就假定这个句子包含了上述的relation. 这样就可以获得大量的标注数据。当然缺陷就是假设太强，会引入了很多噪音数据， 因为包含两个entity的句子不一定可以刻画对应的relation，如下, 对于NYT 语料中的第一个句子的确描述了 Founder的关系，但是第二句就不是这个关系，因此属于噪音:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/dis-1.png">

</div>
<p>在2011年提出了Multi Instance Learning的方法来改进原始的Distance supervision的方法，有一个At-Least-One 的前提: 包含两个entity的所有句子中，至少有一个句子可以体现relation，即至少有一个标注正确的句子。通过结合FreeBase 对NYT语料做Entity Linking，Relation Aligning等操作进行标注， 最终得到一个被广泛使用的关系抽取数据集, 在开始已经描述过详情。正如前面声明，这里面会有噪音数据。 因此在这一系列的文章中，都会针对该噪音问题做一些工作，其中大部分基于Multi Instance来做。简单引用介绍Multi Instance Learning:</p>
<blockquote>
<p>多示例学习可以被描述为：假设训练数据集中的每个数据是一个包(Bag)，每个包都是一个示例(instance)的集合,每个包都有一个训练标记，而包中的示例是没有标记的；如果包中至少存在一个正标记的示例，则包被赋予正标记；而对于一个有负标记的包，其中所有的示例均为负标记。（这里说包中的示例没有标记，而后面又说包中至少存在一个正标记的示例时包为正标记包，是相对训练而言的，也就是说训练的时候是没有给示例标记的，只是给了包的标记，但是示例的标记是确实存在的，存在正负示例来判断正负类别）。通过定义可以看出，与监督学习相比，多示例学习数据集中的样本示例的标记是未知的，而监督学习的训练样本集中，每个示例都有一个一已知的标记；与非监督学习相比，多示例学习仅仅只有包的标记是已知的，而非监督学习样本所有示例均没有标记。但是多示例学习有个特点就是它广泛存在真实的世界中，潜在的应用前景非常大。 from http://blog.csdn.net/tkingreturn/article/details/39959931</p>
</blockquote>
<p>从这个介绍中可以看出来在Multi Instance Learning很适合用在NYT+FreeBase数据集上。</p>
<h3 id="相关文献-1">相关文献</h3>
<p><strong>1.Piecewise Convolutional Neural Networks</strong></p>
<blockquote>
<p>Zeng (2015). Distant Supervision for Relation Extraction via Piecewise Convolutional Neural Networks. EMNLP</p>
</blockquote>
<p>这一篇工作是在Zeng 2014基础上的扩展，从Fully Supervised 到Distant Supervised. <strong>动机</strong></p>
<ul>
<li>Distant supervised 会产生有大量噪音或者被错误标注的数据，直接使用supervised的方法进行关系分类，效果很差。</li>
<li>原始方法大都是基于词法、句法特征来处理， 无法自动提取特征。而且句法树等特征在句子长度边长的话，正确率很显著下降。</li>
</ul>
<p>因此文中使用Multi Instance Learning的at least one假设来解决第一个问题； 在Zeng 2014 的CNN基础上修改了Pooling的方式，解决第二个问题。 先介绍改进的CNN: Piece Wise CNN(PCNN). 总体结构如下, 与Zeng 2014 很类似:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/pcnn-1.png">

</div>
<p>输入仍然是一个sentence，Input Layer依然是word embedding + position embedding, 后面接卷积操作。 之后的Pooling层并没有直接使用全局的Max Pooling, 而是局部的max pooling. 文中把一个句子分为三部分，以两个entity为边界把句子分为三段，然后卷积之后对每一段取max pooling, 这样可以得到三个值，相比传统的max-pooling 每个卷积核只能得到一个值，这样可以更加充分有效的得到句子特征信息。 假设一共有个N个卷积核，最终pooling之后得到的sentence embedding的size为: <span class="math inline">\(3N\)</span>, 后面再加softmax进行分类，最终得到输出向量<span class="math inline">\(o\)</span>, 上面的示意图很清晰了，其中的c1,c2,c3是不同卷积核的结果，然后都分为3段进行Pooling。 下面可以减弱错误label问题的Multi-Instance Learning。这里面有一个概念， 数据中包含两个entity的所有句子称为一个Bag。先做几个定义:</p>
<ul>
<li><span class="math inline">\(M={M_1, M_2, ..., M_T}\)</span> 表示训练数据中的T个bags，每个bags都有一个relation标签.</li>
<li><span class="math inline">\(M_i = {m_i^{1}, m_i^{2},...,m_i^{q_i}}\)</span> 表示第i个bag内有<span class="math inline">\(q_i\)</span>个instance，也就是句子。</li>
<li>$o $ 表示给定<span class="math inline">\(m_i\)</span>的网络模型的输出(未经过softmax)，其中<span class="math inline">\(o_r\)</span> 表示第<span class="math inline">\(r\)</span>个relation的score</li>
</ul>
<p>这样经过softmax 就可以计算每一个类别的概率了</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/pcnn-2.png">

</div>
<p>这里需要说明的是，我们的目的得到每个bag的标签，并不关注bag里面instances的。因为每个bag上的label就是两个entity的relation。 而上面的概率是计算的bag里面某一个instance的，所以需要定义基于Bag的损失函数，文中采取的措施是根据At-Least-One的假设，每个Bag都有至少有一个标注正确的句子，这样就可以从每个bag中找一个得分最高的句子来表示整个bag,于是定义如下的目标函数: 假设训练数据为T个bags: <span class="math inline">\(&lt;M_i, y_i&gt;\)</span>:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/pcnn-3.png">

</div>
<p>这样到此为止，模型部分完毕，完整的算法如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/pcnn-4.png">

</div>
<p><strong>实验</strong></p>
<p>数据集: NYT+FreeBase，过滤掉了样本少的关系，最后使用了26类关系，数据描述如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/pcnn-5.png">

</div>
<p>评测: - Held-Out Evaluation: 直接在Test Dataset上进行评估，使用多分类的Precision-Recall Curve（基于不同的分类阈值）来衡量, PR曲线越靠近右上，证明分类效果越好: <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/pcnn-6.jpg"> 可以看出来，PCNN + MIL 表现最好，Precision和recall都要优于baseline。 - Manual Evaluation: 考虑到FreeBase 还不够完备，对于分类为False Negatives的数据，有可能是两个entity有关系，但是在FreeBase中并没有收录。 从上图的PR-Curve中也可以看出来，recall相对很小，一部分原因是FN过大，尤其是将NA分为其他的relation。 因此文中对那些将NA分为其他类别，按照概率高低，排序，手工检查Top-K 里面的entity pair是否的确有关系，结果如下: <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/pcnn-7.png"> 结果显示PCNN取得了最好的效果。 - 辅助对比实验：原始的CNNs， CNNs + Multi Instance, PCNNs, 以及 PCNNs+Multi Instance的结果: <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/pcnn-8.jpg"> 可以看出来， 文中提出的两种改进的确都有效果。</p>
<p><strong>总结</strong></p>
<p>这篇文章在NYT + FreeBase 数据集上比较好的效果， 也是第一篇使用CNN+Multi Instance来处理Distant Supervision 的关系抽取。相比与Zeng 2014，将Piecewise Pooling 加入到CNN中，以及将Multi Instance来处理Distance Supervised，从而减弱噪音的影响。当然在MIL中直接取了置信度最高的instance作为bag的，肯定会损失一部分信息，因此后续一些文章在上面做了工作。</p>
<p><strong>2.Selective Attention over Instances (Lin 2016)</strong></p>
<blockquote>
<p>Lin (2016). Neural Relation Extraction with Selective Attention over Instances.ACL</p>
</blockquote>
<p>这一篇文章在上一篇Zeng 2015的基础上引入了Attention机制。</p>
<p><strong>动机</strong></p>
<p>在Zeng 2015中的MIL部分，每一个bag仅仅取了置信度最高的instance，这样会丢失很多的信息，因此一个bag内可能有很多个positive instance。应用Attention机制可以减弱噪音，加强正样本，因此可以更充分的利用信息。</p>
<p><strong>模型</strong> 总体结构:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/att-1.png">

</div>
<p>其中<span class="math inline">\(m_i\)</span>是某一个bag的instance，图中的CNN模块与Zeng 2015 (PCNNs)的结果相同(Embedding Layer-&gt;Convolution Layer -&gt; Piecewise Max Pooling Layer) 最终得到每个句子的表示<span class="math inline">\(r_i\)</span>。为了能够充分的利用bag内的信息，我们可以对所有instance取加权: <span class="math inline">\(s= \sum_{i} \alpha_i x_i\)</span> 其中<span class="math inline">\(\alpha\)</span>是权重比例，文中给出了两种定义权重的方法:</p>
<ul>
<li>Average: 直接取平均: <span class="math inline">\(s=\sum\frac{1}{n}x_i\)</span> ，这种将所有instance同等对待的方式还是有缺陷，放大了噪音影响。</li>
<li>Attention: 目标是增加positive instance的权重，减小noise instance的权重。但是并不知道每个instance的groud truth，但是知道每个bag的label，因此就可以用instance 与 该relation label的相关度大小引入Attention: 如下: <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/att-2.png"> <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/att-3.png"> 其中A为Attention对角矩阵，<span class="math inline">\(r\)</span>可以认为是该数据的relation标签的embedding向量。 这样<span class="math inline">\(e_i\)</span>就可以一定程度表示句子与标签的相关性。</li>
</ul>
<p>之后得到了加权的<span class="math inline">\(s\)</span>就是bag-level的embedding。最后的输出需要注意，并不是直接用一个全连接层+softmax.而是根据向量的相似度计算bag的emebdding 与 每个relation的相似度作为relation的score，比如relation r的score计算方式:<span class="math inline">\(o_r = v_r s + b\)</span> 这里的b是一个bias，每个relation的score计算完之后，利用softmax来归一化成概率： <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/att-4.png"></p>
<p>在Test阶段不太一样， 前面说了在计算Attention的时候，使用的是训练数据对应的label 的embedding，但是在test的时候，并没有标签。文中的处理方式是: 对每一个relation r 执行下面的操作:</p>
<ul>
<li>计算一个bag里面所有的句子x与r的Attention权重，这一步与训练相同，然后加权求得这个bag的表示<span class="math inline">\(s\)</span></li>
<li>计算式子10: <span class="math inline">\(o = Ms +d\)</span>, 这里的M就是所有relation的embedding 矩阵。 这样得到了 o 是一个<span class="math inline">\(N_r\)</span> 维度的向量，<span class="math inline">\(N_r\)</span>是relation的number。</li>
<li>取出o里面的relation <span class="math inline">\(r\)</span> 对应的值， 作为这个r的预测概率</li>
</ul>
<p>源码注释如下: <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/att-5.png"></p>
<p>遍历完所有的relation之后， 得到每个relation的概率，取max即可。 这个过程其实相当于一个遍历的过程，复杂度有点高，而且解释性比较差，是一个可以改进的一个地方。</p>
<p><strong>实验</strong></p>
<p>文中首先做了在CNN/PCNN的基础上，如何使用bag内的信息的对比试验，结果如下，其中CNN/PCNN表示没有引入MIL，+ONE表示ZENG2015的取置信度最高的一个，+AVE与+ATT则是文中提出的两个:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/att-6.png">

</div>
<p>可以看出来，Attention的效果最好, 而且+ONE比+AVE要稍微好一些，也很容易理解，+AVE对噪音数据放大了影响。 第二个对比实验则是与传统方法的对比:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/att-7.png">

</div>
<p>最后一组实验则是更进一步验证Attention的作用。 因为在测试数据中，有超过3/4的bags 只有一个句子。 因此文中把拥有多个句子的entity pair 提取出来验证效果，在该子集上使用一下三种设置:</p>
<ul>
<li>One: 表示在每个bag里面随机选择一个句子来进行预测</li>
<li>Two: 表示在每个bag里面随机选择两个句子来进行预测</li>
<li>All: 使用bag里面所有的句子来进行测试</li>
</ul>
<p>评价指标使用P@N，结果如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/att-8.png">

</div>
<p>从结果可以看出来，ATT的确表现最好。</p>
<p><strong>总结</strong></p>
<p>与上一章全监督一样，Attention的作用很明显，而且用在这里很直观， 对bag内的句子赋予不同的权重，一方面可以过滤噪音，又可以充分利用信息。</p>
<p>不足的地方:</p>
<ul>
<li>正如在future work上所述，还是基于Zeng2014/5的基本框架上做改进，这个数据集句子长度要比SemEval 2010要长很多， 可以考虑用RNNs来测试结果如何。一般情况CNN在短文本效果比较好，RNN在长文本略有优势。</li>
<li>上面说的，train过程和test过程不一样，test要更加复杂，复杂度要高很多。</li>
</ul>
<p><strong>3.Multi-instance Multi-label CNNs (Jiang et al., 2016)</strong></p>
<blockquote>
<p>Jiang (2016). Relation Extraction with Multi-instance Multi-label Convolutional Neural Networks. Coling</p>
</blockquote>
<p>这篇文章从另一个角度来解决Zeng 2015的问题，并且考虑了实体对的多关系的问题。</p>
<p><strong>动机</strong></p>
<ul>
<li>Zeng 2015里面仅仅取置信度最高的instance，丢失信息。</li>
<li>在数据集中，有约18.3%的entity pair有多种relation, 其他方法均未考虑。</li>
</ul>
<p><strong>模型</strong></p>
<p>针对以上的两个问题提出了两个解决方法:</p>
<ul>
<li>对bag内部的所有sentence embeding做instance-max-pooling的操作，具体细节后面介绍</li>
<li>对于多标签，使用多个二分类函数来做多标签分类，即： 使用sigmod计算每一个类别的概率, 然后判断该bag是否可能有这种关系。</li>
</ul>
<p>模型的结构如图:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/miml-1.png">

</div>
<p>输入也是一个bag，然后利用CNN/PCNN来计算每个sentence的embedding，之后的融合方式很直接，直接对embedding的每一维度取所有sentence的对应维度的最大值。</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/miml-2.png">

</div>
<p>其中k表示embedding的某一维度，<span class="math inline">\(j\)</span>表示bag中的第j个句子。 这样就可以融合所有sentence的信息了。后面加一个全连接层计算每一个类别的score:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/miml-3.png">

</div>
<p>之后不再是加softmax多分类了，而是使用sigmod函数计算每个relation的概率，然后超过某个阈值，就认为该relation是准确的:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/miml-4.png">

</div>
<p>其中<span class="math inline">\(l\)</span>就是类别的总数。 文中设计了两种损失函数来做对比, Sigmod Loss Vs Squared Loss：</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/miml-5.png">

</div>
<p><strong>实验</strong> 直接看P-R Curve结果，相比PCNN提升比较明显:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/miml-6.jpg">

</div>
<p>再看取max的设计的作用，与直接取平均对比, 这里有点需要说明，在这个实验中，取平均要比PCNN效果好，而在上一篇平均效果差， 这说明的是multi label有提升的作用:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/miml-7.jpg">

</div>
<p>最后一个是两种损失函数的对比:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/miml-8.jpg">

</div>
<p>可以看出，二者在不同的区域各自有优势。</p>
<p><strong>总结</strong></p>
<p>仅仅对bags内的sentence的每一维度取了最大值，就可以得到一个很不错的效果， 可以考虑其他稍微复杂一些的融合方式，从而得到更多的信息，Attention仅仅取权重，其实还是属于线性融合。此外这篇文章仍然也是在该CNN/PCNN基础上进行扩展，从这一点来说创新性有些少。 不过文中提出的Multi Label 则是一个新的方向.</p>
<p><strong>4.Memory Network based (Feng 2017)</strong></p>
<blockquote>
<p>Feng,(2017). Effective deep memory networks for distant supervised relation extraction. IJCAI</p>
</blockquote>
<p>这篇文章使用2014年提出的Memory Network的思想来做关系抽取。</p>
<p><strong>动机</strong></p>
<ul>
<li>不同的word在不同relation下以及对不同的entity pair 重要程度不同，这一点类似于Fully Supervised 中的Multi-Level Attention</li>
<li>relation之间并不是独立的，会有overlap, 也就是上一篇提出的多标签。本质上是因为标签之间有相互依赖关系，如(A, capital, B) 成立的话， (A, contains, B)也会成立。</li>
</ul>
<p>对于第一个问题，借鉴了Multi-Level Attention的input layer attention的思路，即根据word 与 entity pair的相似度，来分配权重，但是这里不使用传统的Attention, 而是基于Memory Network的思想，关于Memory Network的细节，这里不介绍，不了解也不影响对文章的理解。 对于第二个问题，文中使用relation-level的attention 来引入relation之间相关度，下面详细介绍</p>
<p><strong>模型</strong></p>
<p>跟前面的文章类似， 本文也是分为两个大模块，sentence representation 以及 relation classification。 首先从input layer 开始，每个word仍然由word embedding + position embedding两部分组成。 在本文中sentence embedding由两部分组成，其中一部分是传统的CNN/PCNN(Zeng 2015) 中得到的输出，这一块不再赘述，与上面文章相同。 另一部分则是考虑了句子中entity的上下文与entity的相关性得到的一个表示，细节如下:</p>
<p>设每个句子表示为<span class="math inline">\(S=\{e_1,e_2,..,e_l\}\)</span>，其中<span class="math inline">\(e_i \in R^{d \times1}, d=d_w + 2d_p\)</span>, <span class="math inline">\(l\)</span>表示句子长度。 两个entity为<span class="math inline">\(e_h, e_t\)</span>，这样剩下的部分，也就是上下文，表示成一个矩阵:<span class="math inline">\(m=\{e_1,..,e_{h-1}, e_{h+1},...,e_{t-1},e_{t+1}..\} \in R^{d \times (l-2)}\)</span>，文中成为 external memory 矩阵 然后计算<span class="math inline">\(m\)</span> 的每一行也就是每一个上下文与entity pair的相关度，这里并没有使用向量内积来运算，而是一层神经网络来计算score: <span class="math display">\[g_i  = tanh(W_{word-ep}[m_i, e_h, e_t] + b_{word-ep})\]</span> 其中<span class="math inline">\([x1,x2]\)</span>表示向量串联，<span class="math inline">\(W_{word-ep} \in R^{1 \times 3d}, b_{word-ep} \in R^{1\times 1}\)</span>， 再使用softmax来得到权重: <span class="math display">\[\alpha_i = \frac{\exp(g_i)}{\sum \exp(g_j)}\]</span></p>
<p>最后可以得到一个加权之后的输出向量:<span class="math inline">\(x=\sum_{i=1}^{l-2} \alpha_i m_i\)</span>. 在x的基础上，还可以继续上述的过程，只是把entity pair的两个向量，换成<span class="math inline">\(x\)</span>. 可以重复多次，最终得到一个输出作为sentence的另一部分表示。 将该部分与CNN的输出直接串联作为最终句子的表示。</p>
<p>对于一个bag来说，使用上面的模型可以得到每个句子的表示，之后文中使用两阶段的Attention: - Selective Attention over Instances 与Lin 2016 相同， 对每个bag内的instances取加权平均，计算方法也一样，首先利用Attention矩阵计算instance与每个relation的相关性: <span class="math inline">\(z_i = x_i A v_{r_j}\)</span>, 之后进行softmax计算权重: <span class="math inline">\(\beta_i = \frac{\exp(z_i)}{\sum \exp (z_p)}\)</span> ， 最终得到加权的bag表示: <span class="math inline">\(R_{j}=\sum \beta_i x_i\)</span>，其中<span class="math inline">\(R_j\)</span>是relation-specific的 - Selective Attention over Relations 这一层的Attention是用在relations之间的，用来挖掘relation之间的依赖关系，前一层的Attention得到了k个向量:<span class="math inline">\(R=\{R_1,...,R_k\}\)</span>，其中k为relation 数量。 计算关系 i 与 j 的相关性如下: <span class="math inline">\(h_i = R_i B R_j\)</span>, 之后softmax 计算权重: <span class="math inline">\(\gamma_i = \frac{\exp(h_i)}{\sum \exp (h_q)}\)</span>， 然后就可以得到融合多relation依赖关系的输出: <span class="math inline">\(R^{*}_{j} = \sum_{i=1}^{k} = \gamma_i R^i\)</span></p>
<p>之后得到<span class="math inline">\(R={R_{1}^{*}, ..., R_{k}^{*}}\)</span>，对每一个relation 做一个binary classifier， 逻辑回归的sigmod即可 <span class="math display">\[p(i|M,\theta)=\frac{1}{1+\exp(-o_i)}\]</span> 其中<span class="math inline">\(o_i = W_i R^{*}_{i} + b_i\)</span> , 最后使用多标签的损失函数，与上一篇相同:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/mem-1.png">

</div>
<p>到此模型介绍完毕。</p>
<p><strong>实验</strong></p>
<p>同样用held-out以及P@N 结果如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/mem-2.png">

</div>
<p>从结果看， 本文的方法比Lin 2016 要好， 毕竟考虑了关系的依赖性，不过提升不是很明显，可能原因是数据问题，relation的overlapping比较少。 再看一组对比实验: 分别是去掉word-attention 以及去掉 relation-attention:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/mem-3.png">

</div>
<p>可以看出来，去掉relation之后，下降相对来说比去掉word-level更加明显。</p>
<p><strong>总结</strong> 这篇文章虽然以Memory Network 为题，不过可以本质还是用Attention引入相关性。 word-level的动机来自Multi-Level 的那篇文章，计算word与target entity的相关性，并且可以多层，从而挖掘更深层次的关系。relation-level的动机则是考虑到数据中的关系依赖性，使用attention来考虑关系之间的相关性。</p>
<p>这篇文章创新的地方在于引入relation之间的依赖关系。 可能改进的地方有，完全从embedding的角度考虑相关性，抛弃Attention,计算量会少一些。包括word-entity embedding； relation-relation embedding;</p>
<p><strong>5.Multi-Instance With extra Entity Descriptin (Ji 2017)</strong></p>
<blockquote>
<p>Ji (2017). Distant Supervision for Relation Extraction with Sentence-Level Attention and Entity Descriptions. AAAI</p>
</blockquote>
<p>这篇文章引入了额外的Knowledge Graph 信息: 实体描述(entity Description). 比如NYT 数据集是通过Freebase做entity linking等工具来进行连接出句子中的实体，而其实每个实体在FB都有一段文字描述， 文中认为现在的工作都集中在NYT + Freebase数据本身上，却忽略了该数据集中背后的KG信息，其中就有实体的描述信息。因此本文在之前的工作基础上引入了实体描述信息，加强对实体embedding的学习。此外，在处理Multi Instance Learning方面， 本文同样使用了Sentence-Level Attention的机制，类似Lin 2016.</p>
<p><strong>模型</strong></p>
<p>模型分为三部分: 输入模块，Attention 模块， Entity 描述模块。 输入层，每个句子的每个词由word embedding + position embedding 连接表示，这里与其他模型完全一样，不再赘述。 接着是卷积层与Piecewise Max-Pooling层，到这里与PCNN(Zeng 2014)完全一致，最终得到每个句子的embedding。 下面则是用来解决Multi instance Learning的Attention 模块。 计算bag内的每个Instance 与 relation的相关性，确定权重。具体步骤如下:</p>
<ul>
<li>计算相关性: <span class="math inline">\(\omega_i = W^T_{a}(tanh([b_i; v_{relation}])) + b_a\)</span> 其中<span class="math inline">\([x1, x_2]\)</span>表示concat操作, <span class="math inline">\(b_i\)</span>表示上一个步骤得到每个句子的表示，与Lin 2016 使用随机初始化作为<span class="math inline">\(v_{relation}\)</span>的参数不同的是，这里的<span class="math inline">\(v_{relation}\)</span>借鉴KG Embedding的Trans模型，即: <span class="math inline">\(v_{relation}=e_1 - e_2\)</span>。此外这里使用神经网络计算Attention。</li>
<li>归一化: <span class="math inline">\(\alpha_i = \frac{\exp(\omega_i)}{\sum \exp(\omega_j)}\)</span></li>
<li>加权句子表示，得到bag的表示: <span class="math inline">\(b = \sum \alpha_i b_i\)</span></li>
</ul>
<p>后面同样加一个softmax层用来做多分类: <span class="math inline">\(o = W_s b + b_s\)</span>, 归一化: <span class="math inline">\(p(r_i) = \frac{\exp(o_i)}{\sum \exp(o_j)}\)</span> 到这里为止其实与前面的工作相似度非常高， 仅仅是Attention的一点差异。下面介绍文中引入的Entity Description部分。 其实很简单，步骤如下:</p>
<ul>
<li>使用普通的一层CNN+MaxPooling 对 description 建模得到 描述的 embedding向量，假设<span class="math inline">\(d_i\)</span>为第i个实体的description embedding</li>
<li>文中提出的一个约束是：尽可能使得前面得到的entity的word embedding 与 这里得到的 entity 的 description embedding 接近，这样的动机很简单，就是将entity的描述信息融入到模型中，这部分的LOSS直接使用二范距离: <span class="math inline">\(L_e = \sum ||e_i - d_i||_2^2\)</span></li>
</ul>
<p>最终的目标函数，就是将两个损失函数联合训练: <span class="math display">\[\min L = L_A + \lambda L_e\]</span> 其中<span class="math inline">\(L_A = \sum log P(r_i), \lambda\)</span>为调节系数。</p>
<p><strong>实验</strong></p>
<p>数据集说明:</p>
<ul>
<li>NYT + FreeBase</li>
<li>从Freebase 以及 Wikidata中提取出的实体描述</li>
</ul>
<p>首先看Held-Out 实验结果:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/ent-1.png">

</div>
<p>其中实验有: PCNN+MIL 为Zeng 2015的模型， APCNNs为本文提出的不包含实体描述的模型， PCNNs+MIL+D为在zeng 2015的基础上加上实体描述的Loss，APCNNs+D为本文包含描述的模型。 可以看出来加上实体描述的确对效果有提升。此外APCNNs 比 PCNN+MIL要好一些，说明Attention要比取置信度最高的instance要好。</p>
<p>再看manual实验，就是找出那些标签为NA但是预测非NA的数据， 人工检查，检查是否两个entity的确有预测的relation，使用准确率Accuracy来衡量，结果如下,这里的top是指按照预测为某relation的概率大小排序:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/relation-extraction/ent-2.png">

</div>
<p><strong>总结</strong></p>
<p>这篇文章的贡献主要在从KG中引入额外的实体描述信息，加强embedding的学习。 不过两部分的融合有点简单暴力，本质上相当于在原来的基础上加了一个范式约束而已，或者说一个先验的惩罚项, 这种方式提供了一个比较好的思路, 扩展性很强.</p>
<h3 id="总结2">总结2</h3>
<p>general的总结:</p>
<ul>
<li>输入层: 基本word embedding + position emebdding</li>
<li>Sentence Embedding: 基本使用CNN/PCNN</li>
<li>Multi Instance Learning: 基本使用Attention 来处理(Attention的选择有待改进)</li>
<li>Evaluate:
<ul>
<li>Held-out: P-R 曲线</li>
<li>Manual: 选择Negative False的标签为NA的数据， 手工检测是否的确为NA</li>
</ul></li>
</ul>
<p>其他Tips:</p>
<ul>
<li>Word-Entity的相关度，同一个word在不同的entity pair内有不同的重要性。(Deep Memory Network, Multi-Level Att )</li>
<li>Word-Relation的相关度，同一个word在不同的relation下有不同的重要性。()</li>
<li>多标签分类(MIMLCNN, Memory Network)</li>
<li>relation之间的依赖性(Memory Network)</li>
</ul>
<h2 id="附">附</h2>
<p>关系抽取这个方向相对来说比较成熟了, 包括数据集,评测等. 改进的出发点很多; 相对比较容易做的就是从distant supervision下手, 如何减轻噪音数据带来的影响. 此外用一些时髦的东西套过来也可以,比如强化学习(AAAI2018有2篇)</p>
<p>此外正在用Pytorch来复现相关模型, 然而一些经典模型仍然无法复现出结果, 还在调试.</p>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;对近几年(到2017)一些关系抽取/分类(Relation Extraction)的部分文献的一个简单总结。&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="读书笔记" scheme="http://shomy.top/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="Knowledge Graph" scheme="http://shomy.top/tags/Knowledge-Graph/"/>
    
      <category term="NLP" scheme="http://shomy.top/tags/NLP/"/>
    
  </entry>
  
  <entry>
    <title>Feature Scaling 和 Batch Norm 笔记</title>
    <link href="http://shomy.top/2017/12/05/Feature-Scaling-Batch-Norm/"/>
    <id>http://shomy.top/2017/12/05/Feature-Scaling-Batch-Norm/</id>
    <published>2017-12-05T08:50:00.000Z</published>
    <updated>2017-12-06T14:52:25.471Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>Batch Normlization是2015年提出的一个加速神经网络训练的技巧，同时对performance也有一定的提高，现在基本已经广泛使用了。这里相对完整的整理一下学习笔记。主要参考李宏毅老师的机器学习课程。</p>
</blockquote>
<h2 id="从feature-scaling-开始">从Feature Scaling 开始</h2>
<a id="more"></a>
<p>提起BN首先需要从Feature Scaling/Normalization(特征缩放/归一化)说起。 在机器学习中，经常需要对特征做归一化，标准化，具体原因是什么呢？如果输入的特征的尺度差别很大，比如说<span class="math inline">\(x_1\)</span>的取值位于<span class="math inline">\(0-1\)</span>, <span class="math inline">\(x_2\)</span>的尺度位于<span class="math inline">\(1000-2000\)</span>, 这个时候，就需要将特征scaling到同一个尺度，一般来说，特征缩放之后有两个好处。</p>
<ul>
<li><strong>提高学习器的性能</strong>，特别是涉及到距离计算的，比如KNN, SVM等分类器，KMeans聚类等。这个很容易理解，在计算距离的时候， 如果某一个特征取值范围很大，这样这一个特征对距离的计算就会占据主导作用， 其它尺度小的特征起到的作用就会变小，而实际上这些特征的重要性可能是一样的。 因此做了Feature Scaling将特征范围缩放到一个尺度的话，就可以避免这种情况。</li>
<li><p><strong>加快梯度下降优化的收敛速度</strong> 这里经常使用的是吴恩达老师的机器学习课程中预测房价的例子，假设有影响房价两个特征</p>
<ul>
<li><span class="math inline">\(x_1\)</span>:房间数目，一般比较小，位于1-5左右</li>
<li><span class="math inline">\(x_2\)</span>: 房屋面积, 一般取值比较大，比如位于80-200等</li>
</ul>
<p>一般线性回归的关系函数如下: <span class="math display">\[s = \theta_1x_1 + \theta_2x*2 + b\]</span> 取平方误差的话, 损失函数为(仅仅考虑一个数据): <span class="math display">\[LOSS = (s_\theta(x_i)-y_i)^2\]</span> 因为<span class="math inline">\(x_2\)</span>的尺度比<span class="math inline">\(x_1\)</span>大很多，因此即使两个参数<span class="math inline">\(w_1,w_2\)</span>同样大小的变化，乘上<span class="math inline">\(x_i\)</span>之后，对最后的结果LOSS影响差距也很不同, <span class="math inline">\(w_2\)</span>明显要比<span class="math inline">\(w_1\)</span>的作用要大，也就是说，<span class="math inline">\(\theta_2\)</span>很小的变化就会导致LOSS较大变化，在LOSS的等高线上就会呈现出椭圆的形状, 如图1: <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/feature-scaling-BN/scaling-1.png" alt="img"> 这个图的横纵坐标表示两个特征，然后每一圈代表一个LOSS值，圈越大，LOSS越大，最里面的B则是最优解的位置。 然后考虑梯度更新的过程，上述的等高线即LOSS与<span class="math inline">\(\theta_1,\theta_2\)</span>的函数关系， 因此图中椭圆的切线方向就是梯度下降的方向， 从图1中可以看出来，从初始点A到最有点B需要经过很多次的无用的迭代，并不是每次向着最优的方向走，类似于之字形，这样收敛就会很慢了。 反过来看，如果做了Feature Scaling的话，<span class="math inline">\(\theta_1, \theta_2\)</span>的更新变化对LOSS影响一致，这样等高线就会成近圆形如图2所示，这样沿着切线的方向进行梯度更新就会一直沿着最优的方向走，就会加快收敛速度。</p></li>
</ul>
<p>常用的Feature Scaling的方法有:</p>
<ul>
<li>标准化(Standardization): 所有特征量化到标准正态分布上: <span class="math display">\[z = \frac{x- \mu}{\sigma}\]</span> 其中<span class="math inline">\(\mu,\sigma\)</span>就是 <span class="math inline">\(x\)</span>的均值和标准差，这样所有特征的尺度就基本类似了。 这种标准化的缺点就是如果原始数据不近似于高斯分布的话，效果可能会变差，毕竟修改了分布。</li>
<li>最大-最小化 (Min-Max Scaling) 线性归一: <span class="math display">\[z = \frac{x-x_{min}}{x_{max} - x_{min}}\]</span> 这种也比较简单常用。这样所有的数据都会到[0,1]的范围内了，尺度一致。</li>
</ul>
<p>此外， 其实做Feature Scaling或多或少的跟原始数据都有一些差异， 理论上比较好的方法是，不同的Feature设置不同的学习率，尺度大的特征的学习率应该小一点，尺度小的特征的学习率稍微大一些，但是在实际梯度下降中，特征很多，对每个特征设置不通的学习率不切实际，很难实现，因此换一种思路，对特征做scale, 这样也可以避免尺度不一带来的问题。</p>
<h2 id="batch-normalization">Batch Normalization</h2>
<p>开始考虑神经网络， 我们知道神经网络有很多个Layer组成， 每个layer的输入都是上一层layer的输出， 那我们就有理由相信对每一层的输入的特征做Feature Scaling 可能会起到不错的作用。 在说BN之前，需要简单提一下 Internal Covariate Shift(ICS) 这个现象. 我们知道多层的神经网络，每一层的输出数据的分布都在随着训练也就是参数更新发生着变化，比如说第一层的输出的分布随着第一层的权重和bias的训练更新而改变，这种数据分布的改变就称之为: Internal Covariate Shift(ICS). 而如果对每一层的输入都做归一化的话，就相当于每一层的分布都保持一致了(均值方差相同), 这样可以一定程度上减小ICS带来的影响，当然显然并不能从根本上解决，只是减弱一些而已(具体关于ICS的内容这里不再介绍，理论知识偏多)。 下面考虑一下Batch Normalization是怎么做的。 有了前面的Feature Scaling, 貌似问题很简单了， 就对每一层的输出做scaling就可以了，直接使用上述的第一种归一化的方式即可: <span class="math display">\[\hat{x} = \frac{x-\mu}{\sigma}\]</span> 但是这里需要考虑一个问题，在做归一化的时候， 会影响原来学到的特征，比如说在输入到sigmod激活函数之前，如果经过Wx+b之后的结果，分布在S型函数的两侧， 也就是近乎平行坐标轴的那部分区域，这样原本经过激活函数之后就是0或者1了， 但是经过归一化之后，结果数据都到了S型函数的中间部分,如下图: <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/feature-scaling-BN/scaling-2.png" alt="img"></p>
<p>这样会有问题，归一化都到了[-1, 1]那一部分区域， 而对应的sigmod近乎是一条直线了，如红色虚线所示，那这样就成了线性变换了，那神经网络的非线性变化就没有意义了。所以BN在做完归一化，具体来说是标准化之后，又做了一步特征转换的操作: <span class="math display">\[\hat{x} = \frac{x-\mu}{\sigma} //归一化\]</span> <span class="math display">\[z = \gamma \hat{x} + \beta //重构转换\]</span></p>
<p>其中 <span class="math inline">\(\gamma, \beta\)</span> 是两个参数，跟权重矩阵参数一样， 也是需要训练更新的。<span class="math inline">\(\hat{x}\)</span>则是标准化之后的值。可以看出来，如果<span class="math inline">\(\gamma=\sigma, \beta=\mu\)</span>的时候，我们就会发现<span class="math inline">\(x=z\)</span>了，这样就恢复到了原始的特征分布，其实可以这样理解，<span class="math inline">\(\gamma, \beta\)</span>这两个参数其实就是来学习前面的Normalization到底有没有作用，如果没有作用那就做一些转换，抵消一部分Normalization带来的影响。 这样完整的BN的步骤如下，需要注意的是，输入是一个Batch的数据，上述求得均值，方差均是对同一个Batch的数据计算得出的也就是说下图的m就是minbatch size： <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/feature-scaling-BN/scaling-3.png" alt="img"></p>
<p>用图示表示，更加形象一些: <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/feature-scaling-BN/scaling-4.png" alt="img"></p>
<p>需要注意的有两点:</p>
<ul>
<li>理论上BN可以放到激活函数之前，也可以放在其之后，但是一般情况，BN层会放在激活函数之前，这个其实很容易理解，比如激活函数如果是sigmod或者tanh的话，本身的输出都已经在一定程度上有了归一化了，后面再加一个BN的话，意义不大。</li>
<li>batch-size 大一些泛化能力更强。 如果batch很小的话，计算均值和方差其实没有代表性。</li>
</ul>
<p>前面的都是在training阶段，在test的时候，情况不太一样。因为在测试的话，我们没有batch 的输入，如何去计算<span class="math inline">\(\mu, \gamma\)</span>呢。一般都是从训练数据出发, 比如使用所有数据的均值和方差，这种方法在数据集很大的时候，很难实现。因此可以退而求其次，在最后一个epoch的时候，保存每个batch的均值和方差，最后去平均。取最后一个epoch的原因也很简单，最后一个epoch参数的训练已经近乎最优了。</p>
<h2 id="附pytorch-简单实现">附Pytorch 简单实现</h2>
<p>Pytorch已经内置了<code>BatchNorm</code>函数，而且有多种，这里就以简单的神经网络来做个例子，直接在激活函数之前就加入BN即可: <figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">model = nn.Sequential(</div><div class="line">    nn.Linear(<span class="number">10</span>, <span class="number">20</span>),           # 输入<span class="number">10</span>维，隐层<span class="number">20</span>维</div><div class="line">    nn.BatchNorm1d(<span class="number">20</span>, momentum=<span class="number">0.5</span>),     # BN层，参数为隐层的个数</div><div class="line">    nn.tanh(),                        # 激活函数</div><div class="line">    nn.Linear(<span class="number">20</span>, <span class="number">2</span>),             # 输出层</div><div class="line"> )</div></pre></td></tr></table></figure></p>
<h2 id="参考">参考</h2>
<ol style="list-style-type: decimal">
<li>https://arxiv.org/abs/1502.03167</li>
<li>http://blog.csdn.net/lovesophiaw/article/details/58996684</li>
<li>https://zhuanlan.zhihu.com/p/25234554</li>
<li><a href="https://www.youtube.com/watch?v=BZh1ltr5Rkg" target="_blank" rel="external">李宏毅老师 Batch-Norm 教程</a></li>
<li>http://www.voidcn.com/article/p-bptxerfe-bcx.html</li>
<li>https://morvanzhou.github.io/tutorials/machine-learning/torch/5-04-A-batch-normalization/</li>
</ol>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;Batch Normlization是2015年提出的一个加速神经网络训练的技巧，同时对performance也有一定的提高，现在基本已经广泛使用了。这里相对完整的整理一下学习笔记。主要参考李宏毅老师的机器学习课程。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id=&quot;从feature-scaling-开始&quot;&gt;从Feature Scaling 开始&lt;/h2&gt;
    
    </summary>
    
      <category term="读书笔记" scheme="http://shomy.top/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="machine learning" scheme="http://shomy.top/tags/machine-learning/"/>
    
  </entry>
  
  <entry>
    <title>使用coding/github的webhook实现自动化部署</title>
    <link href="http://shomy.top/2017/09/29/codings-webhook/"/>
    <id>http://shomy.top/2017/09/29/codings-webhook/</id>
    <published>2017-09-29T09:48:00.000Z</published>
    <updated>2017-09-29T14:31:55.763Z</updated>
    
    <content type="html"><![CDATA[<h2 id="需求">需求</h2>
<p>一些代码托管网站比如coding.net/github.com 等都提供了webhook的工具， 经常用于自动化部署。 之前写过一篇博客<a href="http://shomy.top/2016/04/14/git-server-hook/">搭建git服务器以及web自动化部署</a> 主要记录的是在自己服务器上如何搭建git服务，并且利用hook进行自动化部署。不过这种方式有个缺点，没有像github或者coding.net的web界面，管理起来可能稍微麻烦，因此我们还是习惯在github,coding.net上进行代码托管。然后再部署到服务器上，这样有些不方便。还好这些托管网站均提供了webhook的工具，功能与git的hook一致。 <a id="more"></a></p>
<p>因此可以用来做自动化部署，用一张图来说明区别(参考<a href="https://laravel-china.org/topics/2096/using-webhook-to-realize-automatic-deployment-of-online-server" target="_blank" rel="external">1</a>]:</p>
<p><img src="http://7xotye.com1.z0.glb.clouddn.com/blog/coding-web-hook/webhook-1.png"> 这样以来， 开发者只需要push代码到github上即可，无需在服务器操作，即可将服务器代码同步最新状态。原理很简单，当coding.net收到<code>PUSH</code>或者<code>PR</code>之后，向服务器发起一个<code>POST</code>的请求， 服务器执行<code>git pull</code>的操作，拉取最新代码。 下面记录下总体的流程(github与coding.net操作基本相同，下面以coding.net为例)</p>
<h2 id="操作">操作</h2>
<p>基本流程分为以下几步: 1. 建立仓库，这一个没什么好说的，就是自己的项目 repo. 2. 将服务器的ssh公钥加入到项目的公钥: <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/coding-web-hook/ssh-coding-1.png"> 3. 建立WebHook <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/coding-web-hook/web-hook-new.png"> 4. 在服务器端写响应API 这部分就是写个响应POST请求的小程序就好，我这里用python的Flask来完成的。代码如下，其中使用了第三方执行命令库<code>sh</code>，可以方便执行一些Unix命令:</p>
<p><figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div></pre></td><td class="code"><pre><div class="line"><span class="comment">#!/usr/bin/env python</span></div><div class="line"><span class="comment"># encoding: utf-8</span></div><div class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask, request</div><div class="line"><span class="keyword">import</span> sh</div><div class="line"><span class="keyword">import</span> logging</div><div class="line">app = Flask(__name__)</div><div class="line"></div><div class="line"><span class="meta">@app.route('/api/git_hook', methods=['POST'])</span></div><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">git_hook</span><span class="params">()</span>:</span></div><div class="line">    token = request.form.get(<span class="string">'token'</span>, <span class="string">''</span>) <span class="comment"># 提取token</span></div><div class="line">    <span class="keyword">if</span> <span class="string">'your-token'</span> == token:</div><div class="line">        sh.cd(<span class="string">'/var/www/html/web'</span>) <span class="comment"># 进入web项目的目录</span></div><div class="line">        <span class="comment"># 执行pull操作</span></div><div class="line">        sh.git.pull([<span class="string">'origin'</span>, <span class="string">'master'</span>])</div><div class="line">        <span class="keyword">return</span> <span class="string">'&lt;pre&gt;pull&lt;/pre&gt;'</span></div><div class="line">    <span class="keyword">else</span>:</div><div class="line">        <span class="keyword">return</span> <span class="string">'&lt;pre&gt;Wrong Toen &lt;/pre&gt;'</span></div><div class="line"></div><div class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</div><div class="line">    app.run(port=<span class="number">5000</span>)</div></pre></td></tr></table></figure></p>
<p>这里需要说明的是，服务器端的用户需要有项目目录的读写权限，最好直接把目录的own设置为用户。 之后直接执行该文件即可: <figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">nohup python app.py <span class="number">2</span>&gt;&amp;<span class="number">1</span> &amp;</div></pre></td></tr></table></figure></p>
<p>这样就可以在后台执行，端口是5000.</p>
<ol start="5" style="list-style-type: decimal">
<li>设置nginx域名转发 这一步其实就是Nginx的转发，将5000端口转到80端口，这样就可以使用域名访问了，直接在Nginx的server 80的配置文件，加上如下的一项:</li>
</ol>
<p><figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">location /api &#123;</div><div class="line">    proxy_set_header    X-REAL-IP $remote_addr;</div><div class="line">    proxy_set_header    Host    $http_host;</div><div class="line">    proxy_pass                http://<span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span>:<span class="number">5000</span></div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<pre><code>这样就可以直接使用域名`http://yoursite.com/api/git_hook`访问了，记得重启下nginx服务。</code></pre>
<ol start="6" style="list-style-type: decimal">
<li>测试 服务器的服务启动之后， 使用webhook做个测试， Test的部分是coding自己的测试，没有携带token。 <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/coding-web-hook/result-1.png"></li>
</ol>
<h2 id="总结">总结</h2>
<p>这样就算完成了利用webhook的自动化部署的过程。 整体比较简单清晰。 这里需要提另外一个代码托管工具，<a href="https://about.gitlab.com/" target="_blank" rel="external">gitlab</a> 它除了github/coding.net的功能之外，提供了在自己服务器上搭建gitlab，同样可以拥有web管理的界面。 但是目前不直接用它的原因，就是这货比较吃配置，最小4G内存(包含SWAP)，而我现在的学生腾讯云服务器仅仅是1G内存，1核的CPU… 以后如果装备上去了 ，肯定会直接上gitlab. 一步到位。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;需求&quot;&gt;需求&lt;/h2&gt;
&lt;p&gt;一些代码托管网站比如coding.net/github.com 等都提供了webhook的工具， 经常用于自动化部署。 之前写过一篇博客&lt;a href=&quot;http://shomy.top/2016/04/14/git-server-hook/&quot;&gt;搭建git服务器以及web自动化部署&lt;/a&gt; 主要记录的是在自己服务器上如何搭建git服务，并且利用hook进行自动化部署。不过这种方式有个缺点，没有像github或者coding.net的web界面，管理起来可能稍微麻烦，因此我们还是习惯在github,coding.net上进行代码托管。然后再部署到服务器上，这样有些不方便。还好这些托管网站均提供了webhook的工具，功能与git的hook一致。
    
    </summary>
    
      <category term="工具使用" scheme="http://shomy.top/categories/%E5%B7%A5%E5%85%B7%E4%BD%BF%E7%94%A8/"/>
    
    
      <category term="hook" scheme="http://shomy.top/tags/hook/"/>
    
  </entry>
  
  <entry>
    <title>Python中tuple+=赋值的四个问题</title>
    <link href="http://shomy.top/2017/08/17/python-tuple-assign/"/>
    <id>http://shomy.top/2017/08/17/python-tuple-assign/</id>
    <published>2017-08-17T14:34:00.000Z</published>
    <updated>2017-08-21T04:16:27.489Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>最近偶尔翻看Fluent Python，遇到有意思的东西就记下来. 下面的是在PyCon2013上提出的一个关于<code>tuple</code>的Augmented Assignment也就是增量赋值的一个问题。 并且基于此问题, 又引申出3个变种问题.</p>
</blockquote>
<h2 id="问题">问题</h2>
<p>首先看第一个问题, 如下面的代码段: <figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">&gt;&gt;&gt; t = (<span class="number">1</span>,<span class="number">2</span>, [<span class="number">30</span>,<span class="number">40</span>])</div><div class="line">&gt;&gt;&gt; t[<span class="number">2</span>] += [<span class="number">50</span>,<span class="number">60</span>]</div></pre></td></tr></table></figure></p>
<a id="more"></a>
<p>会产生什么结果呢？ 给出了四个选项: 1. <code>t</code> 变成 <code>[1,2, [30,40,50,60]</code> 2. <code>TypeError is raised with the message 'tuple' object does not support item assignment</code> 3. Neither 1 nor 2 4. Both 1 and 2</p>
<p>按照之前的理解, <code>tuple</code>里面的元素是不能被修改的，因此会选<code>2</code>. 如果真是这样的话，这篇笔记就没必要了，Fluent Python中也就不会拿出一节来讲了。 正确答案是<code>4</code> <figure class="highlight ruby"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="meta">&gt;&gt;</span>&gt; t = (<span class="number">1</span>,<span class="number">2</span>,[<span class="number">30</span>,<span class="number">40</span>])</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; t[<span class="number">2</span>] += [<span class="number">50</span>,<span class="number">60</span>]</div><div class="line">Traceback (most recent call last)<span class="symbol">:</span></div><div class="line">  File <span class="string">"&lt;stdin&gt;"</span>, line <span class="number">1</span>, <span class="keyword">in</span> &lt;<span class="class"><span class="keyword">module</span>&gt;</span></div><div class="line"><span class="symbol">TypeError:</span> <span class="string">'tuple'</span> object does <span class="keyword">not</span> support item assignment</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; t</div><div class="line">(<span class="number">1</span>, <span class="number">2</span>, [<span class="number">30</span>, <span class="number">40</span>, <span class="number">50</span>, <span class="number">60</span>])</div></pre></td></tr></table></figure></p>
<p>问题来了，为什么异常都出来了， <code>t</code>还是变了? 再看第二种情况，稍微变化一下,将<code>+=</code>变为<code>=</code>: <figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">&gt;&gt;&gt; t = (<span class="number">1</span>,<span class="number">2</span>, [<span class="number">30</span>,<span class="number">40</span>])</div><div class="line">&gt;&gt;&gt; t[<span class="number">2</span>] = [<span class="number">50</span>,<span class="number">60</span>]</div></pre></td></tr></table></figure></p>
<p>结果就成酱紫了: <figure class="highlight ruby"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="meta">&gt;&gt;</span>&gt; t = (<span class="number">1</span>,<span class="number">2</span>, [<span class="number">30</span>,<span class="number">40</span>])</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; t[<span class="number">2</span>] = [<span class="number">50</span>,<span class="number">60</span>]</div><div class="line">Traceback (most recent call last)<span class="symbol">:</span></div><div class="line">  File <span class="string">"&lt;stdin&gt;"</span>, line <span class="number">1</span>, <span class="keyword">in</span> &lt;<span class="class"><span class="keyword">module</span>&gt;</span></div><div class="line"><span class="symbol">TypeError:</span> <span class="string">'tuple'</span> object does <span class="keyword">not</span> support item assignment</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; t</div><div class="line">(<span class="number">1</span>, <span class="number">2</span>, [<span class="number">30</span>, <span class="number">40</span>])</div></pre></td></tr></table></figure></p>
<p>再看第三种情况,只把<code>+=</code>换为<code>extend</code>或者<code>append</code>,: <figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">&gt;&gt;&gt; t = (<span class="number">1</span>, <span class="number">2</span>, [<span class="number">30</span>,<span class="number">40</span>])</div><div class="line">&gt;&gt;&gt; t[<span class="number">2</span>].extend([<span class="number">50</span>,<span class="number">60</span>])</div><div class="line">&gt;&gt;&gt; t</div><div class="line">(<span class="number">1</span>, <span class="number">2</span>, [<span class="number">30</span>, <span class="number">40</span>, <span class="number">50</span>, <span class="number">60</span>])</div><div class="line">&gt;&gt;&gt; t[<span class="number">2</span>].append(<span class="number">70</span>)</div><div class="line">&gt;&gt;&gt; t</div><div class="line">(<span class="number">1</span>, <span class="number">2</span>, [<span class="number">30</span>, <span class="number">40</span>, <span class="number">50</span>, <span class="number">60</span>, <span class="number">70</span>])</div></pre></td></tr></table></figure></p>
<p>又正常了,没抛出异常?</p>
<p>最后第四种情况, 用变量的形式: <figure class="highlight ruby"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line"><span class="meta">&gt;&gt;</span>&gt; a = [<span class="number">30</span>,<span class="number">40</span>]</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; t = (<span class="number">1</span>, <span class="number">2</span>, a)</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; a+=[<span class="number">50</span>,<span class="number">60</span>]</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; a</div><div class="line">[<span class="number">30</span>, <span class="number">40</span>, <span class="number">50</span>, <span class="number">60</span>]</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; t</div><div class="line">(<span class="number">1</span>, <span class="number">2</span>, [<span class="number">30</span>, <span class="number">40</span>, <span class="number">50</span>, <span class="number">60</span>])</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; t[<span class="number">2</span>] += [<span class="number">70</span>,<span class="number">80</span>]</div><div class="line">Traceback (most recent call last)<span class="symbol">:</span></div><div class="line">  File <span class="string">"&lt;stdin&gt;"</span>, line <span class="number">1</span>, <span class="keyword">in</span> &lt;<span class="class"><span class="keyword">module</span>&gt;</span></div><div class="line"><span class="symbol">TypeError:</span> <span class="string">'tuple'</span> object does <span class="keyword">not</span> support item assignment</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; t</div><div class="line">(<span class="number">1</span>, <span class="number">2</span>, [<span class="number">30</span>, <span class="number">40</span>, <span class="number">50</span>, <span class="number">60</span>, <span class="number">70</span>, <span class="number">80</span>])</div></pre></td></tr></table></figure></p>
<p>又是一种情况, 下面就探究一下其中的原因.</p>
<h2 id="原因">原因</h2>
<p>首先需要重温<code>+=</code>这个运算符,如<code>a+=b</code>:</p>
<ul>
<li>对于可变对象(mutable object)如<code>list</code>, <code>+=</code>操作的结果会直接在<code>a</code>对应的变量进行修改，而<code>a</code>对应的地址不变.</li>
<li>对于不可变对象(imutable object)如<code>tuple</code>, <code>+=</code>则是等价于<code>a = a+b</code> 会产生新的变量，然后绑定到<code>a</code>上而已.</li>
</ul>
<p>如下代码段, 可以看出来: <figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div></pre></td><td class="code"><pre><div class="line">&gt;&gt;&gt; a = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]</div><div class="line">&gt;&gt;&gt; id(a)</div><div class="line"><span class="number">53430752</span></div><div class="line">&gt;&gt;&gt; a+=[<span class="number">4</span>,<span class="number">5</span>]</div><div class="line">&gt;&gt;&gt; a</div><div class="line">[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>]</div><div class="line">&gt;&gt;&gt; id(a)</div><div class="line"><span class="number">53430752</span> # 地址没有变化</div><div class="line">&gt;&gt;&gt; b = (<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>)</div><div class="line">&gt;&gt;&gt; id(b)</div><div class="line"><span class="number">49134888</span></div><div class="line">&gt;&gt;&gt; b += (<span class="number">4</span>,<span class="number">5</span>)</div><div class="line">&gt;&gt;&gt; b</div><div class="line">(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>)</div><div class="line">&gt;&gt;&gt; id(b)</div><div class="line"><span class="number">48560912</span> # 地址变化了</div></pre></td></tr></table></figure></p>
<p>此外还需要注意的是, python中的<code>tuple</code>作为不可变对象, 也就是我们平时说的元素不能改变, 实际上从报错信息<code>TypeError: 'tuple' object does not support item assignment</code>来看, 更准确的说法是指其中的元素不支持赋值操作<code>=</code>(<strong>assignment</strong>).</p>
<p>先看最简单的第二种情况, 它的结果是符合我们的预期, 因为<code>=</code>产生了<code>assign</code>的操作.(在<a href="http://shomy.top/2016/03/01/python-namespace-1/">由一个例子到python的名字空间</a> 中指出了赋值操作<code>=</code>就是创建新的变量), 因此<code>s[2]=[50,60]</code>就会抛出异常.</p>
<p>再看第三种情况,包含<code>extend/append</code>的, 结果tuple中的列表值发生了变化,但是没有异常抛出. 这个其实也相对容易理解. 因为我们知道<code>tuple</code>中存储的其实是元素所对应的地址(id), 因此如果没有赋值操作且tuple中的元素的<code>id</code>不变,即可,而<code>list.extend/append</code>只是修改了列表的元素,而列表本身id并没有变化,看看下面的例子: <figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">&gt;&gt;&gt; a=(<span class="number">1</span>,<span class="number">2</span>,[<span class="number">30</span>,<span class="number">40</span>])</div><div class="line">&gt;&gt;&gt; id(a[<span class="number">2</span>])</div><div class="line"><span class="number">140628739513736</span></div><div class="line">&gt;&gt;&gt; a[<span class="number">2</span>].extend([<span class="number">50</span>,<span class="number">60</span>])</div><div class="line">&gt;&gt;&gt; a</div><div class="line">(<span class="number">1</span>, <span class="number">2</span>, [<span class="number">30</span>, <span class="number">40</span>, <span class="number">50</span>, <span class="number">60</span>])</div><div class="line">&gt;&gt;&gt; id(a[<span class="number">2</span>])</div><div class="line"><span class="number">140628739513736</span></div></pre></td></tr></table></figure></p>
<p>目前解决了第二个和第三个问题, 先梳理一下, 其实就是两点:</p>
<ul>
<li>tuple内部的元素不支持赋值操作</li>
<li>在第一条的基础上, 如果元素的<code>id</code>没有变化, 元素其实是可以改变的.</li>
</ul>
<p>现在再来看最初的第一个问题: <code>t[2] += [50,60]</code> 按照上面的结论, 不应该抛异常啊,因为在我们看来<code>+=</code> 对于可变对象<code>t[2]</code>来说, 属于<code>in-place</code>操作,也就是直接修改自身的内容, <code>id</code>并不变, 确认下id并没有变化: <figure class="highlight ruby"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div></pre></td><td class="code"><pre><div class="line"><span class="meta">&gt;&gt;</span>&gt; a=(<span class="number">1</span>,<span class="number">2</span>,[<span class="number">30</span>,<span class="number">40</span>])</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; id(a[<span class="number">2</span>])</div><div class="line"><span class="number">140628739587392</span></div><div class="line"><span class="meta">&gt;&gt;</span>&gt; a[<span class="number">2</span>]+=[<span class="number">50</span>,<span class="number">60</span>]</div><div class="line">Traceback (most recent call last)<span class="symbol">:</span></div><div class="line">  File <span class="string">"&lt;stdin&gt;"</span>, line <span class="number">1</span>, <span class="keyword">in</span> &lt;<span class="class"><span class="keyword">module</span>&gt;</span></div><div class="line"><span class="symbol">TypeError:</span> <span class="string">'tuple'</span> object does <span class="keyword">not</span> support item assignment</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; a</div><div class="line">(<span class="number">1</span>, <span class="number">2</span>, [<span class="number">30</span>, <span class="number">40</span>, <span class="number">50</span>, <span class="number">60</span>])</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; id(a[<span class="number">2</span>]) <span class="comment"># ID 并没有发生改变</span></div><div class="line"><span class="number">140628739587392</span></div></pre></td></tr></table></figure></p>
<p>跟第三个问题仅仅从<code>t[2].extend</code>改成了<code>t[2]+=</code>, 就抛出异常了,所以问题应该是出在<code>+=</code>上了. 下面用<code>dis</code>模块看看它俩执行的步骤: 对下面的代码块执行<code>dis</code>: <figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">t = (<span class="number">1</span>,<span class="number">2</span>, [<span class="number">30</span>,<span class="number">40</span>])</div><div class="line">t[<span class="number">2</span>] += [<span class="number">50</span>,<span class="number">60</span>]</div><div class="line">t[<span class="number">2</span>].extend([<span class="number">70</span>, <span class="number">80</span>])</div></pre></td></tr></table></figure></p>
<p>执行<code>python -m dis test.py</code>,结果如下，下面只保留第2,3行代码的执行过程，以及关键步骤的注释如下: <figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div></pre></td><td class="code"><pre><div class="line"><span class="number">2</span>          <span class="number">21</span> LOAD_NAME                <span class="number">0</span> (t)</div><div class="line">           <span class="number">24</span> LOAD_CONST               <span class="number">1</span> (<span class="number">2</span>)</div><div class="line">           <span class="number">27</span> DUP_TOPX                 <span class="number">2</span></div><div class="line">           <span class="number">30</span> BINARY_SUBSCR							</div><div class="line">           <span class="number">31</span> LOAD_CONST               <span class="number">4</span> (<span class="number">50</span>)</div><div class="line">           <span class="number">34</span> LOAD_CONST               <span class="number">5</span> (<span class="number">60</span>)</div><div class="line">           <span class="number">37</span> BUILD_LIST               <span class="number">2</span>			 </div><div class="line">           <span class="number">40</span> INPLACE_ADD</div><div class="line">           <span class="number">41</span> ROT_THREE</div><div class="line">           <span class="number">42</span> STORE_SUBSCR</div><div class="line"></div><div class="line"><span class="number">3</span>          <span class="number">43</span> LOAD_NAME                <span class="number">0</span> (t)</div><div class="line">           <span class="number">46</span> LOAD_CONST               <span class="number">1</span> (<span class="number">2</span>)</div><div class="line">           <span class="number">49</span> BINARY_SUBSCR</div><div class="line">           <span class="number">50</span> LOAD_ATTR                <span class="number">1</span> (extend)</div><div class="line">           <span class="number">53</span> LOAD_CONST               <span class="number">6</span> (<span class="number">70</span>)</div><div class="line">           <span class="number">56</span> LOAD_CONST               <span class="number">7</span> (<span class="number">80</span>)</div><div class="line">           <span class="number">59</span> BUILD_LIST               <span class="number">2</span></div><div class="line">           <span class="number">62</span> CALL_FUNCTION            <span class="number">1</span></div><div class="line">           <span class="number">65</span> POP_TOP</div><div class="line">           <span class="number">66</span> LOAD_CONST               <span class="number">8</span> (None)</div><div class="line">           <span class="number">69</span> RETURN_VALUE</div></pre></td></tr></table></figure></p>
<p>解释一下关键的语句:</p>
<ul>
<li><code>30 BINARY_SUBSCR</code>: 表示将<code>t[2]</code>的值放在TOS(Top of Stack)，这里是指<code>[30, 40]</code>这个列表</li>
<li><code>40 INPLACE_ADD</code>: 表示<code>TOS += [50,60]</code> 执行这一步是可以成功的，修改了TOS的列表为<code>[30,40,50,60]</code></li>
<li><code>42 STORE_SUBSCR</code>: 表示<code>s[2] = TOS</code> 问题就出在这里了，这里产生了一个<strong>赋值操作</strong>，因此会抛异常！但是上述对列表的修改已经完成, 这也就解释了开篇的第一个问题。</li>
</ul>
<p>再看<code>extend</code>的过程，前面都一样，只有这一行:</p>
<ul>
<li><code>62 CALL_FUNCTION</code>: 这个直接调用内置extend函数完成了对原列表的修改，其中并没有<code>assign</code>操作，因此可以正常执行。</li>
</ul>
<p>现在逐渐清晰了， 换句话说，<code>+=</code><strong>并不是原子操作</strong>，相当于下面的两步: <figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">t[<span class="number">2</span>].extend([<span class="number">50</span>,<span class="number">60</span>])</div><div class="line">t[<span class="number">2</span>] = t[<span class="number">2</span>]</div></pre></td></tr></table></figure></p>
<p>第一步可以正确执行，但是第二步有了<code>=</code>，肯定会抛异常的。 同样这也可以解释在使用<code>+=</code>的时候，为何<code>t[2]</code>的<code>id</code>明明没有变化，但是仍然抛出异常了。</p>
<p>现在用一句话总结下:</p>
<blockquote>
<p>tuple中元素不支持<code>assign</code>操作，但是对于那些是可变对象的元素如列表，字典等，在没有<code>assign</code>操作的基础上，比如一些<code>in-place</code>操作，是可以修改内容的</p>
</blockquote>
<p>可以用第四个问题来简单验证一下，使用一个指向<code>[30,40]</code>的名称<code>a</code>来作为元素的值，然后对<code>a</code>做<code>in-place</code>的修改，其中并没有涉及到对tuple的<code>assign</code>操作，那肯定是正常执行的。</p>
<h2 id="总结">总结</h2>
<p>这个问题其实以前也就遇到过，但是没想过具体的原理，后来翻书的时候又看到了， 于是花了点时间把这一个系列查了部分资料以及结合自己的理解都整理了出来, 算是饭后茶点吧, 不严谨的地方烦请指出.</p>
<p>部分参考如下:</p>
<ul>
<li><a href="http://bugs.python.org/issue11562" target="_blank" rel="external">python bugs</a></li>
<li><a href="https://docs.python.org/2/faq/programming.html#why-does-a-tuple-i-item-raise-an-exception-when-the-addition-works" target="_blank" rel="external">python faq</a></li>
<li><a href="https://stackoverflow.com/questions/10397121/why-does-of-a-list-within-a-python-tuple-raise-typeerror-but-modify-the-list" target="_blank" rel="external">stackoverflow</a></li>
<li>Fluent Python</li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;最近偶尔翻看Fluent Python，遇到有意思的东西就记下来. 下面的是在PyCon2013上提出的一个关于&lt;code&gt;tuple&lt;/code&gt;的Augmented Assignment也就是增量赋值的一个问题。 并且基于此问题, 又引申出3个变种问题.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id=&quot;问题&quot;&gt;问题&lt;/h2&gt;
&lt;p&gt;首先看第一个问题, 如下面的代码段: &lt;figure class=&quot;highlight lsl&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;2&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;&amp;gt;&amp;gt;&amp;gt; t = (&lt;span class=&quot;number&quot;&gt;1&lt;/span&gt;,&lt;span class=&quot;number&quot;&gt;2&lt;/span&gt;, [&lt;span class=&quot;number&quot;&gt;30&lt;/span&gt;,&lt;span class=&quot;number&quot;&gt;40&lt;/span&gt;])&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&amp;gt;&amp;gt;&amp;gt; t[&lt;span class=&quot;number&quot;&gt;2&lt;/span&gt;] += [&lt;span class=&quot;number&quot;&gt;50&lt;/span&gt;,&lt;span class=&quot;number&quot;&gt;60&lt;/span&gt;]&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;&lt;/p&gt;
    
    </summary>
    
      <category term="编程语言" scheme="http://shomy.top/categories/%E7%BC%96%E7%A8%8B%E8%AF%AD%E8%A8%80/"/>
    
    
      <category term="python" scheme="http://shomy.top/tags/python/"/>
    
  </entry>
  
  <entry>
    <title>Hexo-admin后台管理博客</title>
    <link href="http://shomy.top/2017/07/29/Hexo-admin-for-blogs/"/>
    <id>http://shomy.top/2017/07/29/Hexo-admin-for-blogs/</id>
    <published>2017-07-29T07:38:00.000Z</published>
    <updated>2017-08-15T00:52:12.317Z</updated>
    
    <content type="html"><![CDATA[<h2 id="需求">需求</h2>
<p>像Hexo这种静态博客用起来很方便，但是硬伤就是没有后台管理，每次想写博客或者说想要修改原博客的话，都需要在本机上找markdown文件，修改之后发布，尤其是如果机器上没有安装hexo,git等工具的时候，就无法修改了。因此hexo可不可以以像wp,ghost这种有后台管理, 随时随地编辑博客呢? <a id="more"></a></p>
<p>正巧hexo专门有一个插件<a href="https://jaredforsyth.com/hexo-admin/" target="_blank" rel="external">hexo-admin</a> 提供了在web UI下增删改查博客的功能，我也正好有一个VPS, 于是就有了下面的一种方法.</p>
<h2 id="解决方法">解决方法</h2>
<p>首先说一下条件与需求。 我自己有一个的VPS，本来是可以直接将hexo部署到VPS的，但是考虑到自己维护的VPS的稳定性等问题，最后决定将hexo的静态页面部署github-pages上。</p>
<p>我的目的有两个:</p>
<ul>
<li>拥有在线编辑博客的后台</li>
<li>本地机器同时也可以编辑发布博客</li>
<li>二者需要保持同步</li>
</ul>
<p>基于这样的需求，仅仅靠github pages显然不可行了,必须借助VPS了.保持同步的意思是VPS的内容与本地机器上的博客的资源要保持一致。就是说我在线修改了某篇博客，然后之后如果可以很简单的同步到本地，使得两端保持同步，反过来一样。</p>
<p>我的做法是:</p>
<ul>
<li>将hexo目录下的相关文件包括_config.yml,source/等文件建立一个私人repo(个人使用coding.net)</li>
<li>VPS上安装好hexo,具体过程与换台机器安装同理, 可参考这篇博客<a href="http://shomy.top/2017/01/27/hexo-migrate-solution/">hexo迁移重装</a>。</li>
<li>VPS上安装hexo-admin插件，这样可以在线编辑了. 在这一步的时候, 为了方便,我用了一个子域名以及在VPS上用nginx做前段端口转发.</li>
</ul>
<p>到这里后台管理基本没有问题了. 接下来就是在线发布了. 正巧的是hexo-admin提供了这个功能, 有一个Deploy的功能,具体实现是使用nodejs的<code>spawn</code>函数来执行发布脚本的. 一开始不太懂<code>spawn</code>这个函数的使用, 就直接在deploy中随便输入了东西,结果各种 spawn err. 后来查过资料,才知道deploy的输入的位置其实是对<code>hexo-admin</code>的配置里面<code>deployCommand</code>的选项,可以为空. 打个比方,如果我在<code>deployCommand</code>写的是<code>./hexo-deploy</code>, 然后点击deploy按钮之后, 后台就可以执行<code>hexo-deploy</code>脚本的内容了. 有了这个接口就非常灵活了. 因为基于此, 我们不但来发布博客,甚至可以把同步的脚本加进去.具体参考 https://github.com/jaredly/hexo-admin/issues/70</p>
<p>下面说一下发布以及同步的操作:</p>
<ul>
<li>在博客目录写一个有关git同步的小脚本:<code>server-upload.sh</code> 用来同步VPS目录下的变化，主要代码其实就是先<code>git pull</code> 拉取repo最新, 之后在push. 具体脚本代码不再赘述.</li>
<li><p>建一个<code>hexo-deploy</code>的脚本,内容: <figure class="highlight stata"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">hexo <span class="keyword">g</span> &amp;&amp; hexo <span class="built_in">d</span></div><div class="line">./server-upload.<span class="keyword">sh</span></div></pre></td></tr></table></figure></p></li>
<li><p>利用hexo-admin的deploy功能，可以在线发布，注意修改<code>_config.yml</code>: 加上hexo-admin的admin选项, 加一个<code>deployCommand: ./hexo-deploy</code>的字段,如下: <figure class="highlight less"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="attribute">admin</span>:</div><div class="line">	<span class="attribute">username</span>: XXX</div><div class="line">    <span class="attribute">password</span>: XXXXX</div><div class="line">    <span class="attribute">deployCommand</span>: <span class="string">'./hexo-deploy'</span></div></pre></td></tr></table></figure></p></li>
<li>编辑完博客时候，发布的时候，需要点击hexo-admin的deploy。这样在后台就可以执行前面定义的<code>hexo-deloy</code>脚本了</li>
<li><p>本地机器也需要有一个<code>upload.sh</code>的脚本, 每次同样需要先git pull 在git push 到私人repo. 其实就是版本控制那一套.</p></li>
</ul>
<p>到现在基本完成了自己可以随时随地编辑博客并且多端同步的需求了. 之前也考虑过CI持续集成,但是大部分都是再建立一个分支, 需要把博客的source公开, 不太合适. 所以就选择了hexo-admin这种方法.</p>
<p>这篇博客便是在线完成的. <img src="http://7xotye.com1.z0.glb.clouddn.com/blog/hexo-admin/hexo-admin.png"></p>
<p>其实这里面的坑还是蛮多的(主要不熟悉node). 此外开始使用supervisor做后台运行监控, 结果不知为何, 在<code>hexo d</code>包括git的pull或者push等操作,都会报错,提示让我确认git用户, 但是我已经设置了<code>git config</code>的email和name. 查了很多资料也没有解决, 后来直接抛弃supervisor, 直接使用<code>nohup</code>来放后台运行了.每个人的需求肯定都不同，这里只是个人的一个思路。大概是这样了, 有什么问题可以留言或者邮件.</p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;需求&quot;&gt;需求&lt;/h2&gt;
&lt;p&gt;像Hexo这种静态博客用起来很方便，但是硬伤就是没有后台管理，每次想写博客或者说想要修改原博客的话，都需要在本机上找markdown文件，修改之后发布，尤其是如果机器上没有安装hexo,git等工具的时候，就无法修改了。因此hexo可不可以以像wp,ghost这种有后台管理, 随时随地编辑博客呢?
    
    </summary>
    
      <category term="工具使用" scheme="http://shomy.top/categories/%E5%B7%A5%E5%85%B7%E4%BD%BF%E7%94%A8/"/>
    
    
      <category term="hexo" scheme="http://shomy.top/tags/hexo/"/>
    
  </entry>
  
  <entry>
    <title>Word2vec数学原理全家桶</title>
    <link href="http://shomy.top/2017/07/28/word2vec-all/"/>
    <id>http://shomy.top/2017/07/28/word2vec-all/</id>
    <published>2017-07-28T07:42:21.000Z</published>
    <updated>2017-07-31T02:42:34.566Z</updated>
    
    <content type="html"><![CDATA[<p>目前需要做Network Embedding方面的课题，而复杂网络本身就经常借鉴NLP的一些算法模型, Embedding也不例外. 因此先从Word Embedding入手。之前对Word Embedding(暂且翻译为词嵌入或者词向量)的理解就是将单词根据某种特征转为数值向量，再来做其他工作比如文本分类的工作。而word2vec则是word embedding的一种模型，也是目前使用最广的词向量模型, 由Google的Mikolov团队2013年提出。之前仅仅能够使用第三方库来训练直接使用, 对其中原理并没有多少理解, 这篇博客则比较完整的从背景知识到原理，参数训练等方面整理一下word2Vec。</p>
<a id="more"></a>
<p>Mikolov的两篇文章中涉及word2vec的细节甚少. 有不少人都对此模型作出了更加详细的解释, 本文主要沿着Rong, X.word2vec Parameter Learning Explained这篇文章的思路来整理一下，很多公式参考于这篇文章。</p>
<p>参考:</p>
<ul>
<li>Mikolov, T.(2013). Distributed Representations of Words and Phrases and their Compositionality.</li>
<li>Mikolov, T.(2013). Efficient Estimation of Word Representations in Vector Space.</li>
<li>Rong, X. (2014). word2vec Parameter Learning Explained.</li>
</ul>
<h2 id="背景">背景</h2>
<h3 id="神经网络">神经网络</h3>
<p>引入word2vec之前需要先对神经网络的知识有一定了解, 这里只贴一张图说明一个简单三层神经网络, 具体的细节不再赘述, 可以参考之前的一篇专门介绍神经网络的博文<a href="http://shomy.top/2016/12/07/basic-neural-network/">机器学习技法笔记(1)-神经网络</a> ; 如下图, <span class="math inline">\(x=[x_1, x_2,...x_K]\)</span>是模型的输入, 中间经过与权重矩阵<span class="math inline">\(\{w_{ki}\}\)</span> 运算, 矩阵运算结果再经过非线性激活函数得到隐层的结果<span class="math inline">\(h\)</span>, 从隐层到输出层同理. 这样从输入层到输出层既有线性变换,又有非线性变换, 因此可以更好刻画出输入变量的特征.</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/w2v/nn1.png">

</div>
<h3 id="神经语言模型">神经语言模型</h3>
<p>作为Word Embedding的背景, 语言模型(Language Model)也是很有必要简要介绍一下.</p>
<blockquote>
<p>统计语言模型就是用来计算一个句子的概率分布</p>
</blockquote>
<p>简单来说,就是计算一个句子的概率, 语言模型用处很广泛,比如机器翻译中, 如何挑选一个概率尽可能大的句子也就是尽量靠谱的句子返回. 假设一个长度为<span class="math inline">\(m\)</span>的句子,包含词:<span class="math inline">\([(w_1, w_2, w_3,..,w_m)\)</span>, 那么这个句子的概率,也就是这m个词共现的概率:</p>
<p><span class="math display">\[P(sen=(w_1, w_2, .., w_m)) = P(w_1)P(w_2|w_1)P(w_3|w_2,w_1)...P(w_m|w_{m-1}..w_1)\]</span></p>
<p>一般情况, 语言模型都是为了使得条件概率: <span class="math inline">\(P(w_t|w_1,w_2,..,w_{t-1})\)</span>最大化, 不过考虑到近因效应, 当前词与距离它比较近的几个词更加相关,而非前面所有的词都有关, 因此上述公式可以近似为:</p>
<p><span class="math display">\[P(w_t|w_1,w_2,..,w_{t-1})=P(w_t|w_{t_1}, w_{t_2}...w_{t_n+1})\]</span></p>
<p>上述便是经典的<code>n-gram</code>模型的近似表示方式. 下面需要介绍一下神经语言模型(NNLM), 最初由Bengio提出的A Neural Probabilistic Language Mode最为经典, word2vec便是从其中简化训练而来. Bengio通过下面的一个三层神经网络来计算<span class="math inline">\(P(w_t|w_{t_1}, w_{t_2}...w_{t_n+1})\)</span>:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/w2v/nnlm1.png">

</div>
<p>首先第一层输入就是前<span class="math inline">\(t-1\)</span>个词<span class="math inline">\(w_{t_n+1},...,w_{t-1}\)</span> 去预测第<span class="math inline">\(t\)</span>个词是<span class="math inline">\(w_t\)</span> 的概率. 这里面的矩阵<span class="math inline">\(C\in |V| \times d\)</span>维护着词汇表中所有词的词向量, 其中<span class="math inline">\(|V|\)</span>是词汇表中词的个数, <span class="math inline">\(d\)</span>是词向量的维度. 然后根据输入的前<span class="math inline">\(n-1\)</span>个词, 在C中找到它们对应的词向量, 然后直接串联起来成为一个维度为<span class="math inline">\((n-1)d\)</span>的向量x 作为接下来三层神经网络的输入, 后面就是普通神经网络了. 需要说明的是,因为我们那要预测概率最大的<span class="math inline">\(w_t\)</span>, 因此最后输出层的神经元应该与词汇表大小同样为<span class="math inline">\(|V|\)</span>, 这里使用使用softmax函数归一化输出层的值到[0,1], 代表可能的每个词的概率. 此外在原文中, 存在一些直连边, 也就是上图中的虚线, 从输入层直接到输出层, 是一个线性变换, Bingo在文中表示, 直连边的存在大幅降低迭代次数, 但对语言模型效果无提升, 随着计算能力的提高, 后续的工作基本都去掉了直连边.</p>
<p>神经语言模型构建完成之后,就是训练参数了. 这里的参数包括词向量矩阵<span class="math inline">\(C\)</span>, 以及三层神经网络的权重, 偏置等参数. 训练数据就是大堆大堆的预料库. 训练结束之后, 语言模型得到了, 词向量也得到了. 换言之, 词向量是这个语言模型的副产品. 但是这个模型的缺点就是速度问题, 因为词汇表往往很大,几十万几百王, 训练起来就很耗时, Bengo仅仅训练5个epoch就花了3周, 这还是40个CPU并行训练的结果. 因此才会有了后续好多的优化工作, word2vec便是其中一个.</p>
<h2 id="word2vec">Word2Vec</h2>
<h3 id="简介">简介</h3>
<p>背景介绍完毕, 终于到主角了. word2vec是google于2013年的Distributed Representations ofWords and Phrases and their Compositionality 以及后续的Distributed Representations ofWords and Phrases and their Compositionality 两篇文章中提出的一种高效训练词向量的模型, 基本出发点是<strong>上下文相似的两个词,它们的词向量也应该相似</strong>, 比如香蕉和梨在句子中可能经常出现在相同的上下文中，因此这两个词的表示向量应该就比较相似.</p>
<p>word2vec模型中比较重要的概念是词汇的上下文, 说白了就是一个词周围的词, 比如<span class="math inline">\(w_t\)</span>的范围为1的上下文就是<span class="math inline">\(w_{t-1}\)</span>和<span class="math inline">\(w_{t+1}\)</span>. 在word2vec中提出两个模型(假设上下文窗口为3)</p>
<ul>
<li>CBOW(Continuous Bag-of-Word): 以上下文词汇预测当前词: <span class="math inline">\(w_{t-1}, w_{t+1}\)</span> 去预测 <span class="math inline">\(w_t\)</span></li>
<li>SkipGram: 以当前词预测其上下文词汇: <span class="math inline">\(w_t\)</span> 去预测<span class="math inline">\(w_{t-1}, w_{t+1}\)</span></li>
</ul>
<p>两个模型图示如下</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/w2v/word2vec-1.png">

</div>
<p>下面将会从最简单的上下文只有一个词的情形入手, 然后扩展到CBOW以及Skip-gram, 介绍原理以及参数训练过程. 关于word2vec的训练这里将会从完全的BP神经网络的过程来介绍.</p>
<h3 id="one-word-model">One-Word Model</h3>
<p>首先先看简化版入手: 输入输出都只有一个词, 如下图示:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/w2v/one-word.png">

</div>
<p>首先说明符号:</p>
<ul>
<li><p><span class="math inline">\(V\)</span>: 词汇表长度; <span class="math inline">\(N\)</span>: 隐层神经元个数, 同时也是词向量维度</p></li>
<li><p><span class="math inline">\(W \in \mathcal{R}^{V\times N}\)</span>: 输入层到隐层的权重矩阵, 其实就是词向量矩阵,其中每一行代表一个词的词向量</p></li>
<li><p><span class="math inline">\(W^{&#39;} \in \mathcal{R}^{N\times V}\)</span>: 隐层到输出层的权重矩阵, 其中每一列也可以看作额外的一种词向量</p></li>
</ul>
<p>​</p>
<p>下面从神经网络的前向过程开始介绍:</p>
<p>我们需要做的是用输入的词去预测输出的词. 其中 输入层的单词<span class="math inline">\(w_I\)</span>使用<code>one-hot</code>来表示的, 即在上图中<span class="math inline">\(x_1, x_2, x_3,...,x_V\)</span>只有<span class="math inline">\(x_k\)</span>为1, 其余为0, 其中k可以是输入的词在词汇表中的索引下标. 之后就是经过词向量矩阵<span class="math inline">\(W\)</span> 连接输入层和隐层. 其中由于X中只有一个1, 因此经过与W相乘, 相当于取出W中的的第k行，实际也就是输入单词的<span class="math inline">\(w_I\)</span>的<span class="math inline">\(N\)</span>维的词向量，使用<span class="math inline">\(v_{w_I}\)</span>表示，来作为隐层的值，注意<strong>word2vec的隐层并没有激活函数</strong>:</p>
<p><span class="math display">\[\mathbf{h} = W^T \cdot X = v_{w_I}^T \]</span></p>
<p>然后考虑从隐层的<span class="math inline">\(\mathbf{h}\)</span>到输出层<span class="math inline">\(Y\)</span>, 同样h经过矩阵<span class="math inline">\(W^{&#39;}\)</span>相乘，得到一个<span class="math inline">\(V \times 1\)</span>的向量<span class="math inline">\(\mathbf{u}\)</span>:</p>
<p><span class="math display">\[\mathbf{u} = W^{&#39;T} \cdot h\]</span></p>
<p>其中<span class="math inline">\(u\)</span>每个元素<span class="math inline">\(u_j\)</span> 就是<span class="math inline">\(W^{&#39;}\)</span>的第<span class="math inline">\(j\)</span>列用<span class="math inline">\(v^{&#39;}_{w_j}\)</span>表示, 与<span class="math inline">\(h\)</span>做内积得到: <span class="math inline">\(u_j = v_{w_O}^{&#39;T}\cdot h\)</span>，含义就是词汇表中第<span class="math inline">\(j\)</span>个词的分数，我们的目的就是要根据输入词<span class="math inline">\(w_I\)</span>去预测输出的词，因此预测的词就取分数最高的即可，这里为了方便概率表示，使用<code>softmax</code>将<span class="math inline">\(\mathbf{u}\)</span>归一化到[0,1]之间, 从而作为输出词的概率, 其实是一个多项分布, 也就是上图中的<span class="math inline">\(y\)</span>:</p>
<p><span class="math display">\[P(w_j|w_I) = y_j = \frac{\exp(u_j)}{\sum\limits_{k\in V} \exp(u_k)} =   \frac{\exp(v_{w_j}^{&#39;T}\cdot v_{w_I})}{\sum\limits_{k\in V} \exp(v_{w_k}^{&#39;T}\cdot v_{w_I})}\]</span></p>
<p>其中<span class="math inline">\(v_{w}\)</span>与<span class="math inline">\(v_{w}^{&#39;}\)</span> 都称为词<span class="math inline">\(w\)</span>的词向量，一般使用前者作为词向量，而非后者，原因后续会解释。至此前向过程完成，就是给定一个词作为输入，来预测它的上下文词，还是比较简单的，属于简化版的神经语言模型。这个过程中需要用到的参数有两个词向量矩阵<span class="math inline">\(W, W^{&#39;}\)</span>，下面就是重点了，介绍如何根据语料库来训练模型，更新参数，得到最终的词向量。</p>
<hr>
<p>首先明确训练数据的格式，对于一个训练样本(<span class="math inline">\(w_I\)</span>, <span class="math inline">\(w_O\)</span>)，输入是词<span class="math inline">\(w_I\)</span>的<code>one-hot</code> 的维度为<span class="math inline">\(V\)</span>的向量<span class="math inline">\(\mathbf{x}\)</span>，模型预测的输出同样也是一个维度为<span class="math inline">\(V\)</span>的向量<span class="math inline">\(\mathbf{y}\)</span>, 同时真实值<span class="math inline">\(w_O\)</span>也是用one-hot表示，记为<span class="math inline">\(\mathbf{t}=[0,0,0,...1,0,0]\)</span>，其中假设<span class="math inline">\(t_{j^{*}} = 1\)</span>, 也就是说<span class="math inline">\(j^{*}\)</span>是真实单词在词汇表中的下标，那么根据最大似然或者上面的语言模型，目标函数可以定义如下:</p>
<p><span class="math display">\[\begin{align*} O &amp;= \max P(w_O|w_I) \\ &amp; = \max y_{j^{*}} :=  \max \log y_{j^{*}} \\ &amp;= \max \log(\frac{\exp(u_{j^{*}})}{\sum  \exp(u_k)}) = \max u_{j^{*}}-\log\sum_{k=1}^{V}\exp( u_k) \end{align*}\]</span></p>
<p>一般我们习惯于最小化损失函数，因此定义损失函数:</p>
<p><span class="math display">\[E = -u_{j^{*}}+\log\sum_{k=1}^{V}\exp( u_k) \]</span></p>
<p>然后结合反向传播一层层求梯度，使用梯度下降来更新参数。</p>
<p>先求隐层到输出层的向量矩阵<span class="math inline">\(W^{&#39;}\)</span>的梯度:</p>
<p><span class="math display">\[\frac{\partial E}{ \partial w{&#39;}_{ij}} = \frac{\partial E}{\partial u_j} \frac{\partial u_j}{\partial w^{&#39;}_{ij}} = (y_j-t_j) h_i\]</span></p>
<p>这里面的<span class="math inline">\(y_j\)</span>和<span class="math inline">\(t_j\)</span>分别是预测和真实值的第j项，<span class="math inline">\(h_i\)</span>是隐层的第<span class="math inline">\(i\)</span>项。</p>
<blockquote>
<p>考虑<span class="math inline">\(\frac{\partial E}{\partial u_j} = y_j - t_j\)</span>: 直接对原始求导，如下:</p>
<p>先考虑E的对数部分: <span class="math inline">\(\frac{\partial \log \sum\exp(u_k)}{\partial u_j} = \frac{\exp(u_j)}{\sum \exp(u_k)} = y_j\)</span></p>
<p>再看 $ -u_{j^{<em>}}<span class="math inline">\(对\)</span>u_j<span class="math inline">\(的梯度: 当\)</span>j = j^{</em>}<span class="math inline">\(时，\)</span>=-1 = -t_j<span class="math inline">\(, 反之二者不等时，\)</span>=0= -t_j$</p>
<p>所以综合求导<span class="math inline">\(\frac{\partial E}{\partial u_j} = y_j - t_j\)</span>. 这个减法可以理解为输出层的第<span class="math inline">\(j\)</span>项为预测值与真实值的差</p>
</blockquote>
<p>因此梯度下降更新公式为:</p>
<p><span class="math display">\[w^{&#39;}_{ij} = {w^{&#39;}_{ij}}^{(old)} -\eta (y_j - t_j) h_i\]</span></p>
<p>整合为<span class="math inline">\(W^{&#39;}\)</span>的列向量<span class="math inline">\(\mathbf{v^{&#39;}}_{w_j} = \{w^{&#39;}_{ij}| i=1,2,3,...,N\}\)</span>的形式如下:</p>
<p><span class="math display">\[\mathbf{v}^{&#39;}_{w_j} = {\mathbf{v}^{&#39;}_{w_j}}^{(old)} - \eta(y_j-t_j)\mathbf{h},  \ j \in \{ 1,2,3,...,V\}\]</span></p>
<p>也就是说对每个训练样本都需要做一次复杂度为<span class="math inline">\(V\)</span>的操作去更新<span class="math inline">\(W^{&#39;}\)</span>.</p>
<p>接着考虑隐层<span class="math inline">\(\mathbf{h}\)</span>的更新，其实也是输入层到隐层的矩阵<span class="math inline">\(W\)</span>的更新，继续反向传播，跟神经网络的相同, 输出层的<span class="math inline">\(V\)</span>个神经元都会影响<span class="math inline">\(h_i\)</span>:</p>
<p><span class="math display">\[\frac{\partial E}{\partial h_i} = \sum_{j=1}^{V}\frac{\partial E}{\partial u_j}  \frac{\partial u_j}{\partial h_i} = \sum_{j=1}^{V} (y_j-t_j)w^{&#39;}_{ij} =W^{&#39;}_{i} \cdot P\]</span></p>
<p>其中<span class="math inline">\(W^{&#39;}_{i}\)</span>是<span class="math inline">\(W^{&#39;}\)</span>的第i行, 这里为了方便书写, 令<span class="math inline">\(P = \{ y_j - t_j | j= 1,2,3,..,V\}\)</span>, 因此整合成整个隐层的向量<span class="math inline">\(\mathbf{h}\)</span>:</p>
<p><span class="math display">\[\frac{\partial E}{\partial \mathbf{h}} =  W^{&#39;} \cdot P\]</span></p>
<p>得到一个N维的向量，上面已经介绍过，<span class="math inline">\(\mathbf{h}\)</span>就是词向量矩阵<span class="math inline">\(\mathbf{W}\)</span>的一行: $ = W^T X = v_{w_I}^T <span class="math inline">\(, 但是因为X中只有一个1，因此每次只能更新\)</span><span class="math inline">\(的一行\)</span>v_{w_I}<span class="math inline">\(,其余行的梯度\)</span> = 0<span class="math inline">\(, 所以\)</span>v_{w_I}$的更新公式为:</p>
<p><span class="math display">\[v_{w_I}^T = v_{w_I}^T - \eta  W^{&#39;} \cdot P\]</span></p>
<p>到此为止， 一个训练样本的反向传播训练过程就为止了。 我们可以看到，对于输入层到隐层的矩阵<span class="math inline">\(W\)</span> 我们每次训练只需要更新一行向量即可，而对于隐层到输出层的矩阵<span class="math inline">\(W^{&#39;}\)</span>的所有<span class="math inline">\(N \times V\)</span>个元素都需要更新一遍，这里的计算量还是很大的。</p>
<p>这一节主要比较细致的介绍了最简单的输入输出只有一个单词的情况的推理和训练的过程，后面的CBOW(上下文预测单词)以及SG(单词预测上下文)均基于这一节扩展开来。</p>
<h3 id="cbow-model">CBOW Model</h3>
<p>这一部分讲word2vec的第一个形式: Continurous Bag-Of-Word，模型图示如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/w2v/cbow-1.png">

</div>
<p>跟上一个模型唯一的不同就是输入不再是一个词<span class="math inline">\(w_I\)</span>, 而是多个词，上图中一共有C个单词: <span class="math inline">\(x_{1k},x_{2k},...,x_{Ck}\)</span>，每个<span class="math inline">\(x\)</span>都是one-hot表示。 这样隐层的<span class="math inline">\(\mathbf{h}\)</span>的计算就会不同了: 之前一个单词的模型是直接取出<span class="math inline">\(W\)</span>的一行<span class="math inline">\(v_{w_I}\)</span>作为<span class="math inline">\(h\)</span>的值，在CBOW中则是取出<span class="math inline">\(W\)</span>中输入的所有C个单词的词向量，然后直接取平均，如下:</p>
<p><span class="math display">\[\begin{align*} \mathbf{h} &amp;= \frac{1}{C} W^T(x_{1} +x_2+...+x_{C}) \\ &amp;= \frac{1}{C}(v_{w_1} + v_{w_2}+...+v_{w_C})^T\end{align*}\]</span></p>
<p>后面隐层到输出层的过程与One-Word Model 一模一样，包括目标函数定义， 反向传播训练等。将<span class="math inline">\(W^{&#39;}\)</span>的更新公式照抄下来如下,依旧是每次都需要更新所有的行:</p>
<p><span class="math display">\[\mathbf{v}^{&#39;}_{w_j} = {\mathbf{v}^{&#39;}_{w_j}}^{(old)} - \eta(y_j-t_j)\mathbf{h},  \ j \in \{ 1,2,3,...,V\}\]</span></p>
<p>隐层神经元的梯度也相同:</p>
<p><span class="math display">\[\frac{\partial E}{\partial \mathbf{h}} =  W^{&#39;} \cdot P\]</span></p>
<p>下面考虑输入层到隐层稍微有些不同，在One-Word Model里面因为输入只有一个词，因此每次训练只更新这个词对应到<span class="math inline">\(W\)</span>的那一行，但是在CBOW里面有多个词，这里采取的策略是将<span class="math inline">\(\mathbf{h}\)</span>的梯度均摊到每个词上，因此每次训练会更新<span class="math inline">\(W\)</span>中的C行，如下:</p>
<p><span class="math display">\[v_{w_{I,c}}^T = v_{w_{I,c}}^T - \frac{1}{C}\ \eta  W^{&#39;} \cdot P,\ \ c=1,2,...,C\]</span></p>
<p>到此为止 CBOW 的推理和训练过程也介绍完毕，基本跟One-Word Model 一样。</p>
<h3 id="skipgram-model">SkipGram Model</h3>
<p>现在开始介绍word2vec的第二种形式: SkipGram(根据单词预测上下文)，这个模型与One-Word Model不同的地方在于，SG的输出有多个词，而非One-Word 中输出只有一个词，这样输出层就不是一个多项分布了，而是<span class="math inline">\(C\)</span>个多项分布了，模型图示如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/w2v/sg-1.png">

</div>
<p>因此从输入层到隐层部分与One-Word Model 相同，隐层神经元的计算方式如下:</p>
<p><span class="math display">\[\mathbf{h} = W^T \cdot X = v_{w_I}^T \]</span></p>
<p>因为输出层是有C个单词， 因此有C个多项分布: <span class="math inline">\(y_{1}, y_{2}...y_{C}\)</span>, 因此前向计算的过程也需要分开计算，如下公式，用来计算第<span class="math inline">\(c\)</span>个输出单词的预测的多项分布中第j项，相比One-Word Model 多了一个c参数:</p>
<p><span class="math display">\[P(w_{c,j}|w_I) = y_{c,j} = \frac{\exp (u_{c,j})}{\sum_{k=1}^{V} \exp(u_{c,k})}\]</span></p>
<p>需要主要的是这<span class="math inline">\(C\)</span>个输出向量是相互独立的，可以当做是独立的<span class="math inline">\(C\)</span>个One-Word Model 中的输出向量，相互之间没有影响，并且从图中也可以看出，连接隐层与C个输出层的参数矩阵<span class="math inline">\(W^{&#39;}\)</span>是共享的，于是便有: <span class="math inline">\(u_{c,j} = u_{j} ={v^{&#39;}_{w_j}}^T \cdot \mathbf{h}\)</span> 这里的<span class="math inline">\(v^{&#39;}_{w_j}\)</span>的含义与One Word Model 中相同，都代表<span class="math inline">\(W^{&#39;}\)</span>的第j列，同时也是词汇表中第j个单词的一种词向量(虽然实际中不用)。</p>
<p>从前向后 根据上述公式计算出C个输出向量之后，在每个<span class="math inline">\(V\)</span>维向量中选取概率最大的作为输出的单词，这样根据输出单词<span class="math inline">\(w_I\)</span> 就得到了<span class="math inline">\(C\)</span>个输出单词，也就达到了根据单词预测上下文的目的。</p>
<p>下面开始介绍SG的反向传播训练的过程，这个跟前面的有些许的不同, 首先是损失函数:</p>
<p><span class="math display">\[\begin{align*}E &amp;= -\log P(w_{1},w_{2},...,w_{C}| w_I) \\ &amp;=  - \log  \Pi_{c=1}^{C}P(w_c|w_i) \\ &amp;=-\log \Pi_{c=1}^{C} \frac{\exp (u_{c,j})}{\sum_{k=1}^{V} \exp(u_{c,k})} \\ &amp;= -\sum_{c=1}^{C}u_{j_c^{*}} + C \cdot \log \sum_{k=1}^{V} \exp(u_{k}) \end{align*}\]</span></p>
<p>前面说过输出的C个词是相互独立，因此<span class="math inline">\(P(w_1,w_2,...,w_C|W_I) = \Pi P(w_c|w_I)\)</span>, 此外<span class="math inline">\(j_c^{*}\)</span>的含义同One-Word Model 中的<span class="math inline">\(u_{j^{*}}\)</span> 一样，都代表训练的真实的输出单词在词汇表的下标。下面开始从后向前求梯度，对第c个词对应的多项分布的第j项的梯度:</p>
<p><span class="math display">\[\frac{\partial E}{\partial u_{c,j}} = y_{c,j} - t_{c,j}\]</span></p>
<p>然后考虑<span class="math inline">\(W^{&#39;}\)</span>的梯度，考虑到C个多项分布产生影响，因此需要求和:</p>
<p><span class="math display">\[\frac{\partial E}{\partial w^{&#39;}_{ij}}= \sum_{c=1}^{C}\frac{\partial E}{\partial u_{c,j}}  \frac{\partial u_{c,j}}{\partial w_{ij}^{&#39;}} =\sum_{c=1}^{C}(y_{c,j}-t_{c,j})\mathbf{h_{i}} = Q_j \mathbf{h_i}\]</span></p>
<p>跟CBOW一样，为了方便书写定义<span class="math inline">\(Q_j = \sum_{c=1}^{C}(y_{c,j} - t_{c,j}), \ j = 1,2,3,...,V\)</span>，矩阵Q的维度是$V1 $</p>
<p>有了梯度，就可以利用梯度下降 更新<span class="math inline">\(w_{ij}^{&#39;}\)</span>:</p>
<p><span class="math display">\[w_{i,j}^{&#39;}={w_{ij}^{&#39;}}^{(old)} - \eta Q_j\mathbf{h}_i\]</span></p>
<p>或者写成词向量的形式，其实就是<span class="math inline">\(W^{&#39;}\)</span>的一列:</p>
<p><span class="math display">\[v_{w_j}^{&#39;} = {v_{w_j}^{&#39;}}^{(old)} - \eta Q_j \mathbf{h}, \ j = 1,2,3,...,V\]</span></p>
<p>接着考虑对隐层神经元的梯度:</p>
<p><span class="math display">\[\begin{align*}\frac{\partial E}{\partial \mathbf{h}_i} &amp;=\sum_{c=1}^{C}\sum_{j=1}^{V} \frac{\partial E}{\partial u_{c,j}} \frac{\partial u_{c,j}}{\partial \mathbf{h}_i} \\&amp;=\sum_{c=1}^{C}\sum_{j=1}^{V}(y_{c,j}-t_{c,j}) w^{&#39;}_{ij} \\ &amp;= \sum_{j=1}^{V}Q_jw^{&#39;}_{i,j}=W^{&#39;}_{i} \cdot Q \end{align*}\]</span></p>
<p>因此跟One-Word Model一样整合成向量的形式:</p>
<p><span class="math display">\[\frac{\partial E}{\partial \mathbf{h}} = W^{&#39;} \cdot Q\]</span></p>
<p>考虑到输入只有一个词，因此跟One-Word Model 一样: <span class="math inline">\(\mathbf{h} = {v_{w_I}}^T\)</span>, 因此每次训练更新词向量矩阵<span class="math inline">\(W\)</span>的一行:</p>
<p><span class="math display">\[v_{w_I}^T = v_{w_I}^T - \eta  W^{&#39;} \cdot Q\]</span></p>
<p>到此SkipGram模型的前向推理与后向参数的训练也介绍完毕了。</p>
<h2 id="优化">优化</h2>
<h3 id="复杂度">复杂度</h3>
<p>前面的CBOW与SG模型是标准的word2vec模型，或者说是神经语言模型的简化版，去掉了隐层的激活函数，其余的变化不大，因此训练效率还是很低的。我们分析下训练的复杂度。首先明确需要学习的两个词向量矩阵<span class="math inline">\(W, W^{&#39;}\)</span>，从前面的推导中知道对于每一个训练样本，CBOW更新<span class="math inline">\(W\)</span>的<span class="math inline">\(C\)</span>行，SG更新<span class="math inline">\(W\)</span>其中一行，也就是每次更新有限个词的词向量。但是对于<span class="math inline">\(W^{&#39;}\)</span>则不同了，正如前面一直提到的，无论是CBOW还是SG，对每个训练样本(或者Mini Batch)从梯度更新中需要对<span class="math inline">\(W^{&#39;}\)</span>的所有<span class="math inline">\(V \times N\)</span>个元素，也就是词汇表中所有<span class="math inline">\(V\)</span>个单词都需要更新词向量，考虑现实任务词汇表一般是几十万，上百万千万级别的， 这个计算成本是巨大的。</p>
<p>关于计算成本大的原因，除了上面提到的训练部分，还有就是在每次前向计算的时候，隐层到输出层的softmax函数计算输出层<span class="math inline">\(V\)</span>个元素，计算量也是很大，这样整个模型现实意义不大。</p>
<p>考虑到计算量大的部分都是在隐层到输出层上，尤其是<span class="math inline">\(W^{&#39;}\)</span>的更新。因此word2vec使用了两种优化策略: Hierarchical Softmax 和 Negative Sampling。二者的出发点一致，就是在每个训练样本中，<strong><em>不再完全计算或者更新</em></strong><span class="math inline">\(W^{&#39;}\)</span>这个矩阵。二者都不再显示使用<span class="math inline">\(W^{&#39;}\)</span>这个矩阵。因此这也就解释了前面说的为什么不用<span class="math inline">\(W^{&#39;}\)</span>作为最终词向量。</p>
<p>在多一句，其实上述训练和推理的复杂度很大的根本原因是<code>softmax</code>的分母上的<span class="math inline">\(\sum\)</span>，因此在求梯度的时候，就会有<span class="math inline">\(V\)</span>次的计算。因此下面的<strong>两种方法其实是对softmax的优化，不仅仅限制在word2vec</strong>.</p>
<p>两种优化方式使得word2vec的训练速度大大提升，并且词向量的质量几乎没有下降，这也是word2vec在NLP领域如此流行的原因。 下面依次介绍这两种优化算法。</p>
<h3 id="hierarchical-softmax">Hierarchical SoftMax</h3>
<p>首先Hierarchical SoftMax(HS)并不是word2vec提出来的, 而是之前Bengio在2005年最早提出来专门为了加速计算神经语言模型中的softmax的一种方式, 这里介绍如何在word2vec中使用. HS主要基于哈夫曼树(一种二叉数)将计算量大的部分变为了一种二分类的问题. 先看下面的图, 原来的模型在隐层之后通过<span class="math inline">\(W^{&#39;}\)</span>连接输出层, 现在HS则去掉了<span class="math inline">\(W^{&#39;}\)</span>, 隐层<span class="math inline">\(\mathbf{h}\)</span>直接与下面的二叉树的root节点相连:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blog/w2v/hs-1.png">

</div>
<p>其中图中白色的叶子节点表示词汇表中所有的<span class="math inline">\(|V|\)</span>个词, 黑色节点表示非叶子节点, 每一个叶子节点也就是每一个单词, 都对应唯一的一条从root节点出发的路径. 而我们的目的是使的<span class="math inline">\(w=w_O\)</span>这条路径的概率最大，即: <span class="math inline">\(P(w=w_O | w_I)\)</span> 最大, 此时每一个分支都代表一个选择, 向左转还是向右转. 所以如何判断向左还是向右呢? 我们用<span class="math inline">\(n(w,j)\)</span>表示从root到叶子节点<span class="math inline">\(w\)</span>的路径上的第<span class="math inline">\(j\)</span>个非叶子节点, 并且每个非叶子节点都对应一个向量<span class="math inline">\(v^{&#39;}_{n(w, j)}\)</span>, 维度与<span class="math inline">\(\mathbf{h}\)</span>相同, 然后使用一个sigmod函数: <span class="math inline">\(\sigma(x)= \frac{1}{1+\exp(-x)} \in [0, 1]\)</span>, 结合向量的内积, 来判断该向左还是向右, 如下, 第n个节点向左 以及向右的概率定义:</p>
<p><span class="math display">\[\begin{align*}P(n, left ) &amp;= \sigma({v^{&#39;}_{w}} \cdot \mathbf{h}) \\ p(n, right) &amp;=1-\sigma(v^{&#39;}_w \cdot \mathbf{h}) = \sigma(-v_{w}^{&#39;} \cdot h) \end{align*}\]</span></p>
<p>有了上述的概率, 我们可以重新定义<span class="math inline">\(P(w_O|w_i)\)</span>了:</p>
<p><span class="math display">\[P(w=w_O|w_I)= \Pi_{j=1}^{L(w)-1}P(\sigma(I(n(w,j+1==left)  v^{&#39;}_{w} \cdot \mathbf{h}  ))\]</span></p>
<p>其中<span class="math inline">\(I()\)</span>是指示函数, 条件成立值为1, 反之为-1. 而<span class="math inline">\(L(w)\)</span>表示整条路径的长度, 这样整个概率就是从root节点到叶子节点这条路径的概率, 这样我们在训练的时候, 通过训练样本来更新非叶子节点的参数<span class="math inline">\(v{&#39;}_{w}\)</span>.</p>
<p>举个例子, 比如上图中的加粗的黑色路径: <span class="math inline">\((n(w_2,1), n(w_2, 2), n(w_2, 3), w_2\)</span> , 就是说假设有一个训练样本是<span class="math inline">\((w_I, w_2)\)</span>, 我们需要使得<span class="math inline">\(P(w_O=w_2|w_I)\)</span>概率最大:</p>
<p><span class="math display">\[\begin{align*}P(w_2=w_O) &amp;= P(n(w_2,1), left)  \cdot P(n(w_2,2), left) \cdot P(n(w_2, 3), right) \\ &amp;= \sigma({v^{&#39;}_{n(w_2,1)}}^T \mathbf{h}) \cdot \sigma({v^{&#39;}_{n(w_2,2)}}^T \mathbf{h}) \cdot \sigma(-{v^{&#39;}_{n(w_2,3)}}^T \mathbf{h}) \end{align*}\]</span></p>
<p>并且在一个非叶子节点处, 向左向右的概率和为1, 因此一直分裂下去,最后的和肯定还是1. 因此可以很容易得到:</p>
<p><span class="math display">\[\sum\limits_{j=1}^{V}P(w_j=w_O) =1 \]</span></p>
<p>这一点的证明是有必要的, 因为在原始的softmax本身保证了所有单词的概率和是1, 而通过上式也知道了通过HS得到的输出层仍然是一个概率多项分布, 输出所有的单词概率和为1.</p>
<p>讲完了从前向后的如何得到输出单词的概率的过程, 下面开始后向传播的训练过程. 首先需要明确的是训练的参数: 输入层与隐层的词向量矩阵<span class="math inline">\(W\)</span>, 以及二叉树的非叶子节点对应的向量<span class="math inline">\(\{v{&#39;}_{n(w,j)}, j =1,2,3,..,L(w)-1\}\)</span>.</p>
<p>为了书写方便,下面简化一部分的符号: 用<span class="math inline">\([I]\)</span>表示前面的指示函数<span class="math inline">\(I(n(w,j+1)==left)\)</span>, 使用<span class="math inline">\(v^{&#39;}_{j}\)</span>表示<span class="math inline">\(v^{&#39;}_{n(w,j)}\)</span></p>
<p>对于一组训练数据, 损失函数的定义与前面相同, 最大似然(注意这里以One-Word Model为例，CBOW与Skip-Gram按照上述的思路完全一样):</p>
<p><span class="math display">\[E = -\log P(w=w_O|w_I) = - \sum\limits_{j=1}^{L(w)-1}\log \sigma([I] {v^{&#39;}_{j}}^{T} \mathbf{h})\]</span></p>
<p>之后便可以逐项求梯度了, 先考虑<span class="math inline">\(v^T \mathbf{h}\)</span>, 注意<span class="math inline">\(\frac{\partial \sigma(x)}{x} = \sigma(1-\sigma)\)</span>: <span class="math display">\[\begin{align*} \frac{\partial E}{\partial v^{&#39;}_j \mathbf{h}} &amp;= (\sigma([I] {v_{j}^{&#39;}}^T \mathbf{h}))[I] \end{align*}\]</span></p>
<p>之后对<span class="math inline">\([I]\)</span> 分情况讨论, 可得: <span class="math inline">\(\frac{\partial E}{\partial {v^{&#39;}_{j}}^T \mathbf{h}} = \sigma( {v^{&#39;}_{j}}^T \mathbf{h}) - t_j\)</span> 这里如果[I]=1, 那么$t_j =1 $, 否则 <span class="math inline">\(t_j=0\)</span> , 这个公式与前面的<span class="math inline">\(y_j-t_j\)</span>很类似, 可以理解为预测值与实际值的差别。</p>
<p>有了上述的梯度,就可以很简单的求出<span class="math inline">\(v^{&#39;}\)</span>的梯度了:</p>
<p><span class="math display">\[\frac{\partial E}{\partial v^{&#39;}_j} = \frac{\partial E}{\partial  {v^{&#39;}_{j}}^T \mathbf{h}} \frac{\partial  {v^{&#39;}_{j}}^T \mathbf{h}}{\partial   {v^{&#39;}_{j}}^T} = (\sigma( {v^{&#39;}_{j}}^T \mathbf{h}) - t_j) \mathbf{h}\]</span></p>
<p>有了梯度,便可以更新了, 具体公式还是梯度下降:</p>
<p><span class="math display">\[{v^{&#39;}_{j}}^{(new)} = {v^{&#39;}_{j}}^{(old)} - \eta (\sigma( {v^{&#39;}_{j}}^T \mathbf{h}) - t_j) \mathbf{h},\ j=1,2,3,...,L(w)-1\]</span></p>
<p>也就是说对于一个训练样本， 我们只需要更新<span class="math inline">\(L(w)-1\)</span>个向量就好了， 而未优化的版本需要更新<span class="math inline">\(V\)</span>个， 相当于时间复杂度从<span class="math inline">\(O(V)\)</span>降到了<span class="math inline">\(O(\log V)\)</span> , 这个提升还是非常大的， 同时在考察空间复杂度，HS的二叉树的非叶子节点有<span class="math inline">\(V-1\)</span>个，也就是我们需要<span class="math inline">\(V-1\)</span>存储<span class="math inline">\(v^{&#39;}_{j},\ \ j=1,2,3..V-1\)</span> ,优化之前则是<span class="math inline">\(V\)</span>个， 空间复杂度相同， 但是时间复杂度却大大降低。</p>
<p>然后考虑隐层<span class="math inline">\(\mathbf{h}\)</span>的梯度，因为我们的优化目标都是在隐层到输出层，因此前面的几乎不变， 跟One-Word Model 一样，路径上的非叶子节点的表达式都含有<span class="math inline">\(\mathbf{h}\)</span>，因此需要对梯度求和：</p>
<p><span class="math display">\[\begin{align*} \frac{\partial E}{\partial \mathbf{h}} &amp;= \sum\limits_{j=1}^{L(w)-1} \frac{\partial E}{\partial {v^{&#39;}_{j}}^T  \mathbf{h}} \frac{\partial {v^{&#39;}_{j}}^T  \mathbf{h}}{\partial \mathbf{h}} \\ &amp;=\sum\limits_{j=1}^{L(w)-1}(\sigma( {v^{&#39;}_{j}}^T \mathbf{h}) - t_j)\cdot v^{&#39;}_{j}\end{align*}\]</span></p>
<p>其实跟前面很一样了， 只需要替代下导数就好了， 后面就不再赘述了。</p>
<p>整个Hierarchical Softmax的优化算法介绍完了，隐层到输出层的计算量从<span class="math inline">\(O(V)\)</span>, 利用二叉树(更加具体来说是哈夫曼树）降为了<span class="math inline">\(O(\log V)\)</span>.</p>
<h3 id="negative-sampling">Negative Sampling</h3>
<p>相比Hierarchical Softmax用复杂的树形结构来对softmax进行优化， 负采样(Negative Sampling)更加直接简单。因为<code>softmax</code>的存在，在使用梯度下降的时候，每个训练样本需要更新<span class="math inline">\(V\)</span>个向量，这是根本问题。因此负采样NS仅仅更新一小部分向量，一般是5-20个，可以手工设定，而非全部<span class="math inline">\(V\)</span>个(一般来说V都是几万到几百万的量级)。</p>
<p>再来考虑原始模型的训练过程，对于一个训练样本<span class="math inline">\((w_I, w_O)\)</span>, 我们要使得<span class="math inline">\(P(w=w_O|w_I)\)</span>最大， 也就是说要使得输出层的其它单词的概率要比较小一些，基于这种思想， 我们将输出层的<span class="math inline">\(V\)</span>个样本分为正例(Positive Sample)也就是<span class="math inline">\(w_O\)</span>对应的项， 以及剩余<span class="math inline">\(V-1\)</span>个负例(Negative Samples)。举个例子有个样本<code>phone number</code>，这样<span class="math inline">\(w_I=phone, w_O=number\)</span>, 正例就是number这个词， 负例就是不太可能与phone共同出现的词。</p>
<p>负采样的思想便是每次训练只随机取一小部分的负例使他们的概率最小，以及对应的正例概率最大。那么如何对负例进行抽样呢？ 这里就需要定义一个noise distribution，有了分布<span class="math inline">\(P_n(w)\)</span>就可以依据概率分布进行带权随机抽样了。 在word2vec中，作者直接使基于词的频次的词的权重分布:</p>
<p><span class="math display">\[weight(w) = \frac{coun(w)^{0.75}}{\sum _{u}count(w)^{0.75}}\]</span></p>
<p>相比于直接使用频次作为权重， 取0.75幂的好处可以减弱不同频次差异过大带来的影响，使得小频次的单词被采样的概率变大。</p>
<p>下面基于上面的思想，直接给出具体的损失函数:</p>
<p><span class="math display">\[E = -\log \sigma(v^{&#39;}_{w_O}\mathbf{h}) - \sum\limits_{w_j \in \mathcal{W}_{neg}}\log \sigma(-v^{&#39;}_{w_j}\mathbf{h})\]</span></p>
<p><span class="math inline">\(\mathcal{W}_{neg}\)</span>是负例单词集合。 上述公式跟原本的负采样的有些许差别，具体的推导细节可以参考这篇Goldberg, Y. and Levy, O. (2014). word2vec explained: deriving mikolov et al.’s negative- sampling word-embedding method. 这里不再赘述了。 只需要记住，NS也是对softmax函数做的优化，除了word2vec，在其他地方涉及到softmax的均可以采用类似的思想来重写目标函数。</p>
<p>有了损失函数，然后跟HS一样，用梯度下降的方法更新<span class="math inline">\(v^{&#39;}\)</span>即可，推导思路跟HS也是一样，分<span class="math inline">\(w_j\)</span>是否等于<span class="math inline">\(w_O\)</span>来讨论，最终得到的梯度公式与HS一模一样：</p>
<p><span class="math display">\[\frac{\partial E}{\partial {v^{&#39;}_{j}}^T \mathbf{h}} = \sigma( {v^{&#39;}_{j}}^T \mathbf{h}) - t_j\]</span></p>
<p>这里的<span class="math inline">\(t_j\)</span>含义跟前面提到的几次都相同。最后给出更新<span class="math inline">\(v^{&#39;}\)</span>的公式:</p>
<p><span class="math display">\[{v^{&#39;}_{j}}^{(new)} = {v^{&#39;}_{j}}^{(old)} - \eta (\sigma( {v^{&#39;}_{j}}^T \mathbf{h}) - t_j) \mathbf{h},\ w_j \in  \{w_O \} + \mathcal{W}_{neg}\]</span></p>
<p>从公式也可以看出，对于每个训练数据，每次只需要更新很少的几个向量，而非原始的<span class="math inline">\(V\)</span>个，大大降低了训练复杂度。</p>
<p>跟HS相同，NS的优化也是对隐层到输出层的优化，因此前面输入层到隐层的梯度只需要做相应修改即可，没有影响，具体可以参考HS的公式，这里不再重复了。</p>
<p>到此为止，两种针对softmax的优化方式介绍完毕，简单对比一下两种方式:</p>
<ul>
<li>HS实现比较复杂， 需要借助哈夫曼树，而且HS是专门优化softmax的方法</li>
<li>NS相对简单，仅仅是简单采样，NS则是一种概率采样的方法，属于NCE(Noise Contrastive Estimation)的一种简单形式，代码实现也很简单。 训练得到的词向量质量也很高，相对常用一些。</li>
</ul>
<h2 id="后话">后话</h2>
<p>这篇博客侧重word2vec的数学原理，主要是以神经网络的反向传播思路来解释训练过程，以及介绍了优化的原因和思路，基本上相当于一份对w2v相对全面解释的全家桶了，除了具体的代码实现细节，关于代码实现的话，以后看有没有时间整理吧。google官方的c实现的word2vec的代码还是非常非常值得一读的，只有600多行，里面用到了很多技巧，包括快速<span class="math inline">\(\sigma\)</span>计算， 还有负采样的实现等等，使用异步梯度下降多线程训练(ASGD)等。</p>
<blockquote>
<p>在4月份花了几天时间看完了google的两篇原始paper以及word2vec Parameter Learning Explained这篇以后，就想结合论文按照自己的理解整理一下， 不过当时仅仅列出了目录，写了个开头背景，之后因为课程，读NE的paper等各种事情，一直拖着到现在了。 这段时间趁出差期间反正没事，现在算是就把这个坑补上了。 有什么写的不严谨的地方，烦请指出。</p>
</blockquote>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;目前需要做Network Embedding方面的课题，而复杂网络本身就经常借鉴NLP的一些算法模型, Embedding也不例外. 因此先从Word Embedding入手。之前对Word Embedding(暂且翻译为词嵌入或者词向量)的理解就是将单词根据某种特征转为数值向量，再来做其他工作比如文本分类的工作。而word2vec则是word embedding的一种模型，也是目前使用最广的词向量模型, 由Google的Mikolov团队2013年提出。之前仅仅能够使用第三方库来训练直接使用, 对其中原理并没有多少理解, 这篇博客则比较完整的从背景知识到原理，参数训练等方面整理一下word2Vec。&lt;/p&gt;
    
    </summary>
    
      <category term="读书笔记" scheme="http://shomy.top/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="machine learning" scheme="http://shomy.top/tags/machine-learning/"/>
    
      <category term="word2vec" scheme="http://shomy.top/tags/word2vec/"/>
    
  </entry>
  
  <entry>
    <title>Alias Method离散分布随机取样</title>
    <link href="http://shomy.top/2017/05/09/alias-method-sampling/"/>
    <id>http://shomy.top/2017/05/09/alias-method-sampling/</id>
    <published>2017-05-08T16:04:32.000Z</published>
    <updated>2017-07-31T02:42:34.562Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>在图的随机游走中，有一块需要随机取样， 比如当前到达v节点，那么下一次随机会到达哪个节点。这种问题其实就是离散分布的随机变量的取样问题。 查了一些资料， 发现<a href="https://en.wikipedia.org/wiki/Alias_method" target="_blank" rel="external">Alias Method</a> 是一种很高效的方式。<a id="more"></a>在初始好之后，每次取样的复杂度为<span class="math inline">\(O(1)\)</span>。 下面介绍下这种方法的使用，具体原理暂时没有深究，有兴趣转<a href="http://www.keithschwarz.com/darts-dice-coins/" target="_blank" rel="external">Darts,Dice, and coids</a>。</p>
</blockquote>
<h2 id="alias-method-原理">Alias-Method 原理</h2>
<p>大部分资料都会以这个例子讲解：游戏中经常遇到按一定的掉落概率来随机掉落指定物品的情况，例如掉落 银币25%，金币20%, 钻石10%，装备5%，饰品40%。现在要求下一个掉落的物品类型，或者说一个掉落的随机序列，要求符合上述概率。</p>
<p>其实方法很简单，最直接的方法就是直接取样，先构建一个<strong>累加</strong>概率分布列表:<code>[0.25,0.45,0.55, 0.60, 1.0]</code>，之后产生一个随机数(0-1)，假设为0.7，那么在列表中找到第一个大于0.7的数为1.0,对应的类别是第5类。 这种方法很简单直接，但是每次取样复杂度为<span class="math inline">\(O(K)​\)</span>，使用二分搜索之后，降为<span class="math inline">\(O(logK)​\)</span>。</p>
<p>论文中经常使用的是另一种很巧妙的方法: Alias Method，它在初始化之后每次随机取样的复杂度为<span class="math inline">\(O(1)\)</span>。 下面以<a href="http://www.keithschwarz.com/darts-dice-coins/" target="_blank" rel="external">Darts,Dice, and coids</a>文中例子用图示说明整个步骤，原理太繁杂，不做介绍，参考博客即可:</p>
<p>假设概率分布为: <span class="math inline">\(\frac{1}{2}, \frac{1}{3}, \frac{1}{12}. \frac{1}{12}\)</span>。</p>
<ol style="list-style-type: decimal">
<li><p>初始概率分布: 类别数目<span class="math inline">\(K=4\)</span>，以颜色表示不同的类别<img src="http://7xotye.com1.z0.glb.clouddn.com/alias-method-1.png"></p></li>
<li><p>每个类别概率乘以K=4,使得总和为4. 这样分为两类，大于1:第一列与第二列; 小于1: 第三列与第四列。<img src="http://7xotye.com1.z0.glb.clouddn.com/alias-method-2.png"></p></li>
<li>下面通过拼凑，使得每一列的和都为1，但是每一列中，最多只能是两种类型的拼凑，就是<strong>每一列最多两种颜色存在</strong>。
<ul>
<li>将第一列拿出<span class="math inline">\(\frac{2}{3}\)</span>给最后一列，使其变为1，如下:(棕色表示空缺)<img src="http://7xotye.com1.z0.glb.clouddn.com/alias-method-3.png"></li>
<li>将第一列拿出<span class="math inline">\(\frac{2}{3}\)</span>给第三列，使之变为1，如下:<img src="http://7xotye.com1.z0.glb.clouddn.com/alias-method-4.png"></li>
<li>最后一次,第二列给第一列<span class="math inline">\(\frac{1}{3}\)</span>, 最后每一列都是1，且每一列最多两种类型，其中下面一层表示原类的概率，上面层表示另外一种类型的概率，如只有一种比如第二列，那么第二层就是NULL:<img src="http://7xotye.com1.z0.glb.clouddn.com/alias-method-5.png"></li>
</ul></li>
<li><p>写出两个数组:</p></li>
</ol>
<ul>
<li><strong>Probability table</strong> <span class="math inline">\(Prob\)</span>: 落在原类型的概率，每一列第一层的概率，即:<span class="math inline">\([\frac{2}{3}, 1, \frac{1}{3}, \frac{1}{3}]\)</span></li>
<li><strong>Alias table</strong> <span class="math inline">\(Alias\)</span> : 每一列第二层的类型(颜色)，这里用下标表示: <span class="math inline">\([2, null, 1, ,1]\)</span></li>
</ul>
<p>用图表示如下:<img src="http://7xotye.com1.z0.glb.clouddn.com/alias-method-6.png"></p>
<p>到此为止，得到<span class="math inline">\(Prob, Alias\)</span> 表示初始化完成。</p>
<ol start="5" style="list-style-type: decimal">
<li>采样过程: 随机取某一列k(即[1,4]的随机整数)，再随机产生一个[0-1]的小数c，如果<span class="math inline">\(Prob[k]\)</span> 大于 c，那么采样结果就是k，反之则为<span class="math inline">\(Alias[k]\)</span>。</li>
</ol>
<p>需要说明的是，该过程一定可以结束(原文有证明)。此外在初始化完成之后每次采样的复杂度为<span class="math inline">\(O(1)\)</span>，因此应用很广。</p>
<p>举个例子说明采样过程，比如随机取得第1列， 随机产生的小数为<span class="math inline">\(0.5&lt; \frac{2}{3}\)</span>，那么采样的结果就是第一类。 如果随机产生的小数为<span class="math inline">\(0.8 &gt; \frac{2}{3}\)</span>，那么采样结果就是第一列的第二层的类别，也就是<span class="math inline">\(Alias[1] = 2\)</span>(紫色对应的类别: 第二列)。</p>
<p>再简单验证下，看看该方法的采样是不是满足原始的概率分布<span class="math inline">\(\frac{1}{2}, \frac{1}{3}, \frac{1}{12}. \frac{1}{12}\)</span></p>
<p>对于采到第一种的概率，采到第一种有三部分组成，第一列，第三，四列分别求概率求和: <span class="math inline">\(\frac{2}{3} * \frac{1}{4} + (1-\frac{1}{3}) * \frac{1}{4} + ({1-\frac{2}{3}}) * \frac{1}{4} = \frac{1}{2}\)</span> ，因此满足原始概率的分布，其余同理。</p>
<h2 id="代码">代码</h2>
<p>代码官网给了Java版本，不过网上有很多其他语言的版本，下面参考<a href="https://hips.seas.harvard.edu/blog/2013/03/03/the-alias-method-efficient-sampling-with-many-discrete-outcomes/" target="_blank" rel="external">1</a> 做了注释如下:</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div><div class="line">40</div><div class="line">41</div><div class="line">42</div><div class="line">43</div><div class="line">44</div><div class="line">45</div><div class="line">46</div><div class="line">47</div><div class="line">48</div><div class="line">49</div><div class="line">50</div><div class="line">51</div><div class="line">52</div><div class="line">53</div><div class="line">54</div><div class="line">55</div><div class="line">56</div><div class="line">57</div><div class="line">58</div><div class="line">59</div><div class="line">60</div><div class="line">61</div><div class="line">62</div><div class="line">63</div><div class="line">64</div><div class="line">65</div><div class="line">66</div><div class="line">67</div><div class="line">68</div><div class="line">69</div><div class="line">70</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">import</span> numpy        <span class="keyword">as</span> np</div><div class="line"><span class="keyword">import</span> numpy.random <span class="keyword">as</span> npr</div><div class="line"> </div><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">alias_setup</span><span class="params">(probs)</span>:</span></div><div class="line">    <span class="string">'''</span></div><div class="line"><span class="string">    probs： 某个概率分布</span></div><div class="line"><span class="string">    返回: Alias数组与Prob数组</span></div><div class="line"><span class="string">    '''</span></div><div class="line">    K       = len(probs)</div><div class="line">    q       = np.zeros(K) <span class="comment"># 对应Prob数组</span></div><div class="line">    J       = np.zeros(K, dtype=np.int) <span class="comment"># 对应Alias数组</span></div><div class="line">    <span class="comment"># Sort the data into the outcomes with probabilities</span></div><div class="line">    <span class="comment"># that are larger and smaller than 1/K.</span></div><div class="line">    smaller = [] <span class="comment"># 存储比1小的列</span></div><div class="line">    larger  = [] <span class="comment"># 存储比1大的列</span></div><div class="line">    <span class="keyword">for</span> kk, prob <span class="keyword">in</span> enumerate(probs):</div><div class="line">        q[kk] = K*prob <span class="comment"># 概率</span></div><div class="line">        <span class="keyword">if</span> q[kk] &lt; <span class="number">1.0</span>:</div><div class="line">            smaller.append(kk)</div><div class="line">        <span class="keyword">else</span>:</div><div class="line">            larger.append(kk)</div><div class="line"> </div><div class="line">    <span class="comment"># Loop though and create little binary mixtures that</span></div><div class="line">    <span class="comment"># appropriately allocate the larger outcomes over the</span></div><div class="line">    <span class="comment"># overall uniform mixture.</span></div><div class="line">    </div><div class="line">    <span class="comment"># 通过拼凑，将各个类别都凑为1</span></div><div class="line">    <span class="keyword">while</span> len(smaller) &gt; <span class="number">0</span> <span class="keyword">and</span> len(larger) &gt; <span class="number">0</span>:</div><div class="line">        small = smaller.pop()</div><div class="line">        large = larger.pop()</div><div class="line"> </div><div class="line">        J[small] = large <span class="comment"># 填充Alias数组</span></div><div class="line">        q[large] = q[large] - (<span class="number">1.0</span> - q[small]) <span class="comment"># 将大的分到小的上</span></div><div class="line"> </div><div class="line">        <span class="keyword">if</span> q[large] &lt; <span class="number">1.0</span>:</div><div class="line">            smaller.append(large)</div><div class="line">        <span class="keyword">else</span>:</div><div class="line">            larger.append(large)</div><div class="line"> </div><div class="line">    <span class="keyword">return</span> J, q</div><div class="line"> </div><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">alias_draw</span><span class="params">(J, q)</span>:</span></div><div class="line">    <span class="string">'''</span></div><div class="line"><span class="string">    输入: Prob数组和Alias数组</span></div><div class="line"><span class="string">    输出: 一次采样结果</span></div><div class="line"><span class="string">    '''</span></div><div class="line">    K  = len(J)</div><div class="line">    <span class="comment"># Draw from the overall uniform mixture.</span></div><div class="line">    kk = int(np.floor(npr.rand()*K)) <span class="comment"># 随机取一列</span></div><div class="line"> </div><div class="line">    <span class="comment"># Draw from the binary mixture, either keeping the</span></div><div class="line">    <span class="comment"># small one, or choosing the associated larger one.</span></div><div class="line">    <span class="keyword">if</span> npr.rand() &lt; q[kk]: <span class="comment"># 比较</span></div><div class="line">        <span class="keyword">return</span> kk</div><div class="line">    <span class="keyword">else</span>:</div><div class="line">        <span class="keyword">return</span> J[kk]</div><div class="line"> </div><div class="line">K = <span class="number">5</span></div><div class="line">N = <span class="number">100</span></div><div class="line"> </div><div class="line"><span class="comment"># Get a random probability vector.</span></div><div class="line">probs = npr.dirichlet(np.ones(K), <span class="number">1</span>).ravel()</div><div class="line"> </div><div class="line"><span class="comment"># Construct the table.</span></div><div class="line">J, q = alias_setup(probs)</div><div class="line"> </div><div class="line"><span class="comment"># Generate variates.</span></div><div class="line">X = np.zeros(N)</div><div class="line"><span class="keyword">for</span> nn <span class="keyword">in</span> xrange(N):</div><div class="line">    X[nn] = alias_draw(J, q)</div></pre></td></tr></table></figure>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;在图的随机游走中，有一块需要随机取样， 比如当前到达v节点，那么下一次随机会到达哪个节点。这种问题其实就是离散分布的随机变量的取样问题。 查了一些资料， 发现&lt;a href=&quot;https://en.wikipedia.org/wiki/Alias_method&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;Alias Method&lt;/a&gt; 是一种很高效的方式。
    
    </summary>
    
      <category term="读书笔记" scheme="http://shomy.top/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="alias method" scheme="http://shomy.top/tags/alias-method/"/>
    
  </entry>
  
  <entry>
    <title>机器学习技法笔记(10)-AdaBoost(提升算法)</title>
    <link href="http://shomy.top/2017/03/16/adaptive-boosting/"/>
    <id>http://shomy.top/2017/03/16/adaptive-boosting/</id>
    <published>2017-03-16T12:55:55.000Z</published>
    <updated>2017-07-31T02:42:34.562Z</updated>
    
    <content type="html"><![CDATA[<p>继上一节的Aggregation和Bagging，这一节对那一堆的g函数的产生做更深的探究，对训练数据引入权重的机制(Weight-Based)，加大错误数据的权重，缩小正确数据的权重，从而得到更加 Powerful的算法-AdaBoost</p>
<a id="more"></a>
<h2 id="从bagging引入权重">从Bagging引入权重</h2>
<p>回想上节的Bagging，通过Bootstrap来生成多份的数据<span class="math inline">\(D_t\)</span>，这里面可能会有重复，例如:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/adaboost-1.png">

</div>
<p>以分类为例，计算0/1误差: <span class="math inline">\(E_{in}={1\over4}\sum||y \neq h(x)||\)</span>。现在引入新的变量: <span class="math inline">\(u_n\)</span>表示<span class="math inline">\(\mathcal D\)</span>中的第n个数据在采样得到的<span class="math inline">\(\tilde{\mathcal D_t}\)</span>中的个数，对于上面的例子，则有：<span class="math inline">\(u_1=2, u_2=1, u_3=0,u_4=1\)</span>, 那么误差可以换一种方式写:<span class="math inline">\(E_{in}={1\over4}\sum u_n||y \neq h(x)||\)</span>。这样就把<span class="math inline">\(u_n\)</span>引入了错误函数中，可以表示某个数据的权重，这种思想在数据挖掘中很常见，在分类的时候，对于分错的数据，我们需要特别关注，因此把错误数据的重要性放大，或者说人为多复制几份错误数据放进去，这样再迭代的时候，错误数据就会变少了。</p>
<p>我们可以将权重引入一些之前的模型中，比如SVM：</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/adaboost-2.png">

</div>
<p>未加权重之前，每个错误的惩罚系数为<span class="math inline">\(C\)</span>，现在加了权重可以理解为错误惩罚变为了<span class="math inline">\(Cu_n\)</span>, 同时也体现在<span class="math inline">\(\alpha_n\)</span>的上限中。 在比如Logistic Regression中，同样可以引入权重:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/adaboost-3.png">

</div>
<p>这里可以理解为，在使用随机梯度下降优化的时候，同bagging，<span class="math inline">\(u_n\)</span>代表取出的数据的份数。</p>
<h2 id="更新权重">更新权重</h2>
<p>再次回到模型融合，在前一章的aggregation中，我们提到了如果<span class="math inline">\(g_1, g_2,...,g_T\)</span>中效果差距越明显，那么通过融合可以取得更好的结果，现在考察怎么样才可以让<span class="math inline">\(g\)</span>与<span class="math inline">\(g\)</span>之间区分度更明显，考虑<span class="math inline">\(g_t\)</span>与<span class="math inline">\(g_{t+1}\)</span>:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/adaboost-4.png">

</div>
<p><span class="math inline">\(g_t\)</span>的是通过权重系数为<span class="math inline">\(u^{(t)}\)</span>中在假设空间中误差最下的h来得到的，<span class="math inline">\(g_{t+1}\)</span>则是<span class="math inline">\(u^{(t+1)}\)</span>, 如果<span class="math inline">\(g_t\)</span>在权重为<span class="math inline">\(u^{(t+1)}\)</span>的条件下，<strong>表现不好</strong>的话，那么在<span class="math inline">\(t+1\)</span>的时候，我们就不会选择出<span class="math inline">\(g_t\)</span>或者与<span class="math inline">\(g_t\)</span>很近似的h了，而是在<span class="math inline">\(u^{(t+1)}\)</span>的条件下误差最小的<span class="math inline">\(g_{t+1}\)</span>，这样就会使得<span class="math inline">\(g_t\)</span>与<span class="math inline">\(g_{t+1}\)</span>更加diverse。 那么如何来衡量表现不好呢？ 很简单的思路，在二分类问题中，表现不好那就是跟随机瞎猜一样，正确率50%，写成表达式便是:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/adaboost-5.png">

</div>
<p>再剖析这个式子，因为可以将<span class="math inline">\(u_n\)</span>理解为数据有多少份。在<span class="math inline">\(g_t\)</span>中，即分子就是分类错误的数据个数，分母就是所有数据的个数了。因此我们需要的是，在<span class="math inline">\(u^{(t+1)}\)</span>的条件下，<strong>正确的份数==错误的份数</strong>，那<span class="math inline">\(u^{(t+1)}\)</span>应该与<span class="math inline">\(u^{(t)}\)</span>有什么关系呢？ 举个例子，假设在<span class="math inline">\(u^{(t)}\)</span>下, 分类错误1126份，正确6211份，那么在<span class="math inline">\(u^{(t+1)}\)</span>下，二者应该相等，那应该怎么办呢？看下面的几步:</p>
<p><span class="math display">\[\begin{align} &amp; A = \sum u_n^{(t)}||y_n\neq g_t(x_n|| = 1126,  \quad B= \sum u_n^{(t)}||y_n ==\ g_t(x_n)|| = 6211 \\ \Rightarrow &amp;A*6211 = B*1126  \\ \Rightarrow &amp;\left(\sum (6211*u_n^{(t)} \neq)\right)  \mathbf = \left( \sum 1126 * u_n^{(t)} ==   \right)  \end{align}\]</span></p>
<p>因此想要达到在<span class="math inline">\(u^{(t+1)}\)</span>下，<span class="math inline">\(g_t\)</span>正确率到50%，只需要做一个放缩即可:</p>
<ul>
<li>错误的数据: <span class="math inline">\(u_n^{(t+1)} = 6211 * u_n^{(t)}\)</span></li>
<li>正确的数据: <span class="math inline">\(u_n^{(t+1)} = 1126 * u_n^{(t)}\)</span></li>
</ul>
<p>一般来说，我们都是用比率来放缩，假设错误率是<span class="math inline">\(\epsilon_t\)</span>，那么根据上面的思路，就可以更新<span class="math inline">\(u_n^{(t+1)}\)</span>如下:</p>
<ul>
<li>错误数据: <span class="math inline">\(u_n^{(t+1)} = (1-\epsilon_t) * u_n^{(t)}\)</span></li>
<li>正确数据: <span class="math inline">\(u_n^{(t+1)} = \epsilon_t * u_n^{(t)}\)</span></li>
</ul>
<p><span class="math inline">\(g_t\)</span>的效果是可以接受的，因此一般来说<span class="math inline">\(\epsilon_t &lt; {1 \over 2}\)</span>，这样定性来说，其实就是在下一轮的g中，加强了上一轮分类错误数据的权重。</p>
<h2 id="adapive-boosting算法">Adapive Boosting算法</h2>
<p>除了上面使用<span class="math inline">\(\epsilon_t\)</span>作为缩放系数之外，在实际中使用一种等价的方式，定义<span class="math inline">\(\blacklozenge_t = \sqrt{1-\epsilon_t \over \epsilon_t}\)</span>，之后采用下面的策略更新<span class="math inline">\(u^{(t+1)}\)</span>：</p>
<ul>
<li>错误的数据: <span class="math inline">\(u_n^{(t+1)} = u_n^{(t)} \cdot \blacklozenge_t\)</span></li>
<li>正确的数据: <span class="math inline">\(u_n^{(t+1)} = u_n^{(t)} / \blacklozenge_t\)</span></li>
</ul>
<p>意义也是一样的，因为<span class="math inline">\(\blacklozenge_t &gt; 1\)</span>，因此每次对于错误数据加大权重，正确数据减少权重，从而得到一个同<span class="math inline">\(g_t\)</span>不一样的<span class="math inline">\(g_{t+1}\)</span>。</p>
<p>这样我们就可以得到了区分度较大的一堆<span class="math inline">\(g_1, g_2,....,g_T\)</span>，然后就可以用用线性后者非线性的融合方式进行融合。比较常用的是Linear Aggregation，即需要对<span class="math inline">\(g_t\)</span>进行线性组合: <span class="math inline">\(G(\mathbf x ) =sign(\sum\limits_{t=1}^{T}\alpha_t g_t)\)</span>，下面开始考察如何确定<span class="math inline">\(\alpha_t\)</span>。基本思路便是，<strong>效果好的<span class="math inline">\(g_t\)</span>对应的权重<span class="math inline">\(\alpha_t\)</span>应该大一点，反之应该小一点</strong>。 再回想<span class="math inline">\(\blacklozenge_t\)</span>正好是正确率(<span class="math inline">\(1-\epsilon_t\)</span>)的单调增函数，因此在实际中，经常用: <span class="math inline">\(\alpha_t = \ln\blacklozenge_t\)</span>，这样的好处是在计算<span class="math inline">\(g_t\)</span>的时候，就把其对应的权重<span class="math inline">\(\alpha_t\)</span>得到了。比如两种极限情况:</p>
<ul>
<li>如果随机乱分，<span class="math inline">\(\epsilon_t=0.5\)</span>，此时<span class="math inline">\(\blacklozenge_t=1\)</span>，那么<span class="math inline">\(\alpha_t=0\)</span>，也就是说坏的g分配0权重</li>
<li>如果<span class="math inline">\(\epsilon_t=0\)</span>，此时<span class="math inline">\(\blacklozenge_t=\infty\)</span>，那么<span class="math inline">\(\alpha_t=\infty\)</span>也就是说好的g分配无穷大权重</li>
</ul>
<p>最后一个问题，最初的<span class="math inline">\(u^{(1)}\)</span>应该怎么确定？ 因为是初始值，并不知道数据情况，因此一般情况直接将权重初始化为相同:</p>
<p><span class="math display">\[u^{(1)} = [ {1\over N}, {1 \over N},..., {1 \over N}]\]</span></p>
<p>这样AdaBoost算法就完整了，总结一下流程如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/adaboost-6.png">

</div>
<p>这个算法也叫<strong>Linear Aggregation On the Fly</strong>，当然也是属于Adaptive Boosting，叫boosting的原因是因为我们只需要开始有一个很弱的演算法<span class="math inline">\(\mathcal A\)</span>，然后通过一点点增加错误数据的权重，降低正确数据的权重，最后得到一个效果很好的分类器，因此一般也用三个臭皮匠赛过诸葛亮来形容Adaboosting。</p>
<h2 id="vc-维保证">VC 维保证</h2>
<p>这里简单介绍AdaBoost算法的理论保证。有数学证明可以得到Adaboost的VC Bound：</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/adaboost-7.png">

</div>
<p>因为算法一共需要T个g，因此在第二项模型复杂度中多了<span class="math inline">\(TlogT\)</span>这一项，其余不变。有实验证明如果每次的<span class="math inline">\(\epsilon_t &lt; 0.5\)</span>的话，也就是说不是乱猜的话， 只需要经过<span class="math inline">\(T= O(logN)\)</span>次迭代就可以收敛到<span class="math inline">\(E_{in} = 0\)</span>。</p>
<h2 id="总结">总结</h2>
<p>为什么这个算法叫做Adaptive Boost(自适应提升)呢？ 是因为我们一开始只需要一个很简单的弱分类算法，只要比随机分类好就行，也就是<span class="math inline">\(\epsilon_t &lt;0.5\)</span>，然后通过每次加大错误数据的权重，着重训练错误数据，一点点可以提高正确率，直到收敛。这种算法最开始用在人脸检测上，并且取得了当时最好的效果。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;继上一节的Aggregation和Bagging，这一节对那一堆的g函数的产生做更深的探究，对训练数据引入权重的机制(Weight-Based)，加大错误数据的权重，缩小正确数据的权重，从而得到更加 Powerful的算法-AdaBoost&lt;/p&gt;
    
    </summary>
    
      <category term="读书笔记" scheme="http://shomy.top/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="machine learning" scheme="http://shomy.top/tags/machine-learning/"/>
    
      <category term="AdaBoost" scheme="http://shomy.top/tags/AdaBoost/"/>
    
  </entry>
  
  <entry>
    <title>机器学习技法笔记(9)-Blending and Bagging(模型融合)</title>
    <link href="http://shomy.top/2017/03/15/blending-bagging/"/>
    <id>http://shomy.top/2017/03/15/blending-bagging/</id>
    <published>2017-03-15T04:56:38.000Z</published>
    <updated>2017-07-31T02:42:34.562Z</updated>
    
    <content type="html"><![CDATA[<p>前面的课程着重介绍某单一模型，比如逻辑回归，SVM等，从这一节开始以及接下来的3,4节的课程都是在介绍多个模型之间的融合得到一个新的模型的问题。</p>
<h2 id="为什么要进行模型融合">为什么要进行模型融合</h2>
<a id="more"></a>
<p>我们针对某个问题，使用不同的演算法得到了很多模型g，个模型都会有自己擅长的部分，也有不擅长的部分，如果能够可以把它们混合起来，那新模型的效果就可能很好。假设我们已经得到了<span class="math inline">\(g_1,g_2,...,g_T\)</span>等T个model，那我们怎么把它们混合为一个新的模型呢<span class="math inline">\(G(x)\)</span>？方法很多，例如:</p>
<ul>
<li>最简单的，直接在所有的模型里面，选择验证误差最小的作为最后的模型: <span class="math inline">\(G(\mathbf x)=\arg\min\limits_{t \in {1,2,..T}} E_{val}(g_t^{-})\)</span></li>
<li>直接采取所有<span class="math inline">\(g_t\)</span>的意见，对它们同等信任，比如分类问题: <span class="math inline">\(G(\mathbf x) = sign(\sum\limits_{t=1}^{T}1 \cdot g_t(\mathbf x))\)</span></li>
<li>比上一种情况，再更近一层，给每个<span class="math inline">\(g\)</span>不同的投票数目，也就是说它们权重不再相同<span class="math inline">\(G(\mathbf x)=sign(\sum\limits_{t=1}^T\alpha_t \cdot g_t(\mathbf x))\)</span>，这个权重<span class="math inline">\(\alpha_t \geq 0\)</span>可以通过训练得到。</li>
<li>再特殊一点，根据不同的数据的特点，来赋予不同的权重: <span class="math inline">\(G(\mathbf x)=sign(\sum\limits_{t=1}^Tq_t(\mathbf x) \cdot g_t(\mathbf x))\)</span>, 其中<span class="math inline">\(q_t \geq 0\)</span>。</li>
</ul>
<p>除了上面的四种融合之外，还有很多其它的没有列举出来。其中第一种就是前面基石课程中的模型选择的做法，此外这里的<span class="math inline">\(g_{t}^{-}(\mathbf x)\)</span>是指在减去了验证数据集之后的数据作为训练数据得到的模型。</p>
<p>换句话说，有几个很weak的假设空间，通过融合等手段，得到一个更加powerful的假设空间，从而得到效果更好的模型，如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blending-1.png">

</div>
<p>比如我们有一些只能划分水平与竖直方向的分界线的假设空间，如上图中的灰色的线，分类效果肯定很差，但是如果将这些弱的分类器组合起来，就可以得到一个不错的边界，如上图的黑色的线。因此看起来如果对多个模型进行一个合适的融合(aggregaton)，或许就可以得到效果更好的模型。</p>
<h2 id="uniform-blending均匀融合">Uniform Blending(均匀融合)</h2>
<p>这一部分从误差的角度介绍最基本的Uniform Blending(均匀融合)的一些理论依据，Uniform Blending的分类的模型如下，实际上就是少数服从多数的原则:</p>
<p><span class="math display">\[G(\mathbf x)=sign \left( \sum\limits_{t=1}^{T}1 \cdot g_t(\mathbf x)\right)\]</span></p>
<p>回归模型如下，直接求T个模型g的结果的平均值:</p>
<p><span class="math display">\[G(\mathbf x)= {1\over T}\sum\limits_{t=1}^{T}1 \cdot g_t(\mathbf x)\]</span></p>
<p>下面以回归问题分析误差。</p>
<p>预期<span class="math inline">\(g_1,g_2,\cdots,g_T\)</span>的效果应该参差不齐，比如一些<span class="math inline">\(g(\mathbf x)&lt;f(\mathbf x)\)</span>，还有一些<span class="math inline">\(g(\mathbf x) &gt; f(\mathbf x)\)</span>，这里的<span class="math inline">\(f(\mathbf x)\)</span>是理想的未知的函数，这样通过融合平均下来得到的结果会更加准确。反过来，如果每个<span class="math inline">\(g\)</span>的结果都差不多，那就没有必要做融合了。下面来分析融合前后的误差比较。</p>
<p>比较<span class="math inline">\(g_t\)</span>与<span class="math inline">\(f\)</span>的均方误差与融合之后的<span class="math inline">\(G\)</span>与<span class="math inline">\(f\)</span>的均方误差：</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blending-2.png">

</div>
<p>关于上述式子的展开有几个细节需要注意:</p>
<ul>
<li><span class="math inline">\(avg(g_t) = {1\over T}\sum g_t = G\)</span></li>
<li>f与avg无关</li>
</ul>
<p>这样我们就可以得到下面的结论，(如果没有看过基石课程的话，这里的<span class="math inline">\(E_{out}\)</span>可以理解为g与f的差距平方):</p>
<p><span class="math display">\[avg(E_{out}(g_t)) \geq E_{out}(G)\]</span></p>
<p>可以看出<span class="math inline">\(g_t\)</span>的平均误差比融合之后的<span class="math inline">\(G\)</span>要大，也就是说融合模型效果应该会更好。</p>
<p>基于上述的证明，现在考虑一个假想的极限情况，假设我们每次从数据分布中拿出N个数据<span class="math inline">\(\mathcal{D}_t\)</span>，然后通过演算法<span class="math inline">\(\mathcal {A(D_t)}\)</span>得到<span class="math inline">\(g_t\)</span>,(因为每次的数据<span class="math inline">\(D_t\)</span>都不一样，因此每个<span class="math inline">\(g\)</span>的差别也就会变大，这也正是我们所期望的，得到不同给的模型)。如果数量T趋向于无穷大，我们就可以得到一个理想的<span class="math inline">\(\overline{g}\)</span>，它可以被认为在所有的数据得到的模型，可以体现出演算法<span class="math inline">\(\mathcal A\)</span>的在所有的<span class="math inline">\(\mathcal D\)</span>中的表现了</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blending-3.png">

</div>
<p>应用上面的结论，有:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blending-4.png">

</div>
<p>其中<span class="math inline">\(\overline{g}\)</span>可以代表一堆<span class="math inline">\(g_t\)</span>的达成的共识(consensus)。式子的第一部分称为我们的演算法<span class="math inline">\(\mathcal A\)</span>的在不同的数据集上<span class="math inline">\(D_1,...D_T\)</span>的平均表现，右边式子<span class="math inline">\(E_{out}(\overline g)\)</span>的第二部分就是共识的误差，一般称为 <strong>bias</strong>，描述的是共识与理想函数f的差距；第一部分<span class="math inline">\(avg\)</span>则描述的是一种方差，<span class="math inline">\(g_t\)</span>与<span class="math inline">\(\overline g\)</span>的平均差别，一般叫做<strong>variance</strong>， 因此衡量一个演算法的表现，就是bias+varience。 那么我们的Uniform Blending其实就是减少varience的过程。</p>
<p>关于这部分<span class="math inline">\(\mathcal {A,D},g\)</span>等概念，请见<a href="http://shomy.top/2016/10/06/feasibility-of-learning-1/">机器学习基石笔记</a></p>
<h2 id="linear-blending线性融合">Linear Blending(线性融合)</h2>
<p>上面的均匀融合每个g的作用相同，最后的模型可能会变得中庸一些，稍加改变，如果每个g都一个不同的投票权重，效果应该就会更好，这就是线性融合(Linear Blending):</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blending-5.png">

</div>
<p>假设我们已经得到了T个<span class="math inline">\(g_t\)</span>，那么应该如何确定<span class="math inline">\(\alpha_t\)</span>呢，思路就是<span class="math inline">\(\min E_{in}\)</span>，这个式子之前其实遇到过，回想在Linear Regression+Transformation的情况，使用均方误差:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blending-6.png">

</div>
<p>再看看Linear Blending的做回归时候的目标函数:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/blending-7.png">

</div>
<p>二者其实是很类似的，我们也可以将<span class="math inline">\(g_t(\mathbf x)\)</span>作为一种feature transform，这样我们将原始数据转换为: <span class="math inline">\((\mathbf z_n, y_n), \mathbf z_n=(g_1(\mathbf x_n),...,\mathbf g_T(\mathbf x_n))\)</span>，从这个角度来看与Linear Regression就完全一样了，这时候就可以直接用线性回归的方式来求解<span class="math inline">\(\alpha\)</span></p>
<p>然而有一点需要注意的是，Linear Blending有一个约束<span class="math inline">\(\alpha_t \geq 0\)</span>，因为每个模型的投票数不能为负数，而Linear Regression则没有这个约束。其实可以从这角度考虑，来去掉这个限制。对于那些<span class="math inline">\(\alpha_t &lt; 0\)</span>，我们可以认为是<span class="math inline">\(\alpha_t g_t = |\alpha_t|(-g_t)\)</span>，这样就没有问题了，在物理意义上也可以解释，比如在一个分类问题中，对于那些效果很差的g,我们取其相反数，那么就可以得到一个好的g。到此，Linear Blending 与 Linear Regression + Feature Transform 等价了。</p>
<p>求出了<span class="math inline">\(\alpha\)</span>，我们还需要注意下如何选择一堆的<span class="math inline">\(g_t\)</span>:</p>
<p><span class="math display">\[g_1 \in \mathcal H_1, g_2 \in \mathcal H_2...g_T \in \mathcal{H}_T\]</span></p>
<p>一种思路是求出每个<span class="math inline">\(\mathcal H_t\)</span>中<span class="math inline">\(E_{in}\)</span>最小的<span class="math inline">\(g\)</span>，第二种则是<span class="math inline">\(E_{val}\)</span>最小的<span class="math inline">\(g^{-}\)</span>，显然为了防止过拟合，应该使用<span class="math inline">\(g^{-}\)</span>。</p>
<p>注: <span class="math inline">\(g^{}-\)</span>就是使用总数据的N-K个数据做训练，然后用K个数据做验证，选择验证中err最小的g，而非训练误差最小的g。</p>
<p>简单总结下Linear Blending的流程:</p>
<ul>
<li>通过<span class="math inline">\(\min E_{val}\)</span>的方式训练得到T个<span class="math inline">\(g^{-}\)</span></li>
<li>“特征转化”: <span class="math inline">\(\mathbf z_n = \phi^{-}(\mathbf x_n) = (g_1^{-}(\mathbf x_n), g_2^{-}(\mathbf x_n)...g_{T}^{-}(\mathbf x_n))\)</span>，数据变为<span class="math inline">\((z_n, y_n)\)</span></li>
<li>Blengding: 计算<span class="math inline">\(\alpha\)</span>, 现在是一个很简单的线性问题了，通过之前的线性回归，逻辑回归等方法得到投票权重</li>
<li>最终的模型: <span class="math inline">\(G(\mathbf x)=LinearModel(\sum\limits_{n=1}^{N}\alpha^T \phi(\mathbf x))\)</span></li>
</ul>
<p>注: 最后的<span class="math inline">\(\phi=(g_1,g_2....)\)</span>而非<span class="math inline">\(g^{-}\)</span>，因为通常，使用N-K个数据训练得到<span class="math inline">\(\min E_{val}\)</span>得到最好的<span class="math inline">\(g^{-}\)</span>，之后，一般再用全部的N个数据重新训练一遍，得到<span class="math inline">\(g\)</span>，因为一般数据量越大，模型泛化性能就好.</p>
<p>其实除了Linear Blending之外，在得到<span class="math inline">\((z_n, y_n)\)</span>之后，完全可以用一些其它的非线性的模型去解决这个分类或者回归问题，这样可能更加Powerful，但是也就更容易过拟合，因此还是那句话: Linear First。</p>
<p>模型融合在实际数据挖掘中，还是很常用的。在2011的KDDCup上，林老师的台大队伍最后加了使用测试数据的linear blending在比赛封榜最后一个小时，成功逆袭，从第二成为第一，夺得冠军。</p>
<h2 id="bootstrap-aggregationbagging">Bootstrap Aggregation(Bagging)</h2>
<p>前面的一堆的<span class="math inline">\(g\)</span>是通过很多份数据来得到的，但是如果我们只有一份数据呢，也就是只有N个样例，怎么来得到区分度更好的<span class="math inline">\(g\)</span>呢，下面利用一种叫<strong>Bootstrap</strong>的采样方式来利用已有的数据来采样得到新的数据集。</p>
<blockquote>
<p>Bootstrapping: 在已有的<span class="math inline">\(\mathcal D\)</span>中每次有放回的取样，重复N次，得到一组数据<span class="math inline">\(D_t^{t}\)</span>, 包含N个样例，这个采样过程便叫做Bootstrap</p>
</blockquote>
<p>上面的原理很简单，这样得到的每个<span class="math inline">\(D_t\)</span>中有很大的可能出现重复数据，不过这个不影响，反正我们有很多的<span class="math inline">\(g\)</span>,之后再用上面的Blending的方式进行Aggregation,这种方式一般叫做 Bootstrap Aggregation，通常简称<strong>Bagging</strong>。 这种方式在实际中应用很多，尤其是数据不足的情况下。</p>
<h2 id="附">附</h2>
<p>这部分与机器学习基石课程中的一些基础的理论有很大的关系，包括算法，模型，假设空间, <span class="math inline">\(E_{in}, E_{out}\)</span>等概念，还有validation的过程等。 可能是因为这部分还没有写代码实践，因此理解的可能还不到位，整理的有点乱，希望能看到这里的同学多指正。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;前面的课程着重介绍某单一模型，比如逻辑回归，SVM等，从这一节开始以及接下来的3,4节的课程都是在介绍多个模型之间的融合得到一个新的模型的问题。&lt;/p&gt;
&lt;h2 id=&quot;为什么要进行模型融合&quot;&gt;为什么要进行模型融合&lt;/h2&gt;
    
    </summary>
    
      <category term="读书笔记" scheme="http://shomy.top/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="machine learning" scheme="http://shomy.top/tags/machine-learning/"/>
    
      <category term="blending bagging" scheme="http://shomy.top/tags/blending-bagging/"/>
    
  </entry>
  
  <entry>
    <title>TensorFlow常用API</title>
    <link href="http://shomy.top/2017/03/11/tensorflow-basic-api/"/>
    <id>http://shomy.top/2017/03/11/tensorflow-basic-api/</id>
    <published>2017-03-11T11:53:09.000Z</published>
    <updated>2017-08-17T13:19:36.158Z</updated>
    
    <content type="html"><![CDATA[<p>越来越感觉学习python一个第三方库就跟学一门新语言差不多，庞大的TensorFlow更是如此。这篇博客会持续记录关于tensorflow的一些api的用法。</p>
<a id="more"></a>
<h2 id="轴axis">轴(axis)</h2>
<p>在tensorflow中，最基本的变量或者常量都是tensor类型，说直接点就是多维数组，因此涉及到了很多在某一个维度上操作的API，比如<code>tf.reduce_*()</code>，<code>tf_arg*()</code>，<code>tf_expand_dims()</code>等，这些基本的API都有一个共同的参数:<code>axis=</code>，也就说这些操作都是哪些维度(轴)上做运算，除了在tf中，在numpy里面也有这一参数，之前一直是一知半解。现在总结一下这个<strong>axis</strong>，由于numpy与tensorflow的axis基本一样，因此下面以numpy为例。</p>
<p>首先可以从多维数组(tensor)的shape来得到数组的维度: <code>d=len(array.shape)</code>:</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">In [<span class="number">2</span>]: a = np.array([[<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>],[<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>]])</div><div class="line">In [<span class="number">3</span>]: a.shape</div><div class="line">Out[<span class="number">3</span>]: (<span class="number">2</span>, <span class="number">3</span>)</div><div class="line">In [<span class="number">4</span>]: len(a.shape)</div><div class="line">Out[<span class="number">4</span>]: <span class="number">2</span></div></pre></td></tr></table></figure>
<p>也就是说上述的数组a的shape为<code>(2,3)</code>，维度是2维，也就是有两层嵌套数组，看看有多少层<code>[]</code>，因此<code>axis</code>的取值只能为<code>0,1</code>，下面以<code>sum</code>为例，看看不同的axis的结果:</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">In [<span class="number">5</span>]: np.sum(a)</div><div class="line">Out[<span class="number">5</span>]: <span class="number">21</span></div><div class="line"></div><div class="line">In [<span class="number">6</span>]: np.sum(a,axis=<span class="number">0</span>)</div><div class="line">Out[<span class="number">6</span>]: array([<span class="number">5</span>, <span class="number">7</span>, <span class="number">9</span>])</div><div class="line"></div><div class="line">In [<span class="number">7</span>]: np.sum(a,axis=<span class="number">1</span>)</div><div class="line">Out[<span class="number">7</span>]: array([ <span class="number">6</span>, <span class="number">15</span>])</div></pre></td></tr></table></figure>
<p>一个一个的说明。首先需要明白<strong>操作的对象</strong>是谁？这这个例子中，就是那些元素相加。怎么来确定是哪些元素呢？个人使用的原则是:</p>
<blockquote>
<p>对原来数组去掉axis+1层嵌套([])，对剩下的元素再分组进行相应的操作。</p>
</blockquote>
<p>首先如果不加<code>axis</code>参数，则默认对所有元素直接做<code>sum</code>操作，因此结果就是21。当<code>axis=0</code>的时候， 相加的对象，则是变成了<code>[1,2,3]+[4,5,6]</code>，按照上面的原则，a去掉一层嵌套，也就是<code>[]</code>之后，变为: <code>[1,2,3],[4,5,6]</code> 那么<code>sum</code>的对象: <code>[1,2,3]+[4,5,6]=[5,7,9]</code>。 再接着看<code>axis=1</code>，这时候需要去掉两层嵌套，去掉第一层变为:<code>[1,2,3], [4,5,6]</code>，去掉第二层嵌套，剩下了<code>1,2,3</code> 和 <code>4,5,6</code>两个组，因此两个组内各个相加即可，得到的最终的结果为:<code>[6,15]</code>。 下面以同样的思路，计算最小值:</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">In [<span class="number">3</span>]: np.min(a)</div><div class="line">Out[<span class="number">3</span>]: <span class="number">1</span></div><div class="line"></div><div class="line">In [<span class="number">4</span>]: np.min(a, axis=<span class="number">0</span>)</div><div class="line">Out[<span class="number">4</span>]: array([<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>])</div><div class="line"></div><div class="line">In [<span class="number">5</span>]: np.min(a, axis=<span class="number">1</span>)</div><div class="line">Out[<span class="number">5</span>]: array([<span class="number">1</span>, <span class="number">4</span>])</div></pre></td></tr></table></figure>
<p>有一个稍微特殊的是<code>expand_dims</code> 这个是扩展维度。对于这个api它的axis没有上限约束，不过当所有的元素自成一个列表之后，不再变化。它的<code>axis</code>参数不一样的地方，是在<code>axis</code>的位置加入新的一个轴，也就是说此时对原数组去掉<code>axis</code>层嵌套，而非<code>axis+1</code>层，然后再对每组进行加一个轴，看例子:</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div></pre></td><td class="code"><pre><div class="line">In [<span class="number">25</span>]: a</div><div class="line">Out[<span class="number">25</span>]:</div><div class="line">array([[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>],</div><div class="line">       [<span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>]])</div><div class="line"></div><div class="line">In [<span class="number">26</span>]: np.expand_dims(a, axis=<span class="number">0</span>) <span class="comment"># 新加的轴为第0轴</span></div><div class="line">Out[<span class="number">26</span>]:</div><div class="line">array([[[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>],</div><div class="line">        [<span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>]]])</div><div class="line"></div><div class="line">In [<span class="number">27</span>]: np.expand_dims(a, axis=<span class="number">1</span>)<span class="comment"># 新加的轴为第1层</span></div><div class="line">Out[<span class="number">27</span>]:</div><div class="line">array([[[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]],</div><div class="line"></div><div class="line">       [[<span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>]]])</div><div class="line"></div><div class="line">In [<span class="number">28</span>]: np.expand_dims(a, axis=<span class="number">2</span>)</div><div class="line">Out[<span class="number">28</span>]:</div><div class="line">array([[[<span class="number">1</span>],</div><div class="line">        [<span class="number">2</span>],</div><div class="line">        [<span class="number">3</span>]],</div><div class="line">       [[<span class="number">4</span>],</div><div class="line">        [<span class="number">5</span>],</div><div class="line">        [<span class="number">6</span>]]])</div></pre></td></tr></table></figure>
<p>到现在基本上tensorflow以及numpy的axis这个参数基本没有问题了。 下面再看几个常用的函数。</p>
<h2 id="tf.slice">tf.slice</h2>
<p>这个函数主要用于对多维数组的截取(切片)，首先看看原型:</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">tf.slice(input_, begin, size, name=<span class="keyword">None</span>)</div></pre></td></tr></table></figure>
<ul>
<li>input_: 输入</li>
<li>begin: 开始截取的位置</li>
<li>size: 每一个维度截取的长度</li>
</ul>
<p>直接看文档的例子:</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="comment"># 'input' is [[[1, 1, 1], [2, 2, 2]],</span></div><div class="line"><span class="comment">#             [[3, 3, 3], [4, 4, 4]],</span></div><div class="line"><span class="comment">#             [[5, 5, 5], [6, 6, 6]]]</span></div><div class="line">tf.slice(input, [1, 0, 0], [1, 1, 3]) ==&gt; [[[3, 3, 3]]]</div><div class="line">tf.slice(input, [1, 0, 0], [1, 2, 3]) ==&gt; [[[3, 3, 3],</div><div class="line">                                            [<span class="number">4</span>, <span class="number">4</span>, <span class="number">4</span>]]]</div><div class="line">tf.slice(input, [1, 0, 0], [2, 1, 3]) ==&gt; [[[3, 3, 3]],</div><div class="line">                                           [[<span class="number">5</span>, <span class="number">5</span>, <span class="number">5</span>]]]</div></pre></td></tr></table></figure>
<p>先看第一个例子:</p>
<p><code>begin=[1,0,0]</code>也就是开始的元素，这里就是<code>input[1][0][0] = 3</code>,就是第二行<code>[3,3,3]</code>的第一个3,，再看截取距离: <code>[1,1,3]</code>，首先在<code>axis=0</code>的上截取一个距离，得到是第二整行<code>[[3,3,3],[4,4,4]]</code>，在<code>axis=1</code>上截取2个距离，得到<code>[3,3,3]</code>，最后在<code>axis=2</code>上截取了3个距离，也就是<code>[3,3,3]</code>这三个元素都得到，最终结果为:<code>[3,3,3]</code>。</p>
<p>第二个例子解析:</p>
<p>开始的元素仍然是<code>input[1][0][0=0]</code>，在<code>axis=0</code>上截取1个距离，得到仍然是<code>[[[3,3,3],[4,4,4]]]</code> ，第二个维度上截取距离为2,得到<code>[[[3,3,3],[4,4,4]]]</code>，最后在第三个维度上截取3，即三个元素都保留，得到结果。这里如果第三个维度截取2的话，那么得到就是<code>[[[3,3],[4,4]]]</code>。</p>
<p>第三个同理，不再叙述。</p>
<p>参考: <a href="http://blog.csdn.net/qq_32166627/article/details/52718612" target="_blank" rel="external">1</a></p>
<h2 id="tf.nn.embedding_lookup">tf.nn.embedding_lookup</h2>
<p>这个函数在涉及到词向量的NLP的一些task上非常常见，顾名思义，它的功能说白了就是查表。 首先看看基本原型(忽略一些参数):</p>
<figure class="highlight stylus"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">tf<span class="selector-class">.nn</span><span class="selector-class">.embedding_lookup</span>(params, ids, name=None)</div></pre></td></tr></table></figure>
<p>首先<code>params</code>是一个向量或者Tensor, 也就是我们的表。 比如我们使用word2vec训练出了1000个词的词向量，且维度为128, 存到了<code>params</code>里面, 那么<code>params</code>的维度就是<code>(1000,128)</code> 可以看成1000行128列的矩阵,这个矩阵就是我们要查的表.</p>
<p><code>ids</code> 这个参数简单来说则是表示要从表中需要取出哪几行元素。 比如<code>ids=[0,1,3]</code> 表示从上述的<code>params</code>中取出第0,1,3行的向量，这里是整行的取。</p>
<p>先看个简单的例子: <figure class="highlight lua"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">&gt;&gt;a = tf.constant(<span class="string">[[1,2,3],[4,5,6],[7,8,9]]</span>)</div><div class="line">&gt;&gt;tf.nn.embedding_lookup(a, [<span class="number">0</span>,<span class="number">2</span>]).eval()</div><div class="line">&gt;&gt;Out: array(<span class="string">[[1, 2, 3],</span></div><div class="line"><span class="string">       [7, 8, 9]]</span>, dtype=int32)</div></pre></td></tr></table></figure></p>
<p>上述例子中<code>ids=[0,2]</code>表示取出第0行和第2行的内容，也就是<code>[1,2,3]</code>和<code>[7,8,9]</code>.</p>
<p>当然这个函数还可以更强大。 首先<code>ids</code> 可以查多次，并将多次的结果放到数组里:</p>
<figure class="highlight lua"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">&gt;&gt;a = tf.constant(<span class="string">[[1,2,3],[4,5,6],[7,8,9]]</span>)</div><div class="line">&gt;&gt;tf.nn.embedding_lookup(a, <span class="string">[[0,2], [1,2]]</span>).eval()</div><div class="line">&gt;&gt;Out: </div><div class="line">array(<span class="string">[[[1, 2, 3],</span></div><div class="line"><span class="string">        [7, 8, 9]]</span>,</div><div class="line"></div><div class="line">       <span class="string">[[4, 5, 6],</span></div><div class="line"><span class="string">        [7, 8, 9]]</span>], dtype=int32)</div></pre></td></tr></table></figure>
<p>其次，前面都是在说<code>params</code>是二维向量，其实可是高维的Tensor,这样在查的时候，道理一样，结合上面的轴的概念，其实就是取第一层<code>[]</code>里面的某几个元素.</p>
<p>最后举个常用的例子, 在文本分类任务中，如何表示句子呢？有个词汇表V，首先将每个句子用词袋模型表示，就是把每个句子表示成单词在词汇表下标的形式，以此作为<code>ids</code>这个参数，<code>params</code>就是前面训练好的词向量表，利用这个<code>tf.nn.embedding_lookup</code>，就可以取出这个句子的词向量了。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;越来越感觉学习python一个第三方库就跟学一门新语言差不多，庞大的TensorFlow更是如此。这篇博客会持续记录关于tensorflow的一些api的用法。&lt;/p&gt;
    
    </summary>
    
      <category term="工具使用" scheme="http://shomy.top/categories/%E5%B7%A5%E5%85%B7%E4%BD%BF%E7%94%A8/"/>
    
    
      <category term="TensorFlow" scheme="http://shomy.top/tags/TensorFlow/"/>
    
      <category term="api" scheme="http://shomy.top/tags/api/"/>
    
  </entry>
  
  <entry>
    <title>机器学习技法笔记(8)-SVR(支持向量回归)</title>
    <link href="http://shomy.top/2017/03/09/support-vector-regression/"/>
    <id>http://shomy.top/2017/03/09/support-vector-regression/</id>
    <published>2017-03-09T04:08:28.000Z</published>
    <updated>2017-07-31T02:42:34.562Z</updated>
    
    <content type="html"><![CDATA[<p>这一节是核模型的最后一部分了，将Kernel引入到一般的回归模型中: 核岭回归(Kernel Ridge Regression)以及支持向量回归(Support Vector Regression)，此外文末对这一占了整个课程40%的大块内容做了简单总结。</p>
<a id="more"></a>
<h2 id="核岭回归">核岭回归</h2>
<p>上一节的表示定理(Representer Theorem)说明，任何<span class="math inline">\(L_2\)</span>正则化的线性模型的最优解都可以表示成数据的线性组合，也就可以引入Kernel了，进而更加Powerful。现在看之前的岭回归(<span class="math inline">\(L_2\)</span>正则化的线性回归):</p>
<p><span class="math display">\[\min_{\mathbf w} \quad {\lambda\over N}\mathbf w^T\mathbf w+{1\over N}\sum_{n=1}^N\left(y_n-\mathbf w^T\mathbf z_n\right)^2\]</span></p>
<p>显然最优解<span class="math inline">\(\mathbf w_{*} = \sum \beta_n \mathbf z_n\)</span>,那么完全按照上一节KLR的形式，引入Kernel Function，用<span class="math inline">\(\beta\)</span>替换<span class="math inline">\(\mathbf w\)</span>:</p>
<p><span class="math display">\[\begin{align} &amp; \min_\beta \quad {\lambda\over N}\sum_{n=1}^N\sum_{m=1}^N\beta_n\beta_mK(\mathbf x_n,\mathbf x_m)+{1\over N}\sum_{n=1}^N\left(y_n-\sum_{n=1}^N\beta_mK(\mathbf x_n,\mathbf x_m)\right)^2 \\   \Leftrightarrow &amp; \min_\beta \quad {\lambda \over N } \beta^TK\beta +{1 \over N} \left( \mathbf y^T \mathbf y  - 2\beta^TK^T\mathbf y +  \beta^TK^TK\beta\right)\end{align}\]</span></p>
<p>用矩阵表示一来看起来简介，求微分简单，二来矩阵计算比循环要快很多。(PS: 每一个<span class="math inline">\(\sum\)</span>都可以表示为矩阵的乘积，如果一眼看不出来，就展开只能一点点的凑)。</p>
<p>同样得到一个这个无约束的优化问题了， 可以用梯度方式来直接接线性方程(KLR则需要梯度下降来求):</p>
<p><span class="math display">\[{\partial E_{in} \over \partial \beta} = {2 \over N}\left( \lambda K^TI\beta+K^TK\beta - K^T\mathbf y \right)\]</span></p>
<p>(引入<span class="math inline">\(I\)</span>是为了后面与<span class="math inline">\(\lambda\)</span>计算方便)然后令微分为0，得到下面的结果:</p>
<p><span class="math display">\[\beta = (\lambda I +K)^{-1}\mathbf y\]</span></p>
<p>前面在介绍SVM的时候提到过核函数矩阵K是半正定对称方阵，因此逆矩阵一定存在，对于<span class="math inline">\(N \times N\)</span>的矩阵求逆矩阵复杂大是<span class="math inline">\(O(N^3)\)</span>，而且由于核函数矩阵大部分都不为0，最后得到的<span class="math inline">\(\beta\)</span>也是非稀疏矩阵，因此在数据量比较大的时候，计算量还是蛮大的。</p>
<p>到此，上述的模型就称为: Kernel Ridge Regression，完整如下为:</p>
<p><span class="math display">\[g(\mathbf x) = \sum\limits_{n=1}^{N}\beta_nK(\mathbf x_n, \mathbf x)\]</span></p>
<p>通过引入核，可以解决非线性的回归预测问题了。下面对比带核与否的Ridge Regression的一些区别:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svr-1.png">

</div>
<p>两种模型的优缺点与适用场合也就很明显了，只要数据量没有远大于数据维度的话，就可以用kernel ridge regression, 不过还是有一个原则: Linear First。</p>
<h2 id="支持向量回归">支持向量回归</h2>
<p>上面的核岭回归由于核函数矩阵不稀疏，计算量比较大。而之前SVM则只考虑少部分的Support Vectors，得到的<span class="math inline">\(\alpha\)</span>是sparse的，这一节就从Soft-Margin的角度来重新设计Linear Regression的误差衡量。</p>
<p>定义Tube Regression的误差为:</p>
<p><span class="math display">\[err(y,s)=\max(0, |s-y|-\epsilon), \quad s=\mathbf w^Tz_n +b, \ \epsilon &gt; 0\]</span></p>
<p>也就是说，同Soft-Margin SVM分类问题容许偏差的存在，如果预测值与实际值的差距小于<span class="math inline">\(\epsilon\)</span>的时候，就认为err=0，tube可以理解为预测误差在允许范围内，如下图对比tube error与 square error：</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svr-2.png">

</div>
<p>仿照SVM，加上<span class="math inline">\(L_2\)</span>正则项的Tube Regression的目标函数定义为:</p>
<p><span class="math display">\[\min\limits_{b,\mathbf w}\left({1\over 2}\mathbf w^T\mathbf w+C\sum_{n=1}^N\max\left(0, \left\lvert \mathbf w^T\mathbf z_n+b-y_n\right\rvert-\epsilon\right)\right)\]</span></p>
<p>不过由于含有max项，无法微分因此求解很困难，我们可以参考在SVM的方法将问题转为二次规划问题来解决。下面引入新的松弛变量<span class="math inline">\(\xi_n \geq 0\)</span>表示第n个数据的误差，这样目标函数转化为:</p>
<p><span class="math display">\[\begin{align} \min\limits_{b,\mathbf w} \quad  &amp;\left({1\over 2}\mathbf w^T\mathbf w+C\sum_{n=1}^N\xi_n)\right) \\ s.t. \quad &amp; \left\lvert \mathbf w^T\mathbf z_n+b-y_n\right\rvert \leq\epsilon+\xi_n \\ &amp;for \ all \ n, \xi_n \geq 0 \end{align}\]</span></p>
<p>这样的话，对于误差不大于<span class="math inline">\(\epsilon\)</span>的数据，<span class="math inline">\(\xi_n=0\)</span>，反之则<span class="math inline">\(\xi_n &gt; 0\)</span>，不过到此为止，还是不够的，因为约束条件里面有绝对值，这个还是无法用二次规划求解的。下面去掉绝对值，根绝取值的正负分两种情况，把<span class="math inline">\(\xi_n\)</span>拆成两个个<span class="math inline">\(\xi_n^{\wedge}, \xi_n^{\vee}\)</span>，分别真实值大于模型得到的值，以及真实值小于模型得到的值的情形，得到的最终目标函数:</p>
<p><span class="math display">\[\begin{align} \min\limits_{b,\mathbf w} \quad  &amp;\left({1\over 2}\mathbf w^T\mathbf w+C\sum_{n=1}^N(\xi_n^{\vee}+\xi_n^{\wedge})\right) \\ s.t. \quad &amp;  \mathbf y_n-\mathbf w^T\mathbf z_n-b \leq\epsilon+\xi_n^{\wedge} \ \ \ \quad (1) \\ &amp; \mathbf y_n-\mathbf w^T\mathbf z_n-b \geq -\epsilon-\xi_n^{\vee}  \quad (2)\\&amp;\xi_n^{\wedge}, \xi_n^{\vee} \geq 0 \end{align}\]</span></p>
<p>当真实值大于模型值的时候，需要满足约束条件(1)，当真实值小于模型值的时候，需要满足约束条件(2)，这样就是一个标准的二次规划的问题了。</p>
<p>到此为止，支持向量回归的原始问题(SVR Primal)就得到了，考察一下这个QP的规模: 一共有<span class="math inline">\(\tilde{d}+1+2N\)</span>个变量(<span class="math inline">\(\mathbf w\)</span>是Z-Space的权重，维度为<span class="math inline">\(\tilde{d}+1\)</span>)，<span class="math inline">\(2N+2N\)</span>个约束条件，跟SVM的原始问题存在一样的缺陷，原始数据映射到Z-Space之后，维度太高，计算量过大。因此就引出了SVR Dual(支持向量回归的对偶问题)。</p>
<p><strong>SVR Dual</strong></p>
<p>对偶问题首先需要加入拉格朗日乘子，因为每个数据有四个约束条件，因此引入四个<span class="math inline">\(\alpha_n^{\wedge}, \alpha_n^{\vee},\beta_n^{\wedge}, \beta_n^{\vee}\geq0\)</span>:</p>
<p><span class="math display">\[\begin{align}\mathcal L(b, \mathbf w, \alpha^{\vee}, \alpha^{\wedge}) = &amp;{1\over 2}\mathbf w^T \mathbf w+C\sum(\xi_n^{\vee}+\xi_n^{\wedge}) \\ +&amp;\sum\alpha_n^{\wedge}(\mathbf y_n - \mathbf w^T\mathbf z_n -b-\epsilon-\xi_n^{\wedge}) \\+&amp;\sum\alpha_n^{\vee}(-\epsilon-\xi_n^{\wedge}-\mathbf y_n + \mathbf w^T\mathbf z_n +b) \\+ &amp; \sum\beta_n^{\wedge}(-\xi_n^{\wedge}) + \sum\beta_n^{\vee}(-\xi_n^{\vee})\end{align}\]</span></p>
<p>根据强对偶理论，以及在<a href="http://shomy.top/2017/02/17/svm-02-dual/">对偶SVM</a>介绍的内容，Primal SVR 与下面的对偶问题等价:</p>
<p><span class="math display">\[\max \limits_{\alpha_n^{\vee}, \alpha_{n}^{\wedge}&gt;0} \left( \min\limits_{b, \mathbf w, \epsilon} \mathcal L(b, \mathbf w, \xi^{\vee}, \xi^{\wedge})\right)\]</span></p>
<p>下面推导Dual SVR(课程中略):</p>
<p>首先求各个变量微分优化<span class="math inline">\(\min\)</span>:</p>
<p><span class="math display">\[\begin{align} &amp;{ \partial \mathcal L \over \partial \xi_n^{\wedge}} = C-\alpha_n^{\wedge}-\beta_n^{\wedge} = 0 \quad \quad \quad \quad(1) \\ &amp; {\partial \mathcal L \over \partial \xi_n^{\vee}} = C-\alpha_n^{\vee}-\beta_n^{\vee} = 0 \quad \quad \quad \quad (2)\\ &amp;{\partial  \mathcal L \over \partial b} = \sum(\alpha_n^{\vee}-\alpha_n^{\wedge}) = 0 \quad \quad \quad\quad (3)\\ &amp; {\partial \mathcal L \over \partial \mathbf w } = \mathbf w - \sum(\alpha_n^{\wedge}- \alpha_n^{\vee})\mathbf z_n = 0 \quad(4)\end{align}\]</span></p>
<p>一共得到了4个(组)等式，这几个等式最优解也会满足，其中<span class="math inline">\(1 \leq\alpha_n \leq C\)</span>，将<span class="math inline">\(\beta^{\vee}, \beta^{\wedge}\)</span>,以及(3)式代入到<span class="math inline">\(\mathcal L\)</span>中，消去<span class="math inline">\(\beta,b,\xi_n\)</span>,经过化简得到:</p>
<p><span class="math display">\[\begin{align}\min\limits_{\mathbf w}  \mathcal{L}(b, \mathbf w, \xi_{\vee}, \xi^{\wedge}) &amp;\Leftrightarrow \min \quad {1\over 2}\mathbf w^T\mathbf w + \sum(\alpha_n^{\vee}-\alpha_n^{\wedge})\mathbf w^T\mathbf z_n +\sum(\alpha_n^{\wedge}(y_n-\epsilon) + \alpha_n^{\vee}(-\epsilon-y_n) ) \\ &amp;\Leftrightarrow \min \quad  {1\over 2}\mathbf w^T\mathbf w -\mathbf w^T\mathbf w +\sum(\alpha_n^{\wedge}(y_n-\epsilon) + \alpha_n^{\vee}(-\epsilon-y_n) ) \\ &amp;\Leftrightarrow -{1 \over 2}\sum\limits_{n=1}^N\sum\limits_{m=1}^N(\alpha_n^{\wedge}- \alpha_n^{\vee}) (\alpha_m^{\wedge}- \alpha_m^{\vee})K(\mathbf x_n, \mathbf x_m)\\ &amp; \quad \quad  +\sum(\alpha_n^{\wedge}(y_n-\epsilon) + \alpha_n^{\vee}(-\epsilon-y_n) ) \end{align}\]</span></p>
<p>上面的过程我们已经引入了核函数代替<span class="math inline">\(\mathbf {z_nz_m}\)</span>，至此最小化过程完成，开始考虑最外面<span class="math inline">\(\max\)</span>部分，取相反数，结合约束来求最小值，最后的问题如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svr-3.png">

</div>
<p>上式就是一个标准的二次规划问题，一共有<span class="math inline">\(2N\)</span>个变量，按照格式写出QP Solver的Q,p,A,c，即可得到最优解<span class="math inline">\(\alpha_n^{\vee}, \alpha_n^{\wedge}\)</span>。</p>
<p>最后返回到模型，在对偶问题中，我们还有一个很重要的互补条件(complementary slackness)，如下:</p>
<p><span class="math display">\[\begin{align}\alpha_n^\wedge\left(\epsilon+\xi_n^\wedge-y_n+\mathbf w^T\mathbf z_n+b\right)&amp;=0\\ \alpha_n^\vee\left(\epsilon+\xi_n^\vee+y_n-\mathbf w^T\mathbf z_n-b\right)&amp;=0 \\ \beta_n^{\vee}\xi_n^{\vee}=(C-\alpha_n^{\vee})\xi_{n}^{\vee} &amp;=0 \\ \beta_n^{\wedge}\xi_n^{\wedge}=(C-\alpha_n^{\wedge})\xi_{n}^{\wedge} &amp;=0 \end{align}\]</span></p>
<p>现在考虑一些点: <span class="math inline">\(|\mathbf w^T\mathbf z_n +b - y_n| &lt; \epsilon\)</span>,也就是那些在tube内部的，预测值与真实值的误差在允许范围内的点，对于它们，以下条件成立:</p>
<p><span class="math display">\[\begin{align} &amp; \xi_n^{\vee} = \xi_n^{\wedge} = 0 \\ \Rightarrow &amp;\epsilon -y_n+\mathbf w^T\mathbf z +b\neq0  \\ &amp; \epsilon +y_n-\mathbf w^T\mathbf z -b\neq0 \\ \Rightarrow &amp; \alpha_n^{\vee} =0 \\ &amp; \alpha_n^{\wedge}=0 \end{align}\]</span></p>
<p>根据前面条件<span class="math inline">\(\mathbf w=\sum\limits_{n=1}^N(\alpha_n^{\wedge}-\alpha_n^{\vee})\mathbf z_n\)</span>，我们开始的目标之一便是想得到一个比较稀疏的系数, 现在对于在误差范围内的点<span class="math inline">\(\mathbf z_n\)</span>的系数已经为0。只有在tube外面的数据点的不为0，这些点也就是Supports Vectors。</p>
<p>对于<span class="math inline">\(b\)</span>的解法，同soft-margin SVM，从Support Vectors中考虑，只需要找到某个<span class="math inline">\(0&lt;\alpha_s&lt;C\)</span>的数据，这样由互补约束便可知<span class="math inline">\(\xi_s=0\)</span>，从而得到b:</p>
<p><span class="math display">\[b = \epsilon + y_s -\mathbf w^T\mathbf z_s = \epsilon + y_s -\sum(\alpha_n^{\wedge}-\alpha_n^{\vee})K(\mathbf x_s, x_n)\]</span></p>
<p>最后得到<strong>Support Vector Regression(SVR)</strong>模型: <span class="math inline">\(g(\mathbf x)=\sum(\alpha_n^{\wedge}-\alpha_n^{\vee})K(\mathbf x_n, \mathbf x) + b\)</span></p>
<h2 id="核模型kernel-model总结">核模型(Kernel Model)总结</h2>
<p>到此为止，线性模型/核模型这一大块的内容基本完结了，占了技法课程的40%。回顾一下，整理一下，有如下约10个模型或者算法那:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svr-4.png">

</div>
<p>在实际中我们一般使用较少的有 PLA, Linear SVR,因为效果不好，限制很大。而至于Kernel Ridge Regression(核岭回归), Kernel Logistic Regression(核逻辑回归)也使用较少，因为其权重矩阵是稠密的(dense)，计算量较大。相比核岭回归，在实际中更加倾向使用SVR; 相比核逻辑回归，更倾向于使用概率SVM。常用的一般有SVM, SVR, 线性岭回归，L2正则化的逻辑回归等。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;这一节是核模型的最后一部分了，将Kernel引入到一般的回归模型中: 核岭回归(Kernel Ridge Regression)以及支持向量回归(Support Vector Regression)，此外文末对这一占了整个课程40%的大块内容做了简单总结。&lt;/p&gt;
    
    </summary>
    
      <category term="读书笔记" scheme="http://shomy.top/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="machine learning" scheme="http://shomy.top/tags/machine-learning/"/>
    
      <category term="svr" scheme="http://shomy.top/tags/svr/"/>
    
  </entry>
  
  <entry>
    <title>机器学习技法笔记(7)-Kernel LR(核逻辑回归)</title>
    <link href="http://shomy.top/2017/03/07/kernel-lr/"/>
    <id>http://shomy.top/2017/03/07/kernel-lr/</id>
    <published>2017-03-07T14:29:33.000Z</published>
    <updated>2017-07-31T02:42:34.562Z</updated>
    
    <content type="html"><![CDATA[<h1 id="kernel-logistic-regression">Kernel Logistic Regression</h1>
<blockquote>
<p>这一节主要介绍如何将Kernel Trick引入到Logistic Regression，以及LR与SVM的结合</p>
</blockquote>
<h2 id="svm-与-正则化">SVM 与 正则化</h2>
<a id="more"></a>
<p>首先回顾Soft-Margin SVM的原始问题:</p>
<p><span class="math display">\[\begin{align}\min\limits_{b,\mathbf{w}, \xi}  \quad &amp;\frac{1}{2} \mathbf{w}^T\mathbf{w} + C \cdot \sum\limits_{n=1}^{N}\xi_n \\ s.t. \quad &amp; y_n(\mathbf{w}^T\mathbf{z}^n+b) \geq 1-\xi_n, for \ all\ n \end{align}\]</span></p>
<p>其中<span class="math inline">\(\xi_n\)</span>是训练数据违反边界的多少，没有违反的话，<span class="math inline">\(\xi_n =0\)</span>，反之<span class="math inline">\(\xi_n &gt; 0\)</span>，换句话说，目标函数的第二项就可以表示模型的损失。现在换一种方式来写，将二者结合起来: <span class="math inline">\(\xi_n = \max(1- y_n(\mathbf{w}^T\mathbf{z}^n+b),0)\)</span>，这一个等式就代表了上面的约束条件，这样上述问题，就与下面的无约束问题等价:</p>
<p><span class="math display">\[\begin{align} &amp; \min\limits_{b,\mathbf w}\left({1\over 2}\mathbf w^T\mathbf w + C\sum_{n=1}^N\max\left(1-y_n\left(\mathbf w^T\mathbf z_n + b\right), 0\right)\right) \\ \Leftrightarrow &amp;\min\limits_{b,w}\left({1\over 2}\mathbf w^T\mathbf w + C \cdot \sum \widehat{err} \right) \end{align}\]</span></p>
<p>这种形式与之前的<span class="math inline">\(L_2\)</span> 正则项很类似:</p>
<p><span class="math display">\[\min\limits_{b,\mathbf w}\left({\lambda\over N}\mathbf w^T\mathbf w + {1\over N}\sum\mbox{err}\right)\]</span></p>
<p>在<span class="math inline">\(L_2\)</span>中，通过最小化<span class="math inline">\(E_{in}\)</span>的同时控制<span class="math inline">\(\mathbf{w}\)</span>的大小，防止模型过度复杂。从正则化的角度来看的话，Soft-Margin 的SVM其实就属于一种正则化模型，如下的对比:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/klr-1.png">

</div>
<p>其中SVM中的<span class="math inline">\(C\)</span>与<span class="math inline">\(L_2\)</span>中的<span class="math inline">\(\lambda\)</span>对应，<span class="math inline">\(C\)</span>越大，表示注重<span class="math inline">\(E_{in}\)</span>，那么<span class="math inline">\(\lambda\)</span>就越小，正则化的效果相对就弱。</p>
<p>不过从正则化角度考虑Soft-Margin SVM的话，因为错误函数<span class="math inline">\(\widehat{err}\)</span>不是可微分的，因此不容易求解，因此我们采用的是二次规划来求解的。</p>
<h2 id="svm-with-soft-binary-classification">SVM With Soft Binary Classification</h2>
<p>上面我们定义了<span class="math inline">\(\widehat{err}\)</span>作为SVM的一种特殊的误差函数，也被叫做<strong>hinge error mesure</strong>，下面对比一下几种常见二分类的损失函数: PLA, Logistic Regression, Soft-Margin SVM，令<span class="math inline">\(s=\mathbf W^T \mathbf z_n + b\)</span>:</p>
<p><span class="math display">\[\begin{align} PLA: \quad &amp; err_{0/1}(s, y) = [[ys \leq 0]] \\ LR: \quad &amp; err_{SCE}(s,y) = \log_{2}(1+\exp(-ys)) \\ SVM: \quad &amp; \widehat{err}_{SVM}(y,s) = \max(1-ys,0)\end{align}\]</span></p>
<p>然后把三者的图像绘出来，如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/klr-2.png">

</div>
<p>我们可以看出来SVM与LR的误差函数很类似，都表示0/1误差的上限，我们知道误差函数对于一个模型来说是至关重要的，那么从这个角度看的话，再结合第一部分Soft-Margin SVM与<span class="math inline">\(L_2\)</span>的正则项的形式很相似，那我们可以在一定程度上认为:Soft margin SVM $ L_2 $ Regulared LR。 不过二者各有优点，比如SVM可以通过Kernel Trick 分类非线性的数据，而LR通过最大似然的方式则可以得到属于某类的概率值。</p>
<p>那我们可以将二者结合起来，也就是说使用SVM来做“软分类”，最终得到某类的概率值，有两种很直接的方式如下:</p>
<ul>
<li>第一种: 首先使用SVM得到<span class="math inline">\((b_{SVM}, \mathbf w_{SVM})\)</span>，然后直接使用sigmod函数计算概率，得到模型: $g(x) = (W^T_{SVM} x + b_{SVM}) $</li>
<li>第二种: 使用SVM得到<span class="math inline">\((b_{SVM}, \mathbf w_{SVM})\)</span>，然后作为LR的初始<span class="math inline">\(\mathbf w_0\)</span>,再运行LR得到最终的模型</li>
</ul>
<p>第一种方式最直接，也可以得到概率，但是没有用到LR的优点，比如最大似然。而第二种则更没有用到Kernel Trick，充其量只是加快了LR的收敛速度。那么我们应该如何融合两者的优势呢？下面给出一个可能的模型，如下:</p>
<p><span class="math display">\[g(\mathbf x) = \theta(A \cdot (\mathbf w^T_{SVM}\phi(\mathbf x)+b_{SVM}) + B)\]</span></p>
<p>首先使用Kernel SVM得到<span class="math inline">\((\mathbf w_{SVM}, b_{SVM})\)</span>, 对数据做变换，如果令<span class="math inline">\(z_n=\mathbf w^T_{SVM}\phi(\mathbf x)+b_{SVM}\)</span>, 那么后面就是一个很简单的LR模型:<span class="math inline">\(g(x)=\theta(A\cdot z_n + B)\)</span>这样来看，第一个阶段其实就属于一种基于SVM的特征转换，或者说某种映射，将数据映射到了<span class="math inline">\(z\)</span>-space,，得到新的数据:<span class="math inline">\(\{ z_n, y_n\}\)</span>。然后在新数据上跑一个LR模型，得到最终的结果，这里需要注意的是，得到的<span class="math inline">\(z_n\)</span>是一个一维的实数，这样大大减少了LR的计算复杂度。LR的两个参数<span class="math inline">\(A,B\)</span>的作用则是fine-tune(微调)，平移或者放缩。一般情况下，如果SVM的效果好的话，<span class="math inline">\(A&gt;0, B \approx 0\)</span>。</p>
<p>这样一来，新的模型就可以结合了SVM与LR二者的优势，这种模型最早是由Platt提出的，因此也叫作<strong>Platt’s Model of Probabilisitc SVM</strong>(概率模型)，具体步骤总结如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/klr-3.png">

</div>
<p>如果把Kernel体现到模型的话，直接展开即可，最后的模型就是:</p>
<p><span class="math display">\[\theta\left( \sum\limits_{SV}A \alpha_ny_nK(\mathbf x_n, \mathbf x) + Ab_{SVM} +B\right)\]</span></p>
<p>需要说明的是，这种模型是利用SVM的解，然后将其作为了LR的在Z-Space的近似解，实质上并没有真正的在Z-Space求解LR(A,B只是起到了微调作用)，它的主要贡献是得到了概率SVM模型。</p>
<h2 id="kernel-logistic-regression核逻辑回归">Kernel Logistic Regression(核逻辑回归)</h2>
<p>这一部分则是介绍如何真正的在Z-Space使用Log-Reg模型，而非使用SVM的近似解，说到空间映射，Kernel Trick(核技巧)不得不提，那我们如何将Kernel运用到LR中呢？核到底是如何起作用的，回想SVM，我们从SVM的标准问题，转到对偶问题，然后因为对偶问题涉及到了Z-Space的内积，计算量很大，由此引出了Kernel Trick，<span class="math inline">\(\mathbf z_n \mathbf z = K(\mathbf x_n , \mathbf x)\)</span>，进而求出<span class="math inline">\(\mathbf w_{*} = \sum\beta_n \mathbf z_n\)</span>，实际上，只有将最优解<span class="math inline">\(\mathbf w\)</span>表示为<span class="math inline">\(\mathbf z_n\)</span>的线性组合，才能够利用核函数<span class="math inline">\(K\)</span>，因为模型中需要计算<span class="math inline">\(\mathbf w_{*}^{T} \mathbf z\)</span>:</p>
<p><span class="math display">\[\mathbf w_{*}^{T} \mathbf z = \sum\beta_n\mathbf z_n \mathbf z = \sum \beta_n K(\mathbf x_n, \mathbf x)\]</span></p>
<p>试想如果最优解<span class="math inline">\(\mathbf w_{*}\)</span>不是<span class="math inline">\(\mathbf z\)</span>的线性组合，那模型中的<span class="math inline">\(\mathbf w_{*}^{T} \mathbf z\)</span>就无法转为核函数的表达式了，那么核就无用武之地了(反过来想想，我们将数据从X-space转换到Z-Space的一个目的就是可以线性可分)。 因此，我们就可以得到一个结论，<strong>如果模型最优的<span class="math inline">\(\mathbf w_{*}\)</span>是Z-Space的线性组合，那么我们就可以把Kernel Trick运用到该模型中</strong>。举几个前面的例子:</p>
<ul>
<li>SVM: <span class="math inline">\(\mathbf w_{svm} = \sum (\alpha_ny_n)\mathbf z_n\)</span>，其中<span class="math inline">\(\alpha\)</span>是对偶问题中的拉格朗日乘子</li>
<li>PLA: <span class="math inline">\(\mathbf w_{PLA} = \sum (\alpha_ny_n)\mathbf z_n\)</span>，这里的<span class="math inline">\(\alpha\)</span>则是在修正<span class="math inline">\(\mathbf w\)</span>的过程中，<span class="math inline">\(\mathbf z_n\)</span>这个点被分错误的次数</li>
<li>LR: <span class="math inline">\(\mathbf w_{LR} = \sum (\alpha_ny_n)\mathbf z_n\)</span>，这里的<span class="math inline">\(\alpha_n\)</span>可以理解为在用梯度下降法更新<span class="math inline">\(\mathbf w\)</span>的步长</li>
<li>…</li>
</ul>
<p>上面的三种模型中的<span class="math inline">\(\mathbf w\)</span>可以被数据表示，也就是说，我们都可以把Kernel Trick加到模型中(可以用来解决非线性可分)，那到底有什么特征的模型，才满足条件呢？下面给出一个很重要的定理: <strong>Representer Theorem</strong>：</p>
<blockquote>
<p>对于任意的<span class="math inline">\(L_2\)</span>正则化的线性模型，目标函数如下:</p>
<p><span class="math display">\[\min\limits_{\mathbf w}  \quad  {\lambda \over N}\mathbf  w^T \mathbf w + {1 \over N}\sum\limits_{n=1}^{N}err(y_n, \mathbf w^T \mathbf z_n)\]</span></p>
<p>均有最优的参数<span class="math inline">\(\mathbf w_{*} = \sum \beta_n\mathbf z_n\)</span></p>
</blockquote>
<p>简单证明如下:</p>
<blockquote>
<p>设最优解<span class="math inline">\(\mathbf w_{*} = \mathbf w_{||} + \mathbf w_{\perp}\)</span>，其中<span class="math inline">\(\mathbf w_{||} \in span(\mathbf z_n), \mathbf w_{\perp} \perp span(\mathbf z_n)\)</span></p>
<p>反证法，假设<span class="math inline">\(\mathbf w_{\perp} \neq \mathbf 0\)</span>,将上述的<span class="math inline">\(E_{in}\)</span>分为两部分，先看第二部分的err，<span class="math inline">\(err(y_n, \mathbf w_{*}^T\mathbf z_n) = err(y_n, (\mathbf w_{||}+\mathbf w_{\perp})\mathbf z_{n}) = err(y_n, \mathbf w_{||}\mathbf z_n)\)</span>, 这部分与<span class="math inline">\(\mathbf w_{\perp}\)</span> 无关，看第一部分的正则项: <span class="math inline">\(\mathbf w_{*}^T \mathbf w_{*} = \mathbf w_{||}^T \mathbf w_{||} + 2\mathbf {w_{||}^T w_{\perp} } + \mathbf{w_{\perp}^T w_{\perp}} &gt; \mathbf w_{||}^T \mathbf w_{||}\)</span> ,矛盾产生，因为<span class="math inline">\(\mathbf w_{||}\)</span>比<span class="math inline">\(\mathbf w_{*}\)</span>有更小的误差。从而最优解与<span class="math inline">\(\mathbf z\)</span>在同一个超平面，定理得证。</p>
</blockquote>
<p>而前面介绍的<span class="math inline">\(L_2\)</span>正则化的Logistic Regression的目标函数:</p>
<p><span class="math display">\[\min_{\mathbf w} \quad {\lambda\over N}\mathbf w^T\mathbf w+{1\over N}\sum_{n=1}^N\log\left(1+\exp\left(-y_n\mathbf w^T\mathbf z_n\right)\right)\]</span></p>
<p>很显然，LR的最优解: <span class="math inline">\(\mathbf w_{*} = \sum \beta_n\mathbf z_n = \beta^T\mathbf Z\)</span>，既然这样，那么我们就直接不再求<span class="math inline">\(\mathbf w\)</span>,直接求解<span class="math inline">\(\beta\)</span>，然后加入Kernel Function，这样目标函数如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/klr-4.png">

</div>
<p>这是一个无约束的优化问题，可以使用梯度下降等方式来来得到最优解。</p>
<p>到此，我们成功将Kernel 加入到了Logistic Regression，得到的模型一般叫做<strong>Kernel Logistics Regression(KLR)</strong>，这样就可以很好地解决非线性问题了。</p>
<p>最后说一句KLR其实相当于<span class="math inline">\(\beta\)</span>的线性模型了，核函数<span class="math inline">\(K\)</span> 就相当于特征转换，左侧的<span class="math inline">\(\sum\sum\)</span>写成矩阵形式就是: <span class="math inline">\(\beta^TK\beta\)</span>相当于正则项。此外，在Kernel SVM中<span class="math inline">\(\alpha\)</span>很稀疏，有很多项为0，Kernel LR中的<span class="math inline">\(\beta\)</span>则大部分都不为0。</p>
<h2 id="总结">总结</h2>
<p>这一讲主要讲了SVM与LR的相互交叉，得到两个新的模型，有概率的SVM以及带核的LR。Kernel Trick并不是SVM专有，在线性模型中，只需要满足表示定理就可以加入核，下一节我们就会在一般的Linear Regression中引入核。一般来说，引入Kernel，效果会好，不过计算量自然也就大了。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;kernel-logistic-regression&quot;&gt;Kernel Logistic Regression&lt;/h1&gt;
&lt;blockquote&gt;
&lt;p&gt;这一节主要介绍如何将Kernel Trick引入到Logistic Regression，以及LR与SVM的结合&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id=&quot;svm-与-正则化&quot;&gt;SVM 与 正则化&lt;/h2&gt;
    
    </summary>
    
      <category term="读书笔记" scheme="http://shomy.top/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="machine learning" scheme="http://shomy.top/tags/machine-learning/"/>
    
      <category term="kernel" scheme="http://shomy.top/tags/kernel/"/>
    
      <category term="logistic regression" scheme="http://shomy.top/tags/logistic-regression/"/>
    
  </entry>
  
  <entry>
    <title>机器学习技法笔记(6)-RBF Network(径向基函数网络)</title>
    <link href="http://shomy.top/2017/02/26/rbf-network/"/>
    <id>http://shomy.top/2017/02/26/rbf-network/</id>
    <published>2017-02-26T14:18:37.000Z</published>
    <updated>2017-07-31T02:42:34.562Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>2017.3.3更新: TensorFlow版本KMeans与RBFNet</p>
</blockquote>
<blockquote>
<p>2017.2.28更新：Python实现RBFNet</p>
</blockquote>
<blockquote>
<p>这一节利用上节SVM中的高斯核(RBF Kernel)来介绍一个新的模型，RBF Network，属于神经网络(Neural Network)的一种，可以以任意精度来逼近任意的连续函数。</p>
</blockquote>
<a id="more"></a>
<h2 id="radial-basis-function-介绍">Radial Basis Function 介绍</h2>
<p>在SVM中，我们只是提到了RBF(径向基函数)可以作为Kernel Function，并且实现了数据到无穷维度的转化，十分Powerful。这里介绍一下RBF的原本含义，这里参考<a href="http://blog.csdn.net/zouxy09/article/details/13297881" target="_blank" rel="external"><strong>径向基网络（RBF network）之BP监督训练</strong></a> 中的对RBF的说明，写的十分清楚，这里直接引用:</p>
<blockquote>
<p>径向基函数（Radical Basis Function，RBF）方法是Powell在1985年提出的。所谓径向基函数，其实就是某种沿径向对称的标量函数。通常定义为空间中任一点x到某一中心c之间欧氏距离的单调函数，可记作k(||x-c||)，其作用往往是局部的，即当x远离c时函数取值很小。例如高斯径向基函数：</p>
<p><span class="math inline">\(RBF(x,c)=exp\left( - \frac{(x-c)^2}{r^2}\right)\)</span></p>
<p>当年径向基函数的诞生主要是为了解决多变量插值的问题。可以看下面的图。具体的话是先在每个样本上面放一个基函数，图中每个蓝色的点是一个样本，然后中间那个图中绿色虚线对应的，就表示的是每个训练样本对应一个高斯函数（高斯函数中心就是样本点）。然后假设真实的拟合这些训练数据的曲线是蓝色的那根（最右边的图），如果我们有一个新的数据x1，我们想知道它对应的f(x1)是多少，也就是a点的纵坐标是多少。那么由图可以看到，a点的纵坐标等于b点的纵坐标加上c点的纵坐标。而b的纵坐标是第一个样本点的高斯函数的值乘以一个大点权值得到的，c的纵坐标是第二个样本点的高斯函数的值乘以另一个小点的权值得到。而其他样本点的权值全是0，因为我们要插值的点x1在第一和第二个样本点之间，远离其他的样本点，那么插值影响最大的就是离得近的点，离的远的就没什么贡献了。所以x1点的函数值由附近的b和c两个点就可以确定了。拓展到任意的新的x，这些红色的高斯函数乘以一个权值后再在对应的x地方加起来，就可以完美的拟合真实的函数曲线了。</p>
</blockquote>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/rbf-1.png">

</div>
<p>一句话总结就是，RBF(径向基函数)根据某种距离来描述点与某个center点的的相似度。</p>
<h2 id="rbf-network">RBF Network</h2>
<p>有了上述RBF的概念，以及插值问题的介绍，下面直接引入RBF Network:</p>
<p><span class="math display">\[h(\mathbf{x}) = Output\left( \sum\limits_{m=1}^M\beta_mRBF(\mathbf{x}, \mu_m)\right)\]</span></p>
<p>其中<span class="math inline">\(\mu_m​\)</span>是第<span class="math inline">\(m​\)</span>个center，就是上图中的红色图像的峰值对应的<span class="math inline">\(x​\)</span>, <span class="math inline">\(\beta_m​\)</span>则是对应的权重，最外层的<span class="math inline">\(Output()​\)</span> 则是根据目的选择不同的函数，比如想分类的话，那就可以使用一个softmax或者sign等函数，如果打算做回归，或者做函数逼近，这时候就可以不需要用Output函数了，RBF Network可以逼近任意连续的函数。</p>
<p>RBF Network与普通神经网络有什么联系区别呢？如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/rbf-2.png">

</div>
<p>首先网络结构很类似，都是输入层, 隐藏层，输出层构成，此外最后一层的输出层基本类似类似，均是对隐层的输出线性组合(权重控制)然后得到求和的结果。</p>
<p>不过一般情况RBF Network只有三层，其中从输入层到隐层之间并没有权重连接，而是直接将用隐层的RBF计算与不同的中心(隐层神经元)的距离或者相似度，距离越远，相似度越低，神经元的激活程度就越小，作用也就越不明显，此外这个过程也可以以Kernel SVM的角度理解: 把原始低维的数据进行转换到高维空间中(高斯核对应无穷维)的特征转换。因而RBF Network的隐层激活函数可以说就是径向基函数，而一般Neural Network的激活函数则是一些如sigmod, tanh等非线性函数。当然很明显，RBF Network由于只有隐层到输出层的权重连接，因而训练速度会大大加快。</p>
<p>这里多说一点，假设输入数据的维度为<span class="math inline">\((d,1)\)</span>,隐藏层神经元个数为<span class="math inline">\(h_n\)</span>, BP神经网络的隐藏层的每一个神经元对应的维度与输入层的每一个神经元维度相同，都是1维，即就是一个数，那么整个隐藏层的维度就是<span class="math inline">\([h_n,1]\)</span>。而RBFNet 虽然整个隐层维度也是<span class="math inline">\((h_n,1)\)</span>，但是实现不同，RBF的输入层同BP一样，都是一个数据点的所有维度，而隐藏层的每一个神经元则是，整个输入数据与当前隐层神经元也就是那个中心计算RBF函数的结果，代表了相似度，计算完相似度之后，再结合隐含层到输出层的权重，得到最后的结果。</p>
<blockquote>
<p>计算两个点的距离，除了用高斯距离之外，还有一些其他的选择，只要包含<span class="math inline">\(||x-c||^2\)</span>项即可。</p>
</blockquote>
<h2 id="几种形式">几种形式</h2>
<p>RBF的隐层神经元也就是center的选择是个很关键的问题，因为只有中心确定了之后，RBF函数才能够确定。下面基于不同的中心介绍两种类型的RBF。</p>
<h3 id="full-rbf-network">Full RBF Network</h3>
<p>Full RBF Network，顾名思义便是所有的数据节点都作为中心。</p>
<p><span class="math display">\[h(\mathbf{x}) = Output\left( \sum\limits_{m=1}^M\beta_mRBF(\mathbf{x}, \mu_m)\right)\]</span></p>
<p>即<span class="math inline">\(M=N, \mu_m=\mathbf{x}_m\)</span>, 这样的话，预测新的数据点，需要计算该点与所有训练数据点的距离，也就是相似度，然后结合权重<span class="math inline">\(\beta\)</span>进行线性组合，此过程就是将所有的训练数据点对预测点的影响聚集到一起，距离越近，影响越大，这样得到最终的结果。比如用均匀影响做分类的话，即<span class="math inline">\(\beta_m =1 \cdot y_m\)</span>,即:</p>
<p><span class="math display">\[g(\mathbf{x}) = sign \left( \sum\limits_{m=1}^Ny_m \exp\left( -\gamma ||\mathbf{x}-\mathbf{x}_m||^2\right)\right)\]</span></p>
<p>就相当于每个训练数据给新数据投票，通过RBF，可以使得距离远的票数大，近的票数小，同时，<span class="math inline">\(y_m\)</span>可以控制投票的类别，这样通过aggregate所有点的opinion，就可以得到新数据的类别了。</p>
<p>但是很显然， 这种Full RBF Network是一种偷懒的方式，因为直接将所有的点作为了center，因此如果样本量很大的话，那么计算量就太大了，在实际中，很少使用。</p>
<h3 id="k-nearest-neighborknn">K Nearest Neighbor(KNN)</h3>
<p>将上面的方法稍微改变一下，就可以得到另外一种很常见的机器学习方法-KNN(K近邻)。在Full RBF中，我们计算所有的训练数据与新数据的距离，距离最小的与新数据的相似度最高，而且高斯函数衰减很快，距离新数据远的点对它的影响很小，因此我们可以忽略那些，只需要找到几个最靠近新数据的点，然后只计算它们的贡献即可，假设我们找最近的K个点的话， 那就是K近邻算法。这种类型的算法在训练的时候，不用花力气，但是再做测试或者预测的时候，需要对比全部的数据，然后找到几个最近的，计算这几个的贡献得到最后结果，这个过程跟上面的Full RBF 一样，计算量很大。 因此这种方式经常在样本数据较少的时候使用。</p>
<h3 id="空间转换">空间转换</h3>
<p>再看回归问题:</p>
<p><span class="math display">\[h(\mathbf{x}) = \sum\limits_{m=1}^M\beta_mRBF(\mathbf{x}, \mathbf{x}_m)\]</span></p>
<p>如果我们令:</p>
<p><span class="math display">\[\mathbf{z}_n = [RBF(\mathbf{x}_n, \mu_1), RBF(\mu_n, \mathbf{x}_2), \cdots, RBF(\mathbf{x}_n, \mu_M)]\]</span></p>
<p>将相当于前面所说的将<span class="math inline">\(x_n\)</span> 利用RBF转换到<span class="math inline">\(\mathbf{z}\)</span>空间，每一项都是两个点的相似度，将所有的数据这样我们的问题，就是简单的Linear Regression了:</p>
<p><span class="math display">\[h(\mathbf{z}) = \sum\beta_m\mathbf{z}_m\]</span></p>
<p>然后直接得到最优解<span class="math inline">\(\beta=(\mathbf{Z}^T\mathbf{Z})^{-1}\mathbf{Z}^T\mathbf{y}\)</span>，当然这里使用的是伪逆矩阵。如果其中矩阵<span class="math inline">\(\mathbf{Z}\)</span>是由所有的<span class="math inline">\(\mathbf{z_n}, \ n=1,\cdots,N\)</span>组成，维度为: <span class="math inline">\((N,M)\)</span>。</p>
<p>还有一种特殊情况，比如所有的训练数据作为中心的话，那么<span class="math inline">\(N=M\)</span>，此时<span class="math inline">\(\mathbf{Z}\)</span>矩阵为对称矩阵 再加上如下一个定理:</p>
<blockquote>
<p>如果所有的<span class="math inline">\(\mathbf{x}_n\)</span>互不相同，那么Gaussian RBF形成的<span class="math inline">\(\mathbf{Z}\)</span>矩阵一定是可逆的</p>
</blockquote>
<p>这样，如果满足条件的话，<span class="math inline">\(\mathbf{Z}\)</span>可逆并且对称的话，那么经过对<span class="math inline">\(\beta\)</span>的化简，最优的<span class="math inline">\(\beta =\mathbf{Z}^{-1}\mathbf{y}\)</span>,计算量大大减少。(此处暂时省略Regularized Full RBF Netework)</p>
<h2 id="k-means">K-Means</h2>
<p>上述的几种形式基本都属于偷懒的方式，因为我们没有花费力气去找一些好的中心，都是使用所有的数据作为RBF的center, 一旦样本量变大，一则计算量变大，二则更容易过拟合，降低泛化性能，因此我们很有必要花点力气去找一些更加靠谱数量更少的center。其实回想RBF Kernel SVM:</p>
<p><span class="math display">\[g_{svm}(\mathbf{x})= sign\left( \sum\limits_{SV}\alpha_n y_n\exp(-\gamma||\mathbf{x}-\mathbf{x}_n||^2)\right)\]</span></p>
<p>中心点其实就是那些Support Vectors，而非所有的数据。如何去选择中心点，实际就是要将训练点分为几簇，给每一簇要找一个代表，并且簇与簇之间不能有交集，类似与离散数学中的划分的概念，假设所有的数据划分为<span class="math inline">\(M\)</span>个簇:<span class="math inline">\(S_1, S_2,\cdots,S_M\)</span>,其中<span class="math inline">\(\mu_m\)</span>为<span class="math inline">\(S_m\)</span>的中心(prototype)，那么我们就希望：</p>
<p>​ <span class="math display">\[\mathbf{x}_1,\mathbf{x}_2 \in S_m \Leftrightarrow \mu_m \approx \mathbf{x_1} \approx \mathbf{x_2}\]</span></p>
<p>也就是在同一簇中的点，都应该是最相似的。这样我们的问题成为找到这样的簇，以及簇的中心，一个很典型的聚类(Cluster)问题，同时也是无监督的，利用相似度，很容易写出损失函数:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/rbf-3.png">

</div>
<p>其中 <span class="math inline">\(S_1,\cdots,S_m\)</span>是训练数据集合<span class="math inline">\(\{\mathbf{x}_n \}\)</span>的的一个划分。</p>
<p>除了离散项之外，还需要对<span class="math inline">\(S,\mu\)</span>两个变量同时优化，因此原式很难直接求最优解。下面使用一种<strong>alternating optimization</strong>(交替优化)的方法求解，原理就是先固定某一个变量，然后更新优化另一个，然后反过来，继续交替更新，直到收敛。</p>
<p>首先固定中心点，即<span class="math inline">\(\mu_1,\cdots,\mu_M\)</span>确定，更新<span class="math inline">\(S_1,\cdots,S_M\)</span>的元素，我们需要对每个<span class="math inline">\(x_n\)</span>找到它最应该划分到哪个<span class="math inline">\(S\)</span>内，很简单，我们需要找到距离<span class="math inline">\(\mathbf{x}_n\)</span>最近的那个中心<span class="math inline">\(\mu_m\)</span>即可，这样<span class="math inline">\(\mathbf{x}_n\)</span>划到<span class="math inline">\(S_m\)</span>内，即:</p>
<p><span class="math display">\[\arg\min\limits_{m}||\mathbf{x}_n - \mu_m||^2\]</span></p>
<p>然后固定簇，即<span class="math inline">\(S_1,S_2,...,S_M\)</span>确定，更新簇的中心:<span class="math inline">\(\mu_1, \mu_2...\mu_M\)</span>。一旦簇确定了，那么<span class="math inline">\(\mathbf{x}_n \in S_m\)</span>这一项已经确定，要么1要么0，单独考虑其中一项<span class="math inline">\(\mu_m\)</span>,问题就简化如下:</p>
<p><span class="math display">\[E_{in} = \min\limits_{u_m}  \sum\limits_{\mathbf{x}_n \in S_m}||\mathbf{x}_n-\mu_m||^2\]</span></p>
<p>很显然，这是一个无约束的单变量优化问题，直接求一阶微分:</p>
<p><span class="math display">\[\begin{align}\frac{\partial E_{in}}{\partial \mu_m} &amp;= -2\left( \sum\limits_{\mathbf{x}_n \in S_m}\mathbf{x}_n - \mu_m\right)\\&amp;=-2\left(\left( \sum\limits_{\mathbf{x}_n \in S_m}\mathbf{x}_n\right) - |S_m|\mu_m\right)=0 \\  \mu_m &amp;=\frac{\sum\limits_{\mathbf{x}_n \in S_M} \mathbf{x}_n}{|S_m|}\end{align} \]</span></p>
<p>也就是说, <span class="math inline">\(S_m\)</span>的中心等于簇内所有点的平均值。</p>
<p>交替更新完一轮<span class="math inline">\(S,\mu\)</span>之后，重复这个过程直到收敛，整个聚类的算法就是著名的<strong>K-Means</strong>，下面给出基本流程:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/rbf-4.png">

</div>
<p>注:</p>
<ul>
<li>K的选择，一般需要一些先验知识，影响很大</li>
<li><span class="math inline">\(\mu\)</span> 初始化很关键，对整个收敛速度与质量影响很大，需要多测试</li>
<li>收敛性，K-Means可以一定收敛，因为交替更新的两个过程都是在使得<span class="math inline">\(E_{in}\)</span>下降。不过由于<span class="math inline">\(E_{in}\)</span> 并不是凸函数，因此我们得到可能不是全局最优解。</li>
<li>K-Means属于无监督的特征转换过程，类似与AutoEncoder。</li>
</ul>
<h2 id="rbf-net-with-k-means">RBF Net With K-Means</h2>
<p>有了K-Means来聚类，那我们就可以用确定RBFNet的中心了，下面重新给出使用K-Means的RBF Net用来做预测回归的流程:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/rbf-5.png">

</div>
<p>这个过程与上面唯一不同的地方就是，开始先使用K-Means得到了中心，然后再计算特征转换的<span class="math inline">\(Z\)</span>矩阵，其余步骤完全相同。</p>
<p>RBF Net的超参数只有三种: <span class="math inline">\(M\)</span>(中心个数)，RBF函数中的，比如高斯里面的<span class="math inline">\(\gamma\)</span>，以及权重。因此RBFNet是一种简单，快速的逼近算法，可以用于回归，分类，其中做函数逼近更多一些，下一节重点介绍训练以及实现。</p>
<h2 id="实现">实现</h2>
<p>借助上述的理论，使用Python实现了简单的基于K-Means的RBFNet，以及用tensorflow实现了GPU版本的K-Means以及RBFNet，速度明显提升。</p>
<h3 id="数据">数据</h3>
<p>手动生成数据，目的是做函数逼近，提供了两种类型的数据: y = 2x 与 y=sum(x)，生成的数据量以及数据维度都是设定。</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">getData</span><span class="params">(n=<span class="number">100</span>, d=<span class="number">5</span>, method=<span class="string">'sum'</span>)</span>:</span></div><div class="line">    <span class="string">'''</span></div><div class="line"><span class="string">    生成数据</span></div><div class="line"><span class="string">    '''</span></div><div class="line">    X = np.random.uniform(<span class="number">1.</span>, <span class="number">3.0</span>,(n,d))</div><div class="line">    <span class="keyword">if</span> method == <span class="string">'sum'</span>:</div><div class="line">        y = np.sum(X, axis=<span class="number">1</span>)</div><div class="line">    <span class="keyword">else</span>:</div><div class="line">        y = <span class="number">2</span>*X</div><div class="line">    <span class="keyword">return</span> X,y</div></pre></td></tr></table></figure>
<h3 id="k-means-1">K-Means</h3>
<p>核心就是两步交替更新，首先更新簇元素，再更新簇内中心。</p>
<h3 id="rbfnet">RBFNet</h3>
<p>模型如下:</p>
<p><span class="math display">\[\begin{align}h(\mathbf{x}) &amp;= \sum\limits_{m=1}^M\beta_mRBF(\mathbf{x}, \mu_m)  \\ &amp;=\sum\limits_{m=1}^M\beta_m * \exp(-\gamma_m * ||\mathbf{x}-\mathbf{c}||^2) \end{align}\]</span></p>
<p>首先有三个参数需要确定:权重参数<span class="math inline">\(\beta\)</span>， 高斯函数扩展参数<span class="math inline">\(\gamma\)</span>, 另外就是隐层的神经元个数，即K-Means的k。首先看<span class="math inline">\(\gamma\)</span>, 不同的神经元对应的扩展参数理论上可以不一样，可以用下面的方式计算:</p>
<ul>
<li>计算出每个簇内的发散平均程度，即距离中心的平均距离: <span class="math inline">\(\sigma = \frac{1}{m}\sum||\mathbf{x}-\mu_m||\)</span></li>
<li><span class="math inline">\(\gamma_m = \frac{1}{2\sigma^2}\)</span></li>
</ul>
<p>上述方式是一种比较合理的方式计算<span class="math inline">\(\gamma\)</span>,不过也可以直接将所有的<span class="math inline">\(\gamma\)</span>设定为同一个固定值，效果也类似，一般是一个比较小的值，如0.1。</p>
<p>再看隐层神经元个数，在某个层面上说，RBFNet的核心在于将原始数据经过特征转化，因此一般神经元个数要高于原始数据的维度，从而达到升高维度的目的。</p>
<p>最后权重参数<span class="math inline">\(\beta\)</span>。在实验中，函数逼近属于回归问题，这里提供两种求解方式，</p>
<ul>
<li>使用Linear Regression就可以直接解出<span class="math inline">\(\beta\)</span>:<span class="math inline">\(\beta = (\mathbf{Z^TZ})^{-1}\mathbf{Z}^T\mathbf{y}\)</span>，其中<span class="math inline">\(\mathbf{Z}\)</span>是所有训练数据的rbf的结果，<span class="math inline">\(\mathbf{y}\)</span>是所有训练数据的y。</li>
<li>按照梯度下降的方式来训练参数(只在TensorFlow版本实现)</li>
</ul>
<h3 id="结果">结果</h3>
<p>CPU版本：</p>
<ol style="list-style-type: decimal">
<li>使用<span class="math inline">\(y=sum(x)\)</span>的3000条数据，逼近结果如下:</li>
</ol>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div></pre></td><td class="code"><pre><div class="line">$ python run.py</div><div class="line">X: [ 2.76801352  2.09847071  1.26193386  1.24845461  1.40333726]</div><div class="line">real      : 8.78020996497</div><div class="line">prediction: 8.71215929411</div><div class="line">******************************</div><div class="line">X: [ 1.00621598  1.81564319  1.59844746  2.52523288  1.27452441]</div><div class="line">real      : 8.22006392064</div><div class="line">prediction: 8.24858828932</div><div class="line">******************************</div><div class="line">X: [ 2.90835704  2.22760245  1.80146849  1.37866698  1.21111461]</div><div class="line">real      : 9.52720958234</div><div class="line">prediction: 9.40142232908</div><div class="line">******************************</div><div class="line">X: [ 1.93636418  2.54609154  1.80642139  2.33747451  1.25772174]</div><div class="line">real      : 9.88407335723</div><div class="line">prediction: 10.1683670115</div><div class="line">******************************</div><div class="line">X: [ 2.80741503  1.35740491  2.26844426  2.62627921  1.73952771]</div><div class="line">real      : 10.7990711232</div><div class="line">prediction: 10.8892856428</div><div class="line">******************************</div><div class="line">python run.py  9.02s user 0.50s system 106% cpu 8.947 total</div></pre></td></tr></table></figure>
<ol start="2" style="list-style-type: decimal">
<li>使用<span class="math inline">\(y=2x\)</span>的3000条数据，逼近效果如下:</li>
</ol>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div></pre></td><td class="code"><pre><div class="line">$ python run.py </div><div class="line">X: [ 2.64225429  1.16385274  2.69488719  2.58292883  2.31537922]</div><div class="line">real      : [ 5.28450858  2.32770548  5.38977438  5.16585766  4.63075843]</div><div class="line">prediction: [ 5.11075705  2.36091391  5.29326006  5.02440494  4.63639069]</div><div class="line">******************************</div><div class="line">X: [ 2.79241969  2.69607337  2.07437327  2.73446838  1.19582012]</div><div class="line">real      : [ 5.58483938  5.39214674  4.14874654  5.46893677  2.39164025]</div><div class="line">prediction: [ 5.21027141  5.08792887  4.06967137  5.14665798  2.53483961]</div><div class="line">******************************</div><div class="line">X: [ 2.60551718  2.97657156  2.80778824  2.47291552  2.84918172]</div><div class="line">real      : [ 5.21103436  5.95314312  5.61557648  4.94583105  5.69836345]</div><div class="line">prediction: [ 4.84442944  5.35447225  5.0674437   4.57218657  5.18815626]</div><div class="line">******************************</div><div class="line">X: [ 2.44505957  1.70038561  1.99612367  1.1245514   1.18954801]</div><div class="line">real      : [ 4.89011913  3.40077121  3.99224733  2.2491028   2.37909603]</div><div class="line">prediction: [ 4.8029322   3.39746125  3.94098729  2.30900805  2.52864718]</div><div class="line">******************************</div><div class="line">X: [ 1.85397812  2.83347041  1.31929703  1.35131915  2.29165357]</div><div class="line">real      : [ 3.70795624  5.66694081  2.63859405  2.7026383   4.58330715]</div><div class="line">prediction: [ 3.66509186  5.63858479  2.65371075  2.73222885  4.53157419]</div><div class="line">******************************</div><div class="line">python run.py  6.84s user 0.58s system 114% cpu 4.751 total</div></pre></td></tr></table></figure>
<p>GPU版本结果:</p>
<ol style="list-style-type: decimal">
<li>使用<span class="math inline">\(y=sum(x)\)</span>的3000数据:</li>
</ol>
<p><figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div></pre></td><td class="code"><pre><div class="line">X: [ 1.62451696  1.02659273  2.81655526  1.12427032  2.43579245]</div><div class="line">real      : [ 9.02772713]</div><div class="line">prediction: [ 8.7921114]</div><div class="line">******************************</div><div class="line">X: [ 2.53124356  1.34387374  1.38431156  1.20624399  2.59180689]</div><div class="line">real      : [ 9.05747986]</div><div class="line">prediction: [ 8.99416161]</div><div class="line">******************************</div><div class="line">X: [ 1.70616233  2.51688766  1.07848394  1.27796292  1.98958588]</div><div class="line">real      : [ 8.56908321]</div><div class="line">prediction: [ 8.56313705]</div><div class="line">******************************</div><div class="line">X: [ 2.03224301  2.65368748  1.41390944  2.04034257  1.04631364]</div><div class="line">real      : [ 9.18649578]</div><div class="line">prediction: [ 9.23960781]</div><div class="line">******************************</div><div class="line">X: [ 2.16495895  1.28095651  1.34778249  2.75269151  1.90516138]</div><div class="line">real      : [ 9.45155144]</div><div class="line">prediction: [ 9.43926144]</div><div class="line">******************************</div><div class="line">python tf_run.py  3.53s user 0.85s system 166% cpu 2.630 total</div></pre></td></tr></table></figure></p>
<p>​</p>
<ol start="2" style="list-style-type: decimal">
<li>使用<span class="math inline">\(y=2x\)</span>的3000条训练数据:</li>
</ol>
<p><figure class="highlight dns"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div></pre></td><td class="code"><pre><div class="line">X: [ <span class="number">1.06009257</span>  <span class="number">1.647856</span>    <span class="number">1.8015914</span>   <span class="number">1.13952839</span>  <span class="number">2.74504972</span>]</div><div class="line">real      : [ <span class="number">2.12018514</span>  <span class="number">3.29571199</span>  <span class="number">3.60318279</span>  <span class="number">2.27905679</span>  <span class="number">5.49009943</span>]</div><div class="line">prediction: [ <span class="number">2.36333179</span>  <span class="number">3.43519783</span>  <span class="number">3.55674648</span>  <span class="number">2.42750287</span>  <span class="number">5.14753294</span>]</div><div class="line">******************************</div><div class="line">X: [ <span class="number">2.94805002</span>  <span class="number">1.19944155</span>  <span class="number">1.2205795</span>   <span class="number">2.25700951</span>  <span class="number">2.18170953</span>]</div><div class="line">real      : [ <span class="number">5.89610004</span>  <span class="number">2.3988831</span>   <span class="number">2.44115901</span>  <span class="number">4.51401901</span>  <span class="number">4.36341906</span>]</div><div class="line">prediction: [ <span class="number">5.56341791</span>  <span class="number">2.54713631</span>  <span class="number">2.6267767</span>   <span class="number">4.27490234</span>  <span class="number">4.30334949</span>]</div><div class="line">******************************</div><div class="line">X: [ <span class="number">1.93198025</span>  <span class="number">1.48437035</span>  <span class="number">2.22421408</span>  <span class="number">1.9887476</span>   <span class="number">2.1166265</span> ]</div><div class="line">real      : [ <span class="number">3.8639605</span>   <span class="number">2.9687407</span>   <span class="number">4.44842815</span>  <span class="number">3.97749519</span>  <span class="number">4.233253</span>  ]</div><div class="line">prediction: [ <span class="number">4.06275845</span>  <span class="number">2.60474968</span>  <span class="number">4.85847521</span>  <span class="number">4.15260172</span>  <span class="number">4.46441317</span>]</div><div class="line">******************************</div><div class="line">X: [ <span class="number">1.86214089</span>  <span class="number">2.13927388</span>  <span class="number">2.89492273</span>  <span class="number">2.43535662</span>  <span class="number">1.00828433</span>]</div><div class="line">real      : [ <span class="number">3.72428179</span>  <span class="number">4.27854776</span>  <span class="number">5.78984547</span>  <span class="number">4.87071323</span>  <span class="number">2.01656866</span>]</div><div class="line">prediction: [ <span class="number">3.73180866</span>  <span class="number">4.23187256</span>  <span class="number">5.57253933</span>  <span class="number">4.6572423</span>   <span class="number">2.15151691</span>]</div><div class="line">******************************</div><div class="line">X: [ <span class="number">1.35404587</span>  <span class="number">2.86987972</span>  <span class="number">2.46327639</span>  <span class="number">2.51390696</span>  <span class="number">1.88503158</span>]</div><div class="line">real      : [ <span class="number">2.70809174</span>  <span class="number">5.73975945</span>  <span class="number">4.92655277</span>  <span class="number">5.02781391</span>  <span class="number">3.77006316</span>]</div><div class="line">prediction: [ <span class="number">2.71249247</span>  <span class="number">5.52877426</span>  <span class="number">4.94359541</span>  <span class="number">4.8839283</span>   <span class="number">3.81015444</span>]</div><div class="line">******************************</div><div class="line">python tf_run.py  <span class="number">4</span>.<span class="number">70</span>s user <span class="number">0</span>.<span class="number">86</span>s system <span class="number">165</span>% cpu <span class="number">3</span>.<span class="number">363</span> total</div></pre></td></tr></table></figure></p>
<p>首先是时间，tensorflow版的RBFNet要少很多，尤其是在聚类阶段，如果数据量变大， 优势会更加明显。</p>
<p>此外二者在sum数据上拟合效果很好，误差较小。在y=2x上，误差略大，不过这也跟训练数据有关系，通过调整参数，效果还可以提升很多。</p>
<p>此外，又对TensorFlow版的两种实现方式做了对比，数据集采用的是sum的形式:</p>
<ol style="list-style-type: decimal">
<li>Linear Regression方式:</li>
</ol>
<p><figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div></pre></td><td class="code"><pre><div class="line">X: [ 2.71186709  1.1967051   1.31952727  2.1717248   2.57859206]</div><div class="line">real      : [ 9.97841644]</div><div class="line">prediction: [ 9.96531868]</div><div class="line">******************************</div><div class="line">X: [ 2.73147416  1.85858846  1.61863422  2.991575    2.25436425]</div><div class="line">real      : [ 11.45463562]</div><div class="line">prediction: [ 11.45114994]</div><div class="line">******************************</div><div class="line">X: [ 1.70037389  1.58699119  1.53201306  2.21282601  1.39819622]</div><div class="line">real      : [ 8.43039989]</div><div class="line">prediction: [ 8.39311886]</div><div class="line">******************************</div><div class="line">X: [ 2.01777792  1.22136164  1.52503204  2.9318645   1.15536594]</div><div class="line">real      : [ 8.85140228]</div><div class="line">prediction: [ 8.84553528]</div><div class="line">******************************</div><div class="line">X: [ 2.70246601  2.19741511  1.7523278   2.72755265  1.09670925]</div><div class="line">real      : [ 10.47647095]</div><div class="line">prediction: [ 10.4424572]</div><div class="line">******************************</div></pre></td></tr></table></figure></p>
<ol start="2" style="list-style-type: decimal">
<li>梯度下降训练方式:</li>
</ol>
<p><figure class="highlight dns"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div></pre></td><td class="code"><pre><div class="line">X: [ <span class="number">2.3670311</span>   <span class="number">2.37218928</span>  <span class="number">1.41229916</span>  <span class="number">2.26609325</span>  <span class="number">1.49073756</span>]</div><div class="line">real      : [ <span class="number">9.90835094</span>]</div><div class="line">prediction: [ <span class="number">10.1823082</span>]</div><div class="line">******************************</div><div class="line">X: [ <span class="number">2.30584574</span>  <span class="number">1.68435013</span>  <span class="number">1.76450276</span>  <span class="number">2.61775756</span>  <span class="number">2.94909477</span>]</div><div class="line">real      : [ <span class="number">11.32155132</span>]</div><div class="line">prediction: [ <span class="number">11.11611843</span>]</div><div class="line">******************************</div><div class="line">X: [ <span class="number">2.48293662</span>  <span class="number">1.83565235</span>  <span class="number">1.58969617</span>  <span class="number">1.05468404</span>  <span class="number">2.01141191</span>]</div><div class="line">real      : [ <span class="number">8.97438145</span>]</div><div class="line">prediction: [ <span class="number">9.28915977</span>]</div><div class="line">******************************</div><div class="line">X: [ <span class="number">2.92746592</span>  <span class="number">1.39118993</span>  <span class="number">2.33665395</span>  <span class="number">1.11171508</span>  <span class="number">2.27592731</span>]</div><div class="line">real      : [ <span class="number">10.04295254</span>]</div><div class="line">prediction: [ <span class="number">9.89380741</span>]</div><div class="line">******************************</div><div class="line">X: [ <span class="number">1.08498883</span>  <span class="number">1.48899996</span>  <span class="number">1.32145357</span>  <span class="number">2.53989029</span>  <span class="number">2.54879212</span>]</div><div class="line">real      : [ <span class="number">8.98412514</span>]</div><div class="line">prediction: [ <span class="number">9.06929016</span>]</div></pre></td></tr></table></figure></p>
<p>从结果可以看出，梯度下降方式误差相对大一些，这个也符合原理，LinearRegression直接使用所有数据解线性方程，得到的结果应该是很准确的。而梯度下降方式则跟训练数据大小，学习率等关系很大，如果调整不好，自然误差就会变大。</p>
<p>Numpy版KMeans+RBFNet 与 TensorFlow版KMeans+RBFNet的完整代码: <a href="https://github.com/ShomyLiu/stat-learn/tree/master/RBFNet" target="_blank" rel="external">RBFNet With K-Means</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;2017.3.3更新: TensorFlow版本KMeans与RBFNet&lt;/p&gt;
&lt;/blockquote&gt;
&lt;blockquote&gt;
&lt;p&gt;2017.2.28更新：Python实现RBFNet&lt;/p&gt;
&lt;/blockquote&gt;
&lt;blockquote&gt;
&lt;p&gt;这一节利用上节SVM中的高斯核(RBF Kernel)来介绍一个新的模型，RBF Network，属于神经网络(Neural Network)的一种，可以以任意精度来逼近任意的连续函数。&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="读书笔记" scheme="http://shomy.top/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="machine learning" scheme="http://shomy.top/tags/machine-learning/"/>
    
      <category term="rbf" scheme="http://shomy.top/tags/rbf/"/>
    
  </entry>
  
  <entry>
    <title>机器学习技法笔记(5)-Soft Margin SVM</title>
    <link href="http://shomy.top/2017/02/20/svm04-soft-margin/"/>
    <id>http://shomy.top/2017/02/20/svm04-soft-margin/</id>
    <published>2017-02-20T08:30:32.000Z</published>
    <updated>2017-07-31T02:42:34.566Z</updated>
    
    <content type="html"><![CDATA[<p>上节介绍的Kernel SVM的结尾，提到了overfit的问题，那是因为SVM要找到的分类面是必须把所有的训练数据分开，即使有一些噪音数据，或者说是错误标注的数据，也要“坚持”分开，这可能就会形成很复杂的边界，很容易形成overfit，如下:</p>
<a id="more"></a>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm04-1.png">

</div>
<p>我们希望的是左边的分类面，而非右边的，虽然右边的<span class="math inline">\(E_{in} = 0\)</span>，也就是说希望对于一些错误的数据忽略掉，前面介绍PLA的变种Pocket的用的就是这种方式:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm04-2.png">

</div>
<p>Pocket找的是分类错误点尽可能少的分类面，不一定必须完全分开，容许错误的发生。而我们之前的SVM均是要求所有的数据都必须分类正确，如果我们将Pocket与SVM结合，即允许发生错误，如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm04-3.png">

</div>
<p>上式约束条件中，对于分类正确的点，仍然需要满足<span class="math inline">\(\geq 1\)</span>的条件，对于当前分类错误的点不加任何限制。所以目标函数中除了原先的<code>margin</code>，还必须加入了分类错误的个数，对整体做最小化。系数<span class="math inline">\(C\)</span>的作用则是一种对<code>margin</code>和犯错个数的一个折中。<span class="math inline">\(C\)</span>越大表示犯错误越少越好，犯错误个数重要性大于<code>margin</code>，如果太大了， 就容易过拟合了，反之<span class="math inline">\(C\)</span>越小表示希望<code>magin</code> 越宽越好，也就是说<span class="math inline">\(\mathbf{w}\)</span>越小越好，就是说对错误的惩罚很小，这样就会导致分类效果变差。 用<span class="math inline">\(C\)</span>表示更重视<span class="math inline">\(margin\)</span>还是重视犯错误的多少, 如下图:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm04-8.png">

</div>
<p>对于新的优化问题。然而由于存在bool的运算<span class="math inline">\(\neq\)</span>，也就是说该问题是离散的，无法用二次规划QP来求解。此外，还有一个缺点，那就是对所有的错误都是一视同仁的，无论是距离边界近还是远，只要分类错误，都计算在内，也就是说无法区分大错小错。</p>
<p>为了解决上述问题，引入变量<span class="math inline">\(\xi \geq 0\)</span> 表示不满足<span class="math inline">\(y_n(\mathbf{w}^T\mathbf{z}+b) \geq 1\)</span>的数据距离当前分类边界的距离，用来量化偏离的程度，也就是错误的大小，把原问题转为如下:</p>
<p><span class="math display">\[\begin{align} \min\limits_{b,\mathbf{w},\xi} &amp; \quad \frac{1}{2}\mathbf{w}^T\mathbf{w}+C \cdot \sum\limits_{n=1}^N \xi_n \\ s.t. &amp; \quad y_n(\mathbf{w}^T{\mathbf{z}}_n+b) \geq 1 - \xi_n, for \ all \ n; \\ &amp; \quad \xi_n \geq 0, for \ all \ n\end{align}\]</span></p>
<p>对于满足margin条件的点其<span class="math inline">\(\xi =0\)</span>, 不满足margin条件的其<span class="math inline">\(\xi &gt; 0\)</span>; 如下图:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm04-4.png">

</div>
<p>图中有除了A,B两点之外的点均满足条件，<span class="math inline">\(\xi=0\)</span>; 而A点属于没有不满足margin条件的，B点属于分类错误，因此<span class="math inline">\(\xi_{A} &gt;0, \xi_B&gt;0\)</span>。</p>
<p>上述的优化问题已经可以使用二次规划问题的解法去求解了，三个自变量:<span class="math inline">\(b,\mathbf{w},\xi\)</span>，一共有<span class="math inline">\(\tilde{d}+1+N\)</span>维；约束条件的个数为<span class="math inline">\(2N\)</span>个，按照前面的解法即可。</p>
<p>我们把这种容许犯错的SVM成为<strong>Soft-Margin SVM(软间隔SVM)</strong>，与前面几节的<strong>Hard-Margin SVM</strong>相对，并且引入的<span class="math inline">\(\xi\)</span>也叫做 <strong>松弛变量</strong>，顾名思义，允许数据偏离margin, 例如出现上图中A,B那样的数据。</p>
<p>上述的优化问题是Soft-Margin原始问题，下面考虑Soft-Margin的对偶问题(Dual)，同Hard-Margin，只需将两个约束条件加入到Lagrange Function:</p>
<p><span class="math display">\[\begin{align}\mathcal{L}(b, \mathbf{w}, \xi, \alpha, \beta)  = &amp;\frac{1}{2}\mathbf{w}^T\mathbf{w}+C \cdot \sum\limits_{n=1}^N \xi_n \\&amp;+ \sum\limits_{n=1}^{N}\alpha_n(1-\xi_n-y_n(\mathbf{w}^T\mathbf{z}_n+b)) \\ &amp; +\sum\limits_{n=1}^{N}\beta_n(-\xi_n) \end{align} \]</span></p>
<p>然后将原始问题转为如下的对偶问题:</p>
<p><span class="math display">\[\max\limits_{\alpha_n \geq 0,\ \beta \geq 0}\left(\min\limits_{b, \mathbf{w}, \xi} \quad   \mathcal{L(b, \mathbf{w}, \xi, \alpha, \beta)}\right)\]</span></p>
<p>优化思路同Hard-Margin的对偶问题相同，首先对内层最小化部分对变量求微分： <span class="math display">\[\begin{align}&amp; \frac{\partial \mathcal{L}}{\partial \xi_n} = C - \alpha_n - \beta_n = 0 \\ &amp; \Rightarrow  \beta_n=C-\alpha_n \end{align}\]</span></p>
<p>这样利用这个条件，就可以向Hard-Margin消去<span class="math inline">\(b\)</span>一样去掉<span class="math inline">\(\beta_n\)</span>, 同时结合<span class="math inline">\(\beta_n \geq 0\)</span>可以得到$ 0 _n C$。</p>
<p>整理一下式子，提取出<span class="math inline">\(\xi\)</span>的部分：<span class="math inline">\(\sum(C-\alpha_n-\beta_n) \xi_n\)</span>，已知最优解满足<span class="math inline">\(\alpha_n+\beta_n =C\)</span>， 因此<span class="math inline">\(\xi\)</span>部分可以消去，最终如下：</p>
<p><span class="math display">\[\max\limits_{ 0 \leq \alpha_n \leq C}\left(\min\limits_{b, \mathbf{w}} \quad   \frac{1}{2}\mathbf{w}^T{\mathbf{w}} + \sum\limits_{n=1}^{N}\alpha_n(1-y_n(\mathbf{w}^T\mathbf{z}+b))\right)\]</span></p>
<p>除了外层的<span class="math inline">\(\alpha_n\)</span>多了一个上限之外，与Hard-Margin的对偶问题完全一样，接下来对<span class="math inline">\(b, \mathbf{w}\)</span>分别求微分，然后去掉<span class="math inline">\(b,\mathbf{w}\)</span>，再将外层最大化稍微化简，最终转为如下的二次规划(QP)问题：(此过程与Dual SVM中完全一样，不再重写一遍):</p>
<p><span class="math display">\[\begin{align} \min\limits_{\alpha} &amp; \quad  \frac{1}{2}\sum\limits_{n=1}^N\sum\limits_{m=1}^N \alpha_n\alpha_my_ny_m\mathbf{z}_n^T\mathbf{z}_m - \sum\limits_{n=1}^{N}\alpha_n \\ s.t. &amp; \quad \sum\limits_{n=1}^{N}\alpha_ny_n = 0; \\ &amp; \quad 0 \leq \alpha_n \leq C, \ n = 1,2...N \end{align}\]</span></p>
<p>当然还有一些中间条件: <span class="math inline">\(\mathbf{w} = \sum \alpha_ny_n\mathbf{z}_n, \beta_n=C-\alpha_n\)</span>；</p>
<p>到这里我们的问题转为了有<span class="math inline">\(N\)</span>个变量，<span class="math inline">\(2N+1\)</span>个约束条件的二次规划问题，表示出<span class="math inline">\(Q,p,A,c\)</span>然后使用某个QP Solver即可得到最优的<span class="math inline">\(\alpha\)</span>:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm04-5.png">

</div>
<ul>
<li>Q矩阵元素: <span class="math inline">\(q_{n,m} = y_ny_m\mathbf{z}_n^T\mathbf{z}_m\)</span></li>
<li>$ p = -_N$</li>
<li>对于<span class="math inline">\(A,c\)</span>，对应如下:
<ul>
<li><span class="math inline">\(a_{\geq} = y, c_{\geq} = 0\)</span></li>
<li><span class="math inline">\(a_{\leq} = -y, c_{\leq} = 0\)</span></li>
<li>对于<span class="math inline">\(\alpha \geq 0\)</span> 有<span class="math inline">\(a_n^T =\)</span> n-th unit dircection, $c_n = 0 $</li>
<li>对于<span class="math inline">\(\alpha \leq C\)</span> 有<span class="math inline">\(a_n^T =\)</span> n-th unit direction, <span class="math inline">\(c_n = -C\)</span></li>
</ul></li>
</ul>
<p>到此为止，我们得到了对偶问题的最优的<span class="math inline">\(\alpha\)</span>, 再结合上节的Kernel Trick, 我们得到了 <strong>Kernel Soft-Margin SVM</strong>,基本流程如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm04-6.png">

</div>
<p>这里有一点不一样的地方是对<span class="math inline">\(b\)</span>的求解，回想之前是利用转化为拉格朗日对偶问题(min(max) <span class="math inline">\(\Leftrightarrow\)</span> max(min))时候，利用互补条件(complementary slackness)，参考<a href="http://shomy.top/2017/02/17/svm-02-dual/#模型求解">Dual SVM</a>, 但是Soft-Margin有两个条件，即:</p>
<p><span class="math display">\[\begin{align} \alpha_n(1-\xi_n-y_n(\mathbf{w}^T\mathbf{z}_n+b)) &amp;=0 \\ \beta_n \xi_n = (C- \alpha_n)\xi_n &amp; =0\end{align}\]</span></p>
<p>首先对第一个式子，对于SV(<span class="math inline">\(\alpha_s &gt; 0\)</span>)， 那么可以得到 <span class="math inline">\(b=y_s - y_s\xi_s - \mathbf{w}^T\mathbf{z}_s\)</span>，但是里面含有<span class="math inline">\(\xi\)</span>，无法求解。 再看第二个式子，对于<span class="math inline">\(\alpha_s &lt; C\)</span>的那部分SV(也叫free SV)，则有<span class="math inline">\(\xi_s = 0\)</span>，因此利用这部分SV， 加上Kernel Function便可得到<span class="math inline">\(b\)</span>：</p>
<p><span class="math display">\[b=y_s - \mathbf{w}^T\mathbf{z}_s = y_s - \sum\limits_{free \ SV} \alpha_ny_nK(\mathbf{x}_n, \mathbf{x}_s)\]</span></p>
<p>到这里基本完成了<strong>Soft-Margin SVM</strong>的算法，集对偶，核，Soft-Margin于一身，可以解决非线性问题，松弛变量允许噪音数据等，在实际分类问题中，应用很广泛。 不过需要注意的是这种SVM也有可能overfit的，因此需要仔细调整参数<span class="math inline">\(C\)</span>，以及Kernel Function的参数，一般使用模型验证(Validation)手段比如交叉验证去选择最好的参数。</p>
<p>最后再提一下Soft-Margin 中的<span class="math inline">\(\alpha\)</span>的物理意义，看下图:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm04-9.png">

</div>
<p>根据两个互补条件，可以得到三类点点:</p>
<ul>
<li>非SV(支撑向量): 也就是距离分离面比较远的那些数据，如图中的圈圈叉叉，对应的<span class="math inline">\(\alpha_n = 0, \xi_n = 0\)</span></li>
<li>free SV: 也就是说<span class="math inline">\(\alpha_n &lt; C, \xi_n =0\)</span>，它们在边界上, 满足条件。图中方框标记的点，我们使用这些点计算的<span class="math inline">\(b\)</span>。</li>
<li>bounded SV: <span class="math inline">\(\alpha_n =C, \xi_n &gt; 0\)</span>，这种类型的数据，不满足margin条件，或者分错类别的，如图中三角标记的。</li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;上节介绍的Kernel SVM的结尾，提到了overfit的问题，那是因为SVM要找到的分类面是必须把所有的训练数据分开，即使有一些噪音数据，或者说是错误标注的数据，也要“坚持”分开，这可能就会形成很复杂的边界，很容易形成overfit，如下:&lt;/p&gt;
    
    </summary>
    
      <category term="读书笔记" scheme="http://shomy.top/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="machine learning" scheme="http://shomy.top/tags/machine-learning/"/>
    
      <category term="svm" scheme="http://shomy.top/tags/svm/"/>
    
  </entry>
  
  <entry>
    <title>机器学习技法笔记(4)-Kernel SVM</title>
    <link href="http://shomy.top/2017/02/19/svm03-kernel-trick/"/>
    <id>http://shomy.top/2017/02/19/svm03-kernel-trick/</id>
    <published>2017-02-19T05:28:36.000Z</published>
    <updated>2017-07-31T02:42:34.566Z</updated>
    
    <content type="html"><![CDATA[<p>接着上一节的Dual SVM，我们解决了非线性转换维度变高导致求解困难的问题，但是仍然有问题，那就是在求Q矩阵的时候，由于涉及到了高维的内积,计算量仍然很大，这一节引入核机制(Kernel Trick)来高效的求解这一过程，完全不再依赖高维空间。</p>
<a id="more"></a>
<h2 id="核的引入">核的引入</h2>
<p>由上一节可以知道，计算量集中在Q矩阵中元素的计算:</p>
<p><span class="math display">\[\begin{align}q_{n,m} &amp;= y_ny_m\mathbf{z}_n^T\mathbf{z}_m \\ &amp;=y_ny_m\phi(\mathbf{x}_n)^T\phi(\mathbf{x}_m)\end{align}\]</span></p>
<p>这里分为两步，首先是空间转换，然后在高维空间中进行内积运算，这个计算量会很大的，尤其是后面还会有无穷维的转换，那么这一步根本无法完成。 如果我们将这两步合成一步的话， 就会降低计算量。下面以二阶多项式转换为例介绍: 假设原始数据有d维: <span class="math inline">\(\mathbf{x}=(x_1...x_d)\)</span>,</p>
<p><span class="math display">\[\phi_2(\mathbf{x}) =(1, x_1,x_2...,x_d, x_1^2,x_1x_2,...,x_1x_d,...,x_2x_1,x_2^2,...,x_d^2)\]</span></p>
<p>那么对两个数据<span class="math inline">\(\mathbf{x}, \mathbf{x}^{&#39;}\)</span>计算内积如下:</p>
<p><span class="math display">\[\begin{align}\phi_2(\mathbf{x})\phi_2(\mathbf{x}^{&#39;}) &amp;= 1 + \sum\limits_{i=1}^{d}x_ix_{i}^{&#39;} + \sum\limits_{i=1}^{d}\sum\limits_{j=1}^{d}x_ix_jx_i^{&#39;}x_j^{&#39;} \\ &amp;= 1 + \sum\limits_{i=1}^{d}x_ix_{i}^{&#39;} + \sum\limits_{i=1}^{d}x_ix_i^{&#39;}\sum\limits_{j=1}^{d}x_jx_j^{&#39;} \\ &amp;=1 + \mathbf{x}\mathbf{x}^{&#39;} + (\mathbf{x}\mathbf{x}^{&#39;})^2 \end{align}\]</span></p>
<p>可以发现，我们将转换与计算内积的过程转为了一步完成，就是上式，而且计算仅仅在原始维度上进行，复杂度从<span class="math inline">\(O(\tilde{d})\)</span>降到了<span class="math inline">\(O(d)\)</span>，完全去除了对<span class="math inline">\(\tilde{d}\)</span>的依赖。</p>
<p>我们称<span class="math inline">\(K_{\phi}(\mathbf{x},\mathbf{x}^{&#39;}) = \phi_2(\mathbf{x})\phi_2(\mathbf{x}^{&#39;})\)</span>为核函数(Kernel Function)。比如<span class="math inline">\(K_{\phi_2} = 1 + \mathbf{x}\mathbf{x}^{&#39;} + (\mathbf{x}\mathbf{x}^{&#39;})^2\)</span> 。<strong>核函数主要作用就会使得内积计算都在原始维度上，与高维空间无关，属于一种内积的运算简化技巧(Kernel Trick)。</strong></p>
<p>有了核函数，我们便可以将Q矩阵改写如下:</p>
<p><span class="math display">\[q_{n,m} = y_ny_mK(x_n,x_m)\]</span></p>
<p>其余变量不变，求解新的二次规划问题，得到拉格朗日乘子<span class="math inline">\(\alpha\)</span>，由于我们跳过了转换过程，所以无法计算单独的<span class="math inline">\(\mathbf{z}\)</span>，因此利用<span class="math inline">\(\alpha\)</span>求<span class="math inline">\(\mathbf{w},b\)</span>的方法也得做一定修改。</p>
<p>先看<span class="math inline">\(b\)</span>,利用某个SV即<span class="math inline">\(\alpha_s &gt;0\)</span>来求:</p>
<p><span class="math display">\[\begin{align}b &amp;= y_s - \mathbf{w}^T\mathbf{z}_s \\&amp;=y_s - \sum\limits_{n=1}^{N}\alpha_ny_n\mathbf{z}_n\mathbf{z}_s \\&amp;=y_s - \sum\limits_{n=1}^{N}\alpha_ny_nK(\mathbf{x}_n, \mathbf{x}_s) \end{align}\]</span></p>
<p>这样就得到了<span class="math inline">\(b\)</span>的表达式。</p>
<p>再看<span class="math inline">\(\mathbf{w} = \sum\alpha_ny_n\mathbf{z}_n\)</span>,貌似无法求解，因为这里面的<span class="math inline">\(\mathbf{z}_n\)</span>没有办法求解。但是从另一个角度想，我们计算<span class="math inline">\(\mathbf{w}\)</span>的目的就是为了计算<span class="math inline">\(\mathbf{w}^T\mathbf{z}\)</span>，那么我们整体计算看看得到什么:</p>
<p><span class="math display">\[\mathbf{w}^T\mathbf{z} = \sum\limits_{n=1}^{N}\alpha_ny_n\mathbf{z}_n\mathbf{z} = \sum\limits_{n=1}^{N}\alpha_ny_nK(\mathbf{x}_n\mathbf{x} \]</span></p>
<p>这样也间接完成了我们的目的。这样利用Kernel Function，我们就避开了在高维空间的计算，甚至不需要知道用的什么映射，就直接在原始低维数据进行等价计算。</p>
<p>总结一下核机制SVM的一般流程：</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm03-1.png">

</div>
<p>再来看看每一步的复杂度，如下，第一步的<span class="math inline">\(O(N^2)\)</span>是因为Q矩阵的规模是<span class="math inline">\(N*N\)</span>的。</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm03-2.png">

</div>
<p>上面举的例子是二阶的核函数，下面介绍几个常用的核函数。</p>
<h2 id="多项式核">多项式核</h2>
<p>我们将<span class="math inline">\(K_{\phi_2} = 1 + \mathbf{x}\mathbf{x}^{&#39;} + (\mathbf{x}\mathbf{x}^{&#39;})^2\)</span> 加一些系数，得到更加简单更加常用的<span class="math inline">\(K_2(\mathbf{x},\mathbf{x}^{&#39;})=(1+\gamma\mathbf{x}\mathbf{x}^{&#39;})^2\)</span>，如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm03-3.png">

</div>
<p>需要注意的是，<span class="math inline">\(K_2,K_{\phi_2}\)</span>这两种核函数的能力本质上是相同的，也就是说解决同一类问题结果类似，只不过最后求出的边界不同。</p>
<p>此外，多项式核可以推向更一般的情况，如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm03-4.png">

</div>
<p>其中<span class="math inline">\(\zeta,\gamma\)</span>是超参数，<span class="math inline">\(Q\)</span>是阶数。</p>
<p>这里面有一个特殊的: 线性核(Linear Kernel)，计算简单高效:</p>
<p><span class="math display">\[K_1(\mathbf{x},\mathbf{x}^{&#39;}) = (0 + 1 *\mathbf{x}\mathbf{x}^{&#39;})^1 = \mathbf{x}\mathbf{x}^{&#39;}, \quad \zeta=0, \gamma=1, Q=1\]</span></p>
<p>在实际应用中，还是需要遵循<strong>Linear First</strong>的原则，首先尝试<strong>Linear Kernel</strong>，高阶的核函数虽然能力强，但是相对容易造成OverfFitting。</p>
<h2 id="高斯核">高斯核</h2>
<p>除了多项式核函数，另外常用的核函数还有高斯核函数，也叫做径向基函数(Radial Basis Function, RBF):</p>
<p><span class="math display">\[K(\mathbf{x},\mathbf{x}^{&#39;}) = exp(-\gamma\Arrowvert \mathbf{x}-\mathbf{x}^{&#39;}\Arrowvert^2), \quad \gamma &gt;0\]</span></p>
<p>RBF核实现的是无限维的转换，证明思路就是将高斯核函数可以表示为无穷项的和，并且需要是两个向量内积的形式。下面以一维为例，即<span class="math inline">\(\mathbf{x}=(x_1)\)</span>，基于泰勒展开来证明：</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm03-5.png">

</div>
<p>高斯核<span class="math inline">\(K(\mathbf{x},\mathbf{x}^{&#39;}) = exp(-\gamma\Arrowvert \mathbf{x}-\mathbf{x}^{&#39;}\Arrowvert^2), \quad \gamma &gt;0\)</span> 只有一个参数<span class="math inline">\(\gamma\)</span>，在实际应用中，<span class="math inline">\(\gamma\)</span>一般不应该太大，否则很容易过拟合，还有一点高斯核完成的是到无穷维的转换，因此它的描述能力很强，可以作出很复杂的边界，但是由于是无穷维的转换，这个转换过程类似黑盒子，也就是说<span class="math inline">\(\mathbf{w}\)</span>也是无穷维的，我们无法计算出它，因此也就缺乏了物理意义。</p>
<h2 id="核比较">核比较</h2>
<p>下面对线性核，多项式核，高斯核(RBF)做个对比。</p>
<p><strong>线性核(Linear Kernel)</strong> <span class="math inline">\(K(\mathbf{x}, \mathbf{x}^{&#39;}) = \mathbf{x}\mathbf{x}^{&#39;}\)</span>:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm03-6.png">

</div>
<ul>
<li>优点: 泛化性能好，计算快速，应该优先考虑，解释性强，因为各个特征的重要性就直接体现在权重内。</li>
<li>缺点: 模型太简单，限制较大，比如线性不可分的数据</li>
</ul>
<p>主要用于线性可分的情形，速度快，优先考虑。</p>
<p><strong>多项式核(Ploy Kernel)</strong></p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm03-7.png">

</div>
<ul>
<li>优点: 比线性核更加Powerful， 可以解决线性不可分的数据。</li>
<li>缺点: 超参数多(<span class="math inline">\(\zeta, \gamma, Q\)</span>)，不易选择; 另外 Q如果比较大的时候，矩阵的数值范围过大或者过小，不易计算，此外需要注意过拟合</li>
</ul>
<p>一般使用的比较小的Q。 不过使用较少，因为速度不如Linear，能力不如RBF</p>
<p><strong>高斯核(RBF Kernel)</strong></p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm03-8.png">

</div>
<ul>
<li>优点: 分类更加Powful，可以得到很复杂的边界，; 只有一参数<span class="math inline">\(\gamma\)</span>，模型选择更加简单。</li>
<li>缺点: 解释性差，因为无法计算无限维的<span class="math inline">\(w\)</span>,无法知道各个特征的权重; 过拟合更加严重，需要严格控制<span class="math inline">\(\gamma\)</span>。</li>
</ul>
<p>与Linear Kernel 一样，很常用，用于线性不可分的情形，参数多，因为计算量多，相对耗时较多。</p>
<p><strong>总结</strong></p>
<p>一般情况，使用线性核或者高斯核，RBF更加通用，参数好的话，基本会比Linear好，但是耗时多。</p>
<p>吴恩达NG教程中提到的以下经验:</p>
<ul>
<li>如果Features数量大，与样本数量持平或者大于，这时候一般用Linear Kernal</li>
<li>如果Features较少，采用RBF Kernel</li>
<li>如果样本量过大的话，使用带Kernel的SVM优势就不大了。</li>
</ul>
<p>最好的方式是根据具体数据来尝试哪种效果最好。</p>
<p>那么除了上述的Linear Kernel，Poly Kernel，RBF Kernel 之外，还有其它的核函数吗？</p>
<p>我们已经知道核函数是为了简化内积运算的trick，只知道<span class="math inline">\(K(\mathbf{x}, \mathbf{x}^{&#39;}) = \phi(\mathbf{x})\phi(\mathbf{x}^{&#39;})\)</span>，此外我们知道向量内积就是描述向量的相似度，(比如向量垂直,内积为0; 相反，内积为负数)，那么我们能否从某个相似性出发，随意指定某个函数来作为相似度的描述？Not Really。<strong>Mercer Condition</strong>是用来判断一个函数能否作为核函数的条件。我们将数据写成核矩阵<span class="math inline">\(K\)</span>的形式:<span class="math inline">\(K_{i,j} = K(\mathbf{x}_i, \mathbf{x}_j)\)</span>，整个矩阵如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm03-9.png">

</div>
<p>很显然，K应该满足<strong>对称性，半正定</strong>的性质，事实上，<strong>对称，半正定</strong>也是K可以作为核矩阵的充要条件(证明略过)，虽然可以指定某个函数作为核函数，但是在实际中，Linear Kernel和RBF Kernel已经足够用了。</p>
<h2 id="后续">后续</h2>
<p>这一节主要介绍了 Kernel Trick SVM，首先引入核的概念，用来简化内积运算，再到几种常见核函数的优缺点的比较，最后稍微提了Mercer’s Condition指出了作为核函数的充要条件。 到这里似乎SVM的分类问题貌似解决了，但是正如前面提到的，一直存在一个OverFitting的问题，比如说如果存在某些错误噪音数据，那么就算转到高维空间仍然存在线性不可分的情况，此时，就会产生很复杂的分类边界，造成OverFitting，但是我们更希望，忽略个别的错误数据，如下图:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm04-1.png">

</div>
<p>这个问题就是下一节要解决的问题: Soft-Margin。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;接着上一节的Dual SVM，我们解决了非线性转换维度变高导致求解困难的问题，但是仍然有问题，那就是在求Q矩阵的时候，由于涉及到了高维的内积,计算量仍然很大，这一节引入核机制(Kernel Trick)来高效的求解这一过程，完全不再依赖高维空间。&lt;/p&gt;
    
    </summary>
    
      <category term="读书笔记" scheme="http://shomy.top/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="machine learning" scheme="http://shomy.top/tags/machine-learning/"/>
    
      <category term="kernel" scheme="http://shomy.top/tags/kernel/"/>
    
      <category term="svm" scheme="http://shomy.top/tags/svm/"/>
    
  </entry>
  
  <entry>
    <title>机器学习技法笔记(3)-对偶SVM</title>
    <link href="http://shomy.top/2017/02/17/svm-02-dual/"/>
    <id>http://shomy.top/2017/02/17/svm-02-dual/</id>
    <published>2017-02-17T08:31:05.000Z</published>
    <updated>2017-07-31T02:42:34.566Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>对偶SVM主要解决在非线性转换之后维度变高二次规划问题求解困难的问题。</p>
</blockquote>
<a id="more"></a>
<blockquote>
<p>在上一节的Linear SVM的最后提到了，在非线性分类问题中，我们一般作非线性转换<span class="math inline">\(\textbf{z}_n = \phi(\textbf{x}_n)\)</span>，从而在<span class="math inline">\(\textbf{z}\)</span>空间里面进行线性分类，在非线性转换过程一般是一个升维的过程，将<span class="math inline">\(\textbf{z}\)</span>的维度一般设为<span class="math inline">\(\tilde{d}\)</span>, 一般情况 <span class="math inline">\(\tilde{d} &gt;&gt; d\)</span>，(比如2维变5维，3维变19维等是一个非线性升高)，这样在高维空间内部，在二次规划问题中的就会有<span class="math inline">\(\tilde{d}+1\)</span>个变量，那么权值维度同样也为<span class="math inline">\(\tilde{d}+1\)</span>维，以及<span class="math inline">\(N\)</span>个约束，此外<span class="math inline">\(Q\)</span> 矩阵维度会十分大，达到<span class="math inline">\(\tilde{d}^2\)</span>，因此，一旦<span class="math inline">\(\tilde{d}\)</span>变大了， 二次规划问题求解就会变得很困难。</p>
</blockquote>
<p>这一节就是通过引入对偶问题以及下一节的核，来消除SVM解决非线性问题转换到高维空间中对<span class="math inline">\(\tilde{d}\)</span>依赖问题。</p>
<h2 id="对偶的引入">对偶的引入</h2>
<p>我们的目标是<strong>将原始的非线性问题，转化为另外一个对等问题，使得新的对等问题的规模或者说复杂度(主要指二次规划问题的规模，比如变量数目，约束条件数目等)仅仅与原始数据的规模(样本量)有关，与转换的高维空间无关</strong>，如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm02-1.png">

</div>
<h3 id="拉格朗日函数">拉格朗日函数</h3>
<p>在引入对偶问题之前，在回顾上一节的二次规划问题<strong>SVM</strong>:</p>
<p><span class="math display">\[ \begin{align} &amp; \min \limits_{b, \textbf{w}}  \quad \frac{1}{2}\textbf{w}^T\textbf{w} \\&amp; s.t. \quad y_n(\textbf{w}^T\textbf{x}+b) \geq 1 \end{align} \]</span></p>
<p>我们从另一个角度考虑，这里我们引入拉格朗日函数(Lagrange Function)来将约束条件和目标函数结合到一个式子:</p>
<p><span class="math display">\[\mathcal{L}(b, \textbf{w}, \mathbf{\alpha}) = \frac{1}{2}\textbf{w}^T \textbf{w} + \sum\limits_{n=1}^{N}\alpha_n(1-y_n(\mathbf{w}^T\mathbf{x}+b)), \quad \alpha_n \geq 0\]</span></p>
<p>其中<span class="math inline">\(\mathbf{\alpha}\)</span> 为拉格朗日乘子(Lagrange Multiplier)。</p>
<p>下面我们将说明原始SVM二次规划问题与下式等价，即:</p>
<p><span class="math display">\[\mathbf{SVM} \equiv \min \limits_{b, \mathbf{w}} (\max \limits_{\alpha_n \geq 0} \mathcal{L}(b, \mathbf{w},\alpha))\]</span></p>
<p>首先右式表示多元变量的优化问题，首先在固定<span class="math inline">\(b,\mathbf{w}\)</span>下，通过调整<span class="math inline">\(\alpha\)</span>,使得<span class="math inline">\(\mathcal{L}(b, \textbf{w}, \alpha)\)</span> 最大，进而再优化外面的<span class="math inline">\(\min\)</span>部分。</p>
<p>再看<span class="math inline">\(\max\)</span>部分，分两种情况叙述:</p>
<ul>
<li><span class="math inline">\(\mathbf{w},b\)</span>不满足约束条件<span class="math inline">\(y_n(\mathbf{w}^Tx+b) \geq 1\)</span>，即: <span class="math inline">\(1-y_n(\mathbf{w}^T\textbf{x}+b) &gt; 0\)</span>，再结合<span class="math inline">\(\alpha_n \geq 0\)</span>,很显然, <span class="math inline">\(\max(\mathcal{L}) \to \infty\)</span></li>
<li><span class="math inline">\(\mathbf{w},b\)</span>满足<span class="math inline">\(1-y_n(\mathbf{w}^T\textbf{x}+b) \leq 0\)</span>，那么在求<span class="math inline">\(\max\)</span>的时候，显然需要<span class="math inline">\(\alpha_n(1-y_n(\mathbf{\mathbf{w}^T\mathbf{x}+b})) = 0\)</span>,此时<span class="math inline">\(\max(\mathcal{L}) = \frac{1}{2}\textbf{w}^T \textbf{w}\)</span></li>
</ul>
<p>显然，求完了<span class="math inline">\(\max\)</span>之后，在优化外层求<span class="math inline">\(\min\)</span>的时候，对于上述第一种<span class="math inline">\(\infty\)</span>情况，肯定不会是最优解，因此最后的优化方程为: <span class="math inline">\(\min \frac{1}{2}\textbf{w}^T \textbf{w}\)</span>，因此与原始问题等价。这样，我们就把原始的含有约束条件的优化问题，转为不含条件的表达式。</p>
<h3 id="对偶问题转化">对偶问题转化</h3>
<p>接着化简，因为<span class="math inline">\(\max \geq any\)</span>，则对于任意一个给定的<span class="math inline">\(\alpha^{&#39;}\)</span>，下式显然成立:</p>
<p><span class="math display">\[ \min \limits_{b, \mathbf{w}} (\max \limits_{\alpha_n \geq 0} \mathcal{L}(b, \mathbf{w},\alpha)) \geq \min\limits_{b, \mathbf{w}} \mathcal{L}(b, \mathbf{w},\alpha^{&#39;})\]</span></p>
<p>既然上述的<span class="math inline">\(\alpha^{&#39;}\)</span>是任意给定的，那么左式也一定大于等于其中的那个最大的，那么下式也显然成立:</p>
<p><span class="math display">\[ \min \limits_{b, \mathbf{w}} (\max \limits_{\alpha_n \geq 0} \mathcal{L}(b, \mathbf{w},\alpha)) \geq \max\limits _{all \ \alpha_n \geq 0} (\min\limits_{b, \mathbf{w}} \mathcal{L}(b, \mathbf{w},\alpha^{&#39;}))\]</span></p>
<p>到此为止: 观察上述不等式，最大化与最小化做了交换，这样的不等式，右式就称为左式的<strong>对偶问题</strong>，左式的下限就是右式。</p>
<p>此外，上述不等式关系属于一种<strong>弱对偶关系(Weak Duality)</strong>。如果满足下面的几个条件，则可以去掉不等号，从而转为<strong>强对偶关系(Strong Duality)</strong>：</p>
<ul>
<li>原始问题为是凸优化问题 (convex primal)</li>
<li>原始问题线性可分的(feasible primal)</li>
<li>约束条件是线性的(Linear constraints)</li>
</ul>
<p>显然，原始问题满足以上强对偶条件，也就是说存在一组最优解<span class="math inline">\(\mathbf{w},b,\alpha\)</span>同时满足式子两边。到此，我们将原始的问题转化为其如下的对偶问题:</p>
<p><span class="math display">\[\begin{align*}\max\limits_{all \ \alpha_n \geq 0}{\left ( \min\limits_{b,\mathbf{w}}{ \underbrace{\frac{1}{2}\mathbf{w}^T\mathbf{w}+\sum\limits_{n=1}^{N}\alpha_n(1-y_n(\mathbf{w}^T\mathbf{z}_n+b))}_{\mathcal{L}(b, \mathcal{w}, \alpha)}}\right)}\end{align*}\]</span></p>
<p><strong>为什么要将原始问题，转换为对偶问题去求解呢？</strong></p>
<p>开始说了， 目标是降低算法的规模或者说是复杂度。原始问题的最关于后需要解决的<span class="math inline">\(\min\limits_{b,\mathbf{w}}\)</span>部分的复杂度是关于<span class="math inline">\(\mathbf{w}\)</span>的，也就是高维空间的维度，求解很困难。而对偶问题最后要解决的<span class="math inline">\(\max\limits_{\alpha}\)</span>则是关于<span class="math inline">\(\alpha_n\)</span>的，复杂度跟样本数量<span class="math inline">\(N\)</span>有关，在非线性分类中一般情况升维会达到很大的数量级(比如后续的高斯核是无穷维)，远远大于样本数量，这个时候用对偶问题来求解就会容易。</p>
<p>此外，如果是线性分类且样本维度低于样本数量的话，那就直接使用原始问题求解即可，无需对偶。</p>
<h2 id="求解对偶问题">求解对偶问题</h2>
<h3 id="最小化">最小化</h3>
<p>固定<span class="math inline">\(\alpha\)</span>, 对拉格朗日函数<span class="math inline">\(\mathcal{L}\)</span>最小化(凸函数)，<span class="math inline">\(\mathcal{L}\)</span>含有<span class="math inline">\(b, \mathbf{w}\)</span>两个变量，因此分别求一阶微分:</p>
<p>首先对<span class="math inline">\(b\)</span>求偏微分，得到:</p>
<p><span class="math display">\[\begin{align*} \frac{\partial\mathcal{L}(b, \mathbf{w}, \alpha)}{\partial \ b} = \sum\limits_{n=1}^{N}\alpha_ny_n = 0\end{align*}\]</span></p>
<p>因此我们最终得到的最优解应该满足上述条件，接着作一步转化:</p>
<p><span class="math display">\[\min\mathcal{L}(b, \mathbf{w},\alpha) = \min \frac{1}{2}\mathbf{w}^T\mathbf{w} + \sum\alpha_n(1-y_n\mathbf{w}^T\mathbf{z}^n) -  {b*\sum\alpha_ny_n}\]</span></p>
<p>既然我们的最优解需要满足<span class="math inline">\(\sum\alpha_ny_n = 0\)</span>，那么上式也应该满足，那么蓝色部分就会变成0，问题得到了简化。</p>
<p>接着对<span class="math inline">\(\mathbf{w}\)</span>求微分如下:</p>
<p><span class="math display">\[\begin{align*} \frac{\partial\mathcal{L}(b, \mathbf{w}, \alpha)}{\partial \ \mathbf{w}} = \mathbf{w} - \sum\limits_{n=1}^{N}\alpha_ny_n\mathbf{z}_n = \mathbf{0}\end{align*}\]</span></p>
<p>即最优解满足: <span class="math inline">\(\mathbf{w} = \sum\limits_{n=1}^{N}\alpha_ny_n\mathbf{z}_n\)</span>，接着继续化简<span class="math inline">\(\min\mathcal{L}\)</span>:</p>
<p><span class="math display">\[\begin{align} \min\mathcal{L}(b, \mathbf{w},\alpha) &amp; \Leftrightarrow \min \frac{1}{2}\mathbf{w}^T\mathbf{w} + \sum\alpha_n(1-y_n\mathbf{w}^T\mathbf{z}^n) \\ &amp;\Leftrightarrow \min \frac{1}{2}\mathbf{w}^T\mathbf{w} - \sum \alpha_ny_n\mathbf{z}^n\mathbf{w}^T+ \sum\alpha_n \\ &amp; \Leftrightarrow {\min} -\frac{1}{2}\mathbf{w}^T\mathbf{w}  + \sum\limits_{n=1}^{N}\alpha_n \\ &amp; \Leftrightarrow -\frac{1}{2} \Arrowvert \sum\limits_{n=1}^{N}\alpha_ny_n\mathbf{z}_n\Arrowvert + \sum\limits_{n=1}^{N}\alpha_{n}\end{align} \]</span></p>
<p>经过分别求偏微分，并且将得到的条件代入到优化方程，最终去掉了<span class="math inline">\(\min\)</span>，仅仅与<span class="math inline">\(\alpha\)</span>有关。</p>
<p>现在我们的问题化简至下式:</p>
<p><span class="math display">\[\begin{align*}\max\limits_{\alpha_n \geq 0, \  \sum\alpha_ny_n =0, \ \mathbf{w}=\sum\alpha_ny_n\mathbf{z}^n}\left( -\frac{1}{2}\Arrowvert\sum\limits_{n=1}^{N}\alpha_ny_n\mathbf{z}_n\Arrowvert^2+ \sum\limits_{n=1}^{N}\alpha_{n}\right)\end{align*}\]</span></p>
<p>至此最小化部分完成，完全转为了<span class="math inline">\(\alpha\)</span>的问题。</p>
<h3 id="最大化">最大化</h3>
<p>上一步的最小化结束了，这一部分做外层的最大化，这里仅仅与<span class="math inline">\(\alpha\)</span>有关。首先做一个转化，变为求<span class="math inline">\(\min\)</span>，然后展开平方如下:</p>
<p><span class="math display">\[\begin{align} \min\limits_{\alpha} &amp; \quad \frac{1}{2}\sum\limits_{n=1}^{N}\sum\limits_{m=1}^{N}{\alpha_n\alpha_m} y_ny_m\mathbf{z}_n\mathbf{z}_m {-}\sum\limits_{n=1}^{N}\alpha_n \\ s.t. &amp; \quad \alpha _n \geq 0, \ \sum\limits_{n=1}^{N}\alpha_ny_n  =0 \end{align} \]</span></p>
<p>相当与将条件再提出来，就转化为QP问题求解！并且上式中<span class="math inline">\(N\)</span>个变量<span class="math inline">\(\alpha_1...\alpha_N\)</span>，以及<span class="math inline">\(N+1\)</span>个约束条件，这样就看着摆脱了高维，求解相对容易，这便得到就是<strong>标准SVM对偶问题</strong>。</p>
<p>下面便是根据QP问题的格式，写出对应的<span class="math inline">\(Q,p,A,c\)</span>：</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm02-3.png">

</div>
<p>这里需要说明一下:</p>
<ul>
<li><span class="math inline">\(q_{m,n}\)</span>表示矩阵<span class="math inline">\(Q\)</span>的第<span class="math inline">\(m\)</span>行第<span class="math inline">\(n\)</span>列的元素</li>
<li>对于<span class="math inline">\(\sum y_n\alpha_n =0\)</span>，改为两个不等式条件: <span class="math inline">\(\sum y_n \alpha_n \leq 0,\ \sum y_n\alpha_n \geq 0\)</span>,因此一共有<span class="math inline">\(N+2\)</span>个约束条件。</li>
</ul>
<h3 id="模型求解">模型求解</h3>
<p>利用QP Solver 就可以解决上述的二次规划问题，得到最优的<span class="math inline">\(\alpha=[\alpha_1...\alpha_N]\)</span>，下面介绍如何根据<span class="math inline">\(\alpha\)</span>求解最优的<span class="math inline">\(\mathbf{w},b\)</span>，进而得到模型。</p>
<p>到这里整理一下已经有的条件:</p>
<ul>
<li>对于原始问题有: <span class="math inline">\(y_n(\mathbf{w}^T\mathbf{z}_n+b) \geq 1\)</span></li>
<li>对于对偶问题有: <span class="math inline">\(\alpha_n \geq 0\)</span></li>
<li>对于对偶问题里层的<span class="math inline">\(\min\)</span>优化求解中，有: <span class="math inline">\(\sum \alpha_ny_n =0; \ \mathbf{w}=\sum\alpha_ny_n\mathbf{z}_n\)</span></li>
<li>对于原始问题里层的<span class="math inline">\(\max\)</span>问题中：上面得到结论有: <span class="math inline">\(\alpha_n(1-y_n(\mathbf{\mathbf{w}^T\mathbf{z}_n+b})) = 0\)</span> ，被称为complementary slackness</li>
</ul>
<p>注:上述条件即使二次规划问题中的 <strong>KKT条件(Karush-Kuhn-Tucker Conditions)</strong></p>
<p>根据条件3，便可得到 <span class="math inline">\(\mathbf{w} = \sum\limits_{n=1}^{N}\alpha_ny_n\mathbf{z}_n\)</span>。</p>
<p>根据条件4，我们知道<span class="math inline">\(\alpha_n\)</span>以及<span class="math inline">\(1-y_n(\mathbf{w}^T\mathbf{z}_n + b)\)</span> 至少有一个为0，我们只需要找到一个<span class="math inline">\(\alpha_n &gt; 0\)</span>的数据点，这样就会有:<span class="math inline">\(1-y_n(\mathbf{w}^T\mathbf{z}_n + b) = 0\)</span>，进而得到:</p>
<p><span class="math display">\[b = y_n - \mathbf{w}^T \mathbf{z}_n, \quad \alpha_n &gt; 0\]</span></p>
<p>这里需要注意的是: <span class="math inline">\(\alpha_n &gt; 0\)</span>的点满足<span class="math inline">\(y_n(\mathbf{w}^T\mathbf{z}_n + b) = 1\)</span>,即分类边界上的点，也就是<strong>SV</strong>(Support Vector)，这也就说明了 ，我们只需要SV就可以得到<span class="math inline">\(\mathbf{w},b\)</span>，这时候其它的点不需要。</p>
<p>简略总结一下<strong>标准对偶SVM的流程</strong></p>
<ol style="list-style-type: decimal">
<li>根据训练数据，(非线性转换<span class="math inline">\(z_n = \phi(x_n)\)</span>， 写出QP问题的<span class="math inline">\(Q,p,a,c\)</span></li>
<li>使用QP Solover 求出最优的<span class="math inline">\(\alpha\)</span></li>
<li>根据条件约束，求出<span class="math inline">\(\mathbf{w},b\)</span></li>
<li>得到模型：<span class="math inline">\(g_{svm} = sign(\mathbf{w}^T\phi(\mathbf{x})+b)\)</span></li>
</ol>
<h3 id="数据表示模型">数据表示模型</h3>
<p>在开始介绍SVM的时候，就是以PLA为基础的，下面看看二者:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm02-4.png">

</div>
<p>首先在Dual SVM中，<span class="math inline">\(\mathbf{w}\)</span>是以<span class="math inline">\(y_n\mathbf{z}_n\)</span>的线性组合求解的，其中参数为<span class="math inline">\(\alpha_n\)</span>; 在PLA中 同样，每个分类错误的点，都会加上<span class="math inline">\(y_n\mathbf{z}_n\)</span>，因此实质上<span class="math inline">\(\mathbf{w}\)</span>同样线性组合求解得到。</p>
<p>其实还有很多线性模型或者算法，比如前面的Linear Regresson, Logistic Regression等如果初始化的<span class="math inline">\(\mathbf{w} = \mathbf{0}\)</span>，最终得到的最优解同样是数据的线性组合。</p>
<p>这个时候，我们就称参数w是被训练数据表示出来的(represented by data)，只不过SVM的权重参数只需通过<span class="math inline">\(\alpha_n &gt;0\)</span>的SV表示即可。(PS：数据表示的模型可能造成数据泄漏?)</p>
<h2 id="后续">后续</h2>
<p>这一节主要讲的是 <strong>标准对偶SVM</strong>，旨在解决高维空间下，二次规划问题求解困难的问题，下面对比Linear SVM与Dual SVM:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm02-5.png">

</div>
<p>看起来我们的对偶问题的规模成功做到了只与数据样本量<span class="math inline">\(N\)</span>有关，与高维空间的维度<span class="math inline">\(\tilde{d}\)</span>无关，其实不然，实际内部的计算量还是很大的:</p>
<p><span class="math inline">\(Q\)</span>矩阵的元素: <span class="math inline">\(q_{n,m} = y_ny_m\mathbf{z}_m\mathbf{z}_n\)</span>，实际为<span class="math inline">\(R^{\tilde{d}}\)</span>内的内积！ 还是没有摆脱高维。不过对偶问题相比原始问题在非线性数据 的处理上，求解已经更加容易了。 至于如何在求解<span class="math inline">\(q_{n,m}\)</span>的时候，彻底不再依赖<span class="math inline">\(\tilde{d}\)</span>，那就是下节的内容了，引入带核的SVM。</p>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;对偶SVM主要解决在非线性转换之后维度变高二次规划问题求解困难的问题。&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="读书笔记" scheme="http://shomy.top/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="machine learning" scheme="http://shomy.top/tags/machine-learning/"/>
    
      <category term="svm" scheme="http://shomy.top/tags/svm/"/>
    
  </entry>
  
  <entry>
    <title>机器学习技法笔记(2)-Linear SVM</title>
    <link href="http://shomy.top/2017/02/16/svm-01/"/>
    <id>http://shomy.top/2017/02/16/svm-01/</id>
    <published>2017-02-16T07:20:25.000Z</published>
    <updated>2017-07-31T02:42:34.562Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>从这一节开始学习机器学习技法课程中的SVM, 这一节主要介绍标准形式的SVM: Linear SVM</p>
</blockquote>
<h2 id="引入svm">引入SVM</h2>
<a id="more"></a>
<p>首先回顾Percentron Learning Algrithm(感知器算法PLA)是如何分类的，如下图，找到一条线，将两类训练数据点分开即可:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm01-1.png">

</div>
<p>PLA的最后的直线可能有很多条，那到底哪条好呢？好坏的标准则是其泛化性能，即在测试数据集上的正确率，如下，下面三条直线都能正确的分开训练数据，那到底哪个好呢？SVM就是解决这个问题的。</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm01-2.png">

</div>
<h2 id="svm求解">SVM求解</h2>
<p>直觉告诉我们最右的要好一些，因为测试数据的分布应该与训练数据的分布类似，因此看起来它可以容忍更多的噪音，假设有一个数据在第一个红叉叉下方位置，也是属于叉类别，前两直线则容易把它分到圈圈的类别，而造成错误。因此理想的直线或者分类平面应该距离两类训练数据集越远越好：也就是下面的灰色区域越胖越好:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm01-3.png">

</div>
<p>灰色区域的宽度是由直线与训练数据<span class="math inline">\(x_{1..N}\)</span> 中到分类平面最近的距离决定的，这个距离称为<code>margin</code>，因此我们就可以将问题描述如下:</p>
<p><span class="math display">\[\begin{align} &amp; \max\limits_{W,b}(margin(W,b))  \\  &amp; s.t. y_n(W^TX_n+b) &gt; 0  \end{align}\]</span></p>
<p>其中，分类平面由<span class="math inline">\(W,b\)</span>决定，我们要找出最大<span class="math inline">\(margin\)</span>的那条直线，这里的<span class="math inline">\(margin(W)=\min\limits_{n=1...N} distance(x_n,,W,b)\)</span>, 约束条件是这条直线已经将两类完全分开，即:<span class="math inline">\(y_n(X_n+b) &gt;0\)</span> 。下面我们看看点<span class="math inline">\((x_n)\)</span>到平面<span class="math inline">\(W^TX+b = 0\)</span>的距离，看下图:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm01-4.png">

</div>
<p><span class="math inline">\(W\)</span> 为平面<span class="math inline">\(W^TX+b=0\)</span>的法向量，<span class="math inline">\(x^{&#39;}\)</span>与<span class="math inline">\(x^{&#39;&#39;}\)</span>为平面上的两个点，现在如果要求点<span class="math inline">\(X\)</span>到平面的距离<span class="math inline">\(dist(X,W,b)\)</span>，即向量<span class="math inline">\(X-X^{&#39;}\)</span>投影(proj)到法向量<span class="math inline">\(W\)</span>:</p>
<p><span class="math display">\[distance(X,W,b)=\left | \frac{W^T}{\Arrowvert W\Arrowvert }(X-X^{&#39;}) \right |= \frac{1}{\Arrowvert W\Arrowvert }|(W^TX +b) |\]</span></p>
<p>到此，再结合约束条件<span class="math inline">\(y_n(W^TX+b)&gt;0, \ y_n \in \{-1, 1\}\)</span>, 点<span class="math inline">\(X\)</span>到平面的距离可以通过去绝对值符号，转化为如下的式子:</p>
<p><span class="math display">\[distance(X,W,b)=\frac{1}{\Arrowvert W \Arrowvert }y_n(W^TX+b)\]</span></p>
<p>因此问题转化如下的优化问题:</p>
<p><span class="math display">\[\begin{align} &amp; \max\limits_{W,b} \quad &amp;  margin(b,W)  \\ &amp; s.t. &amp; every \ y_n(W^TX_n +b) &gt; 0 \\ &amp; &amp; margin(b,W) = \min\limits_{n=1..N} \frac{1}{\Arrowvert W \Arrowvert}y_n(W^TX_n +b)\end{align}\]</span></p>
<p>另外，考虑到<span class="math inline">\(W^TX+b=0\)</span>与<span class="math inline">\(2W^TX+2b=0\)</span>这两个平面是同一个平面，只是对<span class="math inline">\(W,b\)</span>经过了放缩(scaling)，因此在缩放的过程中，不管是<span class="math inline">\(W,b\)</span>，还是<span class="math inline">\(2W,2b\)</span>, 都是对应同一个平面，因此求距离进行优化的过程不会有影响，利用这个特点，同时为了方便计算，对于某个的超平面，作出如下的假设放缩:</p>
<p><span class="math display">\[\min\limits_{n=1...N}y_n(W^TX_n+b) = 1\]</span></p>
<p>因为对于一个固定的平面，通过放缩，肯定存在一组<span class="math inline">\(W,b\)</span>使得上述的等式成立，同时也保证了条件<span class="math inline">\(y_n(W^TX_n+b) &gt;0\)</span>，这样<span class="math inline">\(margin(W,b) = \frac{1}{\Arrowvert W \Arrowvert}\)</span>，因此我们的优化问题转化为如下：</p>
<p><span class="math display">\[\begin{align} &amp; \max\limits_{W,b} \quad  \frac{1}{\Arrowvert W\Arrowvert} \\  &amp; s.t. \quad  \min\limits_{n=1...N}(y_n(W^TX_n+b)) =1 \end{align}\]</span></p>
<p>上述的约束条件是最小值等于1，对于优化问题来说，一般是某个等式或者不等式，这样容易求解，因此将上述的约束条件转为:<span class="math display">\[y_n(W^TX_n +b) \geq 1, \quad for \ all \ n\]</span></p>
<p>考虑上述转换的正确性: 反证法，假设对于所有的<span class="math inline">\(n\)</span>, 有<span class="math inline">\(y_n(W^TX_n+b) &gt; 1\)</span>， 即不满足最小值为1的条件，不妨设为<span class="math inline">\(\min = K, K&gt;1\)</span>,设此时得到的最优解为<span class="math inline">\(W,b\)</span>,那么<span class="math inline">\(\frac{W}{K},\frac{b}{K}\)</span> 很显然比<span class="math inline">\(W,b\)</span>更优，矛盾。因此下面两式等价:</p>
<p><span class="math display">\[\begin{align}&amp; \min(y_n(W^TX_n + b)) =1 \\ &amp; y_n(W^TX_n+B) \geq 1, \ for \ all \ n \end{align}\]</span></p>
<p>再结合将求max转为min, 求距离，转为求平方等转化，最后我们的优化问题如下:</p>
<p><span class="math display">\[\begin{align} &amp; \min\limits_{W,b} \quad \frac{1}{2}W^TW \\ &amp; s.t. \ y_n(W^TX_n +b) \geq 1, \ for \ all \ n \end{align}\]</span></p>
<p>这个问题的优化方程为二次方程，条件为一次线性约束，是一个典型的二次规划问题(Quadratic Programming)，而QP问题有很多解法，我们只需要将上述优化问题，转化为QP问题解法的格式，然后直接代入QP Solver,即可，如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm01-6.png">

</div>
<p>左侧是我们最后的优化问题，右侧是标准的QP问题的输入格式，一共有4个输入变量: <span class="math inline">\(Q,p,A,c\)</span>, 及一个最终的优化结果: <span class="math inline">\(u\)</span> ，我们只需要表示出上述四个输入变量，就可以输入到某个QP Solver里面即可，通过对应关系，表示如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm01-7.png">

</div>
<p>这样就可以得到最优的<span class="math inline">\(W,b\)</span>，进而求出<span class="math inline">\(g_{SVM} = sign(W^TX+b)\)</span>，进行后续预测。</p>
<p>上述的算法仅仅是最简单的线性SVM, 不过是其它类型SVM的基础，总结一下，流程很简单，如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm01-8.png">

</div>
<p>这种算法同样也可以解决非线性分类问题，通过之前的<a href="http://shomy.top/2016/12/02/nonlinera-transformation/">非线性转化</a>，到线性空间: <span class="math inline">\(z_n = \phi(x_n)\)</span>，理论上即可直接应用到非线性问题中。不过缺陷也很明显，要求数据点必须线性可分，由于这个特点，这种算法也叫做: Linear Hard-Margin SVM。</p>
<h2 id="svm-与-vc-维">SVM 与 VC 维</h2>
<p>这里需要提一下，SVM(Support Vector Machine)的含义，我们了解到，SVM就是要找到最胖的那个分类面，如下:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm01-9.png">

</div>
<p>其中，在边界上的用黑框圈出来的三个点，我们就称为<strong>SV</strong>,Support Vector，因为它们才是求解过程中，真正起到作用的点，其余的点没有起到作用，后续的课程中也有进一步的介绍SV。</p>
<p>简单来说，SVM是基于PLA的一个算法，上面开始说了， 直觉告诉我们最胖的直线应该容错性好，其中的原理其实则是SVM相对于PLA，VC维降低了(注: 算法的VC维的定义不太明确，类比与<span class="math inline">\(\mathcal{H}\)</span>的VC维定义即可, 这里只是定性的介绍)，看下图:</p>
<div class="figure">
<img src="http://7xotye.com1.z0.glb.clouddn.com/svm01-10.png">

</div>
<p>以三个输入的数据为例，PLA可以shatter(能够将<span class="math inline">\(2^3\)</span>种情况都分开)，而无法将4个数据点shatter，因此VC(PLA)=3，我们从另外一个角度来考虑， 假设我们规定了Margin的值为<span class="math inline">\(\rho\)</span> ，也就是胖瘦的程度，那么肯定会存在输入数据量为3,且无法shatter的情形，比如上图的后两种，在<span class="math inline">\(\rho\)</span>下 无法shatter，因此大致看起来，<span class="math inline">\(d_{VC}(\rho) &lt; 3\)</span>的。其实有严格的数学证明，可以得到:</p>
<p><span class="math display">\[d_{VC}(\rho)  \leq d+1 = d_{VC}(PLA)\]</span></p>
<p>VC维减少了，意味着复杂度降低，也就是说更不容易overfitting，泛化性能更好，这也是就是我们要找最胖的分类面的原因。</p>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;从这一节开始学习机器学习技法课程中的SVM, 这一节主要介绍标准形式的SVM: Linear SVM&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id=&quot;引入svm&quot;&gt;引入SVM&lt;/h2&gt;
    
    </summary>
    
      <category term="读书笔记" scheme="http://shomy.top/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="machine learning" scheme="http://shomy.top/tags/machine-learning/"/>
    
      <category term="svm" scheme="http://shomy.top/tags/svm/"/>
    
  </entry>
  
  <entry>
    <title>2016</title>
    <link href="http://shomy.top/2017/01/28/2016-review/"/>
    <id>http://shomy.top/2017/01/28/2016-review/</id>
    <published>2017-01-28T08:18:53.000Z</published>
    <updated>2017-07-31T02:42:34.562Z</updated>
    
    <content type="html"><![CDATA[<p>大年三十贴完了对联，2016也就快过去了，不禁感叹现在都赶不上时间的脚步了。这一年发生的事情还是蛮多的，大部分对我都有直接性甚至决定性的影响。</p>
<p>以前从来没有写过总结，主要是是文笔太差，想不起写啥。不过今年下定决心，各个方面随性写点，主要是为了将来回忆，毕竟时间过的这么快。</p>
<a id="more"></a>
<h2 id="学习">学习</h2>
<blockquote>
<p>活到老，学到老</p>
</blockquote>
<p>今年6月份结束我的本科学习生涯。因为之前获得了保研资格，开始了为期5年的直博生涯，个人觉得这个选择十分冒险，很多未知的东西在等着我，甚至不知道能不能坚持下来，现在只能给自己说加油了。</p>
<p>上半年做了一个很简单的本科毕业设计，没有多少含金量。此外就是实习一个月，就是用<code>php</code>做个网站，处理数据之类的，学到的东西也很少。在实习的空闲时间，学习了<code>Flask</code>,个人还是很喜欢的，不过一直没有派上用场，目测过段时间就会忘的差不多了,QAQ 现在回想上半年感觉过得非常快啊，学习上貌似没有留下什么东西，如果非要说的话，那可能就是成功抛弃了windows，习惯于linux了，此外就是<code>VIM</code>大法好，除了JAVA，开发基本都离不开VIM了。</p>
<p>下半年正式开启直博生涯，课程近10门，虽然很多课程本科都学过，但是到考试了还得花不少时间去准备。读研之后，基本离技术越来越远了，不再去专门探究Python, 偶尔刷个Leetcode(拜<a href="http://kedixa.top" target="_blank" rel="external">ks</a>)，其余精力基本都去跪数学和机器学习了，包括后期的深度学习, 不过我还是不希望扔下技术，只想把重心偏到科研上。 这期间十分感谢台大林轩田老师的机器学习基石以及机器学习技法(ing)教程，个人感觉非常适合机器学习的入门以及深入学习。相比ng的机器学习，林老师的更加注重理论推导以及原理探究。目前看完了基石课程，寒假这段时间，打算把技法的课程看完。此外由于课题需要，开始入门深度学习，主要解决文本分类等NLP问题，在960M的小笔记本用TensorFlow写过几个DNN的实验，效果很一般，这个坑一定得填上(然而我们实验室的方向目前暂无深度学习)，可能这段时间有点着急了。</p>
<p>这期间也有过浮躁，迷茫，担心发不了文，毕不了业-_-，然后看了一篇知乎，说读Paper是治疗一切疑难杂症的妙方，其实还是很有道理的。这学期可以沉下心去读论文也算是一个很大的收获。 希望在新的一年里，至少发一篇文试试水。。。</p>
<h2 id="生活">生活</h2>
<blockquote>
<p>善于发现平凡中的惊喜</p>
</blockquote>
<p>这一部分想了半天，发现不知道写什么，或许是因为不懂如何认识生活吧。</p>
<p>在学校的生活还是很规律的，基本都是几点一线，尤其是我还有4年半的这样的生活，因此生活如果一直按部就班，那就是重复了5年，那无疑有点浪费了。生活中的很小的事情比如ks时不时的说, ’hgqm’都是普通生活中的对自己的小犒劳(23333); 与lhy的每天互安也持续了几年了; 当然还有这学期旁边的hyx同学置办的放在工位上小储物柜….等等很多事情都是我们对平凡生活尝试的不平凡化，希望我接下来这几年也可以保持这样的心境。 此外，五音不全的我却爱上了听歌，还是十分感谢良心的网易云音乐，让我一天不听歌，就不舒服，哈哈。 突然想起来还有一件事情，这学期新校区的健身房在12月份开了，跟几个同学一块去办了会员，这是第一次到健身房健身，一个月下来，去了10次吧，明显感到了身体状态的不一样，体现在学期末spark大作业的连续熬夜，仍然感到很精神(-_-)，很喜欢健身房会员群的群名: 唯有读书与健身不可辜负，所以新的一年要继续坚持下去～，除了朋友，健身，今年的最大的收获还有那个她，我会珍惜的(此处省略<span class="math inline">\(+\infty\)</span>个字)。</p>
<p>就到这里吧，很多想写的东西不知道怎么下笔写。希望在新的一年我的家人，朋友，健健康康，快快乐乐的就行。给自己加油。</p>
<p>于阴历2017年正月初一</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;大年三十贴完了对联，2016也就快过去了，不禁感叹现在都赶不上时间的脚步了。这一年发生的事情还是蛮多的，大部分对我都有直接性甚至决定性的影响。&lt;/p&gt;
&lt;p&gt;以前从来没有写过总结，主要是是文笔太差，想不起写啥。不过今年下定决心，各个方面随性写点，主要是为了将来回忆，毕竟时间过的这么快。&lt;/p&gt;
    
    </summary>
    
      <category term="个人心得" scheme="http://shomy.top/categories/%E4%B8%AA%E4%BA%BA%E5%BF%83%E5%BE%97/"/>
    
    
      <category term="2016" scheme="http://shomy.top/tags/2016/"/>
    
  </entry>
  
  <entry>
    <title>hexo迁移重装</title>
    <link href="http://shomy.top/2017/01/27/hexo-migrate-solution/"/>
    <id>http://shomy.top/2017/01/27/hexo-migrate-solution/</id>
    <published>2017-01-27T14:05:35.000Z</published>
    <updated>2017-07-31T02:42:34.562Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>Hexo属于静态博客，不像WordPress等具有后台系统，可以在任意平台直接使用。而Hexo此类没有统一的后台管理，在换了系统，或者换了电脑，都需要重新安装一下，包括一些配置，如果手动复制迁移，很容易产生错误，因为很多东西不支持直接复制。因此下面记录自己的迁移方式，还是很简单的。</p>
</blockquote>
<a id="more"></a>
<p>我们一般需要备份或者需要恢复的一般有三部分:</p>
<ul>
<li>source文件夹内的博客原文，以及一些自己新建的页面，比如about页面</li>
<li>themes文件夹下的主题</li>
<li>配置文件: <code>_config.yml</code></li>
</ul>
<p>其余部分，每次<code>hexo init</code>都一样，我们没有必要备份。</p>
<h2 id="备份">备份</h2>
<p>主要方式就是使用一些在线仓库存储。如github, coding(提供私人仓库)。</p>
<h3 id="主题">主题</h3>
<p>首先是对主题的迁移。我们一般选定了某个主题，一般都会做一点自定义的改变，比如某些css等，这时候可以先<strong>fork到自己的github</strong>，每次做了修改，直接push到自己的repo上，下次迁移的时候，直接clone自己修改的主题即可。</p>
<h3 id="source文件夹配置文件">source文件夹&amp;&amp;配置文件</h3>
<p>这个涉及到了一些私人配置文件，例如博客源文件，以及如<code>_config.yml</code> 内的涉及到帐号等私人配置，这个时候，需要备份到私人云上，国内的coding.net免费提供了私人repo，可以使用。这样我们就只需在博客的根目录建立一个repo即可，然后提交 source文件夹以及<code>_config.yml</code>即可。 新写了博客的话，也只需要提交push到repo即可，十分方便。</p>
<h2 id="迁移重装">迁移重装</h2>
<p>在换了机器或者系统的时候，需要重新安装<code>Hexo</code>，安装好<code>nodejs</code>和<code>git</code>之后，只需执行一条命令安装:</p>
<figure class="highlight avrasm"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">npm install hexo-<span class="keyword">cli</span> -g</div></pre></td></tr></table></figure>
<p>之后新建一个hexo博客:</p>
<figure class="highlight ebnf"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="attribute">hexo init blog</span></div></pre></td></tr></table></figure>
<p>下面开始恢复备份:</p>
<ol style="list-style-type: decimal">
<li>从github上恢复主题到<code>themes</code>文件夹</li>
<li>从私人repo上克隆source文件夹和<code>_config.yml</code></li>
<li>安装所需的插件:一般有<code>hexo-deploy-git</code>,<code>hexo-generator-feed</code>等等</li>
</ol>
<p>之后用<code>hexo -g</code> 和<code>hexo -s</code> 本地测试，没问题的话，直接<code>hexo -d</code>发布</p>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;Hexo属于静态博客，不像WordPress等具有后台系统，可以在任意平台直接使用。而Hexo此类没有统一的后台管理，在换了系统，或者换了电脑，都需要重新安装一下，包括一些配置，如果手动复制迁移，很容易产生错误，因为很多东西不支持直接复制。因此下面记录自己的迁移方式，还是很简单的。&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="工具使用" scheme="http://shomy.top/categories/%E5%B7%A5%E5%85%B7%E4%BD%BF%E7%94%A8/"/>
    
    
      <category term="hexo" scheme="http://shomy.top/tags/hexo/"/>
    
  </entry>
  
</feed>
