<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>天空的城</title>
  
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://shomy.top/"/>
  <updated>2020-04-07T15:01:57.713Z</updated>
  <id>http://shomy.top/</id>
  
  <author>
    <name>ShomyLiu</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>2019</title>
    <link href="http://shomy.top/2020/04/06/2019-review/"/>
    <id>http://shomy.top/2020/04/06/2019-review/</id>
    <published>2020-04-06T07:38:40.000Z</published>
    <updated>2020-04-07T15:01:57.713Z</updated>
    
    <content type="html"><![CDATA[<p>2019年的总结来的实在晚了，但是一直惦记着，偶尔断断续续的写两句，这次总算写完了。 估计大家都没有想到，这次过年回家能待这么长的时间，看目前形势，估计这学期不用返校了.</p>
<p>放假前一天的雪后图书馆： <img src="http://cdn.htliu.cn/blog/2019-review/lib.jpg" alt="雪后图书馆"></p>
<a id="more"></a>
<h2 id="学术">学术</h2>
<p>过去一年总算是进账(水)了几篇论文，勉勉强强可以够到低保线的节奏， 虽然还是被拒的更多-_- 距离真正的毕业还是需要更多paper，压力还是很大啊。</p>
<p>之前做的关系抽取的几个工作也都有了着落，比较欣慰的是一篇被拒了N次的工作最终拿到了NLPCC的杰出论文奖，也算些许安慰，感谢各位co-authors。</p>
<p>今年重心基本上都转到了review-based recommendation, 总体来说问题定义跟之前做的关系抽取很类似，都是在偏NLP与Graph，本质上属于<code>set pooling</code> 也都是从word到sentence，这也是为什么当时直接转过去的原因。不过每次做一个方向，做到后面都感觉有很大的问题，比如当时关系抽取的evaluation，推荐里面的辅助信息是否真正有用等等，说到底还是应该去多了解下工业界方面的需求，论文只是论文。</p>
<p>还有一点感触就是，作为博士生还是应该多涉猎几个相关领域，不能只关注自己的小领域，这样对自己的限制很明显，道路越走越窄，所以我也在尝试多进入几个其他相关方向，然后也被各种事情耽搁着，至今都躺在备忘录里面，争取今年都完成~</p>
<p>哦对，趁着出去开会的机会，认识了不少朋友，也算是扩大了点圈子，以期以后能够相互交流(抱大腿~)。</p>
<h2 id="生活">生活</h2>
<blockquote>
<p>这几个月在家比较忙碌，都是烧水喂猪干活，每天10点多就睡觉了~</p>
</blockquote>
<p>细细想来，去年2019一年开各种会走了很多地方，让俺这个村里娃见识各种异域风光，认识了不少新朋友，这可能也是当学生的福利了，中了论文可以去开会，顺带旅旅游，增添生活乐趣。</p>
<p>其实去的每个地方都可以单独记录一篇日记，可惜有点懒，再加上文字不行，只能靠拍照纪念走过的路了，话说这几年也渐渐喜欢上了用手机拍照，然后隔段时间翻回去看，真的是一种回忆，一种享受，后面附几张照片。</p>
<p>这一年时间感觉零碎化更加严重，很多繁杂无关事情，写申请书，做PPT什么的，一年也没写几篇博客，让内心越来越浮躁，很难真正踏下心来长时间的去做某件事情，这个感觉很可怕，一直在纠正，可是很难，习惯都是潜移默化的。</p>
<p>现在博士四年级了，还有1-2年毕业，如果能顺利出国交流那是极好的。 现在也需要认真思考自己后面的选择了，还很犹豫，只希望几年后不会后悔就好了。</p>
<p>好了，先到这里吧。</p>
<p>最后</p>
<p>希望我的家人，朋友，被世界温柔以待。希望我能对得起对我好的人。</p>
<p>附图：</p>
<ul>
<li>2019.04: 广州转机-&gt;泰国清迈. 那边夜市很丰富，很多人都会说中文，记得当时去的下午就下了暴雨，无奈在宾馆点了两份外卖。。。 <img src="http://cdn.htliu.cn/blog/2019-review/th.jpg" alt="DASFAA-清迈"></li>
<li>2019.06: 青岛. 跟女票去青岛转了一圈，空气非常湿润，感谢于老板招待~ <img src="http://cdn.htliu.cn/blog/2019-review/qing.jpg" alt="回忆"></li>
<li>2019.07: 法国巴黎，埃菲尔铁塔，塞纳河~印象最深的跟着导师转了一天的卢浮宫，绕着塞纳河一圈，四十来度，很热很渴 哈哈。 <img src="http://cdn.htliu.cn/blog/2019-review/pa.jpg" alt="SIGIR-巴黎"></li>
<li>2019.08: 雅典古城+卡塔尔转机. 雅典的古建筑真的很多啊，大早上去爬山，俯瞰全城很有感觉。 <img src="http://cdn.htliu.cn/blog/2019-review/ath.jpg" alt="KSEM"></li>
<li>2019.10: 敦煌-月牙泉鸣沙山很不错. 落地敦煌就感觉到西北气息，晚上月亮非常大，《又见敦煌》很震撼。 <img src="http://cdn.htliu.cn/blog/2019-review/gan.jpg" alt="NLPCC-又见敦煌"></li>
<li>2019.11: 无锡-包子很好吃呀~ <img src="http://cdn.htliu.cn/blog/2019-review/wuxi.jpg" alt="NSFC-江南"></li>
</ul>
<p>于2020年三月十四, 家</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;2019年的总结来的实在晚了，但是一直惦记着，偶尔断断续续的写两句，这次总算写完了。 估计大家都没有想到，这次过年回家能待这么长的时间，看目前形势，估计这学期不用返校了.&lt;/p&gt;
&lt;p&gt;放假前一天的雪后图书馆： &lt;img src=&quot;http://cdn.htliu.cn/blog/2019-review/lib.jpg&quot; alt=&quot;雪后图书馆&quot;&gt;&lt;/p&gt;
    
    </summary>
    
      <category term="个人心得" scheme="http://shomy.top/categories/%E4%B8%AA%E4%BA%BA%E5%BF%83%E5%BE%97/"/>
    
    
      <category term="2019" scheme="http://shomy.top/tags/2019/"/>
    
  </entry>
  
  <entry>
    <title>python使用futures进行并行处理</title>
    <link href="http://shomy.top/2020/02/14/python-futures-para/"/>
    <id>http://shomy.top/2020/02/14/python-futures-para/</id>
    <published>2020-02-14T06:59:11.000Z</published>
    <updated>2020-02-14T07:11:58.897Z</updated>
    
    <content type="html"><![CDATA[<p>很多时候任务都需要并行，比如IO密集型的爬数据，读写磁盘等，CPU计算密集型的计算任务等等。而Python由于GIL的原因，默认情况下只能单线程运行，无法直接利用硬件的多核多线程，因此效率较低，python也早提供了一些列的多线程多进程的库可以用来使用，比如multiprocessing, queue 等等, 不过使用起来都相对复杂，不易控制。 经过几番尝试发现，发现<code>futures</code>基本是最方便将单线程/进程代码改为并发的代码的一个模块。这篇博客主要是简单记录下futures最常用的方式，以方便后续使用。</p>
<blockquote>
<p>futures 是 从Python3.2 加入到python标准库，python2.x需要额外pip安装，主要是对线程池和进程池做了高度的封装，可以更加灵活的使用并行编程。</p>
</blockquote>
<a id="more"></a>
<p>futures提供了多线程和多进程的接口：</p>
<ul>
<li><code>futures.ThreadPoolExecutor</code>: 多线程，适合IO密集型任务：爬虫，读写处理</li>
<li><code>futures.ThreadPoolExecutor</code>: 多进程，适合CPU密集型任务，比如数学运算等。</li>
</ul>
<p>话不多说，直接先上一个最常用的例子模板：</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div></pre></td><td class="code"><pre><div class="line"><span class="comment">#!/usr/bin/env python</span></div><div class="line"><span class="comment"># encoding: utf-8</span></div><div class="line"></div><div class="line"><span class="keyword">from</span> concurrent <span class="keyword">import</span> futures</div><div class="line"><span class="keyword">import</span> requests</div><div class="line"></div><div class="line">r = requests.Session()</div><div class="line"></div><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">func</span><span class="params">(url)</span>:</span></div><div class="line">    data = r.get(url).text</div><div class="line">    <span class="keyword">return</span> data</div><div class="line"></div><div class="line">task_list = [<span class="string">"https://www.163.com"</span>, <span class="string">"https://www.zhibo8.cc"</span>, <span class="string">"https://www.baidu.com"</span>]</div><div class="line">res = []</div><div class="line"><span class="keyword">with</span> futures.ThreadPoolExecutor(max_workers=<span class="number">2</span>) <span class="keyword">as</span> executor:</div><div class="line">   f_tasks = &#123;executor.submit(func, url): url <span class="keyword">for</span> url <span class="keyword">in</span> task_list&#125;</div><div class="line">   <span class="keyword">for</span> f <span class="keyword">in</span> futures.as_completed(f_tasks):</div><div class="line">            url = f_tasks[f]</div><div class="line">            <span class="keyword">try</span>:</div><div class="line">                data = f.result()</div><div class="line">                res.append(data)</div><div class="line">                print(<span class="string">f"<span class="subst">&#123;url&#125;</span> finished"</span>)</div><div class="line">            <span class="keyword">except</span> Exception <span class="keyword">as</span> exc:</div><div class="line">                print(<span class="string">f"<span class="subst">&#123;url&#125;</span> in wrong:<span class="subst">&#123;exc&#125;</span>"</span>)</div></pre></td></tr></table></figure>
<p>简单解释下：</p>
<ul>
<li>func函数：对于每一个任务的执行函数，对应修改自己的任务即可</li>
<li><code>ThreadPoolExecutor(max_workers=2)</code> 这里用的是多线程，多进程的话只需要修改为ProcessPoolExecutor, <code>max_workers</code> 调解并行线程或者进程数目。</li>
<li><code>executor.submit(func, url)</code> 这个是定义了执行函数，一般不用特别修改，当然如果要传入多个参数的话，也是可以的，直接包装为一个list或者tuple即可。</li>
<li><code>f.result()</code> 是在任务完成时候，取出结果</li>
<li>最后 <code>try / except</code> 一定要保留，不然代码出错的话，也不会抛出异常。</li>
</ul>
<p>当然<code>futures</code>除了上面的用法，还有很多其他的API和用法，比如<code>executor.map</code>等，不过上面的用法是我个人最常用的，这篇博客就是记录下，免得以后用到了还得去查。</p>
<p>想深入了解futures的话，可以看看下面的几个讲解</p>
<ul>
<li>https://www.dongwm.com/post/78/</li>
<li>https://juejin.im/post/5b1e36476fb9a01e4a6e02e4</li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;很多时候任务都需要并行，比如IO密集型的爬数据，读写磁盘等，CPU计算密集型的计算任务等等。而Python由于GIL的原因，默认情况下只能单线程运行，无法直接利用硬件的多核多线程，因此效率较低，python也早提供了一些列的多线程多进程的库可以用来使用，比如multiprocessing, queue 等等, 不过使用起来都相对复杂，不易控制。 经过几番尝试发现，发现&lt;code&gt;futures&lt;/code&gt;基本是最方便将单线程/进程代码改为并发的代码的一个模块。这篇博客主要是简单记录下futures最常用的方式，以方便后续使用。&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;futures 是 从Python3.2 加入到python标准库，python2.x需要额外pip安装，主要是对线程池和进程池做了高度的封装，可以更加灵活的使用并行编程。&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="编程语言" scheme="http://shomy.top/categories/%E7%BC%96%E7%A8%8B%E8%AF%AD%E8%A8%80/"/>
    
    
      <category term="python" scheme="http://shomy.top/tags/python/"/>
    
      <category term="futures" scheme="http://shomy.top/tags/futures/"/>
    
  </entry>
  
  <entry>
    <title>基于评论文本的深度推荐系统总结</title>
    <link href="http://shomy.top/2019/12/31/neu-review-rec/"/>
    <id>http://shomy.top/2019/12/31/neu-review-rec/</id>
    <published>2019-12-31T15:44:37.000Z</published>
    <updated>2020-04-19T01:40:30.618Z</updated>
    
    <content type="html"><![CDATA[<p>本文主要总结下近几年结合评论文本的推荐系统 (review-based recommendation)，侧重深度学习的模型，并且开源了一个代码库: <a href="https://github.com/ShomyLiu/Neu-Review-Rec" target="_blank" rel="external">Neu-Review-Rec</a> 主要完成了数据处理，模型构建，baseline复现等完整的Pipeline。[<strong>长文预警</strong>]</p>
<a id="more"></a>
<h2 id="问题简介">问题简介</h2>
<p>电商网站都允许用户为商品填写评论，这些文本评论能够体现出用户的偏好以及商品特征信息，是一种语义信息丰富的隐式特征。 相比于单纯的利用显式评分特征，文本信息一方面可以弥补评分稀疏性的问题，另一方面在推荐系统的可解释方面也能够做的更好。 从用户对某部电影的评论中，能够看出电影的一些情节特征，还有用户自己的理解与喜好兴趣等。</p>
<p>很多推荐系统工作开始考虑利用评论信息为用户和商品进行建模，在介绍具体的工作前，先简单介绍下常用的数据集，评价指标等。</p>
<h3 id="数据集">数据集</h3>
<p>一般情况，最常用的两大类带评论信息的数据集为：</p>
<ul>
<li>Amazon Review: 亚马逊数据集一般分不同的类别，比如电影，音乐，衣服，工具，游戏等，由McAuley整理发布出来的。现在有两个版本，一个是旧版数据(<a href="http://jmcauley.ucsd.edu/data/amazon/" target="_blank" rel="external">链接</a>，时间为1996-2014年的Amazon数据。另外Mc课题组又更新到了新版数据(<a href="https://nijianmo.github.io/amazon/index.html" target="_blank" rel="external">链接</a>)，新加了2014-2018年间的Amazon Review数据，具体的字段可以到上述链接中查看。 目前大部分已有工作基于旧版数据中的small部分，一般是选择几个分类目录去做实验，当然也有文章把所有的分类都跑了的，比如MPCN。</li>
<li>Yelp Review dataset: Yelp网站上的评论数据 (<a href="https://www.yelp.com/dataset" target="_blank" rel="external">链接</a>) , 常用的几个Yelp数据有 yelp2013, yelp2014, 和 yelp2016。Yelp提供的是完整数据集，需要根据需要自己去筛选不同年份的数据。</li>
</ul>
<p>此外，还有一些用的比较少的数据，比如<a href="https://www.ratebeer.com/" target="_blank" rel="external">Beer Dataset</a>, 之前在SNAP上有相关数据集，后来被要求删除了。 不过一般情况做实验使用Amazon的几个目录以及Yelp的数据集就足够了。 数据格式都可以简化为用户评论商品的四元组： <code>user, item, review text, rating</code>，其中rating一般都是从1-5分。 一般将按照user-item pair将数据集划分为训练接/验证集/测试集三部分，不过这里有一个坑需要注意:</p>
<blockquote>
<p>使用评论文本建模用户和商品，因此需要将在验证集和测试集中的user-item对应的review从训练集中去掉，这一条review在实际中是不存在的，因此不能用来建模用户或者对应的物品，如果这一条review没有移除的话，模型效果可能会不合理的提高。</p>
</blockquote>
<h3 id="评价指标">评价指标</h3>
<p>基于评论的推荐系统的基本目标就是评分预测 (Rating Prediction)，因此本质上是一个回归问题，因此一般情况下，j常用的三种评价指标如下：</p>
<ul>
<li>Mean Squared Error: <span class="math inline">\(MSE = \frac{1}{|D|}\sum_{(u,i) \in D}(r_{u,i} - \hat{r}_{u,i})^2\)</span></li>
<li>Root Mean Squared Error: <span class="math inline">\(RMSE = \sqrt{\frac{1}{|D|}\sum_{(u,i) \in D}(r_{u,i} - \hat{r}_{u,i})}\)</span></li>
<li>Mean Absolute Error: <span class="math inline">\(MAE = \frac{1}{|D|}\sum_{(u,i) \in D}|r_{u,i} - \hat{r}_{u,i}|\)</span></li>
</ul>
<p>其中<span class="math inline">\(r_{u,i}\)</span> 表示用户u对商品i的真实评分，$ _{u,i}$则是模型预测评分。本质上这三个评价指标差别不大，一般常用的是MSE/RMSE, MAE很少单独使用。 不过有一点小trick，就是模型在训练选择loss的时候，可以尝试这三个，有时候performance可能会有些许的不同。</p>
<h3 id="基本框架">基本框架</h3>
<p>推荐系统的一个核心任务就是如何得到更精确的用户和商品表示。基于评论文本的推荐模型也不例外，核心就是利用评论文本信息表示用户和商品。 一般而言， 模型都包含编码层，融合层，评分预测层等几个模块，如下简图所示： <img src="http://cdn.htliu.cn/blog/review-based-rec/frame.png" alt="framework"></p>
<p>其中:</p>
<ul>
<li>编码层：将用户和商品的文档通过模型编码，得到各自的特征，例如利用CNN/RNN，还有各种Attention的变种等。</li>
<li>融合层：如何将用户表示(user embedding)和物品表示(item embedding) 进行融合以及交互，得到统一的向量，常见的element-wise的add，dot以及concatenate等，还有一些其他更复杂的操作。</li>
<li>评分预测层：将融合得到的向量进行最终的rating prediction, 本质就是一个回归问题。常用的有使用MLP，FM, LFM等方式做最后的预测。多提一句，我们的实践发现这一层可能对部分模型效果影响较大，可能换一个输出层，指标就上去了….</li>
</ul>
<p>大部分的工作都会在上述三个模块中动手脚，这里面的核心就是编码层，如何利用用户与商品的评论进行建模， 尽可能的反映出用户偏好和商品特征，这是本质的目的。目前已有很多基于深度学习的工作，下面会对一些比较经典常用的工作分了两类，并做简单介绍。<br>
关于分类标准，本文并没有从方法论的角度分类(如是否用注意力机制)，而是从问题建模的角度出发。</p>
<blockquote>
<p>考虑到一个用户或者一个商品会有多条评论，关于如何处理多条评论，个人将已有工作分为两种:</p>
</blockquote>
<ul>
<li>将用户或商品的多条reviews进行concatenate得到一个较长的document, 然后对该document进行提取特征作为用户和商品特征。 这种方法将所有review聚合，能够得到用户或者商品一个全局的兴趣或者特征。</li>
<li>单独建模每一条review，之后再将review的特征聚合为用户特征。这种方法能够捕获用户对某一特定商品的偏好信息。</li>
</ul>
<p>下面将会按照上述的分类对已有工作进行介绍，此外最后附上几篇角度比较新奇的工作。</p>
<p>此外，这里先给出两种特征：</p>
<ul>
<li>review-based feaure：使用user与item的评论文本信息得到对应的语义特征。</li>
<li>rating-based feature: 可以理解为单纯的user/item 的ID embedding，能够视为每个用户或者商品的独有的特征。</li>
</ul>
<h2 id="基于document建模的方法">基于document建模的方法</h2>
<p>下面介绍的几篇文章都是将用户或者商品的所有评论进行合并成一个长文档然后进行建模的。</p>
<h3 id="convmf-recsys16">ConvMF (RecSys’16)</h3>
<blockquote>
<p>Kim D, Park C, Oh J, et al. Convolutional matrix factorization for document context-aware recommendation[C]//Proceedings of the 10th ACM Conference on Recommender Systems. ACM, 2016: 233-240.</p>
</blockquote>
<p><strong>基本介绍</strong></p>
<p>这大概是较早的探索卷积神经网络在review-based rec上的工作，将CNN与概率矩阵分解(PMF)结合起来进行评分预测，值得注意的是，本文仅仅利用了商品的评论数据，用户的话，还是只有评分数据。这与后面的大部分用户-商品的平行结构是不同的。</p>
<p>代码：<a href="https://github.com/cartopy/ConvMF" target="_blank" rel="external">ConvMF</a>。</p>
<p><strong>方法</strong> 在介绍ConvMF前，先简单介绍下基于评分矩阵的PMF。一般情况下，评分预测任务首先需要学习用户和商品的feature, 即两个latent feature matrix: <span class="math inline">\(U \in R^{N \times k}, V \in R^{M \times k}\)</span>, 其中U，V分别为N个用户和M个商品的特征矩阵。一般情况下，利用已有的观测矩阵向量，可以给出条件概率： <span class="math display">\[p(R|U, V, \sigma^2) = \prod \prod \mathcal{N}(R_{ij}| U_i, V_j, \sigma^2) \]</span></p>
<p>其中U，V也都利用均值为 (向量)的高斯分布来刻画： <span class="math display">\[p(U|\sigma_{U}^2) = \prod_{i=1}^{N} \mathcal{N}(U_i|0,\sigma_{U}^2\mathbf{I}), p(V|\sigma_{V}^2) = \prod_{j=1}^{M} \mathcal{N}(V_j|0,\sigma_{V}^2\mathbf{I})\]</span> 最后使用最大化后验概率来优化函数，然后推导化简最终可以得到 <span class="math display">\[\mathcal{L}(U,V) = \sum_{i=1}^N\sum_{j=1}^M(R_{ij} - U_i^TV_j)^2 + \frac{\lambda_U}{2}\sum_{i=1}^N||U_i||^2_2 + \frac{\lambda_V}{2}\sum_{j=1}^{M}||V_j||^2_2 \]</span> 从最后的损失函数中可以看出来，其实也就是一个MSE损失，后面两项可以看出正则项，所以虽然PMF是概率模型，通过后验概率去优化，不过与直接MSE殊途同归。这样根据训练数据去更新U和V到收敛, 得到用户和商品的潜在特征，最后的评分预测，就直接使用用户和商品特征的dot得到：<span class="math inline">\(r = U_i^TV_j\)</span>。</p>
<p>理解了PMF, ConvMF这篇文章基本就很容易了。 ConvMF在商品的特征向量V的高斯分布上做文章，将经过CNN抽取出的评论信息特征，作为V的高斯分布的均值，而不再使用均值为<span class="math inline">\(\mathbf{0}\)</span>。如下: <span class="math display">\[p(V|W, X, \sigma_{V}^2) = \prod_{j=1}^{M} \mathcal{N}(V_j|cnn(W,X ),\sigma_{V}^2\mathbf{I})\]</span> 这其中<span class="math inline">\(\mathbf{X}\)</span>就是商品的评论文档，而<span class="math inline">\(\mathbf{W}\)</span>则是卷积的参数，包括卷积核和偏置。至于CNN的用法，这里就不再赘述了，毕竟现在看来还是比较简单，利用word embedding将词映射为向量，之后卷积，最大池化得到feature vector。完整的框架图如下: <img src="http://cdn.htliu.cn/blog/review-based-rec/convmf.png" alt="ConvMF"> 有了CNN，相当于在PMF的基础上多了一个W参数，这样在最后的损失函数上，就会变成下面的: <span class="math display">\[\mathcal{L}(U,V,W) = \sum_{i=1}^N\sum_{j=1}^M(R_{ij} - U_i^TV_j)^2 + \frac{\lambda_U}{2}\sum_{i=1}^N||U_i||^2_2 + \frac{\lambda_V}{2}\sum_{j=1}^{M}||V_j-cnn(W,X_j)||^2_2 + \frac{\lambda_W}{2}||W||^2_2 \]</span> 其中商品对应的高斯分布的均值将PMF与CNN的桥梁。 在最终优化的时候，很显然CNN的参数W不能得到解析解，因此这两部分参数还是需要分开优化。在固定U,V的前提下，使用BP更新W。固定W的条件下， 使用坐标梯度下降即可更新U后者V，直到收敛。</p>
<p><strong>总结</strong></p>
<p>现在看来这篇文章的思路其实很简单，利用CNN提取商品的feature，之后将其融入到PMF中。 其实这种方法也是深度学习与概率图模型的结合的一种很直接的方式，用深度模型学到的特征替换某一分布的参数，可以理解为先验。 后来也有不少工作在这个思路上发展，比如偶然看见的WWW2018的一篇Poster：</p>
<blockquote>
<p>Network Embedding Based Recommendation Method in Social Networks</p>
</blockquote>
<p>基本思路也是利用预训练的Embedding替换分布参数。 此外，反过来思考这篇文章只用了商品的评论建模，没有利用用户的历史评论信息，可能是当时推荐系统优先考虑如何学习item embedding, 而对user embedding的研究并不是很多。</p>
<h3 id="deepconn-wsdm17">DeepCoNN (WSDM’17)</h3>
<blockquote>
<p>Zheng L, Noroozi V, Yu P S. Joint deep modeling of users and items using reviews for recommendation[C]//Proceedings of the Tenth ACM International Conference on Web Search and Data Mining. ACM, 2017: 425-434.</p>
</blockquote>
<p><strong>基本介绍</strong></p>
<p>上面说到ConvMFC只用了商品的评论文本，DeepCoNN这篇文章可以说是ConvMF的进阶版，全部使用深度学习模型去建模用户和商品，完全的end-to-end训练。 利用两个平行的卷积结构分别对用户和商品的评论文档建模，最后使用了FM (Factorization Machine) 做评分预测。 需要说明的是：</p>
<blockquote>
<p>DeepCoNN原文在处理数据的时候，并没有去掉在validation 和 test dataset中 的评论。</p>
</blockquote>
<p>第三方复现的代码：<a href="https://github.com/chenchongthu/DeepCoNN" target="_blank" rel="external">DeepCoNN</a> .</p>
<p><strong>方法</strong></p>
<p>模型其实现在看来很简单，首先将用户和商品的所有评论串联起来组成两个长的文档，然后用两个卷积结构（卷积层，池化层）学习文档的表示向量作为用户和商品的表示，最后串联二者输入到FM中预测评分，结构图如下：</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/review-based-rec/deepconn.png" alt="DeepCoNN">
<p class="caption">DeepCoNN</p>
</div>
<p>对应到三个模板的就是：</p>
<ul>
<li>编码层：卷积神经网络, CNN</li>
<li>融合层: concatenation 串联</li>
<li>评分预测层：Factorization Machine 因子分解机</li>
</ul>
<p>具体的细节也就无需再介绍了。</p>
<p><strong>总结</strong></p>
<p>这篇文章应该算是第一篇完全基于深度学习的review-based rec模型，并且设计的用户和商品平行结构 （类似文本语义匹配的双塔结构）也是后面很多工作的基石。</p>
<h3 id="d-attn-recsys17">D-Attn (RecSys’17)</h3>
<blockquote>
<p>Sungyong Seo, Jing Huang, Hao Yang, and Yan Liu. 2017. Interpretable Convolutional Neural Networks with Dual Local and Global Attention for Review Rating Prediction. In Proceedings ofthe Eleventh ACMConference on Recommender Systems.</p>
</blockquote>
<p><strong>基本介绍</strong></p>
<p>这篇文章在DeepCoNN的基础上引入了词级别的attention，不同的词对于建模用户和商品的重要性是不同的， 因此这篇文章引入了两种词级别的注意力机制：local and global attention 都是为了寻找更informative的词，为不同的词赋予不同的权重。</p>
<p><strong>方法</strong></p>
<p>跟DeepCoNN一样，双塔结构。首先review document经过word embedding层，得到词矩阵表示<span class="math inline">\(\mathbf{d_u=[x_1, x_2, ..., x_ T]}\)</span>。之后分别是两种attention + CNN：</p>
<ul>
<li>CNN based on local attention : 这一层利用词的上下文信息，来获得当前词的重要性。
<ul>
<li>具体首先设定一个local window，假设宽度为<span class="math inline">\(w\)</span>, 当前词为第<span class="math inline">\(i\)</span>-th 词， 那么如何结算第<span class="math inline">\(i\)</span>-th词的注意力权重呢？ 首先取出第i个词上下各<span class="math inline">\(w/2\)</span>的词，即: <span class="math inline">\(\mathbf{X_{l-att,i}}[\mathbf{x_{i+\frac{-w+1}{2}}, \cdots, x_i, \cdots, x_{i+\frac{w-1}{2}}}] \in R^{w \times d}\)</span>, 这样组成一个<span class="math inline">\(w*d\)</span>的矩阵，作为<span class="math inline">\(x_i\)</span>的上下文局部信息，之后直接经过一层的MLP得到一个权重标量，作为词的权重: <span class="math inline">\(s_i = MLP(\mathbf{X_{l-att, i}})\)</span>, 之后得到一个带权的document 矩阵<span class="math inline">\([s_1\mathbf{x_1},\cdots,s_i\mathbf{x_i}...]\)</span>。</li>
<li>由于前面的加权词矩阵已经捕获了上下文信息，因此接下来的CNN的filter的窗口直接设置为1，最后使用max pooling得到基于local attention的用户、商品的评论文档特征向量。</li>
</ul></li>
<li>CNN based on global attention：这一部分global attention本质上就是一个普通的窗口大于1 的CNN, 然后池化得到一个特征向量，至于能否称之为global还有待商榷。</li>
<li>将这两部分的特征进行拼接得到用户和商品的表示向量，之后直接进行dot product 预测最终的评分。</li>
</ul>
<p>完整的框架图如下所示：</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/review-based-rec/dattn.png" alt="D-Attn">
<p class="caption">D-Attn</p>
</div>
<p><strong>总结</strong></p>
<p>这篇文章算是第一篇在review-based rec上使用了attention机制，用来indicate那些更加重要的词。不过现在看来global attention的动机稍微不那么convincing，根据文中的描述，本质就是朴素的CNN，当然这篇文章的核心贡献就是引入了注意力机制，但是仅仅是词级别，所以才会有后面的review-level的注意力机制模型。</p>
<h3 id="anrcikm18">ANR(CIKM’18)</h3>
<blockquote>
<p>Chin J Y, Zhao K, Joty S, et al. ANR: Aspect-based neural recommender[C]//Proceedings of the 27th ACM International Conference on Information and Knowledge Management. 2018: 147-156.</p>
</blockquote>
<p><strong>基本介绍</strong></p>
<p>这篇文章是利用评论文本从aspect的角度来建模用户和商品，需要注意的是，不同于显式的预先提取文本的aspect的方法（价格，质量等），ANR是并没有规定抽取哪些Aspect, 而是将不同的aspect抽象为模型不同参数去学习，或许可以称为隐式aspect。</p>
<p>代码: https://github.com/almightyGOSU/ANR</p>
<p><strong>方法</strong></p>
<p>框架图如下所示： <img src="http://cdn.htliu.cn/blog/review-based-rec/anr.png" alt="ANR"></p>
<p>首先编码层ANR没有使用CNN或者RNN等抽取文本评论的特征，而是使用一种结合了aspect信息的类似local attention的方法，出发点就是同一个词或者同一个句子在不同的aspect下含义可能是不一样的，具体如下： 文中先假设有K个aspect， 为每个aspect定义了一个词映射矩阵<span class="math inline">\(W_a\)</span>，用来更新同一个词在不同的aspect下的embedding: <span class="math display">\[M_{u,a}[i] = M_u[i]W_a\]</span> 其中<span class="math inline">\(M_u[i]\)</span>是用户u的评论文档中第<span class="math inline">\(i\)</span>个词的原始词向量，经过这样的映射，得到的新的词向量<span class="math inline">\(M_{u,a}[i]\)</span>则就可以视为在当前aspect下的词特征。</p>
<p>之后就是local attention，首先计算每个word的上下文信息，其实就是把当前word周围的词的词向量直接串联起来: <span class="math inline">\(z_{u,a,i} = (M_{u,a}[i-c/2],..., M_{u,a}[i],...,M_{u,a}[i+c/2])\)</span>$，</p>
<p>再之后就是attention的过程， 计算每个词的权重: <span class="math inline">\(attn_{u,a}[i] = softmax(v_az_{u,a,i}^T)\)</span>, 这里面<span class="math inline">\(v_a\)</span>可以理解为针对某个aspect的查询向量(attention query vector), 也是模型参数， 这样可以计算出每个词的归一化之后的权重，最后直接加权平均得到在当前aspect下的用户u的评论文档的表示: <span class="math display">\[p_{u,a} = \sum_{i=1}^nattn_{u,a}[i] M_{u,a}[i]\]</span> 同理也可以得到商品i在aspect下的表示<span class="math inline">\(q_{i,a}, a \in 1-K\)</span>. 这一部分共有两部分参数: (1)词映射矩阵<span class="math inline">\(W_a\)</span>, (2) 查询向量<span class="math inline">\(v_a\)</span>，通过这两个aspect-specific的参数，可以为用户和商品的得到他们的apsect-level的评论特征矩阵: <span class="math inline">\(P_{u} =[p_{u,1}, p_{u,2}...,p_{u,K}]\)</span> 以及 <span class="math inline">\(Q_{u} =[q_{i,1}, q_{i,2}...,q_{i,K}]\)</span>。</p>
<p>模型的第二部分就是根据两个矩阵<span class="math inline">\(P_{u}\)</span>和<span class="math inline">\(Q_{i}\)</span>来考虑用户和商品的交互了。基本的出发点就是用户在面对不同商品的时候，不同apsect的重要性是不一样的。 这个也很直观，比如用户在对衣服时，可能注重价格，在购买数码产品时，可能注重售后等。反之亦然，即：同一个商品在面对不同的用户时候，它的不同aspect的重要性也是不同的。 其实这个动机就是经常说的考虑上下文信息， <strong>context-aware</strong>，学习一种用户以及当前商品的动态的交互过程。 其实本质上就是如何计算两个矩阵的相似性。 ANR使用的是一种比较常见的利用affinity matrix来计算: <span class="math inline">\(S=\phi(P_uW_sQ_i)\)</span>, 其中<span class="math inline">\(W_s\in R^{K \times K}\)</span>, 这里得到的<span class="math inline">\(S\)</span>就是可以认为用户的K个aspect和物品的K个aspect之间的关联性。最后就是利用两个MLP层来计算这一个user-item pair的aspect的重要性: <span class="math display">\[H_u=\phi(P_uW_x+S^T(Q_iWy)), \beta_u = softmax(H_uv_x)\]</span> 得到的<span class="math inline">\(\beta_u \in R^K\)</span> 则就是在面对商品i的情况下，用户u的K个aspect的重要性。 同理可以得到商品的aspect的重要性$_i $。</p>
<p>最后是预测层使用的是改进的LFM，将<span class="math inline">\(\beta_u\)</span>和<span class="math inline">\(\beta_i\)</span>融合： <span class="math display">\[r_{u,i} = \sum_{a=1}^K \beta_{u,a} \cdot \beta_{i,a} \cdot p_{u,a}q_{i,a} + b_u +b_i +b_0\]</span></p>
<p>基本上到此就是ANR模型的完整过程了，细节较多，相对繁琐。</p>
<p><strong>总结</strong></p>
<p>总的来说，ANR这篇文章用了一堆的矩阵变换，来体现Aspect的作用，本质上说有点新瓶装旧酒的意思，一方面类似借助aspect的名义来进行各种转化，这样在动机上会容易描述一些，另一方面有点类似multi-head attention，都是为了学到更多的语义特征。 不过我个人愚见–没有任何先验知识（没有实现抽取出哪些aspect) 单纯靠模型学习得到的所谓的“aspect”的都不太靠谱。 整体来说，ANR这篇文章模型和实验都很扎实，写的也挺好的，可以把很繁琐细节讲明白。另外就是实验部分，本文居然把Amazon所有数据都跑了，这个算力太强了。 最后说一句ANR这种隐式Aspect的思路，其实可以融合到后面讲的review-level的模型里面。</p>
<h3 id="carltois19">CARL(TOIS’19)</h3>
<blockquote>
<p>Wu L, Quan C, Li C, et al. A context-aware user-item representation learning for item recommendation[J]. ACM Transactions on Information Systems (TOIS), 2019, 37(2): 1-29.</p>
</blockquote>
<p><strong>基本介绍</strong></p>
<p>这篇文章从标题中即可看出这篇文章就是面向context-aware这种场景的。基本出发点前面ANR也说过了，就是同一个用户面对不同商品偏好可能是不同的，因此不管对用户或者商品，为他们学单一/静态的表示就会有缺陷，无法充分捕获用户偏好和商品的特征，因此为用户和商品学习的特征应该是pair-wise的，相互关联动态的，而非独立的。 具体到评论信息的话，同一个词在不同的上下文下或许会有不同的语义信息，因此在建模评论文档的时候，需要把这个考虑进去。 本文就是从这个角度，学习了两层的交互关系（low- and high-lelve) 去得到context-aware representation.</p>
<p>代码: https://github.com/WHUIR/CARL</p>
<p><strong>方法</strong></p>
<p>评论文档建模的主要架构：</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/review-based-rec/anr.png" alt="CARL">
<p class="caption">CARL</p>
</div>
<p>主要包含CNN编码层，以及基于注意力机制的user-item交互层。</p>
<ol style="list-style-type: decimal">
<li><p>编码层的前面部分都类似，首先word embedding，在之后使用卷积神经网络提取特征，这部分不再赘述了，经过卷积操作之后，最终得到用户和商品的两个评论文本的特征矩阵<span class="math inline">\(U \in R^{n \times f}, V \in R^{m \times f}\)</span>，其中m，n表示商品和用户评论文档中有n个单词，f为卷积核的数目，这样这两个矩阵的每一行就表示了每个词的feature vector。</p></li>
<li><p>下面就是考虑如何计算这两个矩阵之间的交互，也就。方法很多，类似ANR，CARL采用的也是affinity matrix的方法，使用一个中间矩阵<span class="math inline">\(T \in R^{f \times f}\)</span>去计算：<span class="math inline">\(R_{j,k} = tanh(c_j^uTc_k^i)\)</span>, 这里面的<span class="math inline">\(c_j^u\)</span>和<span class="math inline">\(c_k^i\)</span>分别表示矩阵U和V中的一行，这样R_{j,k}就可以表示user中第j个word 与 item中第k个word的相关性， 因此直接$R_{j,*} R^m $ 表示user的j个词语与当前item的所有词的相关性，文中直接取平均再归一化得到第j个词的权重： <span class="math display">\[a_j^u = softmax(mean(R_{j,1,}, R_{j,2}...R_{j, m}))\]</span> 这样为每个用户和商品都得到了一个词权重向量<span class="math inline">\(a^u, a^i\)</span>，然后乘到给每个词的表示，得到加权的的评论矩阵<span class="math inline">\(U^w=diag(a^u)U, V^w=diag(a^i)V\)</span>， 到这里就是框架图中的Attention Layer。</p></li>
<li><p>之后如何将加权之后的评论文档矩阵pool成向量呢？本文没有直接使用mean或者max等，而是接着用了一层CNN去捕获更high-level的特征，之后再使用mean做pooling。文中给出的理由是，mean或者max简单操作会引入不相关的噪音信息，而CNN可以获取更丰富的Local Context 特征，也有道理，只是复杂度又多了一些。再加一个MLP，最终得到用户的评论特征: <span class="math display">\[t_u = MLP(CNN(U^w)), t_i=MLP(CNN(V^w))\]</span> 这就是图中的Abstracting Layer了。</p></li>
<li><p>再经过一番点积，串联等操作，得到用户和商品的基于评论的特征：<span class="math inline">\(z_{review} = [(t_u \odot t_i) \oplus t_u \oplus t_i]\)</span>，同时再结合上用户和商品的ID embedding 或者叫 interaction feature 得到rating feature: <span class="math inline">\(z_{int}=[(p_u \odot p_i) \oplus p_u \oplus p_i]\)</span>。其中<span class="math inline">\(p_u\)</span>和<span class="math inline">\(p_i\)</span>是ID embedding。</p></li>
</ol>
<p>最后rating prediction，本文使用将两份特征输入到Factorization Machine (FM) 中得到两个评分，<span class="math inline">\(\hat{y}(z_review)\)</span>和<span class="math inline">\(\hat{y}(z_int)\)</span>，最后使用一种动态参数的小技巧将两份评分融合得到最后的预测。这里不再赘述了，模型部分就结束了。</p>
<p><strong>总结</strong></p>
<p>现在看来其实本文的思想相对比较简单，不过听作者说，CARL其实是2017年投的，不过直到2019年才接收。核心只有一个就是context-aware，如何计算两个矩阵的交互，本文使用的加入一个中间矩阵来计算。其实还有其他的方法来做，比如KDD2019年的DAML: Dual Attention Mutual Learning between Ratings and Reviews for Item Recommendation在本文的基础上，修改了两个地方，(1) 计算交互性的方法，DAML改为使用欧氏距离来计算UV的相似度.(2) 将最终的FM修改为Neural FM，其余地方很类似，这篇文章就不单独介绍了。其实我们自己也按照这个思路，水了一篇文章，其中一个贡献就是利用self attention去计算上下文相关性，去学习用户和商品的表示。</p>
<h2 id="基于review建模的方法">基于review建模的方法</h2>
<p>接下来这一系列的方法都是单独对用户或商品的每一条评论进行建模，得到每一条review的feature，之后将所有的feature进行聚合，作为用户或者商品的特征。</p>
<h3 id="narre-www18">NARRE (WWW’18)</h3>
<blockquote>
<p>Chen C, Zhang M, Liu Y, et al. Neural attentional rating regression with review-level explanations[C]//Proceedings of the 2018 World Wide Web Conference. 2018: 1583-1592.</p>
</blockquote>
<p><strong>基本介绍</strong></p>
<p>这篇文章的出发点很简单，就是不同review的重要性是不同的，有的review包含信息量大，可能对建模用户或者商品更加有用 (usefulness), 而有的review可能没有作用。 因此这篇文章提出了review-level的注意力机制来刻画每一条review的权重。</p>
<p><strong>方法</strong></p>
<p>有了上述的动机，具体的方法就比较清晰了，同样的双塔框架图如下所示： <img src="http://cdn.htliu.cn/blog/review-based-rec/narre.png" alt="NARRE"></p>
<ul>
<li>Review Encoder: 对每一条评论建模使用CNN+Max Pooling 得到review 的 feature, 也就是图中的<span class="math inline">\(O_{u1}, O_{u2}..\)</span></li>
<li>User/Item Encoder: 使用Attention 聚合所有的 <span class="math inline">\(O_{u1}...O_{uj}\)</span>得到用户u的表示，聚合所有的<span class="math inline">\(O_{i1}...O_{ik}\)</span>得到商品i的表示。
<ul>
<li>Attention Score计算：在计算某一个review的score的时候，NARRE并没有随机初始化一个query vector直接做点乘， 而是借助于这条review对应的user 或者 item 的ID Embedding计算，不过是一种交叉计算。 比如聚合用户的review list的时候，使用的是当前review对应的item的 ID embedding, 聚合商品的review list的时候，用的是当前review对应的用户的ID Embedding。其实这里的动机解释的话，大概是这样。 有些用户总喜欢写一些无用的评论，有些用户则倾向于写信息量高的评论，所以融入user的ID embedding 能够包含用户本身usefulness的信息，具体的计算公式，以item为例: <span class="math display">\[a^*_{il}=h^TReLU(WO_{il} + W_uu_{il} + b_1) + b_2\]</span> 得到的<span class="math inline">\(a^*_{il}\)</span> 则是商品i的第l条review的权重.</li>
<li>按照softmax将<span class="math inline">\(a^*\)</span>归一化权重，进而使用加权平均，得到商品从评论文本中得到的feature: 记为<span class="math inline">\(Y_i\)</span>, 同样方法得到user的feature, 记为<span class="math inline">\(X_i\)</span>.</li>
</ul></li>
<li>Rating Prediction: 本文考虑到传统的rating feature (即ID Embedding) 用到最后的评分预测中，即:<span class="math display">\[h= (q_u + X_u) \cdot (p_i + Y_i)\]</span>, 其中<span class="math inline">\(q_u,p_i\)</span>分别为用户u和商品i的ID Embedding。 之后使用Latent Factor Model 计算最终的评分: <span class="math inline">\(r_{u,i} = MLP(h) + b_u + b_i\)</span>，其中 <span class="math inline">\(b_u\)</span>为用户偏置，<span class="math inline">\(b_i\)</span>为商品偏置。</li>
</ul>
<p><strong>总结</strong></p>
<p>这篇文章比较经典，我个人认为他的核心贡献有两点：</p>
<ul>
<li>引入review-level的注意力机制，研究不同评论的usefulness.</li>
<li>ID Embedding的使用：（1）用于Attention的计算。 (2) 最后Rating Prediction中。 实际上在我们的实验中发现， 最后单纯使用id embedding做rating prediction, 在几个小的数据集上表现甚至优于很多复杂的深度学习模型。</li>
</ul>
<h3 id="tarmfwww18">TARMF(WWW’18)</h3>
<blockquote>
<p>Lu Y, Dong R, Smyth B. Coevolutionary recommendation model: Mutual learning between ratings and reviews[C]//Proceedings of the 2018 World Wide Web Conference. 2018: 773-782.</p>
</blockquote>
<p><strong>基本介绍</strong> 这篇文章跟NARRE类似，也是先学习出了用户/商品的每一条review的特征，然后再汇总得到用户/商品的评论特征。本文另外一个贡献点是把PMF融合进来，利用矩阵分解得到用户和商品基于rating matrix的特征，然后使两类特征接近，作为损失函数的正则项，有点类似ConvMFC的扩展版。</p>
<p><strong>方法</strong></p>
<p>简单介绍下方法，框架图其实基本一目了然 （前面ConvMF介绍很详细了）： <img src="http://cdn.htliu.cn/blog/review-based-rec/tarmf.png" alt="TARMF"></p>
<p>一共两大部分，一个是评论特征抽取， 一个是PMF模块。 (1) 首先对用户的每条评论使用RNN+Attention的方法得到每条评论的特征，然后对该用户的所有评论特征直接取平均，作为用户最终的特征向量U，同理可得到商品的评论特征向量V。 (2) 使用PMF分解评分矩阵， 得到用户和商品的latent feature 分别表示为<span class="math inline">\(\hat{U}, \hat{V}\)</span>。</p>
<p>之后这两类特征，应该具有一定的相近性，因此直接将<span class="math inline">\(||U-\hat{U}||\)</span>和<span class="math inline">\(||V-\hat{V}||\)</span>作为PMF的后验概率目标函数的两个正则项共同去优化。</p>
<p><strong>总结</strong></p>
<p>本篇文章相比NARRE来说，亮点稍微少一点，毕竟重心不是在如何做review的聚合，不过整体思路很直接明了，在ConvMF的基础上，结合了attention机制去学习评论表示，在当时(2017)很不错的文章。这种神经网络+概率图模型结合的方向一般错不了~</p>
<h3 id="mpcnkdd18">MPCN(KDD’18)</h3>
<blockquote>
<p>Tay Y, Luu A T, Hui S C. Multi-pointer co-attention networks for recommendation[C]//Proceedings of the 24th ACM SIGKDD International Conference on Knowledge Discovery &amp; Data Mining. 2018: 2309-2318.</p>
</blockquote>
<p><strong>基本介绍</strong></p>
<p>这篇文章属于结合review-level和context-aware，且核心就是为了解决document-level的问题，出发点类似NARRE，用户或者商品的不同的review的重要性是完全不同的，<strong>需要侧重重要的信息</strong>不过这篇文章的思路与NARRE完全不同，从标题中也可以看文中利用text generation中的pointer network来挑出重要的评论从而用来建模当前的user-item，个人觉得动机很不错。</p>
<p>代码: https://github.com/vanzytay/KDD2018_MPCN</p>
<p><strong>方法</strong></p>
<p>框架图如下: <img src="http://cdn.htliu.cn/blog/review-based-rec/mpcn.png" alt="MPCN"></p>
<p>首先对每一条评论建模，利用word embedding 得到评论矩阵，本文没有使用CNN/RNN直接将评论里面所有词的embedding求和作为当前review的特征。前面说这篇文章的核心就是在找出在当前user-item 对的情况下，哪些评论重要与否。因此文章首先对评论做一个预处理，利用一个类似RNN的那种门控机制，预期能够控制一些重要信息进入后面的步骤，这一块总感觉不太make sense（可能没太看明白)。 后面就是review-level co attention 部分挑出重要的review，在之后对挑出的review中的词进行word-level的 coattention，最后利用FM做rating prediction。</p>
<ol style="list-style-type: decimal">
<li><p>Review-level co attention: 前面得到了用户的评论集特征<span class="math inline">\(a\in R^{l_r \times d}\)</span>以及当前商品的评论集特征<span class="math inline">\(b\in R^{l_r \times d}\)</span>，其中<span class="math inline">\(l_r\)</span>是评论的条数。之后为了交互性(context-aware), 同样采用了Affinity Matrix的方法得到用户跟商品的评论相关性矩阵: <span class="math inline">\(s_{i,j} = a_iMb_i\)</span>, 得到的矩阵$s R^{l_r l_r} $的每一行与用户相关，每一列是关于商品的。</p></li>
<li><p>考虑到要挑出最重要的review，因此一种直观的表示，需要根据相关性矩阵s得到一个one-hot的vector，其中的那个1就是最重要的review，为了达到这个目的，本文使用Gumbel-Max，具体的原理这里不再赘述: <span class="math display">\[a^{&#39;} = G(\max_{col}{(s)})a, b^{&#39;} = G(\max_{row}{(s)}) b\]</span> 这里的<span class="math inline">\(G()\)</span>就是Gumbel-Max function, 得到一个one-hot 向量，再从用户评论集合a中得到想要的那条review，记为<span class="math inline">\(a^{&#39;}\)</span>，同理在商品评论集合b中得到<span class="math inline">\(b^{&#39;}\)</span>。</p></li>
<li><p>Word-level co attention: 通过Pointer Network 得到了具体usre-item的review pair之后， 本文继续对review中的word进行了co attention的计算。计算方法跟上面review类似，也是用中间矩阵。这样可以得到两个review的最终的feature vectors。</p></li>
</ol>
<p>上面介绍的只是一个pointer得到一条review，文中为了获取更丰富的表示，使用了 multi pointer, 最终可以为每个用户和商品得到K个reviews，这里又类似multi-head了。 最终本文将这2K个特征（user+item) 进行拼接，得到最终的feature，rating prediction使用的是FM进行评分预测。</p>
<p><strong>总结</strong></p>
<p>这篇文章的动机和解决方法都很直观清晰，为了找到更加有用的review，使用hard selection的方法直接point哪些review去建模用户和当前商品，在context-aware下，解决了 review usefulness的问题，值得精读。</p>
<h2 id="其他">其他</h2>
<p>这个目录下主要分享有意思的其他文章，注重思路。</p>
<h3 id="parlcikm18">PARL(CIKM’18)</h3>
<p>PARL: Let Strangers Speak Out What You Like这篇文章，出发点很简单， 即rating 是sparse的， review也存在sparse的问题，尤其是很多用户、商品的评论很少的时候。本文提出了一种很有意思的的想法，就是利用其他人的review来辅助对自己的建模，就2步： <img src="http://cdn.htliu.cn/blog/review-based-rec/parl.png" alt="PARL"></p>
<p>第一步选出跟对一商品，与当前用户u打同样分数的其他user，第二步随机选择一个user，将该user评论当前item的review加入到当前user的review 集合，作为补充信息。</p>
<p>具体用什么模型建模都可以。总体来说，方法非常简单，却很有意思。</p>
<h2 id="总结">总结</h2>
<p>基于评论文本的推荐系统其实一定程度上跟NLP技术的结合更多，不过场景和动机都是出自推荐领域，本身在实际应用中远少于一些CTR系列的推荐工作，属于一个小领域。上面的几篇文章算是这个领域的有代表的，当然有很多其他优秀的文章由于时间，精力等原因并没有介绍进来，可能后续会继续补充。 从近几年的趋势来看， 大家基本都已经明了这块的工作基本上从框架中的三个部分着手进行改进：编码层，融合层和评分预测层。对于review来说，显然核心是编码层，毕竟融合层和评分预测层毕竟在其他推荐领域也都是共同问题。 虽然自己没有多少文章，还是很小白，不过我们也准备投一篇相关Survey去试试水~</p>
<p>最后上述所有内容均是个人总结，如有纰漏，欢迎指出，共同讨论。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;本文主要总结下近几年结合评论文本的推荐系统 (review-based recommendation)，侧重深度学习的模型，并且开源了一个代码库: &lt;a href=&quot;https://github.com/ShomyLiu/Neu-Review-Rec&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;Neu-Review-Rec&lt;/a&gt; 主要完成了数据处理，模型构建，baseline复现等完整的Pipeline。[&lt;strong&gt;长文预警&lt;/strong&gt;]&lt;/p&gt;
    
    </summary>
    
      <category term="读书笔记" scheme="http://shomy.top/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="recommendation" scheme="http://shomy.top/tags/recommendation/"/>
    
      <category term="deep learning" scheme="http://shomy.top/tags/deep-learning/"/>
    
      <category term="review-based" scheme="http://shomy.top/tags/review-based/"/>
    
  </entry>
  
  <entry>
    <title>推荐系统结合知识图谱简单总结</title>
    <link href="http://shomy.top/2019/03/19/kg-ns-recsys/"/>
    <id>http://shomy.top/2019/03/19/kg-ns-recsys/</id>
    <published>2019-03-19T11:35:17.000Z</published>
    <updated>2020-04-19T09:16:03.007Z</updated>
    
    <content type="html"><![CDATA[<p>新增: <a href="http://shomy.top/2019/12/31/neu-review-rec/">基于评论文本的深度推荐系统模型总结</a></p>
<p>对近几年推荐系统(Recommendation System)领域结合Knowledge Graph Embedding (知识图谱表示学习) 或者 Network Embedding(网络表示)的几篇论文做了极简介绍。 首先对简单介绍推荐系统，之后整理了几篇结合知识表示的论文。</p>
<h1 id="推荐系统简介">推荐系统简介</h1>
<a id="more"></a>
<p>一句话来介绍的话，就是通过分析历史数据，来给用户 推荐 可能会喜欢/购买的商品， 这里面的核心就是用户 (User) 和 商品 (Item)。 更进一步，推荐系统的关键有下面三部分：</p>
<ul>
<li>用户偏好建模：User Preference</li>
<li>商品特征建模: Item Feature</li>
<li>交互: Interaction</li>
</ul>
<p>在RS中，具体的问题有多种，这篇笔记暂时侧重于Click-Through-Rate（点击预测) 问题，即根据用户历史点击/购买的Item列表，来预测 是否会点击/购买当前item，因此不考虑user的一些属性因素的话，本质就是判断历史item集合与当前新的item的相似度。</p>
<p>因此item的建模比较关键，在推荐系统中，目前不少工作开始融合一些结构信息来提高性能与解释性，至于如何建模结构，个人理解已有工作大概可以分为两种类型：</p>
<ul>
<li>结合知识图谱（Knowledge Graph)</li>
<li>结合异质信息网络 (Heterogenerous Network)</li>
</ul>
<p>本篇笔记主要集中在推荐系统结合知识图谱的几篇工作做个非常简单的总结，后续如时间允许，会将这一系列补全。</p>
<h1 id="论文">论文</h1>
<h2 id="cke">CKE</h2>
<blockquote>
<p>Zhang F, Yuan N J, Lian D, et al. Collaborative knowledge base embedding for recommender systems[C]// KDD, 2016: 353-362.</p>
</blockquote>
<p>问题：给用户推荐 一个商品列表， 评价指标是使用Map@K: Recall@K 。</p>
<h3 id="总览">总览</h3>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/kg-ne-recsys/recsys-1.jpg">

</div>
<p>使用item各种外部辅助信息来融合到CF中， 包含:</p>
<ul>
<li>结构信息: 异质结构信息（知识图谱），如 （演员-》出演-电影）</li>
<li>文本信息: item的文本描述，如电影的文字简介。</li>
<li>图片信息: item的图片，如电影海报</li>
</ul>
<p>其中编码方式:</p>
<ul>
<li>结构信息: TransE/R</li>
<li>文本信息/图片信息: SDAE(堆叠自动编码)</li>
</ul>
<p>得到关于item的三种Embedding，与CF的latent factor 加起来，得到item最终的embedding 用户：仅仅有CF的latent factor.</p>
<p>得到item和user的Embedding之后， loss则使用pair-wise 的rankinig loss. 此外需要注意的是，CF过程中 user/item的交互是通过评分矩阵来的, 这里仅仅使用了Postive的评分信息（不小于3）。</p>
<h3 id="评价">评价</h3>
<ul>
<li>局限性稍微大，需要大量的知识图谱中的额外信息，在实际的推荐中不易获得。</li>
<li>融合方法略微简略粗暴，直接使用向量相加</li>
</ul>
<h2 id="dkn">DKN</h2>
<blockquote>
<p>Wang H, Zhang F, Xie X, et al. Dkn: Deep knowledge-aware network for news recommendation[C]//WWW, 2018: 1835-1844.</p>
</blockquote>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/kg-ne-recsys/recsys-2.png">

</div>
<p>本篇文章侧重于新闻推荐场景，将KG Embedding结合到CTR中（预训练的方式)， 基本结合用户点击的历史新闻纪录，来判断是否会点击新的新闻。 本质上相当与 判断 历史新闻集合与新的新闻的相似度。 动机如下图，通过提取新闻标题中的实体，接着通过知识图谱来传递相关其他新闻，如下图： <img src="http://cdn.htliu.cn/blog/kg-ne-recsys/recsys-5.jpg"></p>
<p>下面介绍模型的主要部分。</p>
<h3 id="multi-channel-cnn">Multi Channel CNN</h3>
<p>类似RGB的3通道: 作为CNN的输入（文本为新闻title)</p>
<ul>
<li>word embedding: 词的word embedding</li>
<li>entity emebdding: KG Embedding得到的 entity embedding. 对于非实体的词，直接补全为0即可.</li>
<li>entity context embedding: 对于实体词，其在知识图谱中的一阶邻居实体平均embedding作为补充信息</li>
</ul>
<p>组成三通道作为cnn的输入</p>
<h3 id="user-candidate-news-attention">User-Candidate News Attention</h3>
<ul>
<li>Attention计算：将u点击的新闻与候选新闻embedding进行连接，输入到DNN
<ul>
<li>Query Vector: 候选新闻的标题特征</li>
<li>Key/Value：用户点击的所有历史新闻的标题特征</li>
<li>加权求得用户的点击偏好</li>
</ul></li>
<li>是否点击二分类：aggreate得到user embedding 与 候选新闻进行 相似度计算，这里还是使用dnn</li>
</ul>
<h3 id="总结">总结</h3>
<p>本篇文章更加侧重于对短文本的建模，三通道CNN那里很有技巧性，可以借鉴到其他领域。此外attention的计算方法（target item 为query，history items 为 key） 应用也比较广泛。</p>
<h2 id="ripplenet">RippleNet</h2>
<blockquote>
<p>Wang H, Zhang F, Wang J, et al. Ripplenet: Propagating user preferences on the knowledge graph for recommender systems[C]//CIKM, 2018: 417-426.</p>
</blockquote>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/kg-ne-recsys/recsys-3.png">

</div>
<p>RippleNet: 将知识图谱作为额外信息，融入到CTR/Top-K推荐</p>
<h3 id="动机">动机</h3>
<p>考虑到水波(Ripple)的传播，以user感兴趣的item为seed，在商品知识图谱上向外一圈一圈的扩散到其他的item，这个过程称之为偏好传播(Preference Propagation). 该模型认为外层的item同样属于用户的潜在偏好, 因此在刻画user时候，需要将其考虑进去，而不能仅仅使用观测到的items去表示user偏好。</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/kg-ne-recsys/recsys-6.jpg">

</div>
<h3 id="kg结合rs两种思路">KG结合RS两种思路</h3>
<ul>
<li>Embedding：item以及属性构建知识图谱，然后利用KG Embedding，计算item 的Embedding.</li>
<li>Path-Based： 将user-item以及属性构建异质信息网络(HIN)， 然后利用HIN相关的算法建模(Metapath)</li>
</ul>
<p>一般情况，KG 只能考虑item，而HIN可以考虑user+item. 下面简要介绍下模型部分。</p>
<h3 id="模型介绍">模型介绍</h3>
<ul>
<li>输入： 一个user u 和一个候选的 item i</li>
<li>输出： user会点击item的概率</li>
<li>构建 与 u 先关的 k-hop的 item 集合(知识图谱上以初始的item set向外扩展). [这些item可以作为user的偏好信息）</li>
<li>根据embedding 向量内积，计算候选item i 与 每一层hop上的head item的归一化相似度</li>
<li>根据相似度，对<strong>尾实体 items</strong> 加权求和，作为这一层hop的输出 (本质上，属于Attention，其中Q=候选item i, K=Head Item, V= Tail Item)</li>
<li>重复上述过程k次</li>
<li>将所有k hop的输出向量相加，作为user的Embedding， 与 item的Embedding 内积计算最终的相似度</li>
</ul>
<h3 id="总结-1">总结</h3>
<p>这篇文章动机非常直观，因此可解释性非常强，模型的构建也充满技巧，比如采样等，很多地方值的挖掘。</p>
<hr>
<p>下面简单介绍下结合网络表示或者异质信息网络的的两篇文章。</p>
<h2 id="nerm">NERM</h2>
<blockquote>
<p>Zhao W X, Huang J, Wen J R. Learning distributed representations for recommender systems with a network embedding approach[C]//AIRS, 2016: 224-236.</p>
</blockquote>
<p>将推荐的user,item以及属性作为graph中的节点，构造网络。 之后使用network embedding的方法来学习节点特征向量, 利用相似度来做TopK推荐.</p>
<h3 id="构建graph">构建Graph</h3>
<p>user, item ,feature 作为不同类型的节点， 构建网络：</p>
<ul>
<li>user-item的二分网络，边为user购买或者其它行为item</li>
<li>user-item-tag的三分网络: 将item的tag属性加进来，也作为节点。</li>
</ul>
<h3 id="学习embedding">学习Embedding</h3>
<p>本文完全使用LINE的一阶表示： 即对于a pair nodes (u, v) 如果 二者之间存在边，则二者的向量内积较大</p>
<h3 id="推荐">推荐：</h3>
<p>直接使用上述计算得到的user-item或者属性的 embedding， 利用内积来计算相似度，根据相似度排序来计算TopK</p>
<h3 id="总览-1">总览</h3>
<p>完全使用Network Embedding的方法来做Top-N推荐，属于尝试性工作, 对比工作也很单薄。</p>
<h2 id="cdne">CDNE</h2>
<blockquote>
<p>Gao L, Yang H, Wu J, et al. Recommendation with multi-source heterogeneous information[J]. IJCAI 2018.</p>
</blockquote>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/kg-ne-recsys/recsys-4.jpg">

</div>
<p>问题与CKE(fzzhang)一致：为user推荐Top N可能感兴趣的item. 利用Network Embedding的方法建模辅助信息，继而结合CF: （Network Embedding 模型基本完全参考:Tri-Party Deep Network Representation)</p>
<h3 id="融合辅助信息">融合辅助信息</h3>
<p>为了更好的建模item, 结合辅助信息：</p>
<ul>
<li>item的文本内容(word sequence)</li>
<li>item的标签tag(也是word)</li>
<li>item 与 item 的连接结构信息</li>
</ul>
<h3 id="deep-walkskip-gram-建模辅助信息">Deep Walk/Skip Gram 建模辅助信息</h3>
<ul>
<li>模仿skip gram, 中心词预测上下文的词。这里用中心item来预测在文本内容中word，以及用item的tag来预测word</li>
<li>利用deep walk 来学习结构表示(本质上也是skip gram) 这与Tri-Party Deep Network Representation 完全一样。</li>
</ul>
<p>这样可以学到节点的Embedding， word 的Embedding， tag的Embedding。 其中节点的Embedding用于下面的CF。</p>
<h3 id="结合cf">结合CF</h3>
<p>同CKE, 将上述节点的Embedding 与CF的latent vectors 直接相加，作为item的表示向量， 用户的向量还是CF的latent vectors. 最后利用Min(R-UV) 来优化 (这里区别于CKE的pair wise ranking)</p>
<p>数据: 用的两份CiteULike的数据，其中 item的节点并非使用paper的reference而是根据如果有多于4个相同作者的两篇文章，那么两篇文章之间加一条边。</p>
<h3 id="简要">简要</h3>
<ul>
<li>方法与动机基本类似与CKE, 不过并没有对比CKE。</li>
<li>相当于CKE + Tri-DNR的结合</li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;新增: &lt;a href=&quot;http://shomy.top/2019/12/31/neu-review-rec/&quot;&gt;基于评论文本的深度推荐系统模型总结&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;对近几年推荐系统(Recommendation System)领域结合Knowledge Graph Embedding (知识图谱表示学习) 或者 Network Embedding(网络表示)的几篇论文做了极简介绍。 首先对简单介绍推荐系统，之后整理了几篇结合知识表示的论文。&lt;/p&gt;
&lt;h1 id=&quot;推荐系统简介&quot;&gt;推荐系统简介&lt;/h1&gt;
    
    </summary>
    
      <category term="读书笔记" scheme="http://shomy.top/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="Knowledge Graph" scheme="http://shomy.top/tags/Knowledge-Graph/"/>
    
      <category term="Recommendation System" scheme="http://shomy.top/tags/Recommendation-System/"/>
    
  </entry>
  
  <entry>
    <title>2018</title>
    <link href="http://shomy.top/2019/02/27/2018-review/"/>
    <id>http://shomy.top/2019/02/27/2018-review/</id>
    <published>2019-02-26T16:05:09.000Z</published>
    <updated>2019-05-28T03:01:23.265Z</updated>
    
    <content type="html"><![CDATA[<p><img src="http://cdn.htliu.cn/blog/2018-review/lake.jpg"> 又一年啊, 时间总是这么快。终于赶完了年后的两个deadline, 现在听着音乐，回顾下这2018年。</p>
<a id="more"></a>
<iframe frameborder="no" border="0" marginwidth="0" marginheight="0" width="80%" height="86" src="//music.163.com/outchain/player?type=2&amp;id=529786068&amp;auto=1&amp;height=66">
</iframe>
<h2 id="学习">学习</h2>
<p>三个字总结下今年的科研经历：</p>
<blockquote>
<p>全拒得</p>
</blockquote>
<p>一个字总结就是：</p>
<blockquote>
<p>惨</p>
</blockquote>
<p>自己的几篇投稿7连拒，被拒都快自闭了，更有意思的是与别人合作的文章7连中，都让我开始怀疑是本命年的原因了，哈哈。</p>
<p>主要还是自己火候未到，水平太次。总体来说还是因为不重视写作，各种语法问题，逻辑上的，每次写完之后都会被老师吐槽一顿，这一年感觉也没有什么进步, 十分感谢wfz老师给我的耐心的指导，不厌其烦的指出各种小问题。</p>
<p>当然今年也算是见识到了各种各样的审稿人和审稿意见，比如前面一顿夸，最后说了一句有几个符号表示不清晰，直接reject，还有说paper跟一篇未出版的文章idea类似，给了reject，然而几个月过去了，到现在那篇文章依然unavailable。 最接近的一次感觉是WWW的一篇，前三个意见还可以，最后一个审稿人帮我修改了一遍论文， 从摘要到结语，一行一行的指出语法问题，两页的意见，最后给了弱拒，不过还是比较感谢这位如此负责的审稿人。</p>
<p>中间也换过一次小方向，不过总的还是NLP与复杂网络，偏推荐一些，或者说链接预测。 感觉相比关系抽取，可能更偏实用落地。</p>
<p>相比前两年，今年内心有点急了，压力也就接踵而来， 总想着赶紧发论文，达到毕业条件，欲速则不达，好多东西打磨的太少就着急投稿。</p>
<p>今年的小目标就是达到毕业条件，但愿能完成！</p>
<h2 id="生活">生活</h2>
<p>不知道为啥，现在总没有了本科期间那种冲劲了，做事情开始犹豫了，也更容易浮躁了，很难长时间集中精力了，比如现在还在听着歌。</p>
<p>想起了《天空的城》里面的一句话，“每个人在刚出生的时候，心里都有一座空空的城市，随着自己的长大，浮躁，冷漠，世俗的名利便填满了这座空城，从此变得臃肿不堪，浑浊不清”。 哈哈 感伤了。</p>
<p>去年一年感觉越来越喜欢拿手机拍照了，不知道这是不是心态越来越老的缘故[cry]。</p>
<p>还是需要多出去走走，见见世面，开拓一下眼界。去年出差，去了几次深圳，香港，的确南方北方差异还是比较大。</p>
<p>今年同届本科读硕士的同学也是毕业了，<span class="citation">@ks也去挣大钱去了</span>，还好@noil决定留下再续几年，不过总的来说，身边的旧同学越来越少。</p>
<p>岁月无情，趁着年轻你记得我，我记得你，没事便齐齐到处转转，吃吃饭，喝点酒，免得时间长了，大家都忘了谁是谁。</p>
<p>先这样了，回头想起来什么可以更新上来，还是那句话，希望我的家人，朋友，被世界温柔以待，感谢感恩。</p>
<p>希望，以后，可以做到：</p>
<blockquote>
<p>对得起自己，对得起对自己好的人。</p>
</blockquote>
<p>总之，加油.</p>
<p>于2019年正月二十三.</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;img src=&quot;http://cdn.htliu.cn/blog/2018-review/lake.jpg&quot;&gt; 又一年啊, 时间总是这么快。终于赶完了年后的两个deadline, 现在听着音乐，回顾下这2018年。&lt;/p&gt;
    
    </summary>
    
      <category term="个人心得" scheme="http://shomy.top/categories/%E4%B8%AA%E4%BA%BA%E5%BF%83%E5%BE%97/"/>
    
    
      <category term="2018" scheme="http://shomy.top/tags/2018/"/>
    
  </entry>
  
  <entry>
    <title>ELMo词向量及使用方法记录</title>
    <link href="http://shomy.top/2019/01/01/elmo-1/"/>
    <id>http://shomy.top/2019/01/01/elmo-1/</id>
    <published>2019-01-01T15:59:36.000Z</published>
    <updated>2019-04-03T02:30:05.041Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>2019.4.3更新：新增使用AllenNLP的ELMo做文本分类的例子说明：详见github: <a href="https://github.com/ShomyLiu/pytorch_elmo_example" target="_blank" rel="external">Pytorch-ELMo</a></p>
</blockquote>
<hr>
<p>ELMo不同于word2vec、glove，属于上下文词向量模型, 来自《Deep Contextualized Word Representations》(NAACL2018的best paper), 可以很方便用于下游NLP任务中。</p>
<h2 id="elmo简介">ELMo简介</h2>
<p>ELMo使用双层BiLSTM来训练语言模型，创新是线性组合不同层的word vectors， 作为最终的word representation. 核心公式: <a id="more"></a></p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/elmo-1/elmo.png">

</div>
<ul>
<li>第一层是普通的word embedding 可以用wrod2vec或者glove来得到，或者使用character level得到token embedding》 这部分是general embedding，上下文无关。文中使用的character level的CNN+Highway.</li>
<li>后面连接两个biLSTM 去encode 输入（同时也有残差连接), 每一层LSTM得到的输出（隐状态) 作为每个词的上下文相关的word vectors.</li>
<li>这样每个词就会有（L+1）个词向量，L为biLSTM的层数.</li>
<li>词向量的线性组合，针对不同的任务，不同层的向量做不同的权重加和。 其中权重s是一个维度为L的vector，参与训练。</li>
</ul>
<p>因此ELMo的基本输入单元为句子，每个词没有固定的词向量，是根据词的上下文环境来动态产生当前词的词向量，常见的场景可以较好解决一词多义的问题，这一点跟word2vec与glove等通用词向量模型是不同的。</p>
<h2 id="pytorch版elmo-allennlp">Pytorch版ELMo (Allennlp)</h2>
<p>这个是官方的ELMo的版本，基于Allennlp或者pytorch。<a href="https://github.com/allenai/allennlp/blob/master/tutorials/how_to/elmo.md#using-elmo-as-a-pytorch-module-to-train-a-new-model" target="_blank" rel="external">官方文档</a>给的example有些迷惑，这里记录一番。 Allennlp的ELMo的API为<code>allennlp.modules.elmo.Elmo</code>, 完整的输入参数参见<a href="https://github.com/allenai/allennlp/blob/master/allennlp/modules/elmo.py#L34" target="_blank" rel="external">文档</a>，几个比较重要的如下:</p>
<ul>
<li>options_file : ELMo JSON options file</li>
<li>weight_file : ELMo hdf5 weight file</li>
<li>num_output_representations: The number of ELMo representation layers to output.</li>
<li>requires_grad: optional, If True, compute gradient of ELMo parameters for fine tuning.</li>
</ul>
<p>其中一个关键的参数为<code>num_output_reprensenations</code> 文档描述不太清楚，具体可以看见在github提的<a href="https://github.com/allenai/allennlp/issues/2245" target="_blank" rel="external">issue</a>和<a href="https://github.com/allenai/allennlp/pull/2256" target="_blank" rel="external">pr</a>。 这个num_output_representation 一开始理解这个参数是输出三层中前几层，但是其实并不是这样，因为这个参数的取值可以是任意正整数。 我们知道ELMo文章中最后词向量表示即公式1计算的是三层表示的线性加权，针对不同的任务，可能会有不同的加权比例。 因此<code>num_output_representation</code> 表示输出多种线性加权的词向量，即多个公式1产生的词向量。 而Allennlp的ELMo 并不直接提供中间的三层输出(char-cnn, lstm-1, lstm-2)，不过可以通过稍微修改源代码的方法获得。</p>
<p>再看例子如下： <figure class="highlight makefile"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line">from allennlp.modules.elmo import Elmo, batch_to_ids</div><div class="line"></div><div class="line">options_file = <span class="string">"options.json"</span>  <span class="comment"># 配置文件地址 </span></div><div class="line">weight_file = <span class="string">"weights.hdf5"</span> <span class="comment"># 权重文件地址</span></div><div class="line"><span class="comment"># 这里的1表示产生一组线性加权的词向量。</span></div><div class="line"><span class="comment"># 如果改成2 即产生两组不同的线性加权的词向量。</span></div><div class="line">elmo = Elmo(options_file, weight_file, 1, dropout=0)</div><div class="line"></div><div class="line"><span class="comment"># use batch_to_ids to convert sentences to character ids</span></div><div class="line">sentence_lists = [<span class="string">"I have a dog"</span>, <span class="string">"How are you , today is Monday"</span>,<span class="string">"I am fine thanks"</span>]</div><div class="line">character_ids = batch_to_ids(sentences_lists)</div><div class="line"></div><div class="line">embeddings = elmo(character_ids)['elmo_representations']</div></pre></td></tr></table></figure></p>
<p>最终<code>len(embeddings) == 1</code>，里面的一个元素则是论文中公式1产生的ELMo的词向量, shape为<code>[batch_size, max_length, 1024]</code>, 即:[3, 9, 1024], (根据配置文件不同，最后维度可能不同, 参考<a href="https://allennlp.org/elmo" target="_blank" rel="external">预训练权重</a>)。</p>
<p>如果上述参数1改为2，则表示产生两个权重不同的ELMo词向量，可以用于不同任务的词向量。</p>
<p>一般单任务下，我们直接使用1就可以了。</p>
<h2 id="tensorflow版-elmo">tensorflow版 ELMo</h2>
<p>首先载入ELMo的方法很简单，直接load tfhub即可:</p>
<figure class="highlight elm"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">import</span> tensorflow <span class="keyword">as</span> tf</div><div class="line"><span class="keyword">import</span> tensorflow_hub <span class="keyword">as</span> hub</div><div class="line"><span class="title">url</span> = <span class="string">"https://tfhub.dev/google/elmo/2"</span></div><div class="line"><span class="title">embed</span> = hub.<span class="type">Module</span>(url)</div></pre></td></tr></table></figure>
<p>调用方法也很简单: <figure class="highlight ini"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="attr">sentence_lists</span> = [<span class="string">"I have a dog"</span>, <span class="string">"How are you , today is Monday"</span>,<span class="string">"I am fine thanks"</span>]</div><div class="line"><span class="attr">output</span> = embed(sentence_lists, as_dict=<span class="literal">True</span>)</div></pre></td></tr></table></figure></p>
<p>输出的结果如下: <figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">&#123;</div><div class="line">    'default': <span class="tag">&lt;<span class="name">tf.Tensor</span> '<span class="attr">module_apply_default_6</span>/<span class="attr">truediv:0</span>' <span class="attr">shape</span>=<span class="string">(3,</span> <span class="attr">1024</span>) <span class="attr">dtype</span>=<span class="string">float32</span>&gt;</span>,</div><div class="line">    'elmo': <span class="tag">&lt;<span class="name">tf.Tensor</span> '<span class="attr">module_apply_default_6</span>/<span class="attr">aggregation</span>/<span class="attr">mul_3:0</span>' <span class="attr">shape</span>=<span class="string">(3,</span> <span class="attr">9</span>, <span class="attr">1024</span>) <span class="attr">dtype</span>=<span class="string">float32</span>&gt;</span>,</div><div class="line">    'lstm_outputs1': <span class="tag">&lt;<span class="name">tf.Tensor</span> '<span class="attr">module_apply_default_6</span>/<span class="attr">concat:0</span>' <span class="attr">shape</span>=<span class="string">(3,</span> ?, <span class="attr">1024</span>) <span class="attr">dtype</span>=<span class="string">float32</span>&gt;</span>,</div><div class="line">    'lstm_outputs2': <span class="tag">&lt;<span class="name">tf.Tensor</span> '<span class="attr">module_apply_default_6</span>/<span class="attr">concat_1:0</span>' <span class="attr">shape</span>=<span class="string">(3,</span> ?, <span class="attr">1024</span>) <span class="attr">dtype</span>=<span class="string">float32</span>&gt;</span>,</div><div class="line">    'sequence_len': <span class="tag">&lt;<span class="name">tf.Tensor</span> '<span class="attr">module_apply_default_6</span>/<span class="attr">Sum:0</span>' <span class="attr">shape</span>=<span class="string">(3,)</span> <span class="attr">dtype</span>=<span class="string">int32</span>&gt;</span>,</div><div class="line">    'word_emb': <span class="tag">&lt;<span class="name">tf.Tensor</span> '<span class="attr">module_apply_default_6</span>/<span class="attr">bilm</span>/<span class="attr">Reshape_1:0</span>' <span class="attr">shape</span>=<span class="string">(3,</span> <span class="attr">9</span>, <span class="attr">512</span>) <span class="attr">dtype</span>=<span class="string">float32</span>&gt;</span></div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>解释下这些字段的意思:</p>
<ul>
<li><code>word_emb</code>: ELMo的最开始一层的基于character的word embedding, shape为<code>[batch_size, max_length, 512]</code></li>
<li><code>lstm_outpus1/2</code>: ELMo中的第一层和第二层LSTM的隐状态输出，shape同样为 <code>[batch_size, max_length, 1024]</code></li>
<li><code>elmo</code>: 对应文章中的公式1，每个词的输入层(word_emb)，第一层LSTM输出，第二层LSTM输出的线性加权之后的最终的词向量，shape为<code>[batch_size, max_length, 1024]</code>，此外这个线性权重是可训练的。</li>
<li><code>default</code>: 前面得到的均为word级别的向量，这个选项给出了简单使用mean-pooling求的句子级别的向量，即将上述<code>elmo</code>的所有词取平均，方便后续下游任务。</li>
<li><code>sequence_len</code> 输入中每个句子的长度</li>
</ul>
<p>一般情况使用<code>output ['elmo']</code> 也就是Allennlp版本的<code>elmo_representations</code>， 即可得到每个词的ELMo 词向量，即可用于后续的任务，比如分类等。</p>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;2019.4.3更新：新增使用AllenNLP的ELMo做文本分类的例子说明：详见github: &lt;a href=&quot;https://github.com/ShomyLiu/pytorch_elmo_example&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;Pytorch-ELMo&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;hr&gt;
&lt;p&gt;ELMo不同于word2vec、glove，属于上下文词向量模型, 来自《Deep Contextualized Word Representations》(NAACL2018的best paper), 可以很方便用于下游NLP任务中。&lt;/p&gt;
&lt;h2 id=&quot;elmo简介&quot;&gt;ELMo简介&lt;/h2&gt;
&lt;p&gt;ELMo使用双层BiLSTM来训练语言模型，创新是线性组合不同层的word vectors， 作为最终的word representation. 核心公式:
    
    </summary>
    
      <category term="读书笔记" scheme="http://shomy.top/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="ELMo" scheme="http://shomy.top/tags/ELMo/"/>
    
      <category term="word emebdding" scheme="http://shomy.top/tags/word-emebdding/"/>
    
      <category term="pytorch" scheme="http://shomy.top/tags/pytorch/"/>
    
      <category term="tensorflow" scheme="http://shomy.top/tags/tensorflow/"/>
    
  </entry>
  
  <entry>
    <title>Factorization Machine笔记及Pytorch 实现</title>
    <link href="http://shomy.top/2018/12/31/factorization-machine/"/>
    <id>http://shomy.top/2018/12/31/factorization-machine/</id>
    <published>2018-12-31T08:26:08.000Z</published>
    <updated>2019-01-01T06:25:52.970Z</updated>
    
    <content type="html"><![CDATA[<h2 id="简介">简介</h2>
<p>Factorization Machine(因子分解机)是Steffen Rendle在2010年提出的一种机器学习算法，可以用来做任意实数值向量的预测。对比SVM，基本的优势有：</p>
<ul>
<li>非常适用与稀疏的数据，尤其在推荐系统中。</li>
<li>线性复杂度，在large scale数据里面效率高</li>
<li>适用于任何的实数向量的预测任务，包括:
<ul>
<li>回归</li>
<li>分类</li>
<li>排序</li>
</ul></li>
</ul>
<a id="more"></a>
<p>目前推荐系统中，很多深度学习模型最后都会接一个FM来做评分的预测，或者情感分类等。网上关于FM的讲解资料很多，这里简单记录下。 首先FM的形式如下(后续黑体表示向量，普通字体为实数值)： <span class="math display">\[y(\mathbf{x}) = w_0 + \sum_{i=0}^{n}w_ix_i + \sum_{i=1}^{n}\sum_{j=i+1}^{n}&lt;\mathbf{v_i}, \mathbf{v_j}&gt;x_ix_j\]</span> 其中前两项为基本的线性回归，后面一项为向量的交互项，参数为：</p>
<ul>
<li><span class="math inline">\(\mathbf{x} \in R^{n}\)</span> 为一个输入的向量，维度为<span class="math inline">\(n\)</span></li>
<li><span class="math inline">\(\mathbf{w}\)</span>为线性回归的参数</li>
<li><span class="math inline">\(\mathbf{V} \in R^{n \times k}\)</span>为交互矩阵, k是factor 维度，属于超参数。 相对于为输入<span class="math inline">\(\mathbf{x}\)</span>的每一个维度定义一个特征向量。</li>
<li><span class="math inline">\(&lt;\mathbf{v}_i, \mathbf{v}_j&gt; = \sum_{f=1}^k v_{i,f} \cdot v_{j,f}\)</span> ，向量内积表示交互。</li>
</ul>
<p>相比与SVM, 上述的模型完全是线性复杂度，并且由于对输入每个维度额外定义了一个因子向量<span class="math inline">\(\mathbf{v_i}\)</span>，能够赋予更多的特征信息。 FM可以用于不同任务：</p>
<ul>
<li>回归任务：直接以输出<span class="math inline">\(y\)</span>作为回归值即可</li>
<li>分类任务：在<span class="math inline">\(y\)</span>上接一个<span class="math inline">\(sigmoid\)</span>，即：y = <span class="math inline">\(\frac{1}{1+e^{-y}}\)</span></li>
<li>排序任务: 将<span class="math inline">\(y\)</span>作为输入<span class="math inline">\(x\)</span>的score.</li>
</ul>
<h2 id="实现">实现</h2>
<p>如果直接按照公式来实现的话，第三项的复杂度为<span class="math inline">\(O(kn^2)\)</span>，不过论文中通过因子变化，可以优化到<span class="math inline">\(O(kn)\)</span>，具体推导如下，类似于<span class="math inline">\(2ab = (a+b)^2 - a^2 - b^2\)</span>： <img src="http://cdn.htliu.cn/blog/factorization_machine/fm1.jpg"> 这样复杂度即可降到<span class="math inline">\(O(kn)\)</span>, 转换为矩阵乘法即可。 pytorch的实现如下： <figure class="highlight ruby"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div></pre></td><td class="code"><pre><div class="line">import torch</div><div class="line"><span class="class"><span class="keyword">class</span> <span class="title">FM_Layer</span>(<span class="title">nn</span>.<span class="title">Module</span>):</span></div><div class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(<span class="keyword">self</span>, n=<span class="number">10</span>, k=<span class="number">5</span>)</span></span><span class="symbol">:</span></div><div class="line">        <span class="keyword">super</span>(FM_Layer, <span class="keyword">self</span>).__init_<span class="number">_</span>()</div><div class="line">        <span class="keyword">self</span>.n = n</div><div class="line">        <span class="keyword">self</span>.k = k</div><div class="line">        <span class="keyword">self</span>.linear = nn.Linear(<span class="keyword">self</span>.n, <span class="number">1</span>)   <span class="comment"># 前两项线性层</span></div><div class="line">        <span class="keyword">self</span>.V = nn.Parameter(torch.randn(<span class="keyword">self</span>.n, <span class="keyword">self</span>.k))   <span class="comment"># 交互矩阵</span></div><div class="line">    <span class="function"><span class="keyword">def</span> <span class="title">fm_layer</span><span class="params">(<span class="keyword">self</span>, x)</span></span><span class="symbol">:</span></div><div class="line">        linear_part = <span class="keyword">self</span>.linear(x)</div><div class="line">        interaction_part_1 = torch.mm(x, <span class="keyword">self</span>.V)</div><div class="line">        interaction_part_1 = torch.pow(interaction_part_1, <span class="number">2</span>)</div><div class="line">        interaction_part_2 = torch.mm(torch.pow(x, <span class="number">2</span>), torch.pow(<span class="keyword">self</span>.V, <span class="number">2</span>))</div><div class="line">        output = linear_part + torch.sum(<span class="number">0</span>.<span class="number">5</span> * interaction_part_2 - interaction_part_1)</div><div class="line">        <span class="keyword">return</span> output</div><div class="line">    <span class="function"><span class="keyword">def</span> <span class="title">forward</span><span class="params">(<span class="keyword">self</span>, x)</span></span><span class="symbol">:</span></div><div class="line">        <span class="keyword">return</span> <span class="keyword">self</span>.fm_layer(x)</div><div class="line"></div><div class="line">fm = FM_Layer(<span class="number">10</span>, <span class="number">5</span>)</div><div class="line">x = torch.randn(<span class="number">1</span>, <span class="number">10</span>)</div><div class="line">output = fm(x)</div></pre></td></tr></table></figure></p>
<p>参考:</p>
<ul>
<li>Rendle S. Factorization machines[C]//Data Mining (ICDM), 2010 IEEE 10th International Conference on. IEEE, 2010: 995-1000.</li>
<li>https://github.com/vanzytay/KDD2018_MPCN/blob/master/tylib/lib/compose_op.py</li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;简介&quot;&gt;简介&lt;/h2&gt;
&lt;p&gt;Factorization Machine(因子分解机)是Steffen Rendle在2010年提出的一种机器学习算法，可以用来做任意实数值向量的预测。对比SVM，基本的优势有：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;非常适用与稀疏的数据，尤其在推荐系统中。&lt;/li&gt;
&lt;li&gt;线性复杂度，在large scale数据里面效率高&lt;/li&gt;
&lt;li&gt;适用于任何的实数向量的预测任务，包括:
&lt;ul&gt;
&lt;li&gt;回归&lt;/li&gt;
&lt;li&gt;分类&lt;/li&gt;
&lt;li&gt;排序&lt;/li&gt;
&lt;/ul&gt;&lt;/li&gt;
&lt;/ul&gt;
    
    </summary>
    
      <category term="读书笔记" scheme="http://shomy.top/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="factorization machine" scheme="http://shomy.top/tags/factorization-machine/"/>
    
  </entry>
  
  <entry>
    <title>docker搭建neo4j与elasticsearch数据同步</title>
    <link href="http://shomy.top/2018/10/29/docker-neo4j-es/"/>
    <id>http://shomy.top/2018/10/29/docker-neo4j-es/</id>
    <published>2018-10-29T15:13:28.000Z</published>
    <updated>2018-11-01T08:26:35.744Z</updated>
    
    <content type="html"><![CDATA[<h2 id="需求">需求</h2>
<p>现在有图数据，包含节点和边，节点或者关系都有大段的文本属性，并且需要对全文文本进行检索，因此计划使用Neo4j 进行关系存储以及图算法的应用，使用Elastic Search 进行全文搜索，因此需要Neo4j的数据与Elastic Search进行自动同步，从而实现节点/关系搜索使用Neo4j, 全文搜索使用Elastic Search, 各发挥自己的长处。</p>
<h2 id="方案">方案</h2>
<p>正巧的Neo4j官方提供了一个插件<a href="https://github.com/neo4j-contrib/neo4j-elasticsearch" target="_blank" rel="external">neo-elasticsearch</a>, 可以直接Neo4j的数据实时同步到ElasticSearch中。 不过由于官网文档的说明比较简单，以及本人对docker的熟练度有限，在使用docker搭建的过程中，遇到了很大的坑,记录于此。</p>
<a id="more"></a>
<p>环境如下:</p>
<ul>
<li>Ubuntu 16.04LST</li>
<li>Docker 18.06.0-ce</li>
<li>Neo4j:3.4</li>
<li>ElasticSearch:6.4.2</li>
</ul>
<h3 id="安装elasticsearch">安装ElasticSearch</h3>
<p>ES的安装比较简单，直接按照<a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/docker.html" target="_blank" rel="external">官网说明</a> 即可（2018.10当前版本号6.4.2）。 <figure class="highlight stylus"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">docker run -<span class="selector-tag">p</span> <span class="number">9200</span>:<span class="number">9200</span> -<span class="selector-tag">p</span> <span class="number">9300</span>:<span class="number">9300</span> --name es  \</div><div class="line">-d -e <span class="string">"discovery.type=single-node"</span> docker<span class="selector-class">.elastic</span><span class="selector-class">.co</span>/elasticsearch/elasticsearch:<span class="number">6.4</span>.<span class="number">2</span></div></pre></td></tr></table></figure></p>
<p>这样我们新建了一个名称为<code>es</code>，web端口为9200，在后台运行的的elastic search容器，可以访问<code>http://ip:9200</code>查看</p>
<h3 id="安装neo4j及neo-elasticsearch插件">安装Neo4j及neo-elasticsearch插件</h3>
<p>前面一篇博客<a href="http://shomy.top/2018/06/08/neo4j-start/">Neo4j初探</a> 已经提到了使用Docker安装Neo4j的启动失败的大坑，这里就不再赘述。因为在后续安装插件的时候，需要修改配置文件和拷贝插件，所以在新建neo4j容器的时候暴露出来conf和plugins目录，具体的命令如下: <figure class="highlight crystal"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">docker run -p <span class="number">7474</span>:<span class="number">7474</span> -p <span class="number">7687</span>:<span class="number">7687</span> -v $HOME/neo4j/<span class="symbol">data:</span>/data   \ </div><div class="line">-v $HOME/neo4j/<span class="symbol">conf:</span>/var/<span class="class"><span class="keyword">lib</span>/<span class="title">neo4j</span>/<span class="title">conf</span>  \</span></div><div class="line">-v $HOME/neo4j/<span class="symbol">plugins:</span>/var/<span class="class"><span class="keyword">lib</span>/<span class="title">neo4j</span>/<span class="title">plugins</span> \</span></div><div class="line">--name neo4j  --link <span class="symbol">es:</span>es -d  <span class="symbol">neo4j:</span><span class="number">3.4</span></div></pre></td></tr></table></figure></p>
<p>其中 <code>--link=es:es</code> 表示neo4j这个容器依赖于es容器，可以使用<code>docker ps</code> 以及<code>docker logs -f -t neo4j</code> 查看运行状态和日志。 下面开始安装插件:</p>
<ul>
<li>下载插件jar包: https://github.com/neo4j-contrib/neo4j-elasticsearch/releases 然后将jar包拷到<code>plugins</code> 目录里面</li>
<li><p>修改配置文件（假设在Neo4j中有两类节点Person与Place）: <figure class="highlight groovy"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">elasticsearch.host_name=<span class="string">http:</span><span class="comment">//es:9200</span></div><div class="line">elasticsearch.index_spec=<span class="string">people:</span>Person(first_name,last_name), <span class="string">places:</span>Place(name)</div></pre></td></tr></table></figure></p></li>
<li><p>重启neo4j即可 <code>docker restart neo4j</code>，可以通过日志来验证是否载入了 <code>docker exec neo4j cat /var/lib/neo4j/logs/debug.log | grep elastic</code> <figure class="highlight stylus"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">    <span class="number">396</span>:<span class="number">2018</span>-<span class="number">10</span>-<span class="number">29</span> <span class="number">11</span>:<span class="number">58</span>:<span class="number">13.988</span>+<span class="number">0000</span> INFO [o<span class="selector-class">.n</span><span class="selector-class">.k</span><span class="selector-class">.i</span><span class="selector-class">.DiagnosticsManager</span>] elasticsearch.index_spec=people:Person(first_name,last_name),                places:Place(name)</div><div class="line"><span class="number">403</span>:<span class="number">2018</span>-<span class="number">10</span>-<span class="number">29</span> <span class="number">11</span>:<span class="number">58</span>:<span class="number">13.989</span>+<span class="number">0000</span> INFO [o<span class="selector-class">.n</span><span class="selector-class">.k</span><span class="selector-class">.i</span><span class="selector-class">.DiagnosticsManager</span>] elasticsearch.host_name=http:<span class="comment">//es:9200</span></div><div class="line"><span class="number">530</span>:<span class="number">2018</span>-<span class="number">10</span>-<span class="number">29</span> <span class="number">11</span>:<span class="number">58</span>:<span class="number">14.023</span>+<span class="number">0000</span> INFO [o<span class="selector-class">.n</span><span class="selector-class">.k</span><span class="selector-class">.i</span><span class="selector-class">.DiagnosticsManager</span>] [classpath + loader.<span class="number">0</span>] file:/var/lib/neo4j/plugins/neo4j-elasticsearch-<span class="number">3.4</span>.<span class="number">5</span>.jar</div></pre></td></tr></table></figure></p></li>
</ul>
<p>这里面有一点很关键的是host_name, 因为官方的文档是直接在主机搭建因此直接是<code>localhost</code>， 但是在docker中两个容器是独立的ip，独立的服务，这里需要讲host_name改成上面创建elastic search容器的名称即<code>es</code>， 这是因为在docker中，如果使用了<code>--link</code>的话，docker会给当前容器内部写入一个连接容器的名字的环境变量。通过<code>docker inspect</code>查看两个容器的ip地址，假设neo4j的ip为<code>172.17.0.3</code>, es的ip为<code>172.17.0.2</code>，这样docker会给neo4j注册一个名称为es，指向es容器的环境变量，因此需要这样填写：<code>elasticsearch.host_name=http://es:9200</code>。</p>
<p>此外，<code>elasticsearch.index_spec=people:Person(first_name,last_name), places:Place(name)</code> 这条命令表示将neo4j中的Person节点存为es的people索引，Place节点存为places索引。</p>
<h2 id="测试">测试</h2>
<p>首先使用neo4j新建节点： <figure class="highlight css"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="selector-tag">CREATE</span> (<span class="selector-tag">n</span><span class="selector-pseudo">:Person</span> &#123; <span class="attribute">first_name </span>: <span class="string">"A"</span>, last_name: <span class="string">"Han"</span> &#125;) <span class="selector-tag">RETURN</span> <span class="selector-tag">n</span>;</div></pre></td></tr></table></figure></p>
<p>执行了之后，查看es中是否存在新插入的节点: http://ip:9200/people/_search?pretty=<br>
当然修改，删除等操作都可以来实时同步到es。比较方便</p>
<p>当然也可以使用<code>docker-compose</code>来同时集成两个容器, 附<code>docker-compose.yml</code>:</p>
<figure class="highlight crystal"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div></pre></td><td class="code"><pre><div class="line"><span class="symbol">version:</span> <span class="string">'2'</span></div><div class="line"><span class="symbol">services:</span></div><div class="line">    <span class="symbol">es:</span></div><div class="line">        <span class="symbol">container_name:</span> es</div><div class="line">        <span class="symbol">image:</span> docker.elastic.co/elasticsearch/<span class="symbol">elasticsearch:</span><span class="number">6.4</span>.<span class="number">2</span></div><div class="line">        <span class="symbol">ports:</span></div><div class="line">            - <span class="number">9200</span>:<span class="number">9200</span></div><div class="line">            - <span class="number">9300</span>:<span class="number">9300</span></div><div class="line">        <span class="symbol">environment:</span></div><div class="line">            - <span class="string">"discovery.type=single-node"</span></div><div class="line">    <span class="symbol">neo4j:</span></div><div class="line">        <span class="symbol">image:</span> <span class="symbol">neo4j:</span><span class="number">3.4</span></div><div class="line">        <span class="symbol">container_name:</span> neo4j</div><div class="line">        <span class="symbol">volumes:</span></div><div class="line">            - $HOME/neo4j/<span class="symbol">data:</span>/data</div><div class="line">            - <span class="regexp">/$HOME/neo</span>4j/<span class="symbol">plugins:</span>/var/<span class="class"><span class="keyword">lib</span>/<span class="title">neo4j</span>/<span class="title">plugins</span></span></div><div class="line">            - <span class="regexp">/$HOME/neo</span>4j/<span class="symbol">conf:</span>/var/<span class="class"><span class="keyword">lib</span>/<span class="title">neo4j</span>/<span class="title">conf</span></span></div><div class="line">        <span class="symbol">links:</span></div><div class="line">            - es</div><div class="line">        <span class="symbol">ports:</span></div><div class="line">            - <span class="number">7474</span>:<span class="number">7474</span></div><div class="line">            - <span class="number">7687</span>:<span class="number">7687</span></div></pre></td></tr></table></figure>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;需求&quot;&gt;需求&lt;/h2&gt;
&lt;p&gt;现在有图数据，包含节点和边，节点或者关系都有大段的文本属性，并且需要对全文文本进行检索，因此计划使用Neo4j 进行关系存储以及图算法的应用，使用Elastic Search 进行全文搜索，因此需要Neo4j的数据与Elastic Search进行自动同步，从而实现节点/关系搜索使用Neo4j, 全文搜索使用Elastic Search, 各发挥自己的长处。&lt;/p&gt;
&lt;h2 id=&quot;方案&quot;&gt;方案&lt;/h2&gt;
&lt;p&gt;正巧的Neo4j官方提供了一个插件&lt;a href=&quot;https://github.com/neo4j-contrib/neo4j-elasticsearch&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;neo-elasticsearch&lt;/a&gt;, 可以直接Neo4j的数据实时同步到ElasticSearch中。 不过由于官网文档的说明比较简单，以及本人对docker的熟练度有限，在使用docker搭建的过程中，遇到了很大的坑,记录于此。&lt;/p&gt;
    
    </summary>
    
      <category term="工具使用" scheme="http://shomy.top/categories/%E5%B7%A5%E5%85%B7%E4%BD%BF%E7%94%A8/"/>
    
    
      <category term="neo4j" scheme="http://shomy.top/tags/neo4j/"/>
    
      <category term="docker" scheme="http://shomy.top/tags/docker/"/>
    
      <category term="elasticsearch" scheme="http://shomy.top/tags/elasticsearch/"/>
    
  </entry>
  
  <entry>
    <title>Ubuntu常用代理设置方法</title>
    <link href="http://shomy.top/2018/10/14/ubuntu-proxy/"/>
    <id>http://shomy.top/2018/10/14/ubuntu-proxy/</id>
    <published>2018-10-14T10:12:47.000Z</published>
    <updated>2018-12-19T13:17:30.044Z</updated>
    
    <content type="html"><![CDATA[<p>使用Tinyproxy 在一台可以连接公网的机器上搭建了代理，这样可以让内网机器可以连接通过连接该代理，访问外网资源。 不过很多常用的应用或者命令的代理方式不一致。因此总结一下:</p>
<h2 id="环境变量设置">环境变量设置</h2>
<a id="more"></a>
<p>可以直接在命令行: <code>export http_proxy=http://ip:port</code>. 或者在<code>.bashrc</code>里面加入这一行可以一直生效。 这个可能对于某些命令有效。对于sudo操作，需要在sudo之后加<code>-E</code></p>
<h2 id="apt系列">APT系列</h2>
<p>包含<code>apt update/intall/upgrade</code>等， 一般使用选项设置代理,如:</p>
<figure class="highlight cpp"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">sudo apt -o Acquire::http::proxy=<span class="string">"http://ip:port"</span> update</div></pre></td></tr></table></figure>
<p>或者使用-c 写到文件 <code>apt_proxy.conf</code>,内容:</p>
<figure class="highlight cpp"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">Acquire::http::proxy=<span class="string">"http://ip:port"</span></div></pre></td></tr></table></figure>
<p>这样就可以这样使用代理:</p>
<figure class="highlight stylus"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">sudo apt -c ~/apt_proxy<span class="selector-class">.cong</span> update</div></pre></td></tr></table></figure>
<p>不过有一些命令可能还需要单独设置:</p>
<ul>
<li><p><code>apt-key adv</code> 添加key的时候， 这时候需要用专门的代理选项 <figure class="highlight jboss-cli"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">sudo apt-key adv <span class="params">--keyserver-options</span> http-proxy=http:<span class="string">//ip</span><span class="function">:port</span>/ <span class="params">--keyserver</span> keyserver.ubuntu.com <span class="params">--recv-keys</span> XXXXX</div></pre></td></tr></table></figure></p></li>
<li><p><code>ADD-APT-REPOSITORY</code> 添加源的时候，因为一般是<code>sudo</code> 权限所以即使设置了环境变量，也不能生效，这时候需要用<code>-E</code>选项 <figure class="highlight smali"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">sudo -E<span class="built_in"> add-apt-repository </span>XXXX</div></pre></td></tr></table></figure></p>
<p>这个也属于一种通用的方式，<code>sudo -E</code> 表示使用当前用户的环境变量。</p></li>
</ul>
<h2 id="docker">Docker</h2>
<p>这个直接按照官网教程来即可: https://docs.docker.com/config/daemon/systemd/#httphttps-proxy</p>
<ul>
<li><p>创建一个服务目录：在<code>/etc/systemd/system/</code> 下: <figure class="highlight stylus"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">sudo mkdir  docker<span class="selector-class">.service</span><span class="selector-class">.d</span></div></pre></td></tr></table></figure></p></li>
<li><p>在<code>docker.service.d</code>目录下新建一个 <code>http-proxy.conf</code>的文件, 写入如下的内容 <figure class="highlight ini"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="section">[Service]</span></div><div class="line"><span class="attr">Environment</span>=<span class="string">"HTTP_PROXY=http://ip:port/"</span></div></pre></td></tr></table></figure></p></li>
<li><p>当然如果有内网的Docker仓库，也可以设置不走代理的名单，只需在后面加上即可，如: <figure class="highlight ini"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="section">[Service]</span>    </div><div class="line"><span class="attr">Environment</span>=<span class="string">"HTTP_PROXY=http://proxy.example.com:80/"</span> <span class="string">"NO_PROXY=localhost,127.0.0.1,docker-registry.somecorporation.com"</span></div></pre></td></tr></table></figure></p></li>
<li>刷新系统配置: <code>sudo systemctl daemon-relaod</code></li>
<li>重启docker <code>sudo systemctl restart docker</code></li>
<li><p>查看验证docker的配置: <code>systemctl show --property=Environment docker</code></p></li>
</ul>
<h2 id="maven代理">maven代理</h2>
<p>修改<code>setting.xml</code>文件，一般位于<code>~/.m2/setting.xml</code> 或者 maven安装目录下的<code>/conf/setting.xml</code>, 修改如下信息(去掉注释)：</p>
<figure class="highlight xml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">proxy</span>&gt;</span></div><div class="line">      <span class="tag">&lt;<span class="name">id</span>&gt;</span>optional<span class="tag">&lt;/<span class="name">id</span>&gt;</span></div><div class="line">      <span class="tag">&lt;<span class="name">active</span>&gt;</span>true<span class="tag">&lt;/<span class="name">active</span>&gt;</span></div><div class="line">      <span class="tag">&lt;<span class="name">protocol</span>&gt;</span>http<span class="tag">&lt;/<span class="name">protocol</span>&gt;</span></div><div class="line">      <span class="tag">&lt;<span class="name">username</span>&gt;</span>proxyuser<span class="tag">&lt;/<span class="name">username</span>&gt;</span></div><div class="line">      <span class="tag">&lt;<span class="name">password</span>&gt;</span>proxypass<span class="tag">&lt;/<span class="name">password</span>&gt;</span></div><div class="line">      <span class="tag">&lt;<span class="name">host</span>&gt;</span>proxy.host.net<span class="tag">&lt;/<span class="name">host</span>&gt;</span></div><div class="line">      <span class="tag">&lt;<span class="name">port</span>&gt;</span>80<span class="tag">&lt;/<span class="name">port</span>&gt;</span></div><div class="line">      <span class="tag">&lt;<span class="name">nonProxyHosts</span>&gt;</span>local.net|some.host.com<span class="tag">&lt;/<span class="name">nonProxyHosts</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">proxy</span>&gt;</span></div></pre></td></tr></table></figure>
<p>其中，上述选项有的可选，没用的直接删除即可:</p>
<h2 id="pip代理">pip代理</h2>
<p>直接使用<code>--proxy</code> 选项: <figure class="highlight css"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="selector-tag">sudo</span> <span class="selector-tag">pip</span> <span class="selector-tag">--proxy</span> 127<span class="selector-class">.0</span><span class="selector-class">.0</span><span class="selector-class">.1</span><span class="selector-pseudo">:8888</span> <span class="selector-tag">install</span> <span class="selector-tag">XXXX</span></div></pre></td></tr></table></figure></p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;使用Tinyproxy 在一台可以连接公网的机器上搭建了代理，这样可以让内网机器可以连接通过连接该代理，访问外网资源。 不过很多常用的应用或者命令的代理方式不一致。因此总结一下:&lt;/p&gt;
&lt;h2 id=&quot;环境变量设置&quot;&gt;环境变量设置&lt;/h2&gt;
    
    </summary>
    
      <category term="工具使用" scheme="http://shomy.top/categories/%E5%B7%A5%E5%85%B7%E4%BD%BF%E7%94%A8/"/>
    
    
      <category term="Ubuntu" scheme="http://shomy.top/tags/Ubuntu/"/>
    
  </entry>
  
  <entry>
    <title>pytorch关系抽取实验</title>
    <link href="http://shomy.top/2018/07/05/pytorch-relation-extraction/"/>
    <id>http://shomy.top/2018/07/05/pytorch-relation-extraction/</id>
    <published>2018-07-04T16:02:54.000Z</published>
    <updated>2018-09-29T03:52:04.760Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>接前面笔记:<a href="http://shomy.top/2018/02/28/relation-extraction/">关系抽取总结</a>中最后说的，在关系抽取基于Distant Supervision的NYT+Freebase的数据集有两个版本，目前大部分文章都是在这两份数据集上做的。通过纵向实验发现，同一个模型在不同版本数据集上的表现有不少差异，这篇笔记是基于自己使用Pytorch复现的PCNN(Zeng 2015)与PCNN+ATT(Lin 2016)的实验结果来简单对比。</p>
</blockquote>
<p>代码地址: <a href="https://github.com/ShomyLiu/pytorch-relation-extraction" target="_blank" rel="external">pytorch-relation-extraction</a>，关于实现的细节，调参，踩过的一些坑等，会记录在readme中，这里不再赘述。</p>
<a id="more"></a>
<p>个人属于入门级别, 复现代码可能有问题, 结果供参考.</p>
<h2 id="数据集">数据集</h2>
<p>首先描述一下这两份数据集：</p>
<ul>
<li><p>27类关系，Zeng2015 发布的数据集，做了一些过滤，如删除了两个实体之间距离大于40个词的句子，以及去掉了实例少的关系，相对较小，以SMALL表示：详细数据如下所示: 训练数据集:</p>
<ul>
<li>实体对: 65726; 句子数: 112941; 关系数: 27</li>
</ul>
测试数据集:
<ul>
<li>实体对: 93574; 句子数: 152416</li>
</ul></li>
<li><p>53类关系，Lin2016 发布的数据集，没有做过滤，相对较大，训练数据大概是小数据的4倍，以LARGE表示，详细如下: 训练数据集:</p>
<ul>
<li>实体对: 281270; 句子数: 522611; 关系数: 53</li>
</ul>
测试数据集:
<ul>
<li>实体对: 96678; 句子数: 172448;</li>
</ul></li>
</ul>
<p>下面分数据集分别看这两种模型的效果，目的有两个: (1) 验证自己复现的模型是否达到了论文模型的效果。(2) 比较模型在不同数据集上的效果。</p>
<p>此外如果读过两篇论文的代码的话，可以看出关于最后的Evaluation的步骤是有所差异的：</p>
<ul>
<li>在Zeng 2015 中，直接遍历所有的测试数据集的实体对，然后预测其关系，接着按照预测的概率从大到小排序，忽略那些标签为NA并且预测为NA的样例，因为这部分没有意义且数据量巨大，这样保存好precision和recall，来绘制PR曲线。</li>
<li>在Lin2016中，则是取了预测概率Top N (N=2000+) 的样例，然后同样忽略了NA的情况，然后用这部分数据来绘制PR曲线。</li>
</ul>
<p>总体来说：这两种方法很类似，因为在绘制PR曲线的时候，最后有一个截断，本质相同。</p>
<h2 id="实验结果">实验结果</h2>
<h3 id="large数据集">LARGE数据集</h3>
<p>直接上跑的结果， PR曲线: <img src="http://cdn.htliu.cn/blog/pytorch-relation-extraction/large.png"></p>
<p>其中:</p>
<ul>
<li>PCNN+ATT: Lin2016 论文中给出的结果，直接拿来用</li>
<li>PCNN+ONE+MY: 自己复现的Zeng 2015的模型</li>
<li>PCNN+ATT+MY: 自己复现的Lin 2016的模型</li>
</ul>
<p>当时看到结果的时候，很惊讶：</p>
<ul>
<li>PCNN+ATT+MY 的PR曲线略高于Lin2016的PCNN+ATT，这个结果说明复现的模型基本吻合原模型，可能因为一些框架不同，优化上的因素导致效果更好。</li>
<li>PCNN+ONE+My的 PR曲线基本与Lin 2016的PCNN+ATT 持平，有的位置还要高一些。 这个就有点不太理解了。</li>
<li>PCNN+ONE+My 与 PCNN+ATT+MY 并没有想象中的差距那么多， 这个也很有意思，说明Attention 没有起到很大的作用？</li>
</ul>
<h3 id="small数据集">SMALL数据集</h3>
<p>PR曲线的结果: <img src="http://cdn.htliu.cn/blog/pytorch-relation-extraction/small.png"></p>
<p>结果:</p>
<ul>
<li>测了很多参数，自己实现的PCNN+ONE <strong>在recall比较小的时候，达不到Zeng2015使用Theano实现的模型效果</strong>， 原因还会继续查找。在recall相对较大的时候，基本可以达到Zeng2015的效果。</li>
<li>自己实现的PCNN+ATT 在SMALL数据集上效果一般, 很差异的是, ATT与PCNN+ONE的效果差很多. 复现基本上是参考C++代码来写的,还有调参数等.</li>
</ul>
<h2 id="总结">总结</h2>
<ul>
<li>大部分模型在LARGE数据集上效果要比SMALL数据集要好，可能原因是LARGE训练数据量大，因此模型可以训练得更好。</li>
<li>从个人实现结果来看，对于减弱Distant Supervision的噪音来看，在LARGE数据集上，Attention的效果与Multi Instance(仅仅去最可能正确的instance) 基本差不多， 但是在SMALL数据集上，Attention的效果并没有Multi Instance 的好。</li>
</ul>
<p>当然这个是我个人的实验结果(另外一师弟实现的结果与此类似）， 后续如有可能的话， 会补充一下在Lin2016的C++源码上跑一下SMALL数据集，以及在Zeng2015的Theano数据集上跑一下LARGE数据集，最终的结果会更有说服力。个人复现难免会有遗漏,如果错误,烦请指出.</p>
<h2 id="参考">参考</h2>
<ul>
<li><a href="https://github.com/smilelhh/ds_pcnns" target="_blank" rel="external">PCNN+ONE Zeng 2015</a></li>
<li><a href="https://github.com/thunlp/NRE" target="_blank" rel="external">PCNN+ATT Lin 2016</a></li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;接前面笔记:&lt;a href=&quot;http://shomy.top/2018/02/28/relation-extraction/&quot;&gt;关系抽取总结&lt;/a&gt;中最后说的，在关系抽取基于Distant Supervision的NYT+Freebase的数据集有两个版本，目前大部分文章都是在这两份数据集上做的。通过纵向实验发现，同一个模型在不同版本数据集上的表现有不少差异，这篇笔记是基于自己使用Pytorch复现的PCNN(Zeng 2015)与PCNN+ATT(Lin 2016)的实验结果来简单对比。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;代码地址: &lt;a href=&quot;https://github.com/ShomyLiu/pytorch-relation-extraction&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;pytorch-relation-extraction&lt;/a&gt;，关于实现的细节，调参，踩过的一些坑等，会记录在readme中，这里不再赘述。&lt;/p&gt;
    
    </summary>
    
      <category term="读书笔记" scheme="http://shomy.top/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="pytorch" scheme="http://shomy.top/tags/pytorch/"/>
    
      <category term="relation extraction" scheme="http://shomy.top/tags/relation-extraction/"/>
    
  </entry>
  
  <entry>
    <title>Neo4j导入Aminer论文数据</title>
    <link href="http://shomy.top/2018/06/26/neo4j-aminer-import/"/>
    <id>http://shomy.top/2018/06/26/neo4j-aminer-import/</id>
    <published>2018-06-26T00:49:46.000Z</published>
    <updated>2018-12-02T07:46:29.018Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>2018.12.2更新：使用<code>neo-admin import</code> 导入全量的Aminer数据到Neo4j.</p>
</blockquote>
<a id="more"></a>
<ul>
<li>首先用<a href="https://gist.github.com/ShomyLiu/5e2eaf33c96c30347fa2851bb345b212" target="_blank" rel="external">这个脚本</a>从原始的154个txt中提取出字段，保存为<code>neo-admin import</code>所需要的格式.<a href="https://neo4j.com/docs/operations-manual/current/tools/import/" target="_blank" rel="external">格式要求</a>,</li>
<li><p>脚本会对每个txt文件，均产生parper,org,author, p-o,p-a, a-o六个csv文件，然后归类成如下目录: <figure class="highlight gams"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line"><span class="symbol">$</span> tree   | grep -v <span class="string">'csv'</span></div><div class="line"> .</div><div class="line"> ├── nodes</div><div class="line"> │   ├── author</div><div class="line"> │   ├── org</div><div class="line"> │   └── paper</div><div class="line"> └── rels</div><div class="line">     ├── author_org</div><div class="line">     ├── cite_relation</div><div class="line">     └── paper_author</div><div class="line"> </div><div class="line"> <span class="number">8</span> directories, <span class="number">930</span> <span class="keyword">files</span></div></pre></td></tr></table></figure></p></li>
<li><p>使用<code>neo-admin import</code>导入： <figure class="highlight jboss-cli"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line">bin/neo4j-admin import </div><div class="line"><span class="params">--ignore-duplicate-nodes=true</span> <span class="params">--ignore-extra-columns=true</span> \</div><div class="line"><span class="params">--ignore-missing-nodes=true</span> <span class="params">--high-io=true</span> \</div><div class="line"><span class="params">--nodes</span> <span class="string">/data/out/nodes/org/.</span>*<span class="string">.csv</span> \</div><div class="line"><span class="params">--nodes</span> <span class="string">/data/out/nodes/author/.</span>*<span class="string">.csv</span> \</div><div class="line"><span class="params">--nodes</span> <span class="string">/data/out/nodes/paper/.</span>*<span class="string">.csv</span> \</div><div class="line"><span class="params">--relationships</span> <span class="string">/data/out/rels/author_org/.</span>*<span class="string">.csv</span> \</div><div class="line"><span class="params">--relationships</span> <span class="string">/data/out/rels/cite_relation/.</span>*<span class="string">.csv</span> \</div><div class="line"><span class="params">--relationships</span> <span class="string">/data/out/rels/paper_author/.</span>*<span class="string">.csv</span></div></pre></td></tr></table></figure></p></li>
</ul>
<p>最终导入输出: <figure class="highlight dns"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">IMPORT DONE in <span class="number">1h</span> <span class="number">25m</span> <span class="number">39</span>s <span class="number">635m</span>s.</div><div class="line">Imported:</div><div class="line">  <span class="number">257487502</span> nodes</div><div class="line">  <span class="number">1054515383</span> relationships</div><div class="line">  <span class="number">1497110211</span> properties</div><div class="line">Peak memory usage: <span class="number">17</span>.<span class="number">12</span> GB</div></pre></td></tr></table></figure></p>
<p>截图: <img src="http://cdn.htliu.cn/blog/neo4j-aminer-import/neo-aminer.png"></p>
<p>机器配置:</p>
<ul>
<li>CPU:E5-2650 v4</li>
<li>内存: 128G</li>
</ul>
<hr>
<p>前面一篇介绍了Neo4j一些比较基础的用法。这一篇笔记主要记录下在导入一个大数据集Aminer中MAG论文数据所遇到的一些问题。</p>
<h2 id="数据集介绍">数据集介绍</h2>
<p>首先关于论文数据集的描述可以在 <a href="https://www.aminer.cn/open-academic-graph" target="_blank" rel="external">Aminer_Open_Academic</a>中查看，这里简单介绍一下。 有两套数据，一个是Aminer Papers, 是清华大学整理发布的论文集，MAG Papers是微软学术发布的论文数据集。 我们这里选取了MAG的部分数据集来测试导入到Neo4j中。 MAG数据集一共有9个压缩包，其中每一个压缩包里面有20个txt文件，每个txt文件中每行代表一篇论文，用json表示，<code>mid</code> 为论文唯一id，可以作为主键使用。除了论文的基本属性比如发表时间，摘要，doi号等，需要注意的两个属性为:</p>
<ul>
<li>作者列表：<code>{&quot;name&quot;:  &quot;org&quot;:}</code> 给出了作者的姓名及所在机构。</li>
<li>参考文献列表: <code>[mid]</code> 给出了引文的id号</li>
</ul>
<p>数据示例如下： <figure class="highlight clojure"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">&#123;</div><div class="line"><span class="string">"title"</span>: <span class="string">"Activation of Protein Kinase C (PKC) by 3,4-Methylenedioxymethamphetamine (MDMA) Occurs Through the Stimulation of Serotonin Receptors and Transporter"</span>, <span class="string">"lang"</span>: <span class="string">"en"</span>, <span class="string">"n_citation"</span>: <span class="number">50</span>, <span class="string">"year"</span>: <span class="number">1997</span>,</div><div class="line"><span class="string">"authors"</span>: [&#123;<span class="string">"name"</span>: <span class="string">"M.D. H.Kenneth Kramer"</span>, <span class="string">"org"</span>: <span class="string">"Department of Psychiatry, New York University, New York, New York USA"</span>&#125;, &#123;<span class="string">"name"</span>: <span class="string">"Jose Conrado Poblete"</span>, <span class="string">"org"</span>: <span class="string">"New York University Medical Center and Department of Biology, New York University, New York, New York USA"</span>&#125;...],</div><div class="line"><span class="string">"references"</span>: [<span class="string">"02a308b0-c362-41b3-94ac-8385992a77a3"</span>, <span class="string">"057af91d-eb27-449c-bc2a-ce1ba41b60c8"</span>, <span class="string">"0b97d99a-4f4e-4ed6-b0e0-594367443594"</span>, <span class="string">"0e62c328-9944-4cac-bff9-12f310c829b1"</span>],</div><div class="line"> <span class="string">"abstract"</span>: <span class="string">"..."</span>, <span class="string">"issue"</span>: <span class="string">"3"</span>, <span class="string">"page_end"</span>: <span class="string">"129"</span>, <span class="string">"publisher"</span>: <span class="string">"Nature Publishing Group"</span>, <span class="string">"url"</span>: [<span class="string">"http://www.nature.com/doifinder/10.1016/S0893-133X(97)00026-2"</span>, <span class="string">"http://cat.inist.fr/?aModele=afficheN&amp;cpsidt=2795397"</span>..], <span class="string">"doi"</span>: <span class="string">"10.1016/S0893-133X(97)00026-2"</span>, <span class="string">"id"</span>: <span class="string">"0000017a-4ca6-4860-87fa-2f8d742267dd"</span>, <span class="string">"keywords"</span>: [<span class="string">"second messenger"</span>, <span class="string">"transporter"</span>, <span class="string">"voie intraperitoneale"</span><span class="string">", "</span>fos<span class="string">": ["</span>Biology<span class="string">", "</span>Endocrinology<span class="string">"]</span></div><div class="line"><span class="string">&#125;</span></div></pre></td></tr></table></figure></p>
<p>我们的目的是将论文的数据集导入到Neo4j中来做分析，需要的节点和边类型有: <figure class="highlight haml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line"># 节点</div><div class="line">-<span class="ruby"> Paper： 论文及其字段属性</span></div><div class="line"><span class="ruby">- Author： 作者：姓名</span></div><div class="line"><span class="ruby">- Org： 组织：名字</span></div><div class="line"><span class="ruby"></span></div><div class="line"><span class="ruby"><span class="comment"># 边</span></span></div><div class="line"><span class="ruby">- 引文关系: Paper-Paper</span></div><div class="line"><span class="ruby">- 写作关系: Author-Paper</span></div><div class="line"><span class="ruby">- 隶属关系: Author-Org</span></div></pre></td></tr></table></figure></p>
<h2 id="导入neo4j">导入Neo4j</h2>
<p>这里我们作为测试,只用了一部分数据集，使用了10个txt文件的论文数据集，即1000万篇论文，数据量大概18G，时间可以接受，在尝试过几种方法之后，基本上可以20分钟左右导入完成。 下面这个遇到的问题及解决方案等记录一下。</p>
<p>最开始利用上篇博客的导入Json的方法来直接导入:(事先均建立节点Label索引) <figure class="highlight stylus"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">call apoc<span class="selector-class">.load</span><span class="selector-class">.json</span>(<span class="string">"file:/mag_papers_0.txt"</span>) yield value as paper</div><div class="line">CREATE (<span class="selector-tag">p</span>:Paper&#123;id:paper.id&#125;)</div><div class="line">set <span class="selector-tag">p</span>.lang=paper<span class="selector-class">.lang</span>,<span class="selector-tag">p</span>.doc_type=paper<span class="selector-class">.doc_type</span>,<span class="selector-tag">p</span>.doi=paper<span class="selector-class">.doi</span>,<span class="selector-tag">p</span>.isbn=paper<span class="selector-class">.isbn</span>,<span class="selector-tag">p</span>.fos=paper<span class="selector-class">.fos</span>,<span class="selector-tag">p</span>.url=paper<span class="selector-class">.url</span>,</div><div class="line"><span class="selector-tag">p</span>.abstract=paper<span class="selector-class">.abstract</span>,<span class="selector-tag">p</span>.year=paper<span class="selector-class">.year</span>,<span class="selector-tag">p</span>.publisher=paper<span class="selector-class">.publisher</span>,<span class="selector-tag">p</span>.references=paper<span class="selector-class">.references</span>,</div><div class="line"><span class="selector-tag">p</span>.keywords=paper<span class="selector-class">.keywords</span>,<span class="selector-tag">p</span>.title=paper<span class="selector-class">.title</span>,<span class="selector-tag">p</span>.pdf=paper<span class="selector-class">.pdf</span>,</div><div class="line"><span class="selector-tag">p</span>.n_citation=paper<span class="selector-class">.n_citation</span>,<span class="selector-tag">p</span>.venue=paper<span class="selector-class">.venue</span>,<span class="selector-tag">p</span>.page_start=paper<span class="selector-class">.page_start</span>,</div><div class="line"><span class="selector-tag">p</span>.page_end=paper<span class="selector-class">.page_end</span>,<span class="selector-tag">p</span>.volume=paper<span class="selector-class">.volume</span>,<span class="selector-tag">p</span>.issue=paper<span class="selector-class">.issue</span>,<span class="selector-tag">p</span>.issn=paper.issn</div></pre></td></tr></table></figure></p>
<p>注意这里没有导入Author，原因是在作者属性中，还有组织信息，但是我们仍需要建立作者和组织节点，因此我们选择单独抽取出组织和作者信息，以及边关系。 其实也可以同时建立作者与组织信息，导入如下: <figure class="highlight stylus"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line">call apoc<span class="selector-class">.load</span><span class="selector-class">.json</span>(<span class="string">"file:/mag_papers_0.txt"</span>) yield value as paper</div><div class="line">CREATE (<span class="selector-tag">p</span>:Paper&#123;id:paper.id&#125;)</div><div class="line">set <span class="selector-tag">p</span>.lang=paper<span class="selector-class">.lang</span>,<span class="selector-tag">p</span>.doc_type=paper<span class="selector-class">.doc_type</span>,<span class="selector-tag">p</span>.doi=paper<span class="selector-class">.doi</span>,<span class="selector-tag">p</span>.isbn=paper<span class="selector-class">.isbn</span>,<span class="selector-tag">p</span>.fos=paper<span class="selector-class">.fos</span>,<span class="selector-tag">p</span>.url=paper<span class="selector-class">.url</span>,</div><div class="line">...</div><div class="line">with paper</div><div class="line">UNWIND paper<span class="selector-class">.author</span> as author <span class="comment">// 展开Authorlists</span></div><div class="line">MERGE (<span class="selector-tag">a</span>:Author&#123;name:author.name&#125;  <span class="comment">// 创建作者节点</span></div><div class="line">MERGE (o:Org&#123;name: author.org&#125;  <span class="comment">// 创建组织节点</span></div><div class="line">MERGE (a)-[WORK_IN]-&gt;(o)  <span class="comment">//创建 隶属关系</span></div></pre></td></tr></table></figure></p>
<p>我们当时没有这么处理的原因是，后续的作者和机构需要Merge相对消耗时间较多，因此单独做了数据预处理。</p>
<p>然而…</p>
<p>不过即使这样， 跑着1000万的节点数据， 运行几分钟后，就会crash掉… 连不上服务器，CPU也不再并行，仅仅100%运行，初步猜想可能是因为数据集太大，一次向导入可能会超内存，因为我们使用100万的节点 是可以很快导入的。 于是换了策略，使用apoc的分批导入<code>call apoc.periodic.iterate</code>: <figure class="highlight roboconf"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line">call apoc.periodic.iterate(</div><div class="line">'</div><div class="line">call apoc.load.json("mag_papers_0.txt") yield value as paper return paper',</div><div class="line">'</div><div class="line">CREATE (p:Paper&#123;<span class="attribute">id</span>:paper<span class="variable">.id</span>&#125;)</div><div class="line">set p<span class="variable">.lang</span>=paper<span class="variable">.lang</span>,p<span class="variable">.doc_type</span>=paper<span class="variable">.doc_type</span>,p<span class="variable">.doi</span>=paper<span class="variable">.doi</span>,p<span class="variable">.isbn</span>=paper<span class="variable">.isbn</span>,p<span class="variable">.fos</span>=paper<span class="variable">.fos</span>,p<span class="variable">.url</span>=paper<span class="variable">.url</span>,</div><div class="line">....//略写</div><div class="line">',</div><div class="line"> &#123;batchSize:50000, iterateList:true, parallel:true&#125;);</div></pre></td></tr></table></figure></p>
<p>使用这种方法，结果不仅不会crash, 而且很快就导入了所有数据，大概在3分钟左右。</p>
<p>下面开始考虑导入论文的引文关系, 同样遇到了问题。 开始直接从paper.references属性中导入: <figure class="highlight css"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="selector-tag">MATCH</span> (<span class="selector-tag">n</span><span class="selector-pseudo">:Paper)</span></div><div class="line"><span class="selector-tag">UNWIND</span> <span class="selector-tag">n</span><span class="selector-class">.references</span> <span class="selector-tag">as</span> <span class="selector-tag">F</span></div><div class="line"><span class="selector-tag">MATCH</span> (<span class="selector-tag">m</span><span class="selector-pseudo">:Paper</span> &#123;<span class="attribute">id</span>: F&#125;)</div><div class="line"><span class="selector-tag">MERGE</span> (<span class="selector-tag">n</span>)<span class="selector-tag">-</span><span class="selector-attr">[r:CITES]</span><span class="selector-tag">-</span>&gt;(<span class="selector-tag">m</span>)</div></pre></td></tr></table></figure></p>
<p>同样遇到了运行一段时间就会crash，即neo4j的服务器连不上的问题，即使 使用上面的<code>apoc.periodic.iterate</code> 同样遇到了问题， 百思不得其解。</p>
<p>后来的解决方法是，单独写了脚本文件，将引文关系，作者和结构信息，及隶属关系均提取出来，直接用Python处理千万级别的文件，速度很快，最后导成csv文件，如引用关系: <figure class="highlight autohotkey"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">paper1id,</span> paper2id</div></pre></td></tr></table></figure></p>
<p>然后使用下面的导入代码: <figure class="highlight vim"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">call</span> apoc.periodic.iterate(<span class="comment">"</span></div><div class="line"><span class="keyword">call</span> apoc.load.csv(<span class="string">'paper_cite_relation.csv'</span>) yield <span class="keyword">list</span> <span class="keyword">as</span> <span class="built_in">line</span></div><div class="line">MATCH (p1:Paper&#123;id:<span class="built_in">line</span>[<span class="number">0</span>]&#125;), (p2:Paper&#123;id:<span class="built_in">line</span>[<span class="number">1</span>]&#125;) <span class="keyword">return</span> p1, p2</div><div class="line"><span class="comment">",</span></div><div class="line"><span class="comment">"</span></div><div class="line">CREATE (p1)-[:CITE]-&gt;(p2)</div><div class="line"><span class="comment">",</span></div><div class="line">&#123;batchSize:<span class="number">50000</span>, iterateLis<span class="variable">t:true</span>, paralle<span class="variable">l:true</span>&#125;)</div></pre></td></tr></table></figure></p>
<p>不过还是导入失败，大概10分钟之后，仍然会crash… 最后的解决方法是以大化小..将整个文件，大概有2000多万行，分割为8个小文件，每个大概有400万行关系。 然后使用上面的代码，即可大概2分钟-3分钟即可导入一个文件。 总的时间大概15分钟左右即可，其实这里还可以划分更大一些， 更加节省时间。</p>
<p>最后导入作者和机构信息，基本上照猫画虎即可，这里举一个<code>paper-author</code>的例子: <figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">load</span> csv <span class="keyword">with</span> headers <span class="keyword">from</span> <span class="string">"file:/p_a.csv"</span> <span class="keyword">as</span> line</div><div class="line">FIELDTERMINATOR <span class="string">","</span></div><div class="line"><span class="keyword">match</span> (a:Author&#123;<span class="keyword">name</span>:line.name&#125;), (p:Paper&#123;<span class="keyword">id</span>:line.pid&#125;)</div><div class="line"><span class="keyword">CREATE</span> (a)-[:WRITE]-&gt;(p)</div></pre></td></tr></table></figure></p>
<p>这样总体下来在十几分钟的可接受时间内，导入了1000万论文数据，最后的详细如下: <figure class="highlight dns"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">节点</div><div class="line">Paper: <span class="number">10000000</span></div><div class="line">Author: <span class="number">11233099</span></div><div class="line">Org: <span class="number">3569072</span></div><div class="line">关系</div><div class="line">Paper-Author: <span class="number">25722519</span></div><div class="line">Author-Org: <span class="number">8368895</span></div><div class="line">Paper-Paper: <span class="number">3632542</span></div></pre></td></tr></table></figure></p>
<p>这里需要注意的是关于Paper-Paper仅仅3百万，是因为有一些paper并没有出现在这1000万中，因此直接忽略了。</p>
<p>最后折腾来折腾去， 发现我们直接可以对压缩包里面的每一个txt(100万)分别导入，这样就不会crash，只是需要多次导入，不过省了额外的预处理步骤。 这就算一个折腾的记录吧。</p>
<h2 id="总结">总结</h2>
<p>初次使用Neo4j, 遇到的坑很多，还有很多问题需要解决，比如直接导入全部边会crash, 即使使用batch(猜想Neo4j图数据很吃内存)； 虽然Neo4j提供了一些批导入工具比如<code>neo4j-admin import</code>等，但是更多是针对csv数据，json的暂时还没有调查到。不过一个经验就是将大数据切成小数据来导入，这样肯定没问题。 因为是刚入门Neo4j, 很多优化细节还没掌握，可能很多方法还有优化的地方，恳请指出。</p>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;2018.12.2更新：使用&lt;code&gt;neo-admin import&lt;/code&gt; 导入全量的Aminer数据到Neo4j.&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="工具使用" scheme="http://shomy.top/categories/%E5%B7%A5%E5%85%B7%E4%BD%BF%E7%94%A8/"/>
    
    
      <category term="neo4j" scheme="http://shomy.top/tags/neo4j/"/>
    
      <category term="graph" scheme="http://shomy.top/tags/graph/"/>
    
      <category term="aminer" scheme="http://shomy.top/tags/aminer/"/>
    
  </entry>
  
  <entry>
    <title>Neo4j初探</title>
    <link href="http://shomy.top/2018/06/08/neo4j-start/"/>
    <id>http://shomy.top/2018/06/08/neo4j-start/</id>
    <published>2018-06-08T12:52:49.000Z</published>
    <updated>2018-12-02T07:47:39.010Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>2018.10.28 更新: docker 安装Neo4j的坑</p>
</blockquote>
<p>使用docker安装neo4j，官网文档有误，安装之后启动失败; 官网文档上错误的启动命令: <a id="more"></a></p>
<figure class="highlight routeros"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">docker <span class="builtin-name">run</span> \</div><div class="line">    <span class="attribute">--publish</span>=7474:7474 <span class="attribute">--publish</span>=7687:7687 \</div><div class="line">    <span class="attribute">--volume</span>=<span class="variable">$HOME</span>/neo4j/data:/data \</div><div class="line">    <span class="attribute">--volume</span>=<span class="variable">$HOME</span>/neo4j/logs:/logs \</div><div class="line">    neo4j:3.4</div></pre></td></tr></table></figure>
<p>错误信息大概是:</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line">Active database: graph.db</div><div class="line">2018-04-03 21:52:03.380+0000 ERROR Failed to <span class="keyword">start</span> Neo4j: <span class="keyword">Starting</span> Neo4j <span class="keyword">failed</span>: Component <span class="string">'org.neo4j.server.database.LifecycleManagingDatabase@2fd66ad3'</span> was successfully <span class="keyword">initialized</span>, but <span class="keyword">failed</span> <span class="keyword">to</span> start. Please see the attached cause <span class="keyword">exception</span> <span class="string">"/logs/debug.log (Permission denied)"</span>. <span class="keyword">Starting</span> Neo4j <span class="keyword">failed</span>: Component <span class="string">'org.neo4j.server.database.LifecycleManagingDatabase@2fd66ad3'</span> was successfully <span class="keyword">initialized</span>, but <span class="keyword">failed</span> <span class="keyword">to</span> start. Please see the attached cause <span class="keyword">exception</span> <span class="string">"/logs/debug.log (Permission denied)"</span>.</div><div class="line">org.neo4j.server.ServerStartupException: <span class="keyword">Starting</span> Neo4j <span class="keyword">failed</span>: Component <span class="string">'org.neo4j.server.database.LifecycleManagingDatabase@2fd66ad3'</span> was successfully <span class="keyword">initialized</span>, but <span class="keyword">failed</span> <span class="keyword">to</span> start. Please see the attached cause <span class="keyword">exception</span> <span class="string">"/logs/debug.log (Permission denied)"</span>.</div><div class="line">Caused <span class="keyword">by</span>: org.neo4j.kernel.lifecycle.LifecycleException: Component <span class="string">'org.neo4j.server.database.LifecycleManagingDatabase@2fd66ad3'</span> was successfully <span class="keyword">initialized</span>, but <span class="keyword">failed</span> <span class="keyword">to</span> start. Please see the attached cause <span class="keyword">exception</span> <span class="string">"/logs/debug.log (Permission denied)"</span>.</div><div class="line">        ... <span class="number">3</span> more</div><div class="line">Caused <span class="keyword">by</span>: java.lang.RuntimeException: java.io.FileNotFoundException: /<span class="keyword">logs</span>/debug.log (Permission denied)</div><div class="line">        ... <span class="number">5</span> more</div><div class="line">Caused <span class="keyword">by</span>: java.io.FileNotFoundException: /<span class="keyword">logs</span>/debug.log (Permission denied)</div><div class="line">        ... <span class="number">12</span> more</div><div class="line"><span class="number">2018</span><span class="number">-04</span><span class="number">-03</span> <span class="number">21</span>:<span class="number">52</span>:<span class="number">03.400</span>+<span class="number">0000</span> INFO  Neo4j <span class="keyword">Server</span> <span class="keyword">shutdown</span> initiated <span class="keyword">by</span> request</div></pre></td></tr></table></figure>
<p>原始是因为在neo4j中的<code>logs</code> 在最近的几个版本中，不能再指向<code>/</code>, 因此不能手动<code>--volumns</code>，去掉那一行即可: <figure class="highlight routeros"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">docker <span class="builtin-name">run</span> \</div><div class="line">    <span class="attribute">--publish</span>=7474:7474 <span class="attribute">--publish</span>=7687:7687 \</div><div class="line">    <span class="attribute">--volume</span>=<span class="variable">$HOME</span>/neo4j/data:/data \</div><div class="line">    neo4j:3.4</div></pre></td></tr></table></figure></p>
<p>参考: https://github.com/neo4j/docker-neo4j/issues/131</p>
<p>此外，如果修改了主机的端口，比如<code>-p 7574:7474 -p 7787:7687</code> 在主机登录的时候，需要修改一下web界面上的host那一项(bolt://ip:7787)，不然登录失败。</p>
<p>近期需要处理图数据，考察后打算使用<a href="https://neo4j.com/" target="_blank" rel="external">neo4j</a>, 相比其他一些图数据库，neo4j开源，跨平台，接口友好，文档齐全，完整支持ACID。 首先放一张网上的图片，关系型数据库与图数据库存储网络数据的差异:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/neo4j-start/neo4j-1.jpg">

</div>
<p>初次接触neo4j 踩了不少坑，这里记录一下。</p>
<p>关于如何安装Neo4j和使用web ui进行查询操作就不再赘述。</p>
<h2 id="cypher基本操作">Cypher基本操作</h2>
<p>相比关系型数据库的SQL查询语言，Neo4j的查询语言为<code>Cypher</code>，语法更加友好，更适合图数据做查询操作。首先介绍在图数据里面几个概念:</p>
<ul>
<li>节点(Node): 使用小括号表示<code>(n)</code>表示n这个节点，同时一般都会赋予节点某个标签(Label), 等同于关系书库里面的表名。比如<code>(n: Person)</code>表示n是一个<code>Person</code>类的节点，当然一个节点可以同时有多个label.</li>
<li>关系(Relation)：关系使用中括号表示<code>[r:Knows]</code>表示<code>r</code>是<code>Knows</code>这种关系。两个节点的关系用<code>--</code>表示，如果有方向的话，加个箭头即可。如<code>(a)-[r:Knowns]-&gt;(b)</code>表示节点a和b之间有r关系，其中<code>Knowns</code>为<code>r</code>的类型</li>
<li>属性(Property): 节点和关系都可以附带属性，这个也是图数据库的优势，储存属性非常方便，直接用key-value表示即可。比如<code>(n:Person{name:&quot;John&quot;})</code>表示name为John的节点n。同样关系也可以有属性:<code>[r:Knows]{year: 2018}</code>表示为r赋予一个year属性。</li>
</ul>
<p>几个常用的关键字介绍:</p>
<ul>
<li><strong>MATCH</strong>: 表示查询，是读数据库操作。比如查属于Person的节点:<code>MATCH (n:Person)</code>，查找姓名为“John”的节点:<code>MATCH (n:Person){name: &quot;John&quot;}</code>或者使用<code>where</code>语句:<code>MATCH (n:Person) WHERE n.name=&quot;John&quot;</code>。当然这里面很多语法可以使用，比如正则匹配等，这里就不再赘述了。当然在实际使用中，MATCH不能单独使用，需要结合<code>RETURN</code>。</li>
<li><strong>CREATE</strong>: 表示创建，可以新增节点，关系，索引，约束等等，是一种写操作。比如<code>CREATE (n:Person{name:&quot;Ana&quot;})</code>表示创建一个name为“Ana”的Person节点。在创建的同时可以设置属性:<code>CREATE (n:Person{name:&quot;Ana&quot;}) set n.age=20</code>。同样在某个属性上创建索引:<code>CREATE INDEX ON :Person(name)</code>，这里需要提一下，<strong>尽量所有的Label都设置索引或者UNIQUE约束</strong>，在后续的读操作比如MATCH会大大提高性能(创建索引可以在导入节点之前执行)。</li>
<li><strong>DELETE</strong>: 表示删除节点，关系等，也是写操作。一般需要结合<code>MATCH</code>匹配查询要删除的节点。<code>MATCH (n:Person) DELETE n</code>。如果在删除有关系的节点，这样删除会报错，可以先删除边<code>MATCH (n:Person)-[r:KNOWS]-&gt;() DELETE r</code>再删除节点。不过更推荐使用<code>DETACH DELETE</code>来级联删除，<code>MATCH (n:Person) DETACH DELETE n</code>可以同时删除节点及节点的关系。</li>
<li><strong>MERGE</strong>：合并节点或者关系，属于先读后写操作，相当于<code>MATCH + CREATE</code>，先检查数据库中节点/关系是否存在，如果存在的话就不再创建，反之执行CREATE。如:MERGE(a:Person{name:“John”}) on create set a.age=20 //创建节点，先检测是否存在 // 给节点a,b建立关系，如果a,b已经存在，就无需新建。 MATCH (a:Person{name:“John”}), (b:Person{name:“Ana”}) MERGE (a)-[:KNOWS]-&gt;(b)</li>
</ul>
<p>这几个只是最基本的操作，在复杂查询中，会用到诸如<code>WITH, UNWIND</code>等命令。这里不再详细描述。</p>
<p>几个注意事项:</p>
<ul>
<li>节点名称与节点Label的定义容易混乱。比如<code>CREATE (n:Person)</code>创建了一个属于Person的节点n。这里的n仅仅属于一个变量名，跟节点本身没有关系，命令执行结束，n的生命周期也就结束了，而Person则是节点本身的Label，会一直存在。</li>
<li>索引一定要建立</li>
</ul>
<p>关于Neo4j浏览器的初次使用有几个快捷键:</p>
<ul>
<li>默认单行输入，按回车执行命令</li>
<li>输入一行命令之后，按<code>SHIFT + ENTER</code>进入多行输入状态</li>
<li>在多行输入时，<code>CTRL + ENTER</code>执行命令</li>
<li><code>ESC</code>可以放大输入框至屏幕大小，复杂查询的时候，很方便。</li>
</ul>
<p>关于内存配置的几个参数<a href="https://neo4j.com/docs/operations-manual/3.4/performance/memory-configuration/" target="_blank" rel="external">内存配置</a>:</p>
<ul>
<li><code>dbms.memory.heap.initial_size</code></li>
<li><code>dbms.memory.heap.max_size</code></li>
<li><code>dbms.memory.pagecache.size</code>可以使用<code>neo4j-admin memrec</code>来根据当前数据库数据，查看推荐的内存配置</li>
</ul>
<figure class="highlight routeros"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">bin/neo4j-admin memrec <span class="attribute">--database</span>=graph.db</div></pre></td></tr></table></figure>
<h2 id="导入数据">导入数据</h2>
<p>这一部分主要记录下如何将图数据从文件中导入库，常见的格式为CSV和JSON格式。</p>
<h3 id="导入csv-格式数据">导入CSV 格式数据</h3>
<p>Neo4j内置了命令来导入CSV数据：使用方法也很简单。假设CSV格式如下:</p>
<figure class="highlight 1c"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="string">"Id"</span>,<span class="string">"Name"</span>,<span class="string">"Year"</span></div><div class="line"><span class="string">"1"</span>,<span class="string">"ABBA"</span>,<span class="string">"1992"</span></div><div class="line"><span class="string">"2"</span>,<span class="string">"Roxette"</span>,<span class="string">"1986"</span></div><div class="line"><span class="string">"3"</span>,<span class="string">"Europe"</span>,<span class="string">"1979"</span></div><div class="line"><span class="string">"4"</span>,<span class="string">"The Cardigans"</span>,<span class="string">"1992"</span></div></pre></td></tr></table></figure>
<p>直接使用如下命令导入并直接引用headers来表示属性并创建节点:</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">LOAD</span> CSV <span class="keyword">WITH</span> HEADERS <span class="keyword">FROM</span> <span class="string">'FILE:/artists.csv'</span> <span class="keyword">AS</span> line</div><div class="line"><span class="keyword">CREATE</span> (:Artist &#123; <span class="keyword">name</span>: line.Name, <span class="keyword">year</span>: toInteger(line.Year)&#125;)</div></pre></td></tr></table></figure>
<p>注意事项:</p>
<ul>
<li>分隔符默认是<code>,</code>, 可以用<code>FIELDTERMINATOR</code>自定义分隔符:LOAD CSV WITH HEADERS FROM ‘FILE:/artists.csv’ AS line FIELDTERMINATOR “;”</li>
<li>文件位置: 可以直接使用URL地址作为文件位置，如果是本地文件的话，直接使用“FILE:”表明，文件的位置是相对位置，在配置文件<code>neo4j.conf</code>中的<code>dbms.directories.import</code>参数可以指定，默认是neo4j安装目录下的<code>import</code>文件夹，将CSV文件放到该目录下即可。</li>
<li>对于大规模数据，如果一次性导入可能会超内存，此时可以用<code>PERIODIC COMMIT</code>来分批提交导入数据，默认是1000行提交一次，具体如下:</li>
</ul>
<figure class="highlight routeros"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">USING PERIODIC COMMIT 500</div><div class="line">LOAD CSV WITH HEADERS <span class="keyword">FROM</span><span class="built_in">..</span>.</div><div class="line"><span class="built_in">..</span><span class="built_in">..</span></div></pre></td></tr></table></figure>
<ul>
<li>文本内容存在<code>&quot;</code>的字段需要特殊处理</li>
</ul>
<h3 id="导入json格式数据">导入JSON格式数据</h3>
<p>图数据里面更常见的则是JSON数据， 假设数据格式如下：</p>
<figure class="highlight json"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line">[</div><div class="line">    &#123;</div><div class="line">    <span class="attr">"id"</span>:<span class="number">1</span>, <span class="attr">"friends"</span>:[<span class="number">2</span>,<span class="number">3</span>], <span class="attr">"name"</span>: <span class="string">"Bob"</span>, <span class="attr">"age"</span>: <span class="number">27</span>,</div><div class="line">    <span class="attr">"book"</span>:[&#123;<span class="attr">"name"</span>:<span class="string">"book1"</span>, <span class="attr">"year"</span>:<span class="number">2000</span>&#125;, &#123;<span class="attr">"name"</span>:<span class="string">"book3"</span>, <span class="attr">"year"</span>:<span class="number">1990</span>&#125;]</div><div class="line">    &#125;,</div><div class="line">    &#123;</div><div class="line">    <span class="attr">"id"</span>:<span class="number">2</span>, <span class="attr">"friends"</span>:[<span class="number">1</span>], <span class="attr">"name"</span>: <span class="string">"Alice"</span>, <span class="attr">"age"</span>: <span class="number">29</span>,</div><div class="line">    <span class="attr">"book"</span>:[&#123;<span class="attr">"name"</span>:<span class="string">"book1"</span>, <span class="attr">"year"</span>:<span class="number">2000</span>&#125;, &#123;<span class="attr">"name"</span>:<span class="string">"book2"</span>, <span class="attr">"year"</span>:<span class="number">1999</span>&#125;]</div><div class="line">    &#125;,</div><div class="line">    &#123;</div><div class="line">    <span class="attr">"id"</span>:<span class="number">3</span>, <span class="attr">"friends"</span>:[<span class="number">2</span>], <span class="attr">"name"</span>: <span class="string">"John"</span>, <span class="attr">"age"</span>: <span class="number">20</span>,</div><div class="line">    <span class="attr">"book"</span>:[&#123;<span class="attr">"name"</span>:<span class="string">"book3"</span>, <span class="attr">"year"</span>:<span class="number">1990</span>&#125;]</div><div class="line">    &#125;</div><div class="line">]</div></pre></td></tr></table></figure>
<p>列表中每一个map都代表一个User, 其属性有<code>id</code>,<code>name</code>,<code>age</code>; 同时<code>friends</code>字段表示朋友关系，<code>book</code>字段表示读过某本书。 现在我们需要创建 Person 和 Book两类节点，同时Person和Book 之间有READ关系。 Neo4j 并没有内置直接导入Json的函数，不过在Neo3.3版本之后，推出了一个函数存储包APOC，里面包含了非常丰富的函数和存储过程，如各种图计算算法，是Cypher的有力补充，其中就包含了从Json中导入数据。安装APOC很简单，只需要三步:</p>
<ul>
<li>从<a href="https://github.com/neo4j-contrib/neo4j-apoc-procedures/releases/" target="_blank" rel="external">github</a>中下载与Neo4j对应版本的APOCjar包</li>
<li>将jar包拷贝到neo4j安装目录的plugins目录下</li>
<li>在配置文件neo4j.conf中加入一行允许APOC导入文件:apoc.import.file.enabled=true</li>
<li>重启Neo4j即可</li>
</ul>
<p>在Neo4j浏览器中，输入<code>return apoc.version()</code>即可查看版本号</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/neo4j-start/neo4j-2.jpg">

</div>
<p>此外我们可以看到apoc支持导入非常多格式的数据：</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/neo4j-start/neo4j-3.jpg">

</div>
<p>导入方式很简单，我们要创建两类节点，首先创建索引，方便后续导入。</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">CREATE</span> <span class="keyword">INDEX</span> <span class="keyword">ON</span> :Person(<span class="keyword">id</span>)</div><div class="line"><span class="keyword">CREATE</span> <span class="keyword">INDEX</span> <span class="keyword">ON</span> :Book(<span class="keyword">name</span>)</div></pre></td></tr></table></figure>
<p>导入代码如下:</p>
<figure class="highlight stylus"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line"><span class="comment">// YIELD关键字表示每次导入json数据中的一组数据，即`[...]`中的每一个`&#123;&#125;`, 这里的person.json是系统绝对路径</span></div><div class="line">CALL apoc<span class="selector-class">.load</span><span class="selector-class">.json</span>(<span class="string">"file:///person.json"</span>) YIELD value as person</div><div class="line"><span class="comment">// 需要对book属性进行列表展开，后续建立Person和Book关系的时候，需要用。</span></div><div class="line">UNWIND person<span class="selector-class">.book</span> as book</div><div class="line"><span class="comment">// 创建Person节点</span></div><div class="line">MERGE (<span class="selector-tag">p</span>:Person&#123;id:person.id&#125;)</div><div class="line">SET <span class="selector-tag">p</span>.name=person<span class="selector-class">.name</span>, <span class="selector-tag">p</span>.age=person<span class="selector-class">.age</span>, <span class="selector-tag">p</span>.friends=person.friends</div><div class="line"><span class="comment">//创建book节点</span></div><div class="line">MERGE (<span class="selector-tag">b</span>:Book&#123;name:book.name&#125;)</div><div class="line">SET <span class="selector-tag">b</span>.year=book.year</div><div class="line"><span class="comment">//建立person-&gt;book关系</span></div><div class="line">MERGE (p)-[:READ]-&gt;(b)</div></pre></td></tr></table></figure>
<p>然后再根据已有的<code>friends</code>导入<code>Friend</code>关系:</p>
<figure class="highlight stylus"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="comment">//对每一个 person遍历</span></div><div class="line">MATCH (<span class="selector-tag">p</span>:Person)</div><div class="line"><span class="comment">// 对p的friends进行列表展开，</span></div><div class="line">UNWIND <span class="selector-tag">p</span><span class="selector-class">.friends</span> as f</div><div class="line"><span class="comment">// 根据id搜索Person节点</span></div><div class="line">MATCH (<span class="selector-tag">q</span>:Person&#123;id:f&#125;)</div><div class="line"><span class="comment">// 建立关系</span></div><div class="line">MERGE (p)-[:Friend]-(q)</div></pre></td></tr></table></figure>
<p>执行按成之后，可视化看一下 ，:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/neo4j-start/neo4j-4.jpg">

</div>
<p>到这里导入基本完成了，不过还有一点问题，暂时没有解决，使用<code>UNWIND person.book as book</code>的时候，如果某个节点没有book这个一个属性，那么后续代码将不再执行，即该Person节点不会创建。但是如果将UNWIND放到创建Person之后，建立的READ关系会有问题，还在查找原因。</p>
<h2 id="参考资源">参考资源</h2>
<ul>
<li><a href="https://neo4j.com/docs/developer-manual/current/" target="_blank" rel="external">官方文档</a></li>
<li><a href="https://blog.csdn.net/GraphWay/article/details/78957415" target="_blank" rel="external">APOC</a></li>
<li><a href="http://gitbook.cn/books/5a33782c5778440a9d906017/index.html" target="_blank" rel="external">Neo4j应用</a></li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;2018.10.28 更新: docker 安装Neo4j的坑&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;使用docker安装neo4j，官网文档有误，安装之后启动失败; 官网文档上错误的启动命令:
    
    </summary>
    
      <category term="工具使用" scheme="http://shomy.top/categories/%E5%B7%A5%E5%85%B7%E4%BD%BF%E7%94%A8/"/>
    
    
      <category term="neo4j" scheme="http://shomy.top/tags/neo4j/"/>
    
      <category term="graph" scheme="http://shomy.top/tags/graph/"/>
    
  </entry>
  
  <entry>
    <title>2017</title>
    <link href="http://shomy.top/2018/03/01/2017-review/"/>
    <id>http://shomy.top/2018/03/01/2017-review/</id>
    <published>2018-03-01T03:15:09.000Z</published>
    <updated>2018-09-29T03:52:04.716Z</updated>
    
    <content type="html"><![CDATA[<div class="figure">
<img src="http://cdn.htliu.cn/blog/2017-review/2017-1.png">

</div>
<p>翻看了一下从<a href="http://shomy.top/2017/01/28/2016-review/">2016总结</a>开始，到现在一年时间写了16篇博客，也算是对匆匆学习生活的一种留念吧。</p>
<a id="more"></a>
<h2 id="学习">学习</h2>
<blockquote>
<p>迷茫中前进</p>
</blockquote>
<p>我将自己这一年的学习分为四个阶段。</p>
<p>(一) 记得在研究生第一学期读论文很吃力，连逻辑回归都不知道是回归还是分类。读的第一篇论文十分磕绊，基本上啥也不会，仅仅靠着脑中所存在大一学的那些许高数知识看懂个符号，更别说科研方向了，甚是煎熬，并且浮躁。那会也才真正意识到基础的重要性，于是下决心花了几个月看了林轩田老师的机器学习课程(基石+绝大部分技法部分), 感觉算是半只脚入门机器学习了。</p>
<p>(二) 之后大概在4月份左右也跟老师学长明确了自己的短期的研究小方向: Network Embedding(网络表示)，正好算是复杂网络+深度学习结合的点，自己也比较感兴趣。便开始了所谓的刷论文。在开始读的时候，按部就班的从一篇论文的Abstract开始到Conclusion结束，几乎一个单词都不敢落下，生怕遗漏什么信息，读完还去看看有没有公开源代码来读读，跟同学们讨论，然后尝试着写论文笔记等等，随着阅读量的增多，对于一些文章，基本直接挑重点看了，如果是做自己感兴趣的方向的话，这个过程其实还是比较享受的。</p>
<p>(三) 但是随着论文一篇一篇的读过，总觉得别人的文章写得都很好，自己却不知道如何进行改进；有时候觉得别人的文章的思路模型很简单，自己怎么就想不到？这个过程着实十分郁闷，有点有劲使不上的感觉。后面也慢慢发现这个过程很大可能是因为知识面太狭窄，阅读宽度不够造成的。</p>
<p>(四) 在老师的点拨中，好不容易有了一点idea之后，便开始了做实验尝试，效果不好，找原因，接着换模型，接着调，这个过程是忐忑的，担心效果不好。做实验过程中最难受的是复现不出来别人的实验，这样会认为自己写的代码包括自己的模型有问题。</p>
<p>现在基本就是三四阶段循环交叉体验。 上半年上完课之后，暑假听了两个报告，一个是成都的KDD的预报告，另一个是在北京中文信息学会办的一个讲习班，不少相关内容可以似懂非懂的听个差不多，比本科那会出去听报告完全懵的状态要好不少。 这一年对我来说非常大的惊喜则是从成都回来之后，随ding老师去MSRA听一个讲座，然后非常幸运地获得了在MSRA实习的机会。在亚研院的一个学期里，见到了很多大牛，而在以前只是听过名字，在亚研院基本也是偏research，读论文做实验， 没有project，因此基本也是重复上面的三四阶段，日复一日。</p>
<p>在去年总结中设定的至少投一篇论文的目标算是达到了，不过因为有组实验效果很不好，因此仅仅是试试水而已，估摸会被拒。接下来一年会更加艰苦，仍需付出最大努力。</p>
<h2 id="生活">生活</h2>
<blockquote>
<p>时间总是越过越快，毫无踪迹的溜走。</p>
</blockquote>
<p>在学校的时候，宿舍-实验室-食堂-实验室-宿舍，在实习的时候，宿舍-公司-宿舍，一天结束。我也一时半会想不起来生活这一年给我留下过什么痕迹。或许可以从我的朋友圈，微博中淘一淘。等想起来了在更新上来。</p>
<p>今年的一个流行词： 佛系， 我觉得非常适合现在的我。佛性听歌，佛系生活，佛系学习。</p>
<p>还是想到那写到那。最后希望我的家人，朋友，健健康康，快快乐乐，被世界温柔以待，如伊敏河旁温柔的夏夜一样。</p>
<p>于2018年正月十五</p>
]]></content>
    
    <summary type="html">
    
      &lt;div class=&quot;figure&quot;&gt;
&lt;img src=&quot;http://cdn.htliu.cn/blog/2017-review/2017-1.png&quot;&gt;

&lt;/div&gt;
&lt;p&gt;翻看了一下从&lt;a href=&quot;http://shomy.top/2017/01/28/2016-review/&quot;&gt;2016总结&lt;/a&gt;开始，到现在一年时间写了16篇博客，也算是对匆匆学习生活的一种留念吧。&lt;/p&gt;
    
    </summary>
    
      <category term="个人心得" scheme="http://shomy.top/categories/%E4%B8%AA%E4%BA%BA%E5%BF%83%E5%BE%97/"/>
    
    
      <category term="2017" scheme="http://shomy.top/tags/2017/"/>
    
  </entry>
  
  <entry>
    <title>关系抽取(分类)总结</title>
    <link href="http://shomy.top/2018/02/28/relation-extraction/"/>
    <id>http://shomy.top/2018/02/28/relation-extraction/</id>
    <published>2018-02-28T03:58:30.000Z</published>
    <updated>2019-12-31T15:48:28.211Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>2018.10.14更新: 增加全监督关系抽取PCNN的复现代码: <a href="https://github.com/ShomyLiu/pytorch-pcnn" target="_blank" rel="external">PCNN</a>.</p>
</blockquote>
<blockquote>
<p>2018.07.08更新: 增加对远程监督两份数据集补充实验对比: <a href="http://shomy.top/2018/07/05/pytorch-relation-extraction/">关系抽取实验</a>.</p>
</blockquote>
<blockquote>
<p>2018.04.04更新：增加对NYT+Freebase数据集的两个版本的说明</p>
</blockquote>
<blockquote>
<p>对近几年(到2017)一些关系抽取/分类(Relation Extraction)的部分文献的一个简单总结。</p>
</blockquote>
<a id="more"></a>
<h2 id="基本介绍">基本介绍</h2>
<p><strong>基本定义</strong></p>
<ul>
<li>关系抽取: 从一个句子中判断两个entity是否有关系，一般是一个二分类问题，指定某种关系</li>
<li>关系分类: 一般是判断一个句子中 两个entity是哪种关系，属于多分类问题。</li>
</ul>
<p><strong>常用数据集</strong></p>
<ul>
<li>ACE 2005: 599 docs. 7 types;</li>
<li>SemiEval 2010 Task8 Dataset:
<ul>
<li>19 types</li>
<li>train data: 8000</li>
<li>test data: 2717</li>
</ul></li>
<li>NYT+FreeBase 通过Distant Supervised method 提取，里面会有噪音数据:
<ul>
<li>53 types</li>
<li>train data: 522611 sentences; 需要注意的是，这里面有近80%的句子的标签为NA</li>
<li>test data: 172448 sentences;</li>
</ul></li>
</ul>
<p>下面以学习方法的不同来对这些文章进行分类：</p>
<ul>
<li>Fully Supervised Learning</li>
<li>Distant Supervised Learning</li>
<li>Joint Learning with entity and relation</li>
<li>Tree Based Methods</li>
</ul>
<p>其中Fully Supervised 一般评测使用label完全准确的SemEval 2010 Task 8 数据集。 Distant Supervised 使用NYT+FreeBase数据集。 SemEval 2010 Task 8 训练数据样例: <figure class="highlight dts"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"> The <span class="params">&lt;e1&gt;</span>microphone<span class="params">&lt;/e1&gt;</span> converts sound into an electrical <span class="params">&lt;e2&gt;</span>signal<span class="params">&lt;/e2&gt;</span>.</div><div class="line">Cause-Effect(e1,e2)</div><div class="line"><span class="symbol">Comment:</span></div></pre></td></tr></table></figure></p>
<p>其中第一行为sentence，第二行为两个entity的relation，第三行为备注。 NYT + FreeBase 训练数据集: <figure class="highlight clean"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">m<span class="number">.0</span>ccvx    m<span class="number">.05</span>gf08    queens    belle_harbor    /location/location/contains    .....officials yesterday to reopen their investigation into the fatal crash <span class="keyword">of</span> a passenger jet <span class="keyword">in</span> belle_harbor , queens...... ###END###</div></pre></td></tr></table></figure></p>
<p>一共6列，前两列为两个entity的Freebase mid, 第三四列为两个entity在句子中的string。第五列为relation，最后一列为sentence（有省略），以###END###结尾</p>
<p>这两个数据集相对来说用的最广泛。关于Distant Supervision的NYT+Freebase数据集有两个版本，会在文末说明。 此外关于Joint Learning with entity and relation和Linguistics(NER, POS Tagging, Dependency Tree) Methods 部分暂时还没有关注，仅仅列在这里。</p>
<h2 id="fully-supervised-learning">Fully Supervised Learning</h2>
<h3 id="相关文献">相关文献</h3>
<p>这一部分基本是在SemEval 2010 Task 8 数据集上做关系分类的工作，是一个完全监督的任务。</p>
<p><strong>1. Simple CNN Model (Liu 2013)</strong></p>
<blockquote>
<p>Liu,(2013). Convolution neural network for relation extraction. 8347 LNAI(PART 2), 231–242.</p>
</blockquote>
<p><strong>基本介绍</strong> 这篇文章应该是第一次使用CNN来做关系分类任务的，使用的CNN结构也十分简单，甚至没有Pooling层，输入层也没有用word embedding, 而是对synonym的embedding表示作为输入。 具体的输入层的结构如下:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/1.jpg">

</div>
<p>具体步骤如下:</p>
<ul>
<li>首先构建synonym list，使用Wordnet数据集，来找那些词属于同义词，相当于利用同义词关系，聚成若干类，当然有可能会有某个词没有同义词，那他自己就是一个类别</li>
<li><p>假设Synonym list 一共有<span class="math inline">\(D_s\)</span>个类别，每个词都属于其中一类，这样就可以来对词做one-hot表示了, 如上图所示: <img src="http://cdn.htliu.cn/blog/relation-extraction/2.png"></p></li>
<li><p>使用一个Look-Up Table 转换为一个低维vector</p></li>
</ul>
<p>输入层之后是卷积网络+一个普通的全连接层以及softmax分类，如下: <img src="http://cdn.htliu.cn/blog/relation-extraction/3.jpg"></p>
<p><strong>实验</strong></p>
<p>数据集： ACE 2005数据集 这里需要说明的是， 这篇文章除了使用前面说的Synonym的特征之外，还用了该数据集提供的entity type/subtype 以及后续做了POS tagger 的特征，都是先使用one-hot表示，然后用LookUP Table 再转换为低维向量，然后所有的feature vector concat成最终的向量，作为CNN层的输入。 在大类Type上的实验结果，相对于几个Basedline 提升还是比较明显的：</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/4.png">

</div>
<p><strong>总结</strong></p>
<p>优点:</p>
<ul>
<li>引入CNN 结构来做关系抽取，虽然本质上属于文本分类，但是属于一次尝试，效果还可以。</li>
<li>使用Synonym Embedding作为词的特征，这一点后续一部分工作也在使用，相当于引入额外的信息。</li>
</ul>
<p>缺点:</p>
<ul>
<li>CNN的结构比较简单, 没有Pooling层，可能受噪音比较明显，因为一个句子里面影响两个词的relation的可能只有几个词，一些可能会产生噪音。</li>
<li>仍然使用了一些Linguistic Feature比如POS Tagger, NER 等，并没有完全做到end-to-end的关系抽取</li>
<li>使用Synonym Embedding(随机初始化的LookUp Table) 可以引入一部分额外信息，但是却完全忽略了word embedding的语义信息，这一块在后续工作中都会加入pre-train的word embedding.</li>
</ul>
<p><strong>2. CNN with Max Pooling and word embedding(Zeng 2014)</strong></p>
<blockquote>
<p>Zeng, . (2014). Relation Classification via Convolutional Deep Neural Network. Coling, 2335–2344</p>
</blockquote>
<p>这篇是中科院刘康老师的一篇文章，这篇文章使用了比较经典的CNN的结构，包含了Pooling层，以及设计了Position Features，后面会具体介绍。 先看整体架构:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/5.jpg">

</div>
<ul>
<li>第一步同样是Embedding Layer, 这篇文章用了Pre-Train的词向量，而不是像上一篇的随机初始化，不过用的并不是word2vec，而是(Turian 2010ACL Word representations: a simple and general method for semi-supervised learning.)提供的word embedding.</li>
<li>之后文中设计了两种Features: Lexical Level Feature 就是词法级别，以及sentence-level(CNN) 级别的特征，后面详细介绍。</li>
<li>将两种Feature 直接 concat 输入到一个全连接层+Softmax 来对关系做分类</li>
</ul>
<p>先介绍相对简单的Lexical-Feature，一共有5部分:</p>
<ul>
<li>L1: entity1</li>
<li>L2: entity2</li>
<li>L3: entity1的左右两个tokens</li>
<li>L4: entity2的左右两个tokens</li>
<li>L5: WordNet中两个entity的上位词</li>
</ul>
<p>举个例子，比如说:</p>
<p>The [haft] of the [axe] is made of yew wood.</p>
<ul>
<li>L1: entity1: <em>haft</em></li>
<li>L2: entity2: <em>axe</em></li>
<li>L3: entity1’s context: <em>the</em>, <em>of</em></li>
<li>L4: entity2’s context: <em>the</em>, <em>is</em></li>
<li>L5: 在wordNet中找到两个entity的上位词。上位词就是包含关系，比如<em>parrot</em>这个词的上位词就是<em>bird</em>. 这种包含关系其实类似于指明了实体的类型特征。</li>
</ul>
<p>这5个特征都是lexical-level的，之后在使用它们的word embedding串联起来 作为最后的lexical-feature.</p>
<p>下面介绍使用CNN的sentence-level feature, 跟其他文章中使用CNN对句子建模的结构很类似, 先直接放框架图:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/6.jpg">

</div>
<p>在输入层每个word 有两种特征:</p>
<ul>
<li>WF(word feature): 很简单就是词向量 word embedding</li>
<li>PF(position feature): 位置特征， 每个word与两个entity的相对距离，举个例子如下 <been> 这个单词距离entity1 <people>为2，距离entity2 <downtown>为-4: <img src="http://cdn.htliu.cn/blog/relation-extraction/7.jpg"> 这样每个word 就有了两个位置特征[d1,d2]，这两个特征也需要再额外使用Lookup-Table 做Embedding，投影成低维向量，也就是每个相对距离，都对应到一个低维实数向量。 这个Position Feature 的引入其实是突出了两个entity的作用，实验证明PF对效果的提升很明显。</downtown></people></been></li>
</ul>
<p>这样每个word的feature维度为: <span class="math inline">\(d_w + d_p * 2\)</span>，其中<span class="math inline">\(d_w\)</span> 为word embedding的维度，<span class="math inline">\(d_p\)</span>为 position embedding的维度。</p>
<p>介绍完输入之后，接下来就是卷积操作，这里不多介绍。与上一篇不一样的地方是，这里在卷积之后使用了Max Pooling操作，这样可以提取每一个卷积核的最有用的特征，在之后又接了一个全连接层最终得到了sentence-level feature。有一点需要说明的是，MAXPooling直接对卷积操作之后的向量进行的，并没有经过激活函数。</p>
<p>最终将上面的lexical-feature 与 sentence-level feature 直接串起来，作为整个句子的特征去做分类，如结构图中所示，加一个全连接层和softmax 即可。</p>
<p>模型需要学习参数有: - word embedding - position embedding - 卷积核权重矩阵 - sentence-level 最后的全连接层参数矩阵 - 用于softmax的全连接层参数矩阵</p>
<p><strong>实验</strong></p>
<p>数据集: 使用SemEval 2010 Task 8 Dataset，考虑方向共19种关系。 整体的实验结果如下:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/8.png">

</div>
<p>从结果看，没有使用linguistic的一些特征，基本end-to-end, 取得了state-of-art的效果。 再看看单独特征的影响:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/9.png">

</div>
<p>可以看出来，L5(word net)和PF两个特征比较关键。</p>
<p><strong>总结</strong></p>
<p>优点:</p>
<ul>
<li>使用Pooling 操作，提取了每一个卷积核对应的最优特征</li>
<li>引入Position Feature: 相当于在一定程度上引入了位置信息，因为CNN更多是局部N-Gram特征; PF突出了entity的影响，包括后续的PieceWise Max Pooling，同样也是对entity与其他词分开考虑。</li>
</ul>
<p>缺点:</p>
<ul>
<li>虽然使用了多个卷积核，但是仅仅使用同一个window-size, 提取特征比较单一。</li>
<li>结构还是相对简单，更多的是类似于CNN 文本分类的结构。 而且还是加入了人为构造的Lexical Feature。</li>
</ul>
<p>有一点疑问的是 Lexical中的两个entity 的embedding 其实也属于对entity的强调，作用跟Position Feature 感觉有点类似，都是为了突出是对句子中的那两个词做关系分类，或者说都是为了强调距离target entity 越近，影响越大。但是PF的效果要比L1-L4好。</p>
<p><strong>3. CNN with multi-sized window kernels(Nguyen 2015)</strong></p>
<blockquote>
<p>Nguyen, (2015). Relation Extraction: Perspective from Convolutional Neural Networks.</p>
</blockquote>
<p>这一篇文章，在去年zeng的基础上加入了多尺寸卷积核，这样可以提取更多的N-Gram特征；同时完全不再使用zeng的lexical-level feature，完全使用sentence的特征，完整的结构如下: <img src="http://cdn.htliu.cn/blog/relation-extraction/10.png"></p>
<p>从结构可以看出来，基本与上一篇zeng的sentence-level 部分的CNN很类似，不同的地方有： - 卷积阶段使用<strong>多个尺寸的卷积核</strong> - 卷积之后使用了激活函数</p>
<p><strong>实验</strong></p>
<p>数据集：文中使用了ACE 2005 和 SemEval2010 Task 8 两个数据集。 对ACE 2005数据集，做了窗口尺寸不同以及word embedding是否固定的实验分析，结果如下:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/11.png">

</div>
<p>从实验结果F1-Score值可以看出来，多窗口尺寸的确有效果提升，而且最好的效果则是在word embedding 不固定，需要参与训练的情况下，卷积核窗口为2,3,4,5。</p>
<p>在SemEval2010 数据集的结果达到了state-of-art，比zeng的效果好0.1% 但是是不加任何的lexical-feature的情况下:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/12.png">

</div>
<p><strong>总结 </strong></p>
<p>这篇文章改进相对较少， 不过完全不再使用lexical-feature，起到了不错的效果。 优点:</p>
<ul>
<li>使用多个卷积核，提取文本中更多的卷积特征</li>
<li>完全不再使用词法特征，包括wordnet, 词的上下文等。</li>
</ul>
<p>缺点:</p>
<ul>
<li>仅仅使用CNN的传统结构，因此效果提升不是特别明显</li>
</ul>
<p><strong>4. CNN with Rank Loss(Santos 2015)</strong></p>
<blockquote>
<p>Santos(2015). Classifying Relations by Ranking with Convolutional Neural Networks. ACL</p>
</blockquote>
<p>这篇文章同样是在Zeng 2014的CNN基础上做的改进，最大的变化是损失函数，不再使用softmax+cross-entropy的方式，而是margin based的ranking-loss。下面介绍一下细节。 先看整体架构，跟之前的区别很小:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/13.jpg">

</div>
<ul>
<li>输入层: 利用word embedding + position embedding,同zeng 2014</li>
<li>卷积层: 固定尺寸的卷积核(window-size=3)</li>
<li>Pooling层: 直接Max Pooling得到 <span class="math inline">\(r_x\)</span></li>
<li>全连接层: 得到每个类别的score，其中<span class="math inline">\(W^{classes}\)</span>的每一列可以看成 label 的embedding。因此某个label为c的score即为: <span class="math inline">\(s_{\theta}(x)_c=r_x^{T}[W]_c\)</span></li>
</ul>
<p>图中pooling之后得到的<span class="math inline">\(r_x\)</span> 即作为sentence representation。这一部分跟之前的工作都一样，下面开始引入了margin-based ranking loss, 先看loss function:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/14.png">

</div>
<p>在训练过程中的输入有三部分: 当前的句子 x , 该sen的正确标签<span class="math inline">\(y^{+}\)</span>, 以及采样得到的错误的标签<span class="math inline">\(c^{-}\)</span>。<span class="math inline">\(m^{+}, m^{-}\)</span> 对应两个大于0的margin, <span class="math inline">\(\gamma\)</span>起到了一个缩放的作用。 这样最小化<span class="math inline">\(L\)</span>就要求正样本对应的label得分<span class="math inline">\(s_{\theta}(x)_{ {y}_{+} }\)</span>越大越好，负样本对应的得分<span class="math inline">\(s_{\theta}(x)_{ {c}_{-} }\)</span>越小越好。同时margin的存在使得模型尽可能让正样本的得分大于<span class="math inline">\(m^{+}\)</span> ， 以及负样本的得分小于<span class="math inline">\(-m^{-}\)</span>。这样可以讲正负样本分的更加清楚。</p>
<p>几个Tips:</p>
<ul>
<li>负样本的选择上， 并不是随机选择一个负标签，而是选择score最大的那个负标签，这样可以更好地将比较类似的两种label分开。</li>
<li>关于NA label的特殊处理，NA表示两个entity没有任何关系，属于噪音数据，因此如果将这个噪音类别与其他有意义的类别同等看待的话，会影响模型的性能. 因此文中对NA类做了特殊处理。在train的时候，不再考虑NA这一类别， 对于NA的训练数据，直接让(1)式的第一部分为0即可。 在predict的部分， 如果其他类别的score都是负数，那么就分类为NA。实验证明这个效果对整体的performance有提升。</li>
</ul>
<p><strong>实验</strong></p>
<p>数据集: SemEval2010 Task 8 首先看主要实验结果, 再不使用任何lexical-feature的情况下，达到了start-of-art的效果, F1-Score: 84.1%:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/15.png">

</div>
<p>再看三组对比实验: - 损失函数: Ranking Loss 以及 Softmax-Entropy. <img src="http://cdn.htliu.cn/blog/relation-extraction/16.png"> 上面的CNN+Softmax 是文中复现的Zeng 2014的代码，比原文中要好，可能原因是本文使用了300维的word embedding， 而Zeng使用50维. 从前两行可以看出来，使用Ranking的损失函数，Precision, Recall, F1 都有提升。 - 对NA关系的特殊处理的效果: <img src="http://cdn.htliu.cn/blog/relation-extraction/17.png"></p>
<pre><code>从结果中可以看出来，训练过程中不考虑NA的话，整体的performance有提升。 但是对于NA这一个单独的类的Precision有所下降，也就是说有更多的其他的类分到了NA，导致P降低，不过反过来看其他类的P,R,F1的值都提高了，说明增加的这部分分到NA类的那些case即使在考虑NA的情况下，也是Bad Case.</code></pre>
<ul>
<li><p>使用两个entity之间的word embedding 来代替position feature: <img src="http://cdn.htliu.cn/blog/relation-extraction/18.png"></p>
<p>其中Full sentence 表示 使用完整的句子， Word Position表示使用Zeng 2014提出的Positon Embedding。从第一列和第四列的对比可以看出来，在使用Full Sentence的情况下，Postion 的作用很明显，可以差出10%。不过从第二列，第三列对比可以看出来，如果仅仅使用两个entity之间的words，position的影响就会小很多，因为这种情况下，就可以一定程度上指代两个entity的位置。有一个比较有趣的现象，第三列仅仅使用两个entity之间的word, 就可以达到82.8%的F1，已经可以跟Zeng2014 中，使用各种lexical-feature+position feature达到相同的效果。可能的原因是长文本的噪音比较大。</p></li>
</ul>
<p><strong>总结</strong></p>
<p>这篇文章相对Zeng 2014的文章，改进的地方或者一些尝试很值得思考。而且效果很不错，很多地方可以借鉴，尤其ranking loss这一块。</p>
<p>优点:</p>
<ul>
<li>使用Ranking loss，效果提升2%多，而且在没有使用lexical-feature以及单窗口尺寸的情况下。 有提升的原因可能是使用ranking loss可以更容易区分开一些易于分错的类别，而softmax却没有这样的功能，只可以增强正确类别的概率。</li>
<li>使用仅仅两个entity之间的words 可以在一定程度来替代position的作用，而且实现更简单。</li>
</ul>
<p>缺点:</p>
<ul>
<li>同样仍然在简单CNN的基础上做的修改. 后续相关文章加了各种attention.</li>
</ul>
<p><strong>5. RNN (Zhang 2015)</strong></p>
<blockquote>
<p>Zhang (2015). Relation classification via recurrent neural network.</p>
</blockquote>
<p>这篇文章不再使用CNN作为基本结构，而是开始尝试RNN，可以达到Zeng 2014 的效果，基本结构如下:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/rnn-1.jpg">

</div>
<p>大部分结构相同，有以下几点改进:</p>
<ul>
<li>使用双向RNN来处理，捕获更多的信息。</li>
<li>使用更加简单的Position Indicators(简称PI)，而非Zeng 2014的Position Feature. PI很简单，直接使用<e>标签来表示两个entity的位置. 比如在数据中例子: <em>“<e1> people </e1> have been mov- ing back into <e2> downtown </e2>”</em> 这样就将 <code>&lt;e1&gt;, &lt;\e1&gt;, &lt;e2&gt;, &lt;\e2&gt;</code> 作为四个Indicators. 在训练的时候，直接将这四个标签作为普通的word即可，无需特殊处理. 通过这样的方式来突出两个entity.</e></li>
</ul>
<p><strong>实验</strong></p>
<p>数据集: SemEval 2010 Task 8 首先是一个简单的特征对比实验:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/rnn-2.png">

</div>
<p>可以看出来PI这个特征也可以取得不错的效果。 下面是模型对比实验:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/rnn-3.png">

</div>
<p>可以看出来，再不使用任何Lexical-Feature的情况下， RNN + PI 与使用多窗口的Zguyen 2015 以及使用 WordNet信息的Zeng 2014达到类似的效果。此外从这几篇文章实验可以看出来，word embedding的size也可以很明显提高F值。</p>
<p><strong>总结</strong></p>
<p>这篇属于使用RNN的尝试，效果与CNN类似。 文中后续还有一些小实验，来说明对长文本建模的时候，RNN的记忆优势就可以体现出来。SemEval 2010 Task 8数据集的句子长度都比较小，因此使用CNN便可以很好的建模。</p>
<p><strong>6. BiLSTM Attention (Zhou 2016)</strong></p>
<blockquote>
<p>Zhou. (2016). Attention-Based Bidirectional Long Short-Term Memory Networks for Relation Classification. ACL</p>
</blockquote>
<p>这篇文章同样是基于RNN对句子建模，在上一篇RNN的基础上做了一点改进，使用标准的的Attention + BiLSTM，效果与Ranking Loss 类似 84%，基本结构:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/bilstm-1.png">

</div>
<p>主要介绍一下Attention Layer, 其实就是一个对LSTM的每一个step的输出做一个加权的过程，而非仅仅只取最后一个step. 设 <span class="math inline">\(H=[h_1, h_2, h_3,..h_T]\)</span> 为BiLSTM的所有step的输出矩阵，T为句子长度. 然后经过下面的操作来计算每个step的权重:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/bilstm-2.png">

</div>
<p>这样<span class="math inline">\(\alpha\)</span> 就是weight vector, <span class="math inline">\(r\)</span> 就是最终的Attention-BiLSTM的输出，也即sentence的embedding, 之后使用了softmax来分类，损失函数使用对数似然。</p>
<p><strong>实验</strong></p>
<p>数据集: SemEval 2010 Task 8 dataset 结果:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/bilstm-3.png">

</div>
<p>在没有使用词法/语法特征的情况下，达到84%的F1 Score，使用Attention 可以起到作用，能够减弱句子中的噪音词语影响，增强关键词的影响。</p>
<p><strong>总结</strong></p>
<p>Attention + BiLSTM 作为NLP Task的标配，在Relation Classification上也取得了不错的效果。文中不足是仅仅使用标准的Attention+LSTM直接简单用到这个Task， 创新点几乎没有，并没有针对关系分类这个任务的改进。</p>
<p><strong>7. Multi-Level Attention CNN (Wang 2016)</strong></p>
<blockquote>
<p>Wang. (2016). Relation Classification via Multi-Level Attention CNNs. ACL</p>
</blockquote>
<p>这一篇仍然是基于CNN来做的，按照文中实验效果，这一篇目前是最高的F1：88%。设计了相对复杂的两层Attention机制来尽可能突出句子中哪些部分对relation label有更大的贡献。使用了word similarity来定义句子中word与target entity的相似度，从而根据相似度来引入权重，这是第一层的Attention. 第二层Attention则是对卷积之后Pooling阶段，采用Attention Pooling 而不是Max Pooling. 通过这些操作来减弱噪音，加强相关性强的词的权重。此外也改进了前面Santos提出的Ranking Loss. 下面对文中详细介绍.</p>
<p>首先是Input Layer, 同Zeng 2014, 特征仍然是 word embedding + position embedding. 当然word embedding使用的word2vec 进行 pre-train的。因此当前每个词表示为:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/matt-1.png">

</div>
<p>其中<span class="math inline">\(w_i^d\)</span>为word embedding, 两个<span class="math inline">\(w_i^{p}\)</span>是Position Embedding。在之后，将trigram信息融合进去，设置一个滑动窗口k，以每个word 为中心，左右k/2个词作为上下文，然后直接串起来，这样每个词的embedding size变为:<span class="math inline">\((d_w + 2d_p)*k\)</span>，如下:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/matt-2.png">

</div>
<p>这里需要额外说一句，其实这个n-gram过程现在完成，然后卷积的时候，卷积核的size设置为1就可以了。或者现在不做，卷积核设置为k，可以达到同样的效果。但是后面有Attention，因此在这篇文章中，先做了n-gram。 输入层到这里为止，与其他文章完全一样。 下面就是加入 Input Attention 过程。 首先引入两个对角矩阵<span class="math inline">\(A_1, A_2\)</span> 对应每个句子的两个entity. 然后使用word embedding的向量内积运算来衡量某个词<span class="math inline">\(w_i\)</span> 与 entity <span class="math inline">\(e_j,j =1,2\)</span>的相关性，这样对角矩阵A的元素就是: <span class="math inline">\(A_{i,i}^{j}= f(e_j, w_i)\)</span>，其中f就内积运算。最后使用softamx归一化，来定义Attention的权重:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/matt-3.png">

</div>
<p>额外补充一句，这里的两个对角矩阵完全可以用两个向量来表示。</p>
<p>每个词都有对两个entity的权重:<span class="math inline">\(\alpha^{1}, \alpha^{2}\)</span>. 这样把权重融合到已经得到的<span class="math inline">\(z_i\)</span>中。融合的方法文中给了三个:</p>
<ul>
<li>average: <span class="math inline">\(r_i = z_i \frac{\alpha^1_i+\alpha^2_i}{2}\)</span>，这种方式直接简单，不过从经验感觉，直接求和比平均要好。</li>
<li>concat: <span class="math inline">\(r_i = [(z_i \alpha_i^1)^T, (z_i, \alpha_i^2)T]^T\)</span> 这种方式理论上保留的信息量最大。</li>
<li>substract: <span class="math inline">\(r_i = z_i \frac{\alpha^1_i-\alpha^2_i}{2}\)</span> 这种方式其实有点类似于TranE，将relation视为两个权重的差。</li>
</ul>
<p>这样整个句子表示为: <span class="math inline">\(R=[r_1, r_2, ..., r_n]\)</span>, 至此包含第一层的Attention的Input Layer 完成.</p>
<p>之后是卷积层，这里跟其他文章中卷积相同:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/matt-4.png">

</div>
<p>其中 <span class="math inline">\(W_f\)</span>是卷积核，size为<span class="math inline">\(d^{c} \times k(d^w+2d^p)\)</span>, 前面已经说过了，由于已经在input处做过了tri-gram操作，这里的<span class="math inline">\(d_c\)</span>一般为1.</p>
<p>后面是第二层Attention Based Pooling; 大部分文章直接使用Max Pooling来处理，这样可能会有一些过于简单，有时候并不能抽出跟后续的relation labels 更相关的部分，这也是这篇文章提出Attention Based Pooling的动机。 定义一个关联矩阵表示卷积之后的结果<span class="math inline">\(R^{*}\)</span>的每一个元素与relation labels的相关性，如下:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/matt-5.png">

</div>
<p>其中U是Attention中的权重矩阵，<span class="math inline">\(W^L\)</span> 表示relation labels的embedding，跟word/position embedding一样，需要在训练过程更新。这样的G就是一个相关矩阵，每一个元素<span class="math inline">\(G_{i,j}\)</span>就表示<span class="math inline">\(R^{*}\)</span>的第i个元素与第j个label的相关性。然后在对G做列归一化:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/matt-6.png">

</div>
<p>有了Attention Pooling矩阵，在做max pooling：</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/matt-7.png">

</div>
<p>这样得到的输出直接与relation embedding 最相关，<span class="math inline">\(w_O\)</span>也是模型最后的输出。完整的结构如下, 看起来很复杂，但是每一部分其实都比较简单:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/matt-8.png">

</div>
<p>最后介绍模型改进的损失函数，根据Santos 2015提出的Ranking Loss, 这篇文章同样使用margin based ranking loss function. 一般的margin function都需要定义一个distance/score function, 从而来区分正负样例. 在santos文中，直接使用了网络最后的输出score。这篇文章利用relation embedding <span class="math inline">\(W_{y}^{L}\)</span>, 定义了如下的score distance function:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/matt-9.png">

</div>
<p>来衡量模型的输出<span class="math inline">\(w^{O}\)</span>与正确label对应的vector <span class="math inline">\(W_{y}^{L}\)</span>的相关度，然后就是margin function:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/matt-10.png">

</div>
<p>这里直接设置了1为margin，<span class="math inline">\(y^{-}\)</span>是错误标签，最后用SGD等方法去优化。</p>
<p><strong>实验</strong></p>
<p>数据集同样是: SemEval 2010 Task 8 先看主体实验结果, 最好的达到了88%，比BaseLine 提升非常大:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/matt-11.png">

</div>
<p>其中仅仅使用Input-Attention可以达到87.5%的效果， 再加上Pooling Attention的效果则达到了88%。从这里可以看出了第一层Attention的作用要更加重要。第二层Pooling复杂度提升很多，performance提升很少。</p>
<p>再看几个对比辅助实验:</p>
<ul>
<li><p>Input Attention的三种融合方式, 结果如下: <img src="http://cdn.htliu.cn/blog/relation-extraction/matt-12.png"></p></li>
<li><p>去掉Pooling Attention, 去掉所有的Attention, 不使用自定义的distance function, 三种结果如下: <img src="http://cdn.htliu.cn/blog/relation-extraction/matt-13.png"> 这个结果也可以看出来，第一层的Input Attention 作用很明显。 其中最后一行去掉Attention, 去掉distance function 基本跟Santos 2015那篇文章相同，因此效果也类似。</p></li>
</ul>
<p><strong>总结</strong></p>
<p>优点:</p>
<ul>
<li>两层Attention: 在文章中的两处Attention 均是基于embedding 的内积来运算的。 其中Input Attention 很直观，利用embedding的内积来衡量相关。</li>
<li>Distance Function：从margin based ranking loss 出发，类似TransE等模型，使用embedding的逼近来作为距离函数。</li>
</ul>
<p>缺点:</p>
<ul>
<li>Input Layer的attention 使用word 与 entity的 embedding similarity 来作为attention的权重，这里有些问题。比如“<e1> caused by <e2>” 这里的 caused 很关键，但是与e1, e2的相似度应该比较小。而且不同的词在不同的relation中的重要程度是不一样的。使用统一的embedding 可能会有噪音影响。在一个可能原因是Out Of Vocab 的词语也会降低performance。</e2></e1></li>
<li>结构复杂 复杂度高，收敛困难，尤其是第二个Attention</li>
</ul>
<p><strong>8. Attention CNNs (Zhu 2017)</strong></p>
<blockquote>
<p>Zhu, (2017). Relation Classification via Target-Concentrated Attention CNNs. ICONIP</p>
</blockquote>
<p>这篇文章基本上是以上一篇为模板，稍微改动了一些， 动机则是每个词在不同的relation有不同的权重Attention 矩阵。 几点不同:</p>
<ul>
<li><p>Input Layer 引入一个权重矩阵<span class="math inline">\(M\)</span>，直接计算word 与 relation的相关度， 而不是计算word 与 entity的相关性: <img src="http://cdn.htliu.cn/blog/relation-extraction/zhuatt-1.png"> <img src="http://cdn.htliu.cn/blog/relation-extraction/zhuatt-2.png"></p></li>
<li>去掉了Pool Attention 部分，直接max pooling</li>
<li><p>目标函数: 没有使用上一篇的自定义的距离函数，而是直接使用类似Santos 2015的那种向量内积计算score的方式. 最终仍然使用ranking loss</p></li>
</ul>
<p>实验结果, 并没有对比88%的结果: <img src="http://cdn.htliu.cn/blog/relation-extraction/zhuatt-3.png"></p>
<p>这篇文章在Input Layer的Attention的改进比较符合直观。 在没有使用二级Attention 以及改进的损失函数的情况下， 可以达到87.5%，相对于上一篇的86.1%还是有提升。不过整体来看，文章的创新点比较少。</p>
<h3 id="总结1">总结1</h3>
<p>目前从论文结果来看，在SemEval 2010 Task 8 数据集上最高的F1-Score为88%，不过貌似还未有人可以复现。 总结一下已有的模型:</p>
<ul>
<li>word embedding + position embedding 成为输入层的标配。</li>
<li>损失函数Ranking Loss 比 softmax 效果好。</li>
<li>Attention的作用明显。
<ul>
<li>Input Layer加入Attention给每个word赋予weight，可以是word 与 relation的相关性，也可以是word 与 entity的相关性.</li>
<li>RNN/LSTM的所有step 的输出加一个Attention，本质也属于每个word与relation的相关性。</li>
</ul></li>
</ul>
<p>可能改进的方向: 单纯从神经网络的结构出发，改进的余地很小了，因为这个数据集很封闭， 可以利用的信息仅仅是这些sentence 以及 标注的word entity。 在上述的基础上可能可以考虑的方向，从两个target word与relation的关系上加约束。</p>
<h2 id="distant-supervised-learning">Distant Supervised Learning</h2>
<p>上一章的全监督方法都是在完全正确的标注数据集上来做的，因此数据量很小，SemEval 2010 一共是10000左右条文本。因此开始考虑研究如何在大数据集上做关系抽取。2010年提出Distant Supervision可以自动标注训练样本，原理很简单。利用知识图谱中的两个entity以及对应的某个relation，在corpus中进行回标，如果某个句子中同时包含了两个entity，那么就假定这个句子包含了上述的relation. 这样就可以获得大量的标注数据。当然缺陷就是假设太强，会引入了很多噪音数据， 因为包含两个entity的句子不一定可以刻画对应的relation，如下, 对于NYT 语料中的第一个句子的确描述了 Founder的关系，但是第二句就不是这个关系，因此属于噪音:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/dis-1.png">

</div>
<p>在2011年提出了Multi Instance Learning的方法来改进原始的Distance supervision的方法，有一个At-Least-One 的前提: 包含两个entity的所有句子中，至少有一个句子可以体现relation，即至少有一个标注正确的句子。通过结合FreeBase 对NYT语料做Entity Linking，Relation Aligning等操作进行标注， 最终得到一个被广泛使用的关系抽取数据集, 在开始已经描述过详情。正如前面声明，这里面会有噪音数据。 因此在这一系列的文章中，都会针对该噪音问题做一些工作，其中大部分基于Multi Instance来做。简单引用介绍Multi Instance Learning:</p>
<blockquote>
<p>多示例学习可以被描述为：假设训练数据集中的每个数据是一个包(Bag)，每个包都是一个示例(instance)的集合,每个包都有一个训练标记，而包中的示例是没有标记的；如果包中至少存在一个正标记的示例，则包被赋予正标记；而对于一个有负标记的包，其中所有的示例均为负标记。（这里说包中的示例没有标记，而后面又说包中至少存在一个正标记的示例时包为正标记包，是相对训练而言的，也就是说训练的时候是没有给示例标记的，只是给了包的标记，但是示例的标记是确实存在的，存在正负示例来判断正负类别）。通过定义可以看出，与监督学习相比，多示例学习数据集中的样本示例的标记是未知的，而监督学习的训练样本集中，每个示例都有一个一已知的标记；与非监督学习相比，多示例学习仅仅只有包的标记是已知的，而非监督学习样本所有示例均没有标记。但是多示例学习有个特点就是它广泛存在真实的世界中，潜在的应用前景非常大。 from http://blog.csdn.net/tkingreturn/article/details/39959931</p>
</blockquote>
<p>从这个介绍中可以看出来在Multi Instance Learning很适合用在NYT+FreeBase数据集上。</p>
<h3 id="相关文献-1">相关文献</h3>
<p><strong>1.Piecewise Convolutional Neural Networks</strong></p>
<blockquote>
<p>Zeng (2015). Distant Supervision for Relation Extraction via Piecewise Convolutional Neural Networks. EMNLP</p>
</blockquote>
<p>这一篇工作是在Zeng 2014基础上的扩展，从Fully Supervised 到Distant Supervised. <strong>动机</strong></p>
<ul>
<li>Distant supervised 会产生有大量噪音或者被错误标注的数据，直接使用supervised的方法进行关系分类，效果很差。</li>
<li>原始方法大都是基于词法、句法特征来处理， 无法自动提取特征。而且句法树等特征在句子长度边长的话，正确率很显著下降。</li>
</ul>
<p>因此文中使用Multi Instance Learning的at least one假设来解决第一个问题； 在Zeng 2014 的CNN基础上修改了Pooling的方式，解决第二个问题。 先介绍改进的CNN: Piece Wise CNN(PCNN). 总体结构如下, 与Zeng 2014 很类似:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/pcnn-1.png">

</div>
<p>输入仍然是一个sentence，Input Layer依然是word embedding + position embedding, 后面接卷积操作。 之后的Pooling层并没有直接使用全局的Max Pooling, 而是局部的max pooling. 文中把一个句子分为三部分，以两个entity为边界把句子分为三段，然后卷积之后对每一段取max pooling, 这样可以得到三个值，相比传统的max-pooling 每个卷积核只能得到一个值，这样可以更加充分有效的得到句子特征信息。 假设一共有个N个卷积核，最终pooling之后得到的sentence embedding的size为: <span class="math inline">\(3N\)</span>, 后面再加softmax进行分类，最终得到输出向量<span class="math inline">\(o\)</span>, 上面的示意图很清晰了，其中的c1,c2,c3是不同卷积核的结果，然后都分为3段进行Pooling。 下面可以减弱错误label问题的Multi-Instance Learning。这里面有一个概念， 数据中包含两个entity的所有句子称为一个Bag。先做几个定义:</p>
<ul>
<li><span class="math inline">\(M={M_1, M_2, ..., M_T}\)</span> 表示训练数据中的T个bags，每个bags都有一个relation标签.</li>
<li><span class="math inline">\(M_i = {m_i^{1}, m_i^{2},...,m_i^{q_i}}\)</span> 表示第i个bag内有<span class="math inline">\(q_i\)</span>个instance，也就是句子。</li>
<li>$o $ 表示给定<span class="math inline">\(m_i\)</span>的网络模型的输出(未经过softmax)，其中<span class="math inline">\(o_r\)</span> 表示第<span class="math inline">\(r\)</span>个relation的score</li>
</ul>
<p>这样经过softmax 就可以计算每一个类别的概率了</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/pcnn-2.png">

</div>
<p>这里需要说明的是，我们的目的得到每个bag的标签，并不关注bag里面instances的。因为每个bag上的label就是两个entity的relation。 而上面的概率是计算的bag里面某一个instance的，所以需要定义基于Bag的损失函数，文中采取的措施是根据At-Least-One的假设，每个Bag都有至少有一个标注正确的句子，这样就可以从每个bag中找一个得分最高的句子来表示整个bag,于是定义如下的目标函数: 假设训练数据为T个bags: <span class="math inline">\(&lt;M_i, y_i&gt;\)</span>:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/pcnn-3.png">

</div>
<p>这样到此为止，模型部分完毕，完整的算法如下:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/pcnn-4.png">

</div>
<p><strong>实验</strong></p>
<p>数据集: NYT+FreeBase，过滤掉了样本少的关系，最后使用了26类关系，数据描述如下:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/pcnn-5.png">

</div>
<p>评测: - Held-Out Evaluation: 直接在Test Dataset上进行评估，使用多分类的Precision-Recall Curve（基于不同的分类阈值）来衡量, PR曲线越靠近右上，证明分类效果越好: <img src="http://cdn.htliu.cn/blog/relation-extraction/pcnn-6.jpg"> 可以看出来，PCNN + MIL 表现最好，Precision和recall都要优于baseline。 - Manual Evaluation: 考虑到FreeBase 还不够完备，对于分类为False Negatives的数据，有可能是两个entity有关系，但是在FreeBase中并没有收录。 从上图的PR-Curve中也可以看出来，recall相对很小，一部分原因是FN过大，尤其是将NA分为其他的relation。 因此文中对那些将NA分为其他类别，按照概率高低，排序，手工检查Top-K 里面的entity pair是否的确有关系，结果如下: <img src="http://cdn.htliu.cn/blog/relation-extraction/pcnn-7.png"> 结果显示PCNN取得了最好的效果。 - 辅助对比实验：原始的CNNs， CNNs + Multi Instance, PCNNs, 以及 PCNNs+Multi Instance的结果: <img src="http://cdn.htliu.cn/blog/relation-extraction/pcnn-8.jpg"> 可以看出来， 文中提出的两种改进的确都有效果。</p>
<p><strong>总结</strong></p>
<p>这篇文章在NYT + FreeBase 数据集上比较好的效果， 也是第一篇使用CNN+Multi Instance来处理Distant Supervision 的关系抽取。相比与Zeng 2014，将Piecewise Pooling 加入到CNN中，以及将Multi Instance来处理Distance Supervised，从而减弱噪音的影响。当然在MIL中直接取了置信度最高的instance作为bag的，肯定会损失一部分信息，因此后续一些文章在上面做了工作。</p>
<p><strong>2.Selective Attention over Instances (Lin 2016)</strong></p>
<blockquote>
<p>Lin (2016). Neural Relation Extraction with Selective Attention over Instances.ACL</p>
</blockquote>
<p>这一篇文章在上一篇Zeng 2015的基础上引入了Attention机制。</p>
<p><strong>动机</strong></p>
<p>在Zeng 2015中的MIL部分，每一个bag仅仅取了置信度最高的instance，这样会丢失很多的信息，因此一个bag内可能有很多个positive instance。应用Attention机制可以减弱噪音，加强正样本，因此可以更充分的利用信息。</p>
<p><strong>模型</strong> 总体结构:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/att-1.png">

</div>
<p>其中<span class="math inline">\(m_i\)</span>是某一个bag的instance，图中的CNN模块与Zeng 2015 (PCNNs)的结果相同(Embedding Layer-&gt;Convolution Layer -&gt; Piecewise Max Pooling Layer) 最终得到每个句子的表示<span class="math inline">\(r_i\)</span>。为了能够充分的利用bag内的信息，我们可以对所有instance取加权: <span class="math inline">\(s= \sum_{i} \alpha_i x_i\)</span> 其中<span class="math inline">\(\alpha\)</span>是权重比例，文中给出了两种定义权重的方法:</p>
<ul>
<li>Average: 直接取平均: <span class="math inline">\(s=\sum\frac{1}{n}x_i\)</span> ，这种将所有instance同等对待的方式还是有缺陷，放大了噪音影响。</li>
<li>Attention: 目标是增加positive instance的权重，减小noise instance的权重。但是并不知道每个instance的groud truth，但是知道每个bag的label，因此就可以用instance 与 该relation label的相关度大小引入Attention: 如下: <img src="http://cdn.htliu.cn/blog/relation-extraction/att-2.png"> <img src="http://cdn.htliu.cn/blog/relation-extraction/att-3.png"> 其中A为Attention对角矩阵，<span class="math inline">\(r\)</span>可以认为是该数据的relation标签的embedding向量。 这样<span class="math inline">\(e_i\)</span>就可以一定程度表示句子与标签的相关性。</li>
</ul>
<p>之后得到了加权的<span class="math inline">\(s\)</span>就是bag-level的embedding。最后的输出需要注意，并不是直接用一个全连接层+softmax.而是根据向量的相似度计算bag的emebdding 与 每个relation的相似度作为relation的score，比如relation r的score计算方式:<span class="math inline">\(o_r = v_r s + b\)</span> 这里的b是一个bias，每个relation的score计算完之后，利用softmax来归一化成概率： <img src="http://cdn.htliu.cn/blog/relation-extraction/att-4.png"></p>
<p>在Test阶段不太一样， 前面说了在计算Attention的时候，使用的是训练数据对应的label 的embedding，但是在test的时候，并没有标签。文中的处理方式是: 对每一个relation r 执行下面的操作:</p>
<ul>
<li>计算一个bag里面所有的句子x与r的Attention权重，这一步与训练相同，然后加权求得这个bag的表示<span class="math inline">\(s\)</span></li>
<li>计算式子10: <span class="math inline">\(o = Ms +d\)</span>, 这里的M就是所有relation的embedding 矩阵。 这样得到了 o 是一个<span class="math inline">\(N_r\)</span> 维度的向量，<span class="math inline">\(N_r\)</span>是relation的number。</li>
<li>取出o里面的relation <span class="math inline">\(r\)</span> 对应的值， 作为这个r的预测概率</li>
</ul>
<p>源码注释如下: <img src="http://cdn.htliu.cn/blog/relation-extraction/att-5.png"></p>
<p>遍历完所有的relation之后， 得到每个relation的概率，取max即可。 这个过程其实相当于一个遍历的过程，复杂度有点高，而且解释性比较差，是一个可以改进的一个地方。</p>
<p><strong>实验</strong></p>
<p>文中首先做了在CNN/PCNN的基础上，如何使用bag内的信息的对比试验，结果如下，其中CNN/PCNN表示没有引入MIL，+ONE表示ZENG2015的取置信度最高的一个，+AVE与+ATT则是文中提出的两个:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/att-6.png">

</div>
<p>可以看出来，Attention的效果最好, 而且+ONE比+AVE要稍微好一些，也很容易理解，+AVE对噪音数据放大了影响。 第二个对比实验则是与传统方法的对比:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/att-7.png">

</div>
<p>最后一组实验则是更进一步验证Attention的作用。 因为在测试数据中，有超过3/4的bags 只有一个句子。 因此文中把拥有多个句子的entity pair 提取出来验证效果，在该子集上使用一下三种设置:</p>
<ul>
<li>One: 表示在每个bag里面随机选择一个句子来进行预测</li>
<li>Two: 表示在每个bag里面随机选择两个句子来进行预测</li>
<li>All: 使用bag里面所有的句子来进行测试</li>
</ul>
<p>评价指标使用P@N，结果如下:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/att-8.png">

</div>
<p>从结果可以看出来，ATT的确表现最好。</p>
<p><strong>总结</strong></p>
<p>与上一章全监督一样，Attention的作用很明显，而且用在这里很直观， 对bag内的句子赋予不同的权重，一方面可以过滤噪音，又可以充分利用信息。</p>
<p>不足的地方:</p>
<ul>
<li>正如在future work上所述，还是基于Zeng2014/5的基本框架上做改进，这个数据集句子长度要比SemEval 2010要长很多， 可以考虑用RNNs来测试结果如何。一般情况CNN在短文本效果比较好，RNN在长文本略有优势。</li>
<li>上面说的，train过程和test过程不一样，test要更加复杂，复杂度要高很多。</li>
</ul>
<p><strong>3.Multi-instance Multi-label CNNs (Jiang et al., 2016)</strong></p>
<blockquote>
<p>Jiang (2016). Relation Extraction with Multi-instance Multi-label Convolutional Neural Networks. Coling</p>
</blockquote>
<p>这篇文章从另一个角度来解决Zeng 2015的问题，并且考虑了实体对的多关系的问题。</p>
<p><strong>动机</strong></p>
<ul>
<li>Zeng 2015里面仅仅取置信度最高的instance，丢失信息。</li>
<li>在数据集中，有约18.3%的entity pair有多种relation, 其他方法均未考虑。</li>
</ul>
<p><strong>模型</strong></p>
<p>针对以上的两个问题提出了两个解决方法:</p>
<ul>
<li>对bag内部的所有sentence embeding做instance-max-pooling的操作，具体细节后面介绍</li>
<li>对于多标签，使用多个二分类函数来做多标签分类，即： 使用sigmod计算每一个类别的概率, 然后判断该bag是否可能有这种关系。</li>
</ul>
<p>模型的结构如图:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/miml-1.png">

</div>
<p>输入也是一个bag，然后利用CNN/PCNN来计算每个sentence的embedding，之后的融合方式很直接，直接对embedding的每一维度取所有sentence的对应维度的最大值。</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/miml-2.png">

</div>
<p>其中k表示embedding的某一维度，<span class="math inline">\(j\)</span>表示bag中的第j个句子。 这样就可以融合所有sentence的信息了。后面加一个全连接层计算每一个类别的score:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/miml-3.png">

</div>
<p>之后不再是加softmax多分类了，而是使用sigmod函数计算每个relation的概率，然后超过某个阈值，就认为该relation是准确的:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/miml-4.png">

</div>
<p>其中<span class="math inline">\(l\)</span>就是类别的总数。 文中设计了两种损失函数来做对比, Sigmod Loss Vs Squared Loss：</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/miml-5.png">

</div>
<p><strong>实验</strong> 直接看P-R Curve结果，相比PCNN提升比较明显:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/miml-6.jpg">

</div>
<p>再看取max的设计的作用，与直接取平均对比, 这里有点需要说明，在这个实验中，取平均要比PCNN效果好，而在上一篇平均效果差， 这说明的是multi label有提升的作用:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/miml-7.jpg">

</div>
<p>最后一个是两种损失函数的对比:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/miml-8.jpg">

</div>
<p>可以看出，二者在不同的区域各自有优势。</p>
<p><strong>总结</strong></p>
<p>仅仅对bags内的sentence的每一维度取了最大值，就可以得到一个很不错的效果， 可以考虑其他稍微复杂一些的融合方式，从而得到更多的信息，Attention仅仅取权重，其实还是属于线性融合。此外这篇文章仍然也是在该CNN/PCNN基础上进行扩展，从这一点来说创新性有些少。 不过文中提出的Multi Label 则是一个新的方向.</p>
<p><strong>4.Memory Network based (Feng 2017)</strong></p>
<blockquote>
<p>Feng,(2017). Effective deep memory networks for distant supervised relation extraction. IJCAI</p>
</blockquote>
<p>这篇文章使用2014年提出的Memory Network的思想来做关系抽取。</p>
<p><strong>动机</strong></p>
<ul>
<li>不同的word在不同relation下以及对不同的entity pair 重要程度不同，这一点类似于Fully Supervised 中的Multi-Level Attention</li>
<li>relation之间并不是独立的，会有overlap, 也就是上一篇提出的多标签。本质上是因为标签之间有相互依赖关系，如(A, capital, B) 成立的话， (A, contains, B)也会成立。</li>
</ul>
<p>对于第一个问题，借鉴了Multi-Level Attention的input layer attention的思路，即根据word 与 entity pair的相似度，来分配权重，但是这里不使用传统的Attention, 而是基于Memory Network的思想，关于Memory Network的细节，这里不介绍，不了解也不影响对文章的理解。 对于第二个问题，文中使用relation-level的attention 来引入relation之间相关度，下面详细介绍</p>
<p><strong>模型</strong></p>
<p>跟前面的文章类似， 本文也是分为两个大模块，sentence representation 以及 relation classification。 首先从input layer 开始，每个word仍然由word embedding + position embedding两部分组成。 在本文中sentence embedding由两部分组成，其中一部分是传统的CNN/PCNN(Zeng 2015) 中得到的输出，这一块不再赘述，与上面文章相同。 另一部分则是考虑了句子中entity的上下文与entity的相关性得到的一个表示，细节如下:</p>
<p>设每个句子表示为<span class="math inline">\(S=\{e_1,e_2,..,e_l\}\)</span>，其中<span class="math inline">\(e_i \in R^{d \times1}, d=d_w + 2d_p\)</span>, <span class="math inline">\(l\)</span>表示句子长度。 两个entity为<span class="math inline">\(e_h, e_t\)</span>，这样剩下的部分，也就是上下文，表示成一个矩阵:<span class="math inline">\(m=\{e_1,..,e_{h-1}, e_{h+1},...,e_{t-1},e_{t+1}..\} \in R^{d \times (l-2)}\)</span>，文中成为 external memory 矩阵 然后计算<span class="math inline">\(m\)</span> 的每一行也就是每一个上下文与entity pair的相关度，这里并没有使用向量内积来运算，而是一层神经网络来计算score: <span class="math display">\[g_i  = tanh(W_{word-ep}[m_i, e_h, e_t] + b_{word-ep})\]</span> 其中<span class="math inline">\([x1,x2]\)</span>表示向量串联，<span class="math inline">\(W_{word-ep} \in R^{1 \times 3d}, b_{word-ep} \in R^{1\times 1}\)</span>， 再使用softmax来得到权重: <span class="math display">\[\alpha_i = \frac{\exp(g_i)}{\sum \exp(g_j)}\]</span></p>
<p>最后可以得到一个加权之后的输出向量:<span class="math inline">\(x=\sum_{i=1}^{l-2} \alpha_i m_i\)</span>. 在x的基础上，还可以继续上述的过程，只是把entity pair的两个向量，换成<span class="math inline">\(x\)</span>. 可以重复多次，最终得到一个输出作为sentence的另一部分表示。 将该部分与CNN的输出直接串联作为最终句子的表示。</p>
<p>对于一个bag来说，使用上面的模型可以得到每个句子的表示，之后文中使用两阶段的Attention: - Selective Attention over Instances 与Lin 2016 相同， 对每个bag内的instances取加权平均，计算方法也一样，首先利用Attention矩阵计算instance与每个relation的相关性: <span class="math inline">\(z_i = x_i A v_{r_j}\)</span>, 之后进行softmax计算权重: <span class="math inline">\(\beta_i = \frac{\exp(z_i)}{\sum \exp (z_p)}\)</span> ， 最终得到加权的bag表示: <span class="math inline">\(R_{j}=\sum \beta_i x_i\)</span>，其中<span class="math inline">\(R_j\)</span>是relation-specific的 - Selective Attention over Relations 这一层的Attention是用在relations之间的，用来挖掘relation之间的依赖关系，前一层的Attention得到了k个向量:<span class="math inline">\(R=\{R_1,...,R_k\}\)</span>，其中k为relation 数量。 计算关系 i 与 j 的相关性如下: <span class="math inline">\(h_i = R_i B R_j\)</span>, 之后softmax 计算权重: <span class="math inline">\(\gamma_i = \frac{\exp(h_i)}{\sum \exp (h_q)}\)</span>， 然后就可以得到融合多relation依赖关系的输出: <span class="math inline">\(R^{*}_{j} = \sum_{i=1}^{k} = \gamma_i R^i\)</span></p>
<p>之后得到<span class="math inline">\(R={R_{1}^{*}, ..., R_{k}^{*}}\)</span>，对每一个relation 做一个binary classifier， 逻辑回归的sigmod即可 <span class="math display">\[p(i|M,\theta)=\frac{1}{1+\exp(-o_i)}\]</span> 其中<span class="math inline">\(o_i = W_i R^{*}_{i} + b_i\)</span> , 最后使用多标签的损失函数，与上一篇相同:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/mem-1.png">

</div>
<p>到此模型介绍完毕。</p>
<p><strong>实验</strong></p>
<p>同样用held-out以及P@N 结果如下:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/mem-2.png">

</div>
<p>从结果看， 本文的方法比Lin 2016 要好， 毕竟考虑了关系的依赖性，不过提升不是很明显，可能原因是数据问题，relation的overlapping比较少。 再看一组对比实验: 分别是去掉word-attention 以及去掉 relation-attention:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/mem-3.png">

</div>
<p>可以看出来，去掉relation之后，下降相对来说比去掉word-level更加明显。</p>
<p><strong>总结</strong> 这篇文章虽然以Memory Network 为题，不过可以本质还是用Attention引入相关性。 word-level的动机来自Multi-Level 的那篇文章，计算word与target entity的相关性，并且可以多层，从而挖掘更深层次的关系。relation-level的动机则是考虑到数据中的关系依赖性，使用attention来考虑关系之间的相关性。</p>
<p>这篇文章创新的地方在于引入relation之间的依赖关系。 可能改进的地方有，完全从embedding的角度考虑相关性，抛弃Attention,计算量会少一些。包括word-entity embedding； relation-relation embedding;</p>
<p><strong>5.Multi-Instance With extra Entity Descriptin (Ji 2017)</strong></p>
<blockquote>
<p>Ji (2017). Distant Supervision for Relation Extraction with Sentence-Level Attention and Entity Descriptions. AAAI</p>
</blockquote>
<p>这篇文章引入了额外的Knowledge Graph 信息: 实体描述(entity Description). 比如NYT 数据集是通过Freebase做entity linking等工具来进行连接出句子中的实体，而其实每个实体在FB都有一段文字描述， 文中认为现在的工作都集中在NYT + Freebase数据本身上，却忽略了该数据集中背后的KG信息，其中就有实体的描述信息。因此本文在之前的工作基础上引入了实体描述信息，加强对实体embedding的学习。此外，在处理Multi Instance Learning方面， 本文同样使用了Sentence-Level Attention的机制，类似Lin 2016.</p>
<p><strong>模型</strong></p>
<p>模型分为三部分: 输入模块，Attention 模块， Entity 描述模块。 输入层，每个句子的每个词由word embedding + position embedding 连接表示，这里与其他模型完全一样，不再赘述。 接着是卷积层与Piecewise Max-Pooling层，到这里与PCNN(Zeng 2014)完全一致，最终得到每个句子的embedding。 下面则是用来解决Multi instance Learning的Attention 模块。 计算bag内的每个Instance 与 relation的相关性，确定权重。具体步骤如下:</p>
<ul>
<li>计算相关性: <span class="math inline">\(\omega_i = W^T_{a}(tanh([b_i; v_{relation}])) + b_a\)</span> 其中<span class="math inline">\([x1, x_2]\)</span>表示concat操作, <span class="math inline">\(b_i\)</span>表示上一个步骤得到每个句子的表示，与Lin 2016 使用随机初始化作为<span class="math inline">\(v_{relation}\)</span>的参数不同的是，这里的<span class="math inline">\(v_{relation}\)</span>借鉴KG Embedding的Trans模型，即: <span class="math inline">\(v_{relation}=e_1 - e_2\)</span>。此外这里使用神经网络计算Attention。</li>
<li>归一化: <span class="math inline">\(\alpha_i = \frac{\exp(\omega_i)}{\sum \exp(\omega_j)}\)</span></li>
<li>加权句子表示，得到bag的表示: <span class="math inline">\(b = \sum \alpha_i b_i\)</span></li>
</ul>
<p>后面同样加一个softmax层用来做多分类: <span class="math inline">\(o = W_s b + b_s\)</span>, 归一化: <span class="math inline">\(p(r_i) = \frac{\exp(o_i)}{\sum \exp(o_j)}\)</span> 到这里为止其实与前面的工作相似度非常高， 仅仅是Attention的一点差异。下面介绍文中引入的Entity Description部分。 其实很简单，步骤如下:</p>
<ul>
<li>使用普通的一层CNN+MaxPooling 对 description 建模得到 描述的 embedding向量，假设<span class="math inline">\(d_i\)</span>为第i个实体的description embedding</li>
<li>文中提出的一个约束是：尽可能使得前面得到的entity的word embedding 与 这里得到的 entity 的 description embedding 接近，这样的动机很简单，就是将entity的描述信息融入到模型中，这部分的LOSS直接使用二范距离: <span class="math inline">\(L_e = \sum ||e_i - d_i||_2^2\)</span></li>
</ul>
<p>最终的目标函数，就是将两个损失函数联合训练: <span class="math display">\[\min L = L_A + \lambda L_e\]</span> 其中<span class="math inline">\(L_A = \sum log P(r_i), \lambda\)</span>为调节系数。</p>
<p><strong>实验</strong></p>
<p>数据集说明:</p>
<ul>
<li>NYT + FreeBase</li>
<li>从Freebase 以及 Wikidata中提取出的实体描述</li>
</ul>
<p>首先看Held-Out 实验结果:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/ent-1.png">

</div>
<p>其中实验有: PCNN+MIL 为Zeng 2015的模型， APCNNs为本文提出的不包含实体描述的模型， PCNNs+MIL+D为在zeng 2015的基础上加上实体描述的Loss，APCNNs+D为本文包含描述的模型。 可以看出来加上实体描述的确对效果有提升。此外APCNNs 比 PCNN+MIL要好一些，说明Attention要比取置信度最高的instance要好。</p>
<p>再看manual实验，就是找出那些标签为NA但是预测非NA的数据， 人工检查，检查是否两个entity的确有预测的relation，使用准确率Accuracy来衡量，结果如下,这里的top是指按照预测为某relation的概率大小排序:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/relation-extraction/ent-2.png">

</div>
<p><strong>总结</strong></p>
<p>这篇文章的贡献主要在从KG中引入额外的实体描述信息，加强embedding的学习。 不过两部分的融合有点简单暴力，本质上相当于在原来的基础上加了一个范式约束而已，或者说一个先验的惩罚项, 这种方式提供了一个比较好的思路, 扩展性很强.</p>
<h3 id="总结2">总结2</h3>
<p>general的总结:</p>
<ul>
<li>输入层: 基本word embedding + position emebdding</li>
<li>Sentence Embedding: 基本使用CNN/PCNN</li>
<li>Multi Instance Learning: 基本使用Attention 来处理(Attention的选择有待改进)</li>
<li>Evaluate:
<ul>
<li>Held-out: P-R 曲线</li>
<li>Manual: 选择Negative False的标签为NA的数据， 手工检测是否的确为NA</li>
</ul></li>
</ul>
<p>其他Tips:</p>
<ul>
<li>Word-Entity的相关度，同一个word在不同的entity pair内有不同的重要性。(Deep Memory Network, Multi-Level Att )</li>
<li>Word-Relation的相关度，同一个word在不同的relation下有不同的重要性。()</li>
<li>多标签分类(MIMLCNN, Memory Network)</li>
<li>relation之间的依赖性(Memory Network)</li>
</ul>
<h2 id="附">附</h2>
<p>关系抽取这个方向相对来说比较成熟了, 包括数据集,评测等. 改进的出发点很多; 相对比较容易做的就是从distant supervision下手, 如何减轻噪音数据带来的影响. 此外用一些时髦的东西套过来也可以,比如强化学习(AAAI2018有2篇)</p>
<blockquote>
<p>此外正在用Pytorch来复现相关模型, 然而一些经典模型仍然无法复现出结果, 还在调试.</p>
</blockquote>
<p>大概原因找到了， 数据集的问题。 在NYT数据集上，常用的有两个版本的数据集(刘康老师组他们在AAAI2018的文章 Large Scaled Relation Extraction with Reinforcement Learning Task 也提到了):</p>
<ul>
<li>27类关系，Zeng2015,Ji2017等用到的经过过滤之后的数据集，相对较小，以SMALL表示。</li>
<li>53类关系，Lin2016 发布的数据集，相对较大，训练数据大概是小数据的4倍，以LARGE表示。</li>
</ul>
<p>根据个人及同学的使用Pytorch复现的实验结果，代码:<a href="https://github.com/ShomyLiu/pytorch-relation-extraction" target="_blank" rel="external">pytorch-relation-extraction</a>, 有下面经验结论（可能有误差):</p>
<ul>
<li>PCNN+ATT(Lin 2016) 在SMALL数据集上很难达到在LARGE数据集上的效果，甚至达不到PCNN+ONE(Zeng 2015)的效果, 原因正在探索</li>
<li>在LARGE数据集上，复现的模型中， PCNN+ATT可以达到Lin 2016文中的效果，PCNN+ONE在该版本数据集上的表现也比SMALL数据集要好, 而且基本与PCNN+ATT持平。</li>
</ul>
<p>完整实验情况见: <a href="http://shomy.top/2018/07/05/pytorch-relation-extraction/">关系抽取实验</a></p>
<p>鉴于上述的发现，正好也有个很有趣的现象与之对应，使用SMALL数据集的文章貌似没有与PCNN+ATT这一经典模型做对比实验的。另外在刘康老师组的AAAI2018的文中，也是在两个数据集上分开比较的，仅仅在LARGE数据集上与PCNN+ATT模型做了对比。</p>
<p>…待续</p>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;2018.10.14更新: 增加全监督关系抽取PCNN的复现代码: &lt;a href=&quot;https://github.com/ShomyLiu/pytorch-pcnn&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;PCNN&lt;/a&gt;.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;blockquote&gt;
&lt;p&gt;2018.07.08更新: 增加对远程监督两份数据集补充实验对比: &lt;a href=&quot;http://shomy.top/2018/07/05/pytorch-relation-extraction/&quot;&gt;关系抽取实验&lt;/a&gt;.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;blockquote&gt;
&lt;p&gt;2018.04.04更新：增加对NYT+Freebase数据集的两个版本的说明&lt;/p&gt;
&lt;/blockquote&gt;
&lt;blockquote&gt;
&lt;p&gt;对近几年(到2017)一些关系抽取/分类(Relation Extraction)的部分文献的一个简单总结。&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="读书笔记" scheme="http://shomy.top/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="Knowledge Graph" scheme="http://shomy.top/tags/Knowledge-Graph/"/>
    
      <category term="NLP" scheme="http://shomy.top/tags/NLP/"/>
    
      <category term="relation-extraction" scheme="http://shomy.top/tags/relation-extraction/"/>
    
  </entry>
  
  <entry>
    <title>Feature Scaling 和 Batch Norm 笔记</title>
    <link href="http://shomy.top/2017/12/05/Feature-Scaling-Batch-Norm/"/>
    <id>http://shomy.top/2017/12/05/Feature-Scaling-Batch-Norm/</id>
    <published>2017-12-05T08:50:00.000Z</published>
    <updated>2018-09-29T03:52:04.752Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>Batch Normlization是2015年提出的一个加速神经网络训练的技巧，同时对performance也有一定的提高，现在基本已经广泛使用了。这里相对完整的整理一下学习笔记。主要参考李宏毅老师的机器学习课程。</p>
</blockquote>
<h2 id="从feature-scaling-开始">从Feature Scaling 开始</h2>
<a id="more"></a>
<p>提起BN首先需要从Feature Scaling/Normalization(特征缩放/归一化)说起。 在机器学习中，经常需要对特征做归一化，标准化，具体原因是什么呢？如果输入的特征的尺度差别很大，比如说<span class="math inline">\(x_1\)</span>的取值位于<span class="math inline">\(0-1\)</span>, <span class="math inline">\(x_2\)</span>的尺度位于<span class="math inline">\(1000-2000\)</span>, 这个时候，就需要将特征scaling到同一个尺度，一般来说，特征缩放之后有两个好处。</p>
<ul>
<li><strong>提高学习器的性能</strong>，特别是涉及到距离计算的，比如KNN, SVM等分类器，KMeans聚类等。这个很容易理解，在计算距离的时候， 如果某一个特征取值范围很大，这样这一个特征对距离的计算就会占据主导作用， 其它尺度小的特征起到的作用就会变小，而实际上这些特征的重要性可能是一样的。 因此做了Feature Scaling将特征范围缩放到一个尺度的话，就可以避免这种情况。</li>
<li><p><strong>加快梯度下降优化的收敛速度</strong> 这里经常使用的是吴恩达老师的机器学习课程中预测房价的例子，假设有影响房价两个特征</p>
<ul>
<li><span class="math inline">\(x_1\)</span>:房间数目，一般比较小，位于1-5左右</li>
<li><span class="math inline">\(x_2\)</span>: 房屋面积, 一般取值比较大，比如位于80-200等</li>
</ul>
<p>一般线性回归的关系函数如下: <span class="math display">\[s = \theta_1x_1 + \theta_2x*2 + b\]</span> 取平方误差的话, 损失函数为(仅仅考虑一个数据): <span class="math display">\[LOSS = (s_\theta(x_i)-y_i)^2\]</span> 因为<span class="math inline">\(x_2\)</span>的尺度比<span class="math inline">\(x_1\)</span>大很多，因此即使两个参数<span class="math inline">\(w_1,w_2\)</span>同样大小的变化，乘上<span class="math inline">\(x_i\)</span>之后，对最后的结果LOSS影响差距也很不同, <span class="math inline">\(w_2\)</span>明显要比<span class="math inline">\(w_1\)</span>的作用要大，也就是说，<span class="math inline">\(\theta_2\)</span>很小的变化就会导致LOSS较大变化，在LOSS的等高线上就会呈现出椭圆的形状, 如图1: <img src="http://cdn.htliu.cn/blog/feature-scaling-BN/scaling-1.png" alt="img"> 这个图的横纵坐标表示两个特征，然后每一圈代表一个LOSS值，圈越大，LOSS越大，最里面的B则是最优解的位置。 然后考虑梯度更新的过程，上述的等高线即LOSS与<span class="math inline">\(\theta_1,\theta_2\)</span>的函数关系， 因此图中椭圆的切线方向就是梯度下降的方向， 从图1中可以看出来，从初始点A到最有点B需要经过很多次的无用的迭代，并不是每次向着最优的方向走，类似于之字形，这样收敛就会很慢了。 反过来看，如果做了Feature Scaling的话，<span class="math inline">\(\theta_1, \theta_2\)</span>的更新变化对LOSS影响一致，这样等高线就会成近圆形如图2所示，这样沿着切线的方向进行梯度更新就会一直沿着最优的方向走，就会加快收敛速度。</p></li>
</ul>
<p>常用的Feature Scaling的方法有:</p>
<ul>
<li>标准化(Standardization): 所有特征量化到标准正态分布上: <span class="math display">\[z = \frac{x- \mu}{\sigma}\]</span> 其中<span class="math inline">\(\mu,\sigma\)</span>就是 <span class="math inline">\(x\)</span>的均值和标准差，这样所有特征的尺度就基本类似了。 这种标准化的缺点就是如果原始数据不近似于高斯分布的话，效果可能会变差，毕竟修改了分布。</li>
<li>最大-最小化 (Min-Max Scaling) 线性归一: <span class="math display">\[z = \frac{x-x_{min}}{x_{max} - x_{min}}\]</span> 这种也比较简单常用。这样所有的数据都会到[0,1]的范围内了，尺度一致。</li>
</ul>
<p>此外， 其实做Feature Scaling或多或少的跟原始数据都有一些差异， 理论上比较好的方法是，不同的Feature设置不同的学习率，尺度大的特征的学习率应该小一点，尺度小的特征的学习率稍微大一些，但是在实际梯度下降中，特征很多，对每个特征设置不通的学习率不切实际，很难实现，因此换一种思路，对特征做scale, 这样也可以避免尺度不一带来的问题。</p>
<h2 id="batch-normalization">Batch Normalization</h2>
<p>开始考虑神经网络， 我们知道神经网络有很多个Layer组成， 每个layer的输入都是上一层layer的输出， 那我们就有理由相信对每一层的输入的特征做Feature Scaling 可能会起到不错的作用。 在说BN之前，需要简单提一下 Internal Covariate Shift(ICS) 这个现象. 我们知道多层的神经网络，每一层的输出数据的分布都在随着训练也就是参数更新发生着变化，比如说第一层的输出的分布随着第一层的权重和bias的训练更新而改变，这种数据分布的改变就称之为: Internal Covariate Shift(ICS). 而如果对每一层的输入都做归一化的话，就相当于每一层的分布都保持一致了(均值方差相同), 这样可以一定程度上减小ICS带来的影响，当然显然并不能从根本上解决，只是减弱一些而已(具体关于ICS的内容这里不再介绍，理论知识偏多)。 下面考虑一下Batch Normalization是怎么做的。 有了前面的Feature Scaling, 貌似问题很简单了， 就对每一层的输出做scaling就可以了，直接使用上述的第一种归一化的方式即可: <span class="math display">\[\hat{x} = \frac{x-\mu}{\sigma}\]</span> 但是这里需要考虑一个问题，在做归一化的时候， 会影响原来学到的特征，比如说在输入到sigmod激活函数之前，如果经过Wx+b之后的结果，分布在S型函数的两侧， 也就是近乎平行坐标轴的那部分区域，这样原本经过激活函数之后就是0或者1了， 但是经过归一化之后，结果数据都到了S型函数的中间部分,如下图: <img src="http://cdn.htliu.cn/blog/feature-scaling-BN/scaling-2.png" alt="img"></p>
<p>这样会有问题，归一化都到了[-1, 1]那一部分区域， 而对应的sigmod近乎是一条直线了，如红色虚线所示，那这样就成了线性变换了，那神经网络的非线性变化就没有意义了。所以BN在做完归一化，具体来说是标准化之后，又做了一步特征转换的操作: <span class="math display">\[\hat{x} = \frac{x-\mu}{\sigma} //归一化\]</span> <span class="math display">\[z = \gamma \hat{x} + \beta //重构转换\]</span></p>
<p>其中 <span class="math inline">\(\gamma, \beta\)</span> 是两个参数，跟权重矩阵参数一样， 也是需要训练更新的。<span class="math inline">\(\hat{x}\)</span>则是标准化之后的值。可以看出来，如果<span class="math inline">\(\gamma=\sigma, \beta=\mu\)</span>的时候，我们就会发现<span class="math inline">\(x=z\)</span>了，这样就恢复到了原始的特征分布，其实可以这样理解，<span class="math inline">\(\gamma, \beta\)</span>这两个参数其实就是来学习前面的Normalization到底有没有作用，如果没有作用那就做一些转换，抵消一部分Normalization带来的影响。 这样完整的BN的步骤如下，需要注意的是，输入是一个Batch的数据，上述求得均值，方差均是对同一个Batch的数据计算得出的也就是说下图的m就是minbatch size： <img src="http://cdn.htliu.cn/blog/feature-scaling-BN/scaling-3.png" alt="img"></p>
<p>用图示表示，更加形象一些: <img src="http://cdn.htliu.cn/blog/feature-scaling-BN/scaling-4.png" alt="img"></p>
<p>需要注意的有两点:</p>
<ul>
<li>理论上BN可以放到激活函数之前，也可以放在其之后，但是一般情况，BN层会放在激活函数之前，这个其实很容易理解，比如激活函数如果是sigmod或者tanh的话，本身的输出都已经在一定程度上有了归一化了，后面再加一个BN的话，意义不大。</li>
<li>batch-size 大一些泛化能力更强。 如果batch很小的话，计算均值和方差其实没有代表性。</li>
</ul>
<p>前面的都是在training阶段，在test的时候，情况不太一样。因为在测试的话，我们没有batch 的输入，如何去计算<span class="math inline">\(\mu, \gamma\)</span>呢。一般都是从训练数据出发, 比如使用所有数据的均值和方差，这种方法在数据集很大的时候，很难实现。因此可以退而求其次，在最后一个epoch的时候，保存每个batch的均值和方差，最后去平均。取最后一个epoch的原因也很简单，最后一个epoch参数的训练已经近乎最优了。</p>
<h2 id="附pytorch-简单实现">附Pytorch 简单实现</h2>
<p>Pytorch已经内置了<code>BatchNorm</code>函数，而且有多种，这里就以简单的神经网络来做个例子，直接在激活函数之前就加入BN即可: <figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">model = nn.Sequential(</div><div class="line">    nn.Linear(<span class="number">10</span>, <span class="number">20</span>),           # 输入<span class="number">10</span>维，隐层<span class="number">20</span>维</div><div class="line">    nn.BatchNorm1d(<span class="number">20</span>, momentum=<span class="number">0.5</span>),     # BN层，参数为隐层的个数</div><div class="line">    nn.tanh(),                        # 激活函数</div><div class="line">    nn.Linear(<span class="number">20</span>, <span class="number">2</span>),             # 输出层</div><div class="line"> )</div></pre></td></tr></table></figure></p>
<h2 id="参考">参考</h2>
<ol style="list-style-type: decimal">
<li>https://arxiv.org/abs/1502.03167</li>
<li>http://blog.csdn.net/lovesophiaw/article/details/58996684</li>
<li>https://zhuanlan.zhihu.com/p/25234554</li>
<li><a href="https://www.youtube.com/watch?v=BZh1ltr5Rkg" target="_blank" rel="external">李宏毅老师 Batch-Norm 教程</a></li>
<li>http://www.voidcn.com/article/p-bptxerfe-bcx.html</li>
<li>https://morvanzhou.github.io/tutorials/machine-learning/torch/5-04-A-batch-normalization/</li>
</ol>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;Batch Normlization是2015年提出的一个加速神经网络训练的技巧，同时对performance也有一定的提高，现在基本已经广泛使用了。这里相对完整的整理一下学习笔记。主要参考李宏毅老师的机器学习课程。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id=&quot;从feature-scaling-开始&quot;&gt;从Feature Scaling 开始&lt;/h2&gt;
    
    </summary>
    
      <category term="读书笔记" scheme="http://shomy.top/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="machine learning" scheme="http://shomy.top/tags/machine-learning/"/>
    
  </entry>
  
  <entry>
    <title>使用coding/github的webhook实现自动化部署</title>
    <link href="http://shomy.top/2017/09/29/codings-webhook/"/>
    <id>http://shomy.top/2017/09/29/codings-webhook/</id>
    <published>2017-09-29T09:48:00.000Z</published>
    <updated>2018-09-29T03:52:04.736Z</updated>
    
    <content type="html"><![CDATA[<h2 id="需求">需求</h2>
<p>一些代码托管网站比如coding.net/github.com 等都提供了webhook的工具， 经常用于自动化部署。 之前写过一篇博客<a href="http://shomy.top/2016/04/14/git-server-hook/">搭建git服务器以及web自动化部署</a> 主要记录的是在自己服务器上如何搭建git服务，并且利用hook进行自动化部署。不过这种方式有个缺点，没有像github或者coding.net的web界面，管理起来可能稍微麻烦，因此我们还是习惯在github,coding.net上进行代码托管。然后再部署到服务器上，这样有些不方便。还好这些托管网站均提供了webhook的工具，功能与git的hook一致。 <a id="more"></a></p>
<p>因此可以用来做自动化部署，用一张图来说明区别(参考<a href="https://laravel-china.org/topics/2096/using-webhook-to-realize-automatic-deployment-of-online-server" target="_blank" rel="external">1</a>]:</p>
<p><img src="http://cdn.htliu.cn/blog/coding-web-hook/webhook-1.png"> 这样以来， 开发者只需要push代码到github上即可，无需在服务器操作，即可将服务器代码同步最新状态。原理很简单，当coding.net收到<code>PUSH</code>或者<code>PR</code>之后，向服务器发起一个<code>POST</code>的请求， 服务器执行<code>git pull</code>的操作，拉取最新代码。 下面记录下总体的流程(github与coding.net操作基本相同，下面以coding.net为例)</p>
<h2 id="操作">操作</h2>
<p>基本流程分为以下几步: 1. 建立仓库，这一个没什么好说的，就是自己的项目 repo. 2. 将服务器的ssh公钥加入到项目的公钥: <img src="http://cdn.htliu.cn/blog/coding-web-hook/ssh-coding-1.png"> 3. 建立WebHook <img src="http://cdn.htliu.cn/blog/coding-web-hook/web-hook-new.png"> 4. 在服务器端写响应API 这部分就是写个响应POST请求的小程序就好，我这里用python的Flask来完成的。代码如下，其中使用了第三方执行命令库<code>sh</code>，可以方便执行一些Unix命令:</p>
<p><figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div></pre></td><td class="code"><pre><div class="line"><span class="comment">#!/usr/bin/env python</span></div><div class="line"><span class="comment"># encoding: utf-8</span></div><div class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask, request</div><div class="line"><span class="keyword">import</span> sh</div><div class="line"><span class="keyword">import</span> logging</div><div class="line">app = Flask(__name__)</div><div class="line"></div><div class="line"><span class="meta">@app.route('/api/git_hook', methods=['POST'])</span></div><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">git_hook</span><span class="params">()</span>:</span></div><div class="line">    token = request.form.get(<span class="string">'token'</span>, <span class="string">''</span>) <span class="comment"># 提取token</span></div><div class="line">    <span class="keyword">if</span> <span class="string">'your-token'</span> == token:</div><div class="line">        sh.cd(<span class="string">'/var/www/html/web'</span>) <span class="comment"># 进入web项目的目录</span></div><div class="line">        <span class="comment"># 执行pull操作</span></div><div class="line">        sh.git.pull([<span class="string">'origin'</span>, <span class="string">'master'</span>])</div><div class="line">        <span class="keyword">return</span> <span class="string">'&lt;pre&gt;pull&lt;/pre&gt;'</span></div><div class="line">    <span class="keyword">else</span>:</div><div class="line">        <span class="keyword">return</span> <span class="string">'&lt;pre&gt;Wrong Toen &lt;/pre&gt;'</span></div><div class="line"></div><div class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</div><div class="line">    app.run(port=<span class="number">5000</span>)</div></pre></td></tr></table></figure></p>
<p>这里需要说明的是，服务器端的用户需要有项目目录的读写权限，最好直接把目录的own设置为用户。 之后直接执行该文件即可: <figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">nohup python app.py <span class="number">2</span>&gt;&amp;<span class="number">1</span> &amp;</div></pre></td></tr></table></figure></p>
<p>这样就可以在后台执行，端口是5000.</p>
<ol start="5" style="list-style-type: decimal">
<li>设置nginx域名转发 这一步其实就是Nginx的转发，将5000端口转到80端口，这样就可以使用域名访问了，直接在Nginx的server 80的配置文件，加上如下的一项:</li>
</ol>
<p><figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">location /api &#123;</div><div class="line">    proxy_set_header    X-REAL-IP $remote_addr;</div><div class="line">    proxy_set_header    Host    $http_host;</div><div class="line">    proxy_pass          http://<span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span>:<span class="number">5000</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<pre><code>这样就可以直接使用域名`http://yoursite.com/api/git_hook`访问了，记得重启下nginx服务。</code></pre>
<ol start="6" style="list-style-type: decimal">
<li>测试 服务器的服务启动之后， 使用webhook做个测试， Test的部分是coding自己的测试，没有携带token。 <img src="http://cdn.htliu.cn/blog/coding-web-hook/result-1.png"></li>
</ol>
<h2 id="总结">总结</h2>
<p>这样就算完成了利用webhook的自动化部署的过程。 整体比较简单清晰。 这里需要提另外一个代码托管工具，<a href="https://about.gitlab.com/" target="_blank" rel="external">gitlab</a> 它除了github/coding.net的功能之外，提供了在自己服务器上搭建gitlab，同样可以拥有web管理的界面。 但是目前不直接用它的原因，就是这货比较吃配置，最小4G内存(包含SWAP)，而我现在的学生腾讯云服务器仅仅是1G内存，1核的CPU… 以后如果装备上去了 ，肯定会直接上gitlab. 一步到位。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;需求&quot;&gt;需求&lt;/h2&gt;
&lt;p&gt;一些代码托管网站比如coding.net/github.com 等都提供了webhook的工具， 经常用于自动化部署。 之前写过一篇博客&lt;a href=&quot;http://shomy.top/2016/04/14/git-server-hook/&quot;&gt;搭建git服务器以及web自动化部署&lt;/a&gt; 主要记录的是在自己服务器上如何搭建git服务，并且利用hook进行自动化部署。不过这种方式有个缺点，没有像github或者coding.net的web界面，管理起来可能稍微麻烦，因此我们还是习惯在github,coding.net上进行代码托管。然后再部署到服务器上，这样有些不方便。还好这些托管网站均提供了webhook的工具，功能与git的hook一致。
    
    </summary>
    
      <category term="工具使用" scheme="http://shomy.top/categories/%E5%B7%A5%E5%85%B7%E4%BD%BF%E7%94%A8/"/>
    
    
      <category term="hook" scheme="http://shomy.top/tags/hook/"/>
    
  </entry>
  
  <entry>
    <title>Python中tuple+=赋值的四个问题</title>
    <link href="http://shomy.top/2017/08/17/python-tuple-assign/"/>
    <id>http://shomy.top/2017/08/17/python-tuple-assign/</id>
    <published>2017-08-17T14:34:00.000Z</published>
    <updated>2018-09-29T03:52:04.760Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>最近偶尔翻看Fluent Python，遇到有意思的东西就记下来. 下面的是在PyCon2013上提出的一个关于<code>tuple</code>的Augmented Assignment也就是增量赋值的一个问题。 并且基于此问题, 又引申出3个变种问题.</p>
</blockquote>
<h2 id="问题">问题</h2>
<p>首先看第一个问题, 如下面的代码段: <figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">&gt;&gt;&gt; t = (<span class="number">1</span>,<span class="number">2</span>, [<span class="number">30</span>,<span class="number">40</span>])</div><div class="line">&gt;&gt;&gt; t[<span class="number">2</span>] += [<span class="number">50</span>,<span class="number">60</span>]</div></pre></td></tr></table></figure></p>
<a id="more"></a>
<p>会产生什么结果呢？ 给出了四个选项: 1. <code>t</code> 变成 <code>[1,2, [30,40,50,60]</code> 2. <code>TypeError is raised with the message 'tuple' object does not support item assignment</code> 3. Neither 1 nor 2 4. Both 1 and 2</p>
<p>按照之前的理解, <code>tuple</code>里面的元素是不能被修改的，因此会选<code>2</code>. 如果真是这样的话，这篇笔记就没必要了，Fluent Python中也就不会拿出一节来讲了。 正确答案是<code>4</code> <figure class="highlight ruby"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="meta">&gt;&gt;</span>&gt; t = (<span class="number">1</span>,<span class="number">2</span>,[<span class="number">30</span>,<span class="number">40</span>])</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; t[<span class="number">2</span>] += [<span class="number">50</span>,<span class="number">60</span>]</div><div class="line">Traceback (most recent call last)<span class="symbol">:</span></div><div class="line">  File <span class="string">"&lt;stdin&gt;"</span>, line <span class="number">1</span>, <span class="keyword">in</span> &lt;<span class="class"><span class="keyword">module</span>&gt;</span></div><div class="line"><span class="symbol">TypeError:</span> <span class="string">'tuple'</span> object does <span class="keyword">not</span> support item assignment</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; t</div><div class="line">(<span class="number">1</span>, <span class="number">2</span>, [<span class="number">30</span>, <span class="number">40</span>, <span class="number">50</span>, <span class="number">60</span>])</div></pre></td></tr></table></figure></p>
<p>问题来了，为什么异常都出来了， <code>t</code>还是变了? 再看第二种情况，稍微变化一下,将<code>+=</code>变为<code>=</code>: <figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">&gt;&gt;&gt; t = (<span class="number">1</span>,<span class="number">2</span>, [<span class="number">30</span>,<span class="number">40</span>])</div><div class="line">&gt;&gt;&gt; t[<span class="number">2</span>] = [<span class="number">50</span>,<span class="number">60</span>]</div></pre></td></tr></table></figure></p>
<p>结果就成酱紫了: <figure class="highlight ruby"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="meta">&gt;&gt;</span>&gt; t = (<span class="number">1</span>,<span class="number">2</span>, [<span class="number">30</span>,<span class="number">40</span>])</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; t[<span class="number">2</span>] = [<span class="number">50</span>,<span class="number">60</span>]</div><div class="line">Traceback (most recent call last)<span class="symbol">:</span></div><div class="line">  File <span class="string">"&lt;stdin&gt;"</span>, line <span class="number">1</span>, <span class="keyword">in</span> &lt;<span class="class"><span class="keyword">module</span>&gt;</span></div><div class="line"><span class="symbol">TypeError:</span> <span class="string">'tuple'</span> object does <span class="keyword">not</span> support item assignment</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; t</div><div class="line">(<span class="number">1</span>, <span class="number">2</span>, [<span class="number">30</span>, <span class="number">40</span>])</div></pre></td></tr></table></figure></p>
<p>再看第三种情况,只把<code>+=</code>换为<code>extend</code>或者<code>append</code>,: <figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">&gt;&gt;&gt; t = (<span class="number">1</span>, <span class="number">2</span>, [<span class="number">30</span>,<span class="number">40</span>])</div><div class="line">&gt;&gt;&gt; t[<span class="number">2</span>].extend([<span class="number">50</span>,<span class="number">60</span>])</div><div class="line">&gt;&gt;&gt; t</div><div class="line">(<span class="number">1</span>, <span class="number">2</span>, [<span class="number">30</span>, <span class="number">40</span>, <span class="number">50</span>, <span class="number">60</span>])</div><div class="line">&gt;&gt;&gt; t[<span class="number">2</span>].append(<span class="number">70</span>)</div><div class="line">&gt;&gt;&gt; t</div><div class="line">(<span class="number">1</span>, <span class="number">2</span>, [<span class="number">30</span>, <span class="number">40</span>, <span class="number">50</span>, <span class="number">60</span>, <span class="number">70</span>])</div></pre></td></tr></table></figure></p>
<p>又正常了,没抛出异常?</p>
<p>最后第四种情况, 用变量的形式: <figure class="highlight ruby"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line"><span class="meta">&gt;&gt;</span>&gt; a = [<span class="number">30</span>,<span class="number">40</span>]</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; t = (<span class="number">1</span>, <span class="number">2</span>, a)</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; a+=[<span class="number">50</span>,<span class="number">60</span>]</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; a</div><div class="line">[<span class="number">30</span>, <span class="number">40</span>, <span class="number">50</span>, <span class="number">60</span>]</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; t</div><div class="line">(<span class="number">1</span>, <span class="number">2</span>, [<span class="number">30</span>, <span class="number">40</span>, <span class="number">50</span>, <span class="number">60</span>])</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; t[<span class="number">2</span>] += [<span class="number">70</span>,<span class="number">80</span>]</div><div class="line">Traceback (most recent call last)<span class="symbol">:</span></div><div class="line">  File <span class="string">"&lt;stdin&gt;"</span>, line <span class="number">1</span>, <span class="keyword">in</span> &lt;<span class="class"><span class="keyword">module</span>&gt;</span></div><div class="line"><span class="symbol">TypeError:</span> <span class="string">'tuple'</span> object does <span class="keyword">not</span> support item assignment</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; t</div><div class="line">(<span class="number">1</span>, <span class="number">2</span>, [<span class="number">30</span>, <span class="number">40</span>, <span class="number">50</span>, <span class="number">60</span>, <span class="number">70</span>, <span class="number">80</span>])</div></pre></td></tr></table></figure></p>
<p>又是一种情况, 下面就探究一下其中的原因.</p>
<h2 id="原因">原因</h2>
<p>首先需要重温<code>+=</code>这个运算符,如<code>a+=b</code>:</p>
<ul>
<li>对于可变对象(mutable object)如<code>list</code>, <code>+=</code>操作的结果会直接在<code>a</code>对应的变量进行修改，而<code>a</code>对应的地址不变.</li>
<li>对于不可变对象(imutable object)如<code>tuple</code>, <code>+=</code>则是等价于<code>a = a+b</code> 会产生新的变量，然后绑定到<code>a</code>上而已.</li>
</ul>
<p>如下代码段, 可以看出来: <figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div></pre></td><td class="code"><pre><div class="line">&gt;&gt;&gt; a = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]</div><div class="line">&gt;&gt;&gt; id(a)</div><div class="line"><span class="number">53430752</span></div><div class="line">&gt;&gt;&gt; a+=[<span class="number">4</span>,<span class="number">5</span>]</div><div class="line">&gt;&gt;&gt; a</div><div class="line">[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>]</div><div class="line">&gt;&gt;&gt; id(a)</div><div class="line"><span class="number">53430752</span> # 地址没有变化</div><div class="line">&gt;&gt;&gt; b = (<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>)</div><div class="line">&gt;&gt;&gt; id(b)</div><div class="line"><span class="number">49134888</span></div><div class="line">&gt;&gt;&gt; b += (<span class="number">4</span>,<span class="number">5</span>)</div><div class="line">&gt;&gt;&gt; b</div><div class="line">(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>)</div><div class="line">&gt;&gt;&gt; id(b)</div><div class="line"><span class="number">48560912</span> # 地址变化了</div></pre></td></tr></table></figure></p>
<p>此外还需要注意的是, python中的<code>tuple</code>作为不可变对象, 也就是我们平时说的元素不能改变, 实际上从报错信息<code>TypeError: 'tuple' object does not support item assignment</code>来看, 更准确的说法是指其中的元素不支持赋值操作<code>=</code>(<strong>assignment</strong>).</p>
<p>先看最简单的第二种情况, 它的结果是符合我们的预期, 因为<code>=</code>产生了<code>assign</code>的操作.(在<a href="http://shomy.top/2016/03/01/python-namespace-1/">由一个例子到python的名字空间</a> 中指出了赋值操作<code>=</code>就是创建新的变量), 因此<code>s[2]=[50,60]</code>就会抛出异常.</p>
<p>再看第三种情况,包含<code>extend/append</code>的, 结果tuple中的列表值发生了变化,但是没有异常抛出. 这个其实也相对容易理解. 因为我们知道<code>tuple</code>中存储的其实是元素所对应的地址(id), 因此如果没有赋值操作且tuple中的元素的<code>id</code>不变,即可,而<code>list.extend/append</code>只是修改了列表的元素,而列表本身id并没有变化,看看下面的例子: <figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">&gt;&gt;&gt; a=(<span class="number">1</span>,<span class="number">2</span>,[<span class="number">30</span>,<span class="number">40</span>])</div><div class="line">&gt;&gt;&gt; id(a[<span class="number">2</span>])</div><div class="line"><span class="number">140628739513736</span></div><div class="line">&gt;&gt;&gt; a[<span class="number">2</span>].extend([<span class="number">50</span>,<span class="number">60</span>])</div><div class="line">&gt;&gt;&gt; a</div><div class="line">(<span class="number">1</span>, <span class="number">2</span>, [<span class="number">30</span>, <span class="number">40</span>, <span class="number">50</span>, <span class="number">60</span>])</div><div class="line">&gt;&gt;&gt; id(a[<span class="number">2</span>])</div><div class="line"><span class="number">140628739513736</span></div></pre></td></tr></table></figure></p>
<p>目前解决了第二个和第三个问题, 先梳理一下, 其实就是两点:</p>
<ul>
<li>tuple内部的元素不支持赋值操作</li>
<li>在第一条的基础上, 如果元素的<code>id</code>没有变化, 元素其实是可以改变的.</li>
</ul>
<p>现在再来看最初的第一个问题: <code>t[2] += [50,60]</code> 按照上面的结论, 不应该抛异常啊,因为在我们看来<code>+=</code> 对于可变对象<code>t[2]</code>来说, 属于<code>in-place</code>操作,也就是直接修改自身的内容, <code>id</code>并不变, 确认下id并没有变化: <figure class="highlight ruby"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div></pre></td><td class="code"><pre><div class="line"><span class="meta">&gt;&gt;</span>&gt; a=(<span class="number">1</span>,<span class="number">2</span>,[<span class="number">30</span>,<span class="number">40</span>])</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; id(a[<span class="number">2</span>])</div><div class="line"><span class="number">140628739587392</span></div><div class="line"><span class="meta">&gt;&gt;</span>&gt; a[<span class="number">2</span>]+=[<span class="number">50</span>,<span class="number">60</span>]</div><div class="line">Traceback (most recent call last)<span class="symbol">:</span></div><div class="line">  File <span class="string">"&lt;stdin&gt;"</span>, line <span class="number">1</span>, <span class="keyword">in</span> &lt;<span class="class"><span class="keyword">module</span>&gt;</span></div><div class="line"><span class="symbol">TypeError:</span> <span class="string">'tuple'</span> object does <span class="keyword">not</span> support item assignment</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; a</div><div class="line">(<span class="number">1</span>, <span class="number">2</span>, [<span class="number">30</span>, <span class="number">40</span>, <span class="number">50</span>, <span class="number">60</span>])</div><div class="line"><span class="meta">&gt;&gt;</span>&gt; id(a[<span class="number">2</span>]) <span class="comment"># ID 并没有发生改变</span></div><div class="line"><span class="number">140628739587392</span></div></pre></td></tr></table></figure></p>
<p>跟第三个问题仅仅从<code>t[2].extend</code>改成了<code>t[2]+=</code>, 就抛出异常了,所以问题应该是出在<code>+=</code>上了. 下面用<code>dis</code>模块看看它俩执行的步骤: 对下面的代码块执行<code>dis</code>: <figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">t = (<span class="number">1</span>,<span class="number">2</span>, [<span class="number">30</span>,<span class="number">40</span>])</div><div class="line">t[<span class="number">2</span>] += [<span class="number">50</span>,<span class="number">60</span>]</div><div class="line">t[<span class="number">2</span>].extend([<span class="number">70</span>, <span class="number">80</span>])</div></pre></td></tr></table></figure></p>
<p>执行<code>python -m dis test.py</code>,结果如下，下面只保留第2,3行代码的执行过程，以及关键步骤的注释如下: <figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div></pre></td><td class="code"><pre><div class="line"><span class="number">2</span>          <span class="number">21</span> LOAD_NAME                <span class="number">0</span> (t)</div><div class="line">           <span class="number">24</span> LOAD_CONST               <span class="number">1</span> (<span class="number">2</span>)</div><div class="line">           <span class="number">27</span> DUP_TOPX                 <span class="number">2</span></div><div class="line">           <span class="number">30</span> BINARY_SUBSCR							</div><div class="line">           <span class="number">31</span> LOAD_CONST               <span class="number">4</span> (<span class="number">50</span>)</div><div class="line">           <span class="number">34</span> LOAD_CONST               <span class="number">5</span> (<span class="number">60</span>)</div><div class="line">           <span class="number">37</span> BUILD_LIST               <span class="number">2</span>			 </div><div class="line">           <span class="number">40</span> INPLACE_ADD</div><div class="line">           <span class="number">41</span> ROT_THREE</div><div class="line">           <span class="number">42</span> STORE_SUBSCR</div><div class="line"></div><div class="line"><span class="number">3</span>          <span class="number">43</span> LOAD_NAME                <span class="number">0</span> (t)</div><div class="line">           <span class="number">46</span> LOAD_CONST               <span class="number">1</span> (<span class="number">2</span>)</div><div class="line">           <span class="number">49</span> BINARY_SUBSCR</div><div class="line">           <span class="number">50</span> LOAD_ATTR                <span class="number">1</span> (extend)</div><div class="line">           <span class="number">53</span> LOAD_CONST               <span class="number">6</span> (<span class="number">70</span>)</div><div class="line">           <span class="number">56</span> LOAD_CONST               <span class="number">7</span> (<span class="number">80</span>)</div><div class="line">           <span class="number">59</span> BUILD_LIST               <span class="number">2</span></div><div class="line">           <span class="number">62</span> CALL_FUNCTION            <span class="number">1</span></div><div class="line">           <span class="number">65</span> POP_TOP</div><div class="line">           <span class="number">66</span> LOAD_CONST               <span class="number">8</span> (None)</div><div class="line">           <span class="number">69</span> RETURN_VALUE</div></pre></td></tr></table></figure></p>
<p>解释一下关键的语句:</p>
<ul>
<li><code>30 BINARY_SUBSCR</code>: 表示将<code>t[2]</code>的值放在TOS(Top of Stack)，这里是指<code>[30, 40]</code>这个列表</li>
<li><code>40 INPLACE_ADD</code>: 表示<code>TOS += [50,60]</code> 执行这一步是可以成功的，修改了TOS的列表为<code>[30,40,50,60]</code></li>
<li><code>42 STORE_SUBSCR</code>: 表示<code>s[2] = TOS</code> 问题就出在这里了，这里产生了一个<strong>赋值操作</strong>，因此会抛异常！但是上述对列表的修改已经完成, 这也就解释了开篇的第一个问题。</li>
</ul>
<p>再看<code>extend</code>的过程，前面都一样，只有这一行:</p>
<ul>
<li><code>62 CALL_FUNCTION</code>: 这个直接调用内置extend函数完成了对原列表的修改，其中并没有<code>assign</code>操作，因此可以正常执行。</li>
</ul>
<p>现在逐渐清晰了， 换句话说，<code>+=</code><strong>并不是原子操作</strong>，相当于下面的两步: <figure class="highlight lsl"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">t[<span class="number">2</span>].extend([<span class="number">50</span>,<span class="number">60</span>])</div><div class="line">t[<span class="number">2</span>] = t[<span class="number">2</span>]</div></pre></td></tr></table></figure></p>
<p>第一步可以正确执行，但是第二步有了<code>=</code>，肯定会抛异常的。 同样这也可以解释在使用<code>+=</code>的时候，为何<code>t[2]</code>的<code>id</code>明明没有变化，但是仍然抛出异常了。</p>
<p>现在用一句话总结下:</p>
<blockquote>
<p>tuple中元素不支持<code>assign</code>操作，但是对于那些是可变对象的元素如列表，字典等，在没有<code>assign</code>操作的基础上，比如一些<code>in-place</code>操作，是可以修改内容的</p>
</blockquote>
<p>可以用第四个问题来简单验证一下，使用一个指向<code>[30,40]</code>的名称<code>a</code>来作为元素的值，然后对<code>a</code>做<code>in-place</code>的修改，其中并没有涉及到对tuple的<code>assign</code>操作，那肯定是正常执行的。</p>
<h2 id="总结">总结</h2>
<p>这个问题其实以前也就遇到过，但是没想过具体的原理，后来翻书的时候又看到了， 于是花了点时间把这一个系列查了部分资料以及结合自己的理解都整理了出来, 算是饭后茶点吧, 不严谨的地方烦请指出.</p>
<p>部分参考如下:</p>
<ul>
<li><a href="http://bugs.python.org/issue11562" target="_blank" rel="external">python bugs</a></li>
<li><a href="https://docs.python.org/2/faq/programming.html#why-does-a-tuple-i-item-raise-an-exception-when-the-addition-works" target="_blank" rel="external">python faq</a></li>
<li><a href="https://stackoverflow.com/questions/10397121/why-does-of-a-list-within-a-python-tuple-raise-typeerror-but-modify-the-list" target="_blank" rel="external">stackoverflow</a></li>
<li>Fluent Python</li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;最近偶尔翻看Fluent Python，遇到有意思的东西就记下来. 下面的是在PyCon2013上提出的一个关于&lt;code&gt;tuple&lt;/code&gt;的Augmented Assignment也就是增量赋值的一个问题。 并且基于此问题, 又引申出3个变种问题.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id=&quot;问题&quot;&gt;问题&lt;/h2&gt;
&lt;p&gt;首先看第一个问题, 如下面的代码段: &lt;figure class=&quot;highlight lsl&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;2&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;&amp;gt;&amp;gt;&amp;gt; t = (&lt;span class=&quot;number&quot;&gt;1&lt;/span&gt;,&lt;span class=&quot;number&quot;&gt;2&lt;/span&gt;, [&lt;span class=&quot;number&quot;&gt;30&lt;/span&gt;,&lt;span class=&quot;number&quot;&gt;40&lt;/span&gt;])&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&amp;gt;&amp;gt;&amp;gt; t[&lt;span class=&quot;number&quot;&gt;2&lt;/span&gt;] += [&lt;span class=&quot;number&quot;&gt;50&lt;/span&gt;,&lt;span class=&quot;number&quot;&gt;60&lt;/span&gt;]&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;&lt;/p&gt;
    
    </summary>
    
      <category term="编程语言" scheme="http://shomy.top/categories/%E7%BC%96%E7%A8%8B%E8%AF%AD%E8%A8%80/"/>
    
    
      <category term="python" scheme="http://shomy.top/tags/python/"/>
    
  </entry>
  
  <entry>
    <title>Hexo-admin后台管理博客</title>
    <link href="http://shomy.top/2017/07/29/Hexo-admin-for-blogs/"/>
    <id>http://shomy.top/2017/07/29/Hexo-admin-for-blogs/</id>
    <published>2017-07-29T07:38:00.000Z</published>
    <updated>2018-09-29T03:52:04.756Z</updated>
    
    <content type="html"><![CDATA[<h2 id="需求">需求</h2>
<p>像Hexo这种静态博客用起来很方便，但是硬伤就是没有后台管理，每次想写博客或者说想要修改原博客的话，都需要在本机上找markdown文件，修改之后发布，尤其是如果机器上没有安装hexo,git等工具的时候，就无法修改了。因此hexo可不可以以像wp,ghost这种有后台管理, 随时随地编辑博客呢? <a id="more"></a></p>
<p>正巧hexo专门有一个插件<a href="https://jaredforsyth.com/hexo-admin/" target="_blank" rel="external">hexo-admin</a> 提供了在web UI下增删改查博客的功能，我也正好有一个VPS, 于是就有了下面的一种方法.</p>
<h2 id="解决方法">解决方法</h2>
<p>首先说一下条件与需求。 我自己有一个的VPS，本来是可以直接将hexo部署到VPS的，但是考虑到自己维护的VPS的稳定性等问题，最后决定将hexo的静态页面部署github-pages上。</p>
<p>我的目的有两个:</p>
<ul>
<li>拥有在线编辑博客的后台</li>
<li>本地机器同时也可以编辑发布博客</li>
<li>二者需要保持同步</li>
</ul>
<p>基于这样的需求，仅仅靠github pages显然不可行了,必须借助VPS了.保持同步的意思是VPS的内容与本地机器上的博客的资源要保持一致。就是说我在线修改了某篇博客，然后之后如果可以很简单的同步到本地，使得两端保持同步，反过来一样。</p>
<p>我的做法是:</p>
<ul>
<li>将hexo目录下的相关文件包括_config.yml,source/等文件建立一个私人repo(个人使用coding.net)</li>
<li>VPS上安装好hexo,具体过程与换台机器安装同理, 可参考这篇博客<a href="http://shomy.top/2017/01/27/hexo-migrate-solution/">hexo迁移重装</a>。</li>
<li>VPS上安装hexo-admin插件，这样可以在线编辑了. 在这一步的时候, 为了方便,我用了一个子域名以及在VPS上用nginx做前段端口转发.</li>
</ul>
<p>到这里后台管理基本没有问题了. 接下来就是在线发布了. 正巧的是hexo-admin提供了这个功能, 有一个Deploy的功能,具体实现是使用nodejs的<code>spawn</code>函数来执行发布脚本的. 一开始不太懂<code>spawn</code>这个函数的使用, 就直接在deploy中随便输入了东西,结果各种 spawn err. 后来查过资料,才知道deploy的输入的位置其实是对<code>hexo-admin</code>的配置里面<code>deployCommand</code>的选项,可以为空. 打个比方,如果我在<code>deployCommand</code>写的是<code>./hexo-deploy</code>, 然后点击deploy按钮之后, 后台就可以执行<code>hexo-deploy</code>脚本的内容了. 有了这个接口就非常灵活了. 因为基于此, 我们不但来发布博客,甚至可以把同步的脚本加进去.具体参考 https://github.com/jaredly/hexo-admin/issues/70</p>
<p>下面说一下发布以及同步的操作:</p>
<ul>
<li>在博客目录写一个有关git同步的小脚本:<code>server-upload.sh</code> 用来同步VPS目录下的变化，主要代码其实就是先<code>git pull</code> 拉取repo最新, 之后在push. 具体脚本代码不再赘述.</li>
<li><p>建一个<code>hexo-deploy</code>的脚本,内容: <figure class="highlight stata"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">hexo <span class="keyword">g</span> &amp;&amp; hexo <span class="built_in">d</span></div><div class="line">./server-upload.<span class="keyword">sh</span></div></pre></td></tr></table></figure></p></li>
<li><p>利用hexo-admin的deploy功能，可以在线发布，注意修改<code>_config.yml</code>: 加上hexo-admin的admin选项, 加一个<code>deployCommand: ./hexo-deploy</code>的字段,如下: <figure class="highlight less"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="attribute">admin</span>:</div><div class="line">	<span class="attribute">username</span>: XXX</div><div class="line">    <span class="attribute">password</span>: XXXXX</div><div class="line">    <span class="attribute">deployCommand</span>: <span class="string">'./hexo-deploy'</span></div></pre></td></tr></table></figure></p></li>
<li>编辑完博客时候，发布的时候，需要点击hexo-admin的deploy。这样在后台就可以执行前面定义的<code>hexo-deloy</code>脚本了</li>
<li><p>本地机器也需要有一个<code>upload.sh</code>的脚本, 每次同样需要先git pull 在git push 到私人repo. 其实就是版本控制那一套.</p></li>
</ul>
<p>到现在基本完成了自己可以随时随地编辑博客并且多端同步的需求了. 之前也考虑过CI持续集成,但是大部分都是再建立一个分支, 需要把博客的source公开, 不太合适. 所以就选择了hexo-admin这种方法.</p>
<p>这篇博客便是在线完成的. <img src="http://cdn.htliu.cn/blog/hexo-admin/hexo-admin.png"></p>
<p>其实这里面的坑还是蛮多的(主要不熟悉node). 此外开始使用supervisor做后台运行监控, 结果不知为何, 在<code>hexo d</code>包括git的pull或者push等操作,都会报错,提示让我确认git用户, 但是我已经设置了<code>git config</code>的email和name. 查了很多资料也没有解决, 后来直接抛弃supervisor, 直接使用<code>nohup</code>来放后台运行了.每个人的需求肯定都不同，这里只是个人的一个思路。大概是这样了, 有什么问题可以留言或者邮件.</p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;需求&quot;&gt;需求&lt;/h2&gt;
&lt;p&gt;像Hexo这种静态博客用起来很方便，但是硬伤就是没有后台管理，每次想写博客或者说想要修改原博客的话，都需要在本机上找markdown文件，修改之后发布，尤其是如果机器上没有安装hexo,git等工具的时候，就无法修改了。因此hexo可不可以以像wp,ghost这种有后台管理, 随时随地编辑博客呢?
    
    </summary>
    
      <category term="工具使用" scheme="http://shomy.top/categories/%E5%B7%A5%E5%85%B7%E4%BD%BF%E7%94%A8/"/>
    
    
      <category term="hexo" scheme="http://shomy.top/tags/hexo/"/>
    
  </entry>
  
  <entry>
    <title>Word2vec数学原理全家桶</title>
    <link href="http://shomy.top/2017/07/28/word2vec-all/"/>
    <id>http://shomy.top/2017/07/28/word2vec-all/</id>
    <published>2017-07-28T07:42:21.000Z</published>
    <updated>2020-01-11T07:54:37.346Z</updated>
    
    <content type="html"><![CDATA[<p>目前需要做Network Embedding方面的课题，而复杂网络本身就经常借鉴NLP的一些算法模型, Embedding也不例外. 因此先从Word Embedding入手。之前对Word Embedding(暂且翻译为词嵌入或者词向量)的理解就是将单词根据某种特征转为数值向量，再来做其他工作比如文本分类的工作。而word2vec则是word embedding的一种模型，也是目前使用最广的词向量模型, 由Google的Mikolov团队2013年提出。之前仅仅能够使用第三方库来训练直接使用, 对其中原理并没有多少理解, 这篇博客则比较完整的从背景知识到原理，参数训练等方面整理一下word2Vec。</p>
<a id="more"></a>
<p>Mikolov的两篇文章中涉及word2vec的细节甚少. 有不少人都对此模型作出了更加详细的解释, 本文主要沿着Rong, X.word2vec Parameter Learning Explained这篇文章的思路来整理一下，很多公式参考于这篇文章。</p>
<p>参考:</p>
<ul>
<li>Mikolov, T.(2013). Distributed Representations of Words and Phrases and their Compositionality.</li>
<li>Mikolov, T.(2013). Efficient Estimation of Word Representations in Vector Space.</li>
<li>Rong, X. (2014). word2vec Parameter Learning Explained.</li>
</ul>
<h2 id="背景">背景</h2>
<h3 id="神经网络">神经网络</h3>
<p>引入word2vec之前需要先对神经网络的知识有一定了解, 这里只贴一张图说明一个简单三层神经网络, 具体的细节不再赘述, 可以参考之前的一篇专门介绍神经网络的博文<a href="http://shomy.top/2016/12/07/basic-neural-network/">机器学习技法笔记(1)-神经网络</a> ; 如下图, <span class="math inline">\(x=[x_1, x_2,...x_K]\)</span>是模型的输入, 中间经过与权重矩阵<span class="math inline">\(\{w_{ki}\}\)</span> 运算, 矩阵运算结果再经过非线性激活函数得到隐层的结果<span class="math inline">\(h\)</span>, 从隐层到输出层同理. 这样从输入层到输出层既有线性变换,又有非线性变换, 因此可以更好刻画出输入变量的特征.</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/w2v/nn1.png">

</div>
<h3 id="神经语言模型">神经语言模型</h3>
<p>作为Word Embedding的背景, 语言模型(Language Model)也是很有必要简要介绍一下.</p>
<blockquote>
<p>统计语言模型就是用来计算一个句子的概率分布</p>
</blockquote>
<p>简单来说,就是计算一个句子的概率, 语言模型用处很广泛,比如机器翻译中, 如何挑选一个概率尽可能大的句子也就是尽量靠谱的句子返回. 假设一个长度为<span class="math inline">\(m\)</span>的句子,包含词:<span class="math inline">\([(w_1, w_2, w_3,..,w_m)\)</span>, 那么这个句子的概率,也就是这m个词共现的概率:</p>
<p><span class="math display">\[P(sen=(w_1, w_2, .., w_m)) = P(w_1)P(w_2|w_1)P(w_3|w_2,w_1)...P(w_m|w_{m-1}..w_1)\]</span></p>
<p>一般情况, 语言模型都是为了使得条件概率: <span class="math inline">\(P(w_t|w_1,w_2,..,w_{t-1})\)</span>最大化, 不过考虑到近因效应, 当前词与距离它比较近的n个词更加相关(一般n不超过5),而非前面所有的词都有关, 因此上述公式可以近似为:</p>
<p><span class="math display">\[P(w_t|w_1,w_2,..,w_{t-1})=P(w_t|w_{t-1}, w_{t-2}...w_{t-(n+1)})\]</span></p>
<p>上述便是经典的<code>n-gram</code>模型的近似表示方式. 下面需要介绍一下神经语言模型(NNLM), 最初由Bengio提出的A Neural Probabilistic Language Mode最为经典, word2vec便是从其中简化训练而来. Bengio通过下面的一个三层神经网络来计算<span class="math inline">\(P(w_t|w_{t-1}, w_{t-2}...w_{t-n(+1)})\)</span>:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/w2v/nnlm1.png">

</div>
<p>首先第一层输入就是前<span class="math inline">\(n-1\)</span>个词<span class="math inline">\(w_{t-(n+1)},...,w_{t-1}\)</span> 去预测第<span class="math inline">\(t\)</span>个词是<span class="math inline">\(w_t\)</span> 的概率. 这里面的矩阵<span class="math inline">\(C\in |V| \times d\)</span>维护着词汇表中所有词的词向量, 其中<span class="math inline">\(|V|\)</span>是词汇表中词的个数, <span class="math inline">\(d\)</span>是词向量的维度. 然后根据输入的前<span class="math inline">\(n-1\)</span>个词, 在C中找到它们对应的词向量, 然后直接串联起来成为一个维度为<span class="math inline">\((n-1)d\)</span>的向量x 作为接下来三层神经网络的输入, 后面就是普通神经网络了. 需要说明的是,因为我们那要预测概率最大的<span class="math inline">\(w_t\)</span>, 因此最后输出层的神经元应该与词汇表大小同样为<span class="math inline">\(|V|\)</span>, 这里使用使用softmax函数归一化输出层的值到[0,1], 代表可能的每个词的概率. 此外在原文中, 存在一些直连边, 也就是上图中的虚线, 从输入层直接到输出层, 是一个线性变换, Bingo在文中表示, 直连边的存在大幅降低迭代次数, 但对语言模型效果无提升, 随着计算能力的提高, 后续的工作基本都去掉了直连边.</p>
<p>神经语言模型构建完成之后,就是训练参数了. 这里的参数包括词向量矩阵<span class="math inline">\(C\)</span>, 以及三层神经网络的权重, 偏置等参数. 训练数据就是大堆大堆的预料库. 训练结束之后, 语言模型得到了, 词向量也得到了. 换言之, 词向量是这个语言模型的副产品. 但是这个模型的缺点就是速度问题, 因为词汇表往往很大,几十万几百王, 训练起来就很耗时, Bengo仅仅训练5个epoch就花了3周, 这还是40个CPU并行训练的结果. 因此才会有了后续好多的优化工作, word2vec便是其中一个.</p>
<h2 id="word2vec">Word2Vec</h2>
<h3 id="简介">简介</h3>
<p>背景介绍完毕, 终于到主角了. word2vec是google于2013年的Distributed Representations ofWords and Phrases and their Compositionality 以及后续的Distributed Representations ofWords and Phrases and their Compositionality 两篇文章中提出的一种高效训练词向量的模型, 基本出发点是<strong>上下文相似的两个词,它们的词向量也应该相似</strong>, 比如香蕉和梨在句子中可能经常出现在相同的上下文中，因此这两个词的表示向量应该就比较相似.</p>
<p>word2vec模型中比较重要的概念是词汇的上下文, 说白了就是一个词周围的词, 比如<span class="math inline">\(w_t\)</span>的范围为1的上下文就是<span class="math inline">\(w_{t-1}\)</span>和<span class="math inline">\(w_{t+1}\)</span>. 在word2vec中提出两个模型(假设上下文窗口为3)</p>
<ul>
<li>CBOW(Continuous Bag-of-Word): 以上下文词汇预测当前词: <span class="math inline">\(w_{t-1}, w_{t+1}\)</span> 去预测 <span class="math inline">\(w_t\)</span></li>
<li>SkipGram: 以当前词预测其上下文词汇: <span class="math inline">\(w_t\)</span> 去预测<span class="math inline">\(w_{t-1}, w_{t+1}\)</span></li>
</ul>
<p>两个模型图示如下</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/w2v/word2vec-1.png">

</div>
<p>下面将会从最简单的上下文只有一个词的情形入手, 然后扩展到CBOW以及Skip-gram, 介绍原理以及参数训练过程. 关于word2vec的训练这里将会从完全的BP神经网络的过程来介绍.</p>
<h3 id="one-word-model">One-Word Model</h3>
<p>首先先看简化版入手: 输入输出都只有一个词, 如下图示:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/w2v/one-word.png">

</div>
<p>首先说明符号:</p>
<ul>
<li><p><span class="math inline">\(V\)</span>: 词汇表长度; <span class="math inline">\(N\)</span>: 隐层神经元个数, 同时也是词向量维度</p></li>
<li><p><span class="math inline">\(W \in \mathcal{R}^{V\times N}\)</span>: 输入层到隐层的权重矩阵, 其实就是词向量矩阵,其中每一行代表一个词的词向量</p></li>
<li><p><span class="math inline">\(W^{&#39;} \in \mathcal{R}^{N\times V}\)</span>: 隐层到输出层的权重矩阵, 其中每一列也可以看作额外的一种词向量</p></li>
</ul>
<p>​</p>
<p>下面从神经网络的前向过程开始介绍:</p>
<p>我们需要做的是用输入的词去预测输出的词. 其中 输入层的单词<span class="math inline">\(w_I\)</span>使用<code>one-hot</code>来表示的, 即在上图中<span class="math inline">\(x_1, x_2, x_3,...,x_V\)</span>只有<span class="math inline">\(x_k\)</span>为1, 其余为0, 其中k可以是输入的词在词汇表中的索引下标. 之后就是经过词向量矩阵<span class="math inline">\(W\)</span> 连接输入层和隐层. 其中由于X中只有一个1, 因此经过与W相乘, 相当于取出W中的的第k行，实际也就是输入单词的<span class="math inline">\(w_I\)</span>的<span class="math inline">\(N\)</span>维的词向量，使用<span class="math inline">\(v_{w_I}\)</span>表示，来作为隐层的值，注意<strong>word2vec的隐层并没有激活函数</strong>:</p>
<p><span class="math display">\[\mathbf{h} = W^T \cdot X = v_{w_I}^T \]</span></p>
<p>然后考虑从隐层的<span class="math inline">\(\mathbf{h}\)</span>到输出层<span class="math inline">\(Y\)</span>, 同样h经过矩阵<span class="math inline">\(W^{&#39;}\)</span>相乘，得到一个<span class="math inline">\(V \times 1\)</span>的向量<span class="math inline">\(\mathbf{u}\)</span>:</p>
<p><span class="math display">\[\mathbf{u} = W^{&#39;T} \cdot h\]</span></p>
<p>其中<span class="math inline">\(u\)</span>每个元素<span class="math inline">\(u_j\)</span> 就是<span class="math inline">\(W^{&#39;}\)</span>的第<span class="math inline">\(j\)</span>列用<span class="math inline">\(v^{&#39;}_{w_j}\)</span>表示, 与<span class="math inline">\(h\)</span>做内积得到: <span class="math inline">\(u_j = v_{w_O}^{&#39;T}\cdot h\)</span>，含义就是词汇表中第<span class="math inline">\(j\)</span>个词的分数，我们的目的就是要根据输入词<span class="math inline">\(w_I\)</span>去预测输出的词，因此预测的词就取分数最高的即可，这里为了方便概率表示，使用<code>softmax</code>将<span class="math inline">\(\mathbf{u}\)</span>归一化到[0,1]之间, 从而作为输出词的概率, 其实是一个多项分布, 也就是上图中的<span class="math inline">\(y\)</span>:</p>
<p><span class="math display">\[P(w_j|w_I) = y_j = \frac{\exp(u_j)}{\sum\limits_{k\in V} \exp(u_k)} =   \frac{\exp(v_{w_j}^{&#39;T}\cdot v_{w_I})}{\sum\limits_{k\in V} \exp(v_{w_k}^{&#39;T}\cdot v_{w_I})}\]</span></p>
<p>其中<span class="math inline">\(v_{w}\)</span>与<span class="math inline">\(v_{w}^{&#39;}\)</span> 都称为词<span class="math inline">\(w\)</span>的词向量，一般使用前者作为词向量，而非后者，原因后续会解释。至此前向过程完成，就是给定一个词作为输入，来预测它的上下文词，还是比较简单的，属于简化版的神经语言模型。这个过程中需要用到的参数有两个词向量矩阵<span class="math inline">\(W, W^{&#39;}\)</span>，下面就是重点了，介绍如何根据语料库来训练模型，更新参数，得到最终的词向量。</p>
<hr>
<p>首先明确训练数据的格式，对于一个训练样本(<span class="math inline">\(w_I\)</span>, <span class="math inline">\(w_O\)</span>)，输入是词<span class="math inline">\(w_I\)</span>的<code>one-hot</code> 的维度为<span class="math inline">\(V\)</span>的向量<span class="math inline">\(\mathbf{x}\)</span>，模型预测的输出同样也是一个维度为<span class="math inline">\(V\)</span>的向量<span class="math inline">\(\mathbf{y}\)</span>, 同时真实值<span class="math inline">\(w_O\)</span>也是用one-hot表示，记为<span class="math inline">\(\mathbf{t}=[0,0,0,...1,0,0]\)</span>，其中假设<span class="math inline">\(t_{j^{*}} = 1\)</span>, 也就是说<span class="math inline">\(j^{*}\)</span>是真实单词在词汇表中的下标，那么根据最大似然或者上面的语言模型，目标函数可以定义如下:</p>
<p><span class="math display">\[\begin{align*} O &amp;= \max P(w_O|w_I) \\ &amp; = \max y_{j^{*}} :=  \max \log y_{j^{*}} \\ &amp;= \max \log(\frac{\exp(u_{j^{*}})}{\sum  \exp(u_k)}) = \max u_{j^{*}}-\log\sum_{k=1}^{V}\exp( u_k) \end{align*}\]</span></p>
<p>一般我们习惯于最小化损失函数，因此定义损失函数:</p>
<p><span class="math display">\[E = -u_{j^{*}}+\log\sum_{k=1}^{V}\exp( u_k) \]</span></p>
<p>然后结合反向传播一层层求梯度，使用梯度下降来更新参数。</p>
<p>先求隐层到输出层的向量矩阵<span class="math inline">\(W^{&#39;}\)</span>的梯度:</p>
<p><span class="math display">\[\frac{\partial E}{ \partial w{&#39;}_{ij}} = \frac{\partial E}{\partial u_j} \frac{\partial u_j}{\partial w^{&#39;}_{ij}} = (y_j-t_j) h_i\]</span></p>
<p>这里面的<span class="math inline">\(y_j\)</span>和<span class="math inline">\(t_j\)</span>分别是预测和真实值的第j项，<span class="math inline">\(h_i\)</span>是隐层的第<span class="math inline">\(i\)</span>项。</p>
<blockquote>
<p>考虑<span class="math inline">\(\frac{\partial E}{\partial u_j} = y_j - t_j\)</span>: 直接对原始求导，如下:</p>
<p>先考虑E的对数部分: <span class="math inline">\(\frac{\partial \log \sum\exp(u_k)}{\partial u_j} = \frac{\exp(u_j)}{\sum \exp(u_k)} = y_j\)</span></p>
<p>再看 <span class="math inline">\(-u_{j^{*}}\)</span> 对 <span class="math inline">\(u_j\)</span> 的梯度: 当<span class="math inline">\(j = j^{*}\)</span>时，<span class="math inline">\(\frac{\partial -u_{j^{*}}}{\partial u_j}=-1 = -t_j\)</span>, 反之二者不等时，<span class="math inline">\(\frac{\partial -u_{j^{*}}}{\partial u_j}=0= -t_j\)</span></p>
<p>所以综合求导<span class="math inline">\(\frac{\partial E}{\partial u_j} = y_j - t_j\)</span>. 这个减法可以理解为输出层的第<span class="math inline">\(j\)</span>项为预测值与真实值的差</p>
</blockquote>
<p>因此梯度下降更新公式为:</p>
<p><span class="math display">\[w^{&#39;}_{ij} = {w^{&#39;}_{ij}}^{(old)} -\eta (y_j - t_j) h_i\]</span></p>
<p>整合为<span class="math inline">\(W^{&#39;}\)</span>的列向量<span class="math inline">\(\mathbf{v^{&#39;}}_{w_j} = \{w^{&#39;}_{ij}| i=1,2,3,...,N\}\)</span>的形式如下:</p>
<p><span class="math display">\[\mathbf{v}^{&#39;}_{w_j} = {\mathbf{v}^{&#39;}_{w_j}}^{(old)} - \eta(y_j-t_j)\mathbf{h},  \ j \in \{ 1,2,3,...,V\}\]</span></p>
<p>也就是说对每个训练样本都需要做一次复杂度为<span class="math inline">\(V\)</span>的操作去更新<span class="math inline">\(W^{&#39;}\)</span>.</p>
<p>接着考虑隐层<span class="math inline">\(\mathbf{h}\)</span>的更新，其实也是输入层到隐层的矩阵<span class="math inline">\(W\)</span>的更新，继续反向传播，跟神经网络的相同, 输出层的<span class="math inline">\(V\)</span>个神经元都会影响<span class="math inline">\(h_i\)</span>:</p>
<p><span class="math display">\[\frac{\partial E}{\partial h_i} = \sum_{j=1}^{V}\frac{\partial E}{\partial u_j}  \frac{\partial u_j}{\partial h_i} = \sum_{j=1}^{V} (y_j-t_j)w^{&#39;}_{ij} =W^{&#39;}_{i} \cdot P\]</span></p>
<p>其中<span class="math inline">\(W^{&#39;}_{i}\)</span>是<span class="math inline">\(W^{&#39;}\)</span>的第i行, 这里为了方便书写, 令<span class="math inline">\(P = \{ y_j - t_j | j= 1,2,3,..,V\}\)</span>, 因此整合成整个隐层的向量<span class="math inline">\(\mathbf{h}\)</span>:</p>
<p><span class="math display">\[\frac{\partial E}{\partial \mathbf{h}} =  W^{&#39;} \cdot P\]</span></p>
<p>得到一个N维的向量，上面已经介绍过，<span class="math inline">\(\mathbf{h}\)</span>就是词向量矩阵<span class="math inline">\(\mathbf{W}\)</span>的一行: $ = W^T X = v_{w_I}^T <span class="math inline">\(, 但是因为X中只有一个1，因此每次只能更新\)</span><span class="math inline">\(的一行\)</span>v_{w_I}<span class="math inline">\(,其余行的梯度\)</span> = 0<span class="math inline">\(, 所以\)</span>v_{w_I}$的更新公式为:</p>
<p><span class="math display">\[v_{w_I}^T = v_{w_I}^T - \eta  W^{&#39;} \cdot P\]</span></p>
<p>到此为止， 一个训练样本的反向传播训练过程就为止了。 我们可以看到，对于输入层到隐层的矩阵<span class="math inline">\(W\)</span> 我们每次训练只需要更新一行向量即可，而对于隐层到输出层的矩阵<span class="math inline">\(W^{&#39;}\)</span>的所有<span class="math inline">\(N \times V\)</span>个元素都需要更新一遍，这里的计算量还是很大的。</p>
<p>这一节主要比较细致的介绍了最简单的输入输出只有一个单词的情况的推理和训练的过程，后面的CBOW(上下文预测单词)以及SG(单词预测上下文)均基于这一节扩展开来。</p>
<h3 id="cbow-model">CBOW Model</h3>
<p>这一部分讲word2vec的第一个形式: Continurous Bag-Of-Word，模型图示如下:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/w2v/cbow-1.png">

</div>
<p>跟上一个模型唯一的不同就是输入不再是一个词<span class="math inline">\(w_I\)</span>, 而是多个词，上图中一共有C个单词: <span class="math inline">\(x_{1k},x_{2k},...,x_{Ck}\)</span>，每个<span class="math inline">\(x\)</span>都是one-hot表示。 这样隐层的<span class="math inline">\(\mathbf{h}\)</span>的计算就会不同了: 之前一个单词的模型是直接取出<span class="math inline">\(W\)</span>的一行<span class="math inline">\(v_{w_I}\)</span>作为<span class="math inline">\(h\)</span>的值，在CBOW中则是取出<span class="math inline">\(W\)</span>中输入的所有C个单词的词向量，然后直接取平均，如下:</p>
<p><span class="math display">\[\begin{align*} \mathbf{h} &amp;= \frac{1}{C} W^T(x_{1} +x_2+...+x_{C}) \\ &amp;= \frac{1}{C}(v_{w_1} + v_{w_2}+...+v_{w_C})^T\end{align*}\]</span></p>
<p>后面隐层到输出层的过程与One-Word Model 一模一样，包括目标函数定义， 反向传播训练等。将<span class="math inline">\(W^{&#39;}\)</span>的更新公式照抄下来如下,依旧是每次都需要更新所有的行:</p>
<p><span class="math display">\[\mathbf{v}^{&#39;}_{w_j} = {\mathbf{v}^{&#39;}_{w_j}}^{(old)} - \eta(y_j-t_j)\mathbf{h},  \ j \in \{ 1,2,3,...,V\}\]</span></p>
<p>隐层神经元的梯度也相同:</p>
<p><span class="math display">\[\frac{\partial E}{\partial \mathbf{h}} =  W^{&#39;} \cdot P\]</span></p>
<p>下面考虑输入层到隐层稍微有些不同，在One-Word Model里面因为输入只有一个词，因此每次训练只更新这个词对应到<span class="math inline">\(W\)</span>的那一行，但是在CBOW里面有多个词，这里采取的策略是将<span class="math inline">\(\mathbf{h}\)</span>的梯度均摊到每个词上，因此每次训练会更新<span class="math inline">\(W\)</span>中的C行，如下:</p>
<p><span class="math display">\[v_{w_{I,c}}^T = v_{w_{I,c}}^T - \frac{1}{C}\ \eta  W^{&#39;} \cdot P,\ \ c=1,2,...,C\]</span></p>
<p>到此为止 CBOW 的推理和训练过程也介绍完毕，基本跟One-Word Model 一样。</p>
<h3 id="skipgram-model">SkipGram Model</h3>
<p>现在开始介绍word2vec的第二种形式: SkipGram(根据单词预测上下文)，这个模型与One-Word Model不同的地方在于，SG的输出有多个词，而非One-Word 中输出只有一个词，这样输出层就不是一个多项分布了，而是<span class="math inline">\(C\)</span>个多项分布了，模型图示如下:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/w2v/sg-1.png">

</div>
<p>因此从输入层到隐层部分与One-Word Model 相同，隐层神经元的计算方式如下:</p>
<p><span class="math display">\[\mathbf{h} = W^T \cdot X = v_{w_I}^T \]</span></p>
<p>因为输出层是有C个单词， 因此有C个多项分布: <span class="math inline">\(y_{1}, y_{2}...y_{C}\)</span>, 因此前向计算的过程也需要分开计算，如下公式，用来计算第<span class="math inline">\(c\)</span>个输出单词的预测的多项分布中第j项，相比One-Word Model 多了一个c参数:</p>
<p><span class="math display">\[P(w_{c,j}|w_I) = y_{c,j} = \frac{\exp (u_{c,j})}{\sum_{k=1}^{V} \exp(u_{c,k})}\]</span></p>
<p>需要主要的是这<span class="math inline">\(C\)</span>个输出向量是相互独立的，可以当做是独立的<span class="math inline">\(C\)</span>个One-Word Model 中的输出向量，相互之间没有影响，并且从图中也可以看出，连接隐层与C个输出层的参数矩阵<span class="math inline">\(W^{&#39;}\)</span>是共享的，于是便有: <span class="math inline">\(u_{c,j} = u_{j} ={v^{&#39;}_{w_j}}^T \cdot \mathbf{h}\)</span> 这里的<span class="math inline">\(v^{&#39;}_{w_j}\)</span>的含义与One Word Model 中相同，都代表<span class="math inline">\(W^{&#39;}\)</span>的第j列，同时也是词汇表中第j个单词的一种词向量(虽然实际中不用)。</p>
<p>从前向后 根据上述公式计算出C个输出向量之后，在每个<span class="math inline">\(V\)</span>维向量中选取概率最大的作为输出的单词，这样根据输出单词<span class="math inline">\(w_I\)</span> 就得到了<span class="math inline">\(C\)</span>个输出单词，也就达到了根据单词预测上下文的目的。</p>
<p>下面开始介绍SG的反向传播训练的过程，这个跟前面的有些许的不同, 首先是损失函数:</p>
<p><span class="math display">\[\begin{align*}E &amp;= -\log P(w_{1},w_{2},...,w_{C}| w_I) \\ &amp;=  - \log  \Pi_{c=1}^{C}P(w_c|w_i) \\ &amp;=-\log \Pi_{c=1}^{C} \frac{\exp (u_{c,j})}{\sum_{k=1}^{V} \exp(u_{c,k})} \\ &amp;= -\sum_{c=1}^{C}u_{j_c^{*}} + C \cdot \log \sum_{k=1}^{V} \exp(u_{k}) \end{align*}\]</span></p>
<p>前面说过输出的C个词是相互独立，因此<span class="math inline">\(P(w_1,w_2,...,w_C|W_I) = \Pi P(w_c|w_I)\)</span>, 此外<span class="math inline">\(j_c^{*}\)</span>的含义同One-Word Model 中的<span class="math inline">\(u_{j^{*}}\)</span> 一样，都代表训练的真实的输出单词在词汇表的下标。下面开始从后向前求梯度，对第c个词对应的多项分布的第j项的梯度:</p>
<p><span class="math display">\[\frac{\partial E}{\partial u_{c,j}} = y_{c,j} - t_{c,j}\]</span></p>
<p>然后考虑<span class="math inline">\(W^{&#39;}\)</span>的梯度，考虑到C个多项分布产生影响，因此需要求和:</p>
<p><span class="math display">\[\frac{\partial E}{\partial w^{&#39;}_{ij}}= \sum_{c=1}^{C}\frac{\partial E}{\partial u_{c,j}}  \frac{\partial u_{c,j}}{\partial w_{ij}^{&#39;}} =\sum_{c=1}^{C}(y_{c,j}-t_{c,j})\mathbf{h_{i}} = Q_j \mathbf{h_i}\]</span></p>
<p>跟CBOW一样，为了方便书写定义<span class="math inline">\(Q_j = \sum_{c=1}^{C}(y_{c,j} - t_{c,j}), \ j = 1,2,3,...,V\)</span>，矩阵Q的维度是$V1 $</p>
<p>有了梯度，就可以利用梯度下降 更新<span class="math inline">\(w_{ij}^{&#39;}\)</span>:</p>
<p><span class="math display">\[w_{i,j}^{&#39;}={w_{ij}^{&#39;}}^{(old)} - \eta Q_j\mathbf{h}_i\]</span></p>
<p>或者写成词向量的形式，其实就是<span class="math inline">\(W^{&#39;}\)</span>的一列:</p>
<p><span class="math display">\[v_{w_j}^{&#39;} = {v_{w_j}^{&#39;}}^{(old)} - \eta Q_j \mathbf{h}, \ j = 1,2,3,...,V\]</span></p>
<p>接着考虑对隐层神经元的梯度:</p>
<p><span class="math display">\[\begin{align*}\frac{\partial E}{\partial \mathbf{h}_i} &amp;=\sum_{c=1}^{C}\sum_{j=1}^{V} \frac{\partial E}{\partial u_{c,j}} \frac{\partial u_{c,j}}{\partial \mathbf{h}_i} \\&amp;=\sum_{c=1}^{C}\sum_{j=1}^{V}(y_{c,j}-t_{c,j}) w^{&#39;}_{ij} \\ &amp;= \sum_{j=1}^{V}Q_jw^{&#39;}_{i,j}=W^{&#39;}_{i} \cdot Q \end{align*}\]</span></p>
<p>因此跟One-Word Model一样整合成向量的形式:</p>
<p><span class="math display">\[\frac{\partial E}{\partial \mathbf{h}} = W^{&#39;} \cdot Q\]</span></p>
<p>考虑到输入只有一个词，因此跟One-Word Model 一样: <span class="math inline">\(\mathbf{h} = {v_{w_I}}^T\)</span>, 因此每次训练更新词向量矩阵<span class="math inline">\(W\)</span>的一行:</p>
<p><span class="math display">\[v_{w_I}^T = v_{w_I}^T - \eta  W^{&#39;} \cdot Q\]</span></p>
<p>到此SkipGram模型的前向推理与后向参数的训练也介绍完毕了。</p>
<h2 id="优化">优化</h2>
<h3 id="复杂度">复杂度</h3>
<p>前面的CBOW与SG模型是标准的word2vec模型，或者说是神经语言模型的简化版，去掉了隐层的激活函数，其余的变化不大，因此训练效率还是很低的。我们分析下训练的复杂度。首先明确需要学习的两个词向量矩阵<span class="math inline">\(W, W^{&#39;}\)</span>，从前面的推导中知道对于每一个训练样本，CBOW更新<span class="math inline">\(W\)</span>的<span class="math inline">\(C\)</span>行，SG更新<span class="math inline">\(W\)</span>其中一行，也就是每次更新有限个词的词向量。但是对于<span class="math inline">\(W^{&#39;}\)</span>则不同了，正如前面一直提到的，无论是CBOW还是SG，对每个训练样本(或者Mini Batch)从梯度更新中需要对<span class="math inline">\(W^{&#39;}\)</span>的所有<span class="math inline">\(V \times N\)</span>个元素，也就是词汇表中所有<span class="math inline">\(V\)</span>个单词都需要更新词向量，考虑现实任务词汇表一般是几十万，上百万千万级别的， 这个计算成本是巨大的。</p>
<p>关于计算成本大的原因，除了上面提到的训练部分，还有就是在每次前向计算的时候，隐层到输出层的softmax函数计算输出层<span class="math inline">\(V\)</span>个元素，计算量也是很大，这样整个模型现实意义不大。</p>
<p>考虑到计算量大的部分都是在隐层到输出层上，尤其是<span class="math inline">\(W^{&#39;}\)</span>的更新。因此word2vec使用了两种优化策略: Hierarchical Softmax 和 Negative Sampling。二者的出发点一致，就是在每个训练样本中，<strong><em>不再完全计算或者更新</em></strong><span class="math inline">\(W^{&#39;}\)</span>这个矩阵。二者都不再显示使用<span class="math inline">\(W^{&#39;}\)</span>这个矩阵。因此这也就解释了前面说的为什么不用<span class="math inline">\(W^{&#39;}\)</span>作为最终词向量。</p>
<p>在多一句，其实上述训练和推理的复杂度很大的根本原因是<code>softmax</code>的分母上的<span class="math inline">\(\sum\)</span>，因此在求梯度的时候，就会有<span class="math inline">\(V\)</span>次的计算。因此下面的<strong>两种方法其实是对softmax的优化，不仅仅限制在word2vec</strong>.</p>
<p>两种优化方式使得word2vec的训练速度大大提升，并且词向量的质量几乎没有下降，这也是word2vec在NLP领域如此流行的原因。 下面依次介绍这两种优化算法。</p>
<h3 id="hierarchical-softmax">Hierarchical SoftMax</h3>
<p>首先Hierarchical SoftMax(HS)并不是word2vec提出来的, 而是之前Bengio在2005年最早提出来专门为了加速计算神经语言模型中的softmax的一种方式, 这里介绍如何在word2vec中使用. HS主要基于哈夫曼树(一种二叉数)将计算量大的部分变为了一种二分类的问题. 先看下面的图, 原来的模型在隐层之后通过<span class="math inline">\(W^{&#39;}\)</span>连接输出层, 现在HS则去掉了<span class="math inline">\(W^{&#39;}\)</span>, 隐层<span class="math inline">\(\mathbf{h}\)</span>直接与下面的二叉树的root节点相连:</p>
<div class="figure">
<img src="http://cdn.htliu.cn/blog/w2v/hs-1.png">

</div>
<p>其中图中白色的叶子节点表示词汇表中所有的<span class="math inline">\(|V|\)</span>个词, 黑色节点表示非叶子节点, 每一个叶子节点也就是每一个单词, 都对应唯一的一条从root节点出发的路径. 而我们的目的是使的<span class="math inline">\(w=w_O\)</span>这条路径的概率最大，即: <span class="math inline">\(P(w=w_O | w_I)\)</span> 最大, 此时每一个分支都代表一个选择, 向左转还是向右转. 所以如何判断向左还是向右呢? 我们用<span class="math inline">\(n(w,j)\)</span>表示从root到叶子节点<span class="math inline">\(w\)</span>的路径上的第<span class="math inline">\(j\)</span>个非叶子节点, 并且每个非叶子节点都对应一个向量<span class="math inline">\(v^{&#39;}_{n(w, j)}\)</span>, 维度与<span class="math inline">\(\mathbf{h}\)</span>相同, 然后使用一个sigmod函数: <span class="math inline">\(\sigma(x)= \frac{1}{1+\exp(-x)} \in [0, 1]\)</span>, 结合向量的内积, 来判断该向左还是向右, 如下, 第n个节点向左 以及向右的概率定义:</p>
<p><span class="math display">\[\begin{align*}P(n, left ) &amp;= \sigma({v^{&#39;}_{w}} \cdot \mathbf{h}) \\ p(n, right) &amp;=1-\sigma(v^{&#39;}_w \cdot \mathbf{h}) = \sigma(-v_{w}^{&#39;} \cdot h) \end{align*}\]</span></p>
<p>有了上述的概率, 我们可以重新定义<span class="math inline">\(P(w_O|w_i)\)</span>了:</p>
<p><span class="math display">\[P(w=w_O|w_I)= \Pi_{j=1}^{L(w)-1}P(\sigma(I(n(w,j+1==left)  v^{&#39;}_{w} \cdot \mathbf{h}  ))\]</span></p>
<p>其中<span class="math inline">\(I()\)</span>是指示函数, 条件成立值为1, 反之为-1. 而<span class="math inline">\(L(w)\)</span>表示整条路径的长度, 这样整个概率就是从root节点到叶子节点这条路径的概率, 这样我们在训练的时候, 通过训练样本来更新非叶子节点的参数<span class="math inline">\(v{&#39;}_{w}\)</span>.</p>
<p>举个例子, 比如上图中的加粗的黑色路径: <span class="math inline">\((n(w_2,1), n(w_2, 2), n(w_2, 3), w_2\)</span> , 就是说假设有一个训练样本是<span class="math inline">\((w_I, w_2)\)</span>, 我们需要使得<span class="math inline">\(P(w_O=w_2|w_I)\)</span>概率最大:</p>
<p><span class="math display">\[\begin{align*}P(w_2=w_O) &amp;= P(n(w_2,1), left)  \cdot P(n(w_2,2), left) \cdot P(n(w_2, 3), right) \\ &amp;= \sigma({v^{&#39;}_{n(w_2,1)}}^T \mathbf{h}) \cdot \sigma({v^{&#39;}_{n(w_2,2)}}^T \mathbf{h}) \cdot \sigma(-{v^{&#39;}_{n(w_2,3)}}^T \mathbf{h}) \end{align*}\]</span></p>
<p>并且在一个非叶子节点处, 向左向右的概率和为1, 因此一直分裂下去,最后的和肯定还是1. 因此可以很容易得到:</p>
<p><span class="math display">\[\sum\limits_{j=1}^{V}P(w_j=w_O) =1 \]</span></p>
<p>这一点的证明是有必要的, 因为在原始的softmax本身保证了所有单词的概率和是1, 而通过上式也知道了通过HS得到的输出层仍然是一个概率多项分布, 输出所有的单词概率和为1.</p>
<p>讲完了从前向后的如何得到输出单词的概率的过程, 下面开始后向传播的训练过程. 首先需要明确的是训练的参数: 输入层与隐层的词向量矩阵<span class="math inline">\(W\)</span>, 以及二叉树的非叶子节点对应的向量<span class="math inline">\(\{v{&#39;}_{n(w,j)}, j =1,2,3,..,L(w)-1\}\)</span>.</p>
<p>为了书写方便,下面简化一部分的符号: 用<span class="math inline">\([I]\)</span>表示前面的指示函数<span class="math inline">\(I(n(w,j+1)==left)\)</span>, 使用<span class="math inline">\(v^{&#39;}_{j}\)</span>表示<span class="math inline">\(v^{&#39;}_{n(w,j)}\)</span></p>
<p>对于一组训练数据, 损失函数的定义与前面相同, 最大似然(注意这里以One-Word Model为例，CBOW与Skip-Gram按照上述的思路完全一样):</p>
<p><span class="math display">\[E = -\log P(w=w_O|w_I) = - \sum\limits_{j=1}^{L(w)-1}\log \sigma([I] {v^{&#39;}_{j}}^{T} \mathbf{h})\]</span></p>
<p>之后便可以逐项求梯度了, 先考虑<span class="math inline">\(v^T \mathbf{h}\)</span>, 注意<span class="math inline">\(\frac{\partial \sigma(x)}{x} = \sigma(1-\sigma)\)</span>: <span class="math display">\[\begin{align*} \frac{\partial E}{\partial v^{&#39;}_j \mathbf{h}} &amp;= (\sigma([I] {v_{j}^{&#39;}}^T \mathbf{h}))[I] \end{align*}\]</span></p>
<p>之后对<span class="math inline">\([I]\)</span> 分情况讨论, 可得: <span class="math inline">\(\frac{\partial E}{\partial {v^{&#39;}_{j}}^T \mathbf{h}} = \sigma( {v^{&#39;}_{j}}^T \mathbf{h}) - t_j\)</span> 这里如果[I]=1, 那么$t_j =1 $, 否则 <span class="math inline">\(t_j=0\)</span> , 这个公式与前面的<span class="math inline">\(y_j-t_j\)</span>很类似, 可以理解为预测值与实际值的差别。</p>
<p>有了上述的梯度,就可以很简单的求出<span class="math inline">\(v^{&#39;}\)</span>的梯度了:</p>
<p><span class="math display">\[\frac{\partial E}{\partial v^{&#39;}_j} = \frac{\partial E}{\partial  {v^{&#39;}_{j}}^T \mathbf{h}} \frac{\partial  {v^{&#39;}_{j}}^T \mathbf{h}}{\partial   {v^{&#39;}_{j}}^T} = (\sigma( {v^{&#39;}_{j}}^T \mathbf{h}) - t_j) \mathbf{h}\]</span></p>
<p>有了梯度,便可以更新了, 具体公式还是梯度下降:</p>
<p><span class="math display">\[{v^{&#39;}_{j}}^{(new)} = {v^{&#39;}_{j}}^{(old)} - \eta (\sigma( {v^{&#39;}_{j}}^T \mathbf{h}) - t_j) \mathbf{h},\ j=1,2,3,...,L(w)-1\]</span></p>
<p>也就是说对于一个训练样本， 我们只需要更新<span class="math inline">\(L(w)-1\)</span>个向量就好了， 而未优化的版本需要更新<span class="math inline">\(V\)</span>个， 相当于时间复杂度从<span class="math inline">\(O(V)\)</span>降到了<span class="math inline">\(O(\log V)\)</span> , 这个提升还是非常大的， 同时在考察空间复杂度，HS的二叉树的非叶子节点有<span class="math inline">\(V-1\)</span>个，也就是我们需要<span class="math inline">\(V-1\)</span>存储<span class="math inline">\(v^{&#39;}_{j},\ \ j=1,2,3..V-1\)</span> ,优化之前则是<span class="math inline">\(V\)</span>个， 空间复杂度相同， 但是时间复杂度却大大降低。</p>
<p>然后考虑隐层<span class="math inline">\(\mathbf{h}\)</span>的梯度，因为我们的优化目标都是在隐层到输出层，因此前面的几乎不变， 跟One-Word Model 一样，路径上的非叶子节点的表达式都含有<span class="math inline">\(\mathbf{h}\)</span>，因此需要对梯度求和：</p>
<p><span class="math display">\[\begin{align*} \frac{\partial E}{\partial \mathbf{h}} &amp;= \sum\limits_{j=1}^{L(w)-1} \frac{\partial E}{\partial {v^{&#39;}_{j}}^T  \mathbf{h}} \frac{\partial {v^{&#39;}_{j}}^T  \mathbf{h}}{\partial \mathbf{h}} \\ &amp;=\sum\limits_{j=1}^{L(w)-1}(\sigma( {v^{&#39;}_{j}}^T \mathbf{h}) - t_j)\cdot v^{&#39;}_{j}\end{align*}\]</span></p>
<p>其实跟前面很一样了， 只需要替代下导数就好了， 后面就不再赘述了。</p>
<p>整个Hierarchical Softmax的优化算法介绍完了，隐层到输出层的计算量从<span class="math inline">\(O(V)\)</span>, 利用二叉树(更加具体来说是哈夫曼树）降为了<span class="math inline">\(O(\log V)\)</span>.</p>
<h3 id="negative-sampling">Negative Sampling</h3>
<p>相比Hierarchical Softmax用复杂的树形结构来对softmax进行优化， 负采样(Negative Sampling)更加直接简单。因为<code>softmax</code>的存在，在使用梯度下降的时候，每个训练样本需要更新<span class="math inline">\(V\)</span>个向量，这是根本问题。因此负采样NS仅仅更新一小部分向量，一般是5-20个，可以手工设定，而非全部<span class="math inline">\(V\)</span>个(一般来说V都是几万到几百万的量级)。</p>
<p>再来考虑原始模型的训练过程，对于一个训练样本<span class="math inline">\((w_I, w_O)\)</span>, 我们要使得<span class="math inline">\(P(w=w_O|w_I)\)</span>最大， 也就是说要使得输出层的其它单词的概率要比较小一些，基于这种思想， 我们将输出层的<span class="math inline">\(V\)</span>个样本分为正例(Positive Sample)也就是<span class="math inline">\(w_O\)</span>对应的项， 以及剩余<span class="math inline">\(V-1\)</span>个负例(Negative Samples)。举个例子有个样本<code>phone number</code>，这样<span class="math inline">\(w_I=phone, w_O=number\)</span>, 正例就是number这个词， 负例就是不太可能与phone共同出现的词。</p>
<p>负采样的思想便是每次训练只随机取一小部分的负例使他们的概率最小，以及对应的正例概率最大。那么如何对负例进行抽样呢？ 这里就需要定义一个noise distribution，有了分布<span class="math inline">\(P_n(w)\)</span>就可以依据概率分布进行带权随机抽样了。 在word2vec中，作者直接使基于词的频次的词的权重分布:</p>
<p><span class="math display">\[weight(w) = \frac{coun(w)^{0.75}}{\sum _{u}count(w)^{0.75}}\]</span></p>
<p>相比于直接使用频次作为权重， 取0.75幂的好处可以减弱不同频次差异过大带来的影响，使得小频次的单词被采样的概率变大。</p>
<p>下面基于上面的思想，直接给出具体的损失函数:</p>
<p><span class="math display">\[E = -\log \sigma(v^{&#39;}_{w_O}\mathbf{h}) - \sum\limits_{w_j \in \mathcal{W}_{neg}}\log \sigma(-v^{&#39;}_{w_j}\mathbf{h})\]</span></p>
<p><span class="math inline">\(\mathcal{W}_{neg}\)</span>是负例单词集合。 上述公式跟原本的负采样的有些许差别，具体的推导细节可以参考这篇Goldberg, Y. and Levy, O. (2014). word2vec explained: deriving mikolov et al.’s negative- sampling word-embedding method. 这里不再赘述了。 只需要记住，NS也是对softmax函数做的优化，除了word2vec，在其他地方涉及到softmax的均可以采用类似的思想来重写目标函数。</p>
<p>有了损失函数，然后跟HS一样，用梯度下降的方法更新<span class="math inline">\(v^{&#39;}\)</span>即可，推导思路跟HS也是一样，分<span class="math inline">\(w_j\)</span>是否等于<span class="math inline">\(w_O\)</span>来讨论，最终得到的梯度公式与HS一模一样：</p>
<p><span class="math display">\[\frac{\partial E}{\partial {v^{&#39;}_{j}}^T \mathbf{h}} = \sigma( {v^{&#39;}_{j}}^T \mathbf{h}) - t_j\]</span></p>
<p>这里的<span class="math inline">\(t_j\)</span>含义跟前面提到的几次都相同。最后给出更新<span class="math inline">\(v^{&#39;}\)</span>的公式:</p>
<p><span class="math display">\[{v^{&#39;}_{j}}^{(new)} = {v^{&#39;}_{j}}^{(old)} - \eta (\sigma( {v^{&#39;}_{j}}^T \mathbf{h}) - t_j) \mathbf{h},\ w_j \in  \{w_O \} + \mathcal{W}_{neg}\]</span></p>
<p>从公式也可以看出，对于每个训练数据，每次只需要更新很少的几个向量，而非原始的<span class="math inline">\(V\)</span>个，大大降低了训练复杂度。</p>
<p>跟HS相同，NS的优化也是对隐层到输出层的优化，因此前面输入层到隐层的梯度只需要做相应修改即可，没有影响，具体可以参考HS的公式，这里不再重复了。</p>
<p>到此为止，两种针对softmax的优化方式介绍完毕，简单对比一下两种方式:</p>
<ul>
<li>HS实现比较复杂， 需要借助哈夫曼树，而且HS是专门优化softmax的方法</li>
<li>NS相对简单，仅仅是简单采样，NS则是一种概率采样的方法，属于NCE(Noise Contrastive Estimation)的一种简单形式，代码实现也很简单。 训练得到的词向量质量也很高，相对常用一些。</li>
</ul>
<h2 id="后话">后话</h2>
<p>这篇博客侧重word2vec的数学原理，主要是以神经网络的反向传播思路来解释训练过程，以及介绍了优化的原因和思路，基本上相当于一份对w2v相对全面解释的全家桶了，除了具体的代码实现细节，关于代码实现的话，以后看有没有时间整理吧。google官方的c实现的word2vec的代码还是非常非常值得一读的，只有600多行，里面用到了很多技巧，包括快速<span class="math inline">\(\sigma\)</span>计算， 还有负采样的实现等等，使用异步梯度下降多线程训练(ASGD)等。</p>
<blockquote>
<p>在4月份花了几天时间看完了google的两篇原始paper以及word2vec Parameter Learning Explained这篇以后，就想结合论文按照自己的理解整理一下， 不过当时仅仅列出了目录，写了个开头背景，之后因为课程，读NE的paper等各种事情，一直拖着到现在了。 这段时间趁出差期间反正没事，现在算是就把这个坑补上了。 有什么写的不严谨的地方，烦请指出。</p>
</blockquote>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;目前需要做Network Embedding方面的课题，而复杂网络本身就经常借鉴NLP的一些算法模型, Embedding也不例外. 因此先从Word Embedding入手。之前对Word Embedding(暂且翻译为词嵌入或者词向量)的理解就是将单词根据某种特征转为数值向量，再来做其他工作比如文本分类的工作。而word2vec则是word embedding的一种模型，也是目前使用最广的词向量模型, 由Google的Mikolov团队2013年提出。之前仅仅能够使用第三方库来训练直接使用, 对其中原理并没有多少理解, 这篇博客则比较完整的从背景知识到原理，参数训练等方面整理一下word2Vec。&lt;/p&gt;
    
    </summary>
    
      <category term="读书笔记" scheme="http://shomy.top/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="machine learning" scheme="http://shomy.top/tags/machine-learning/"/>
    
      <category term="word2vec" scheme="http://shomy.top/tags/word2vec/"/>
    
  </entry>
  
  <entry>
    <title>Alias Method离散分布随机取样</title>
    <link href="http://shomy.top/2017/05/09/alias-method-sampling/"/>
    <id>http://shomy.top/2017/05/09/alias-method-sampling/</id>
    <published>2017-05-08T16:04:32.000Z</published>
    <updated>2019-04-09T14:06:31.785Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>在图的随机游走中，有一块需要随机取样， 比如当前到达v节点，那么下一次随机会到达哪个节点。这种问题其实就是离散分布的随机变量的取样问题。 查了一些资料， 发现<a href="https://en.wikipedia.org/wiki/Alias_method" target="_blank" rel="external">Alias Method</a> 是一种很高效的方式。<a id="more"></a>在初始好之后，每次取样的复杂度为<span class="math inline">\(O(1)\)</span>。 下面介绍下这种方法的使用，具体原理暂时没有深究，有兴趣转<a href="http://www.keithschwarz.com/darts-dice-coins/" target="_blank" rel="external">Darts,Dice, and coids</a>。</p>
</blockquote>
<h2 id="alias-method-原理">Alias-Method 原理</h2>
<p>大部分资料都会以这个例子讲解：游戏中经常遇到按一定的掉落概率来随机掉落指定物品的情况，例如掉落 银币25%，金币20%, 钻石10%，装备5%，饰品40%。现在要求下一个掉落的物品类型，或者说一个掉落的随机序列，要求符合上述概率。</p>
<p>其实方法很简单，最直接的方法就是直接取样，先构建一个<strong>累加</strong>概率分布列表:<code>[0.25,0.45,0.55, 0.60, 1.0]</code>，之后产生一个随机数(0-1)，假设为0.7，那么在列表中找到第一个大于0.7的数为1.0,对应的类别是第5类。 这种方法很简单直接，但是每次取样复杂度为<span class="math inline">\(O(K)​\)</span>，使用二分搜索之后，降为<span class="math inline">\(O(logK)​\)</span>。</p>
<p>论文中经常使用的是另一种很巧妙的方法: Alias Method，它在初始化之后每次随机取样的复杂度为<span class="math inline">\(O(1)\)</span>。 下面以<a href="http://www.keithschwarz.com/darts-dice-coins/" target="_blank" rel="external">Darts,Dice, and coids</a>文中例子用图示说明整个步骤，原理太繁杂，不做介绍，参考博客即可:</p>
<p>假设概率分布为: <span class="math inline">\(\frac{1}{2}, \frac{1}{3}, \frac{1}{12}. \frac{1}{12}\)</span>。</p>
<ol style="list-style-type: decimal">
<li><p>初始概率分布: 类别数目<span class="math inline">\(K=4\)</span>，以颜色表示不同的类别<img src="http://cdn.htliu.cn/alias-method-1.png"></p></li>
<li><p>每个类别概率乘以K=4,使得总和为4. 这样分为两类，大于1:第一列与第二列; 小于1: 第三列与第四列。<img src="http://cdn.htliu.cn/alias-method-2.png"></p></li>
<li>下面通过拼凑，使得每一列的和都为1，但是每一列中，最多只能是两种类型的拼凑，就是<strong>每一列最多两种颜色存在</strong>。
<ul>
<li>将第一列拿出<span class="math inline">\(\frac{2}{3}\)</span>给最后一列，使其变为1，如下:(棕色表示空缺)<img src="http://cdn.htliu.cn/alias-method-3.png"></li>
<li>将第一列拿出<span class="math inline">\(\frac{2}{3}\)</span>给第三列，使之变为1，如下:<img src="http://cdn.htliu.cn/alias-method-4.png"></li>
<li>最后一次,第二列给第一列<span class="math inline">\(\frac{1}{3}\)</span>, 最后每一列都是1，且每一列最多两种类型，其中下面一层表示原类的概率，上面层表示另外一种类型的概率，如只有一种比如第二列，那么第二层就是NULL:<img src="http://cdn.htliu.cn/alias-method-5.png"></li>
</ul></li>
<li><p>写出两个数组:</p></li>
</ol>
<ul>
<li><strong>Probability table</strong> <span class="math inline">\(Prob\)</span>: 落在原类型的概率，每一列第一层的概率，即:<span class="math inline">\([\frac{2}{3}, 1, \frac{1}{3}, \frac{1}{3}]\)</span></li>
<li><strong>Alias table</strong> <span class="math inline">\(Alias\)</span> : 每一列第二层的类型(颜色)，这里用下标表示: <span class="math inline">\([2, null, 1, ,1]\)</span></li>
</ul>
<p>用图表示如下:<img src="http://cdn.htliu.cn/alias-method-6.png"></p>
<p>到此为止，得到<span class="math inline">\(Prob, Alias\)</span> 表示初始化完成。</p>
<ol start="5" style="list-style-type: decimal">
<li>采样过程: 随机取某一列k(即[1,4]的随机整数)，再随机产生一个[0-1]的小数c，如果<span class="math inline">\(Prob[k]\)</span> 大于 c，那么采样结果就是k，反之则为<span class="math inline">\(Alias[k]\)</span>。</li>
</ol>
<p>需要说明的是，该过程一定可以结束(原文有证明)。此外在初始化完成之后每次采样的复杂度为<span class="math inline">\(O(1)\)</span>，因此应用很广。</p>
<p>举个例子说明采样过程，比如随机取得第1列， 随机产生的小数为<span class="math inline">\(0.5&lt; \frac{2}{3}\)</span>，那么采样的结果就是第一类。 如果随机产生的小数为<span class="math inline">\(0.8 &gt; \frac{2}{3}\)</span>，那么采样结果就是第一列的第二层的类别，也就是<span class="math inline">\(Alias[1] = 2\)</span>(紫色对应的类别: 第二列)。</p>
<p>再简单验证下，看看该方法的采样是不是满足原始的概率分布<span class="math inline">\(\frac{1}{2}, \frac{1}{3}, \frac{1}{12}. \frac{1}{12}\)</span></p>
<p>对于采到第一种的概率，采到第一种有三部分组成，第一列，第三，四列分别求概率求和: <span class="math inline">\(\frac{2}{3} * \frac{1}{4} + (1-\frac{1}{3}) * \frac{1}{4} + ({1-\frac{1}{3}}) * \frac{1}{4} = \frac{1}{2}\)</span> ，因此满足原始概率的分布，其余同理。</p>
<h2 id="代码">代码</h2>
<p>代码官网给了Java版本，不过网上有很多其他语言的版本，下面参考<a href="https://hips.seas.harvard.edu/blog/2013/03/03/the-alias-method-efficient-sampling-with-many-discrete-outcomes/" target="_blank" rel="external">1</a> 做了注释如下:</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div><div class="line">40</div><div class="line">41</div><div class="line">42</div><div class="line">43</div><div class="line">44</div><div class="line">45</div><div class="line">46</div><div class="line">47</div><div class="line">48</div><div class="line">49</div><div class="line">50</div><div class="line">51</div><div class="line">52</div><div class="line">53</div><div class="line">54</div><div class="line">55</div><div class="line">56</div><div class="line">57</div><div class="line">58</div><div class="line">59</div><div class="line">60</div><div class="line">61</div><div class="line">62</div><div class="line">63</div><div class="line">64</div><div class="line">65</div><div class="line">66</div><div class="line">67</div><div class="line">68</div><div class="line">69</div><div class="line">70</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">import</span> numpy        <span class="keyword">as</span> np</div><div class="line"><span class="keyword">import</span> numpy.random <span class="keyword">as</span> npr</div><div class="line"> </div><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">alias_setup</span><span class="params">(probs)</span>:</span></div><div class="line">    <span class="string">'''</span></div><div class="line"><span class="string">    probs： 某个概率分布</span></div><div class="line"><span class="string">    返回: Alias数组与Prob数组</span></div><div class="line"><span class="string">    '''</span></div><div class="line">    K       = len(probs)</div><div class="line">    q       = np.zeros(K) <span class="comment"># 对应Prob数组</span></div><div class="line">    J       = np.zeros(K, dtype=np.int) <span class="comment"># 对应Alias数组</span></div><div class="line">    <span class="comment"># Sort the data into the outcomes with probabilities</span></div><div class="line">    <span class="comment"># that are larger and smaller than 1/K.</span></div><div class="line">    smaller = [] <span class="comment"># 存储比1小的列</span></div><div class="line">    larger  = [] <span class="comment"># 存储比1大的列</span></div><div class="line">    <span class="keyword">for</span> kk, prob <span class="keyword">in</span> enumerate(probs):</div><div class="line">        q[kk] = K*prob <span class="comment"># 概率</span></div><div class="line">        <span class="keyword">if</span> q[kk] &lt; <span class="number">1.0</span>:</div><div class="line">            smaller.append(kk)</div><div class="line">        <span class="keyword">else</span>:</div><div class="line">            larger.append(kk)</div><div class="line"> </div><div class="line">    <span class="comment"># Loop though and create little binary mixtures that</span></div><div class="line">    <span class="comment"># appropriately allocate the larger outcomes over the</span></div><div class="line">    <span class="comment"># overall uniform mixture.</span></div><div class="line">    </div><div class="line">    <span class="comment"># 通过拼凑，将各个类别都凑为1</span></div><div class="line">    <span class="keyword">while</span> len(smaller) &gt; <span class="number">0</span> <span class="keyword">and</span> len(larger) &gt; <span class="number">0</span>:</div><div class="line">        small = smaller.pop()</div><div class="line">        large = larger.pop()</div><div class="line"> </div><div class="line">        J[small] = large <span class="comment"># 填充Alias数组</span></div><div class="line">        q[large] = q[large] - (<span class="number">1.0</span> - q[small]) <span class="comment"># 将大的分到小的上</span></div><div class="line"> </div><div class="line">        <span class="keyword">if</span> q[large] &lt; <span class="number">1.0</span>:</div><div class="line">            smaller.append(large)</div><div class="line">        <span class="keyword">else</span>:</div><div class="line">            larger.append(large)</div><div class="line"> </div><div class="line">    <span class="keyword">return</span> J, q</div><div class="line"> </div><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">alias_draw</span><span class="params">(J, q)</span>:</span></div><div class="line">    <span class="string">'''</span></div><div class="line"><span class="string">    输入: Prob数组和Alias数组</span></div><div class="line"><span class="string">    输出: 一次采样结果</span></div><div class="line"><span class="string">    '''</span></div><div class="line">    K  = len(J)</div><div class="line">    <span class="comment"># Draw from the overall uniform mixture.</span></div><div class="line">    kk = int(np.floor(npr.rand()*K)) <span class="comment"># 随机取一列</span></div><div class="line"> </div><div class="line">    <span class="comment"># Draw from the binary mixture, either keeping the</span></div><div class="line">    <span class="comment"># small one, or choosing the associated larger one.</span></div><div class="line">    <span class="keyword">if</span> npr.rand() &lt; q[kk]: <span class="comment"># 比较</span></div><div class="line">        <span class="keyword">return</span> kk</div><div class="line">    <span class="keyword">else</span>:</div><div class="line">        <span class="keyword">return</span> J[kk]</div><div class="line"> </div><div class="line">K = <span class="number">5</span></div><div class="line">N = <span class="number">100</span></div><div class="line"> </div><div class="line"><span class="comment"># Get a random probability vector.</span></div><div class="line">probs = npr.dirichlet(np.ones(K), <span class="number">1</span>).ravel()</div><div class="line"> </div><div class="line"><span class="comment"># Construct the table.</span></div><div class="line">J, q = alias_setup(probs)</div><div class="line"> </div><div class="line"><span class="comment"># Generate variates.</span></div><div class="line">X = np.zeros(N)</div><div class="line"><span class="keyword">for</span> nn <span class="keyword">in</span> xrange(N):</div><div class="line">    X[nn] = alias_draw(J, q)</div></pre></td></tr></table></figure>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;在图的随机游走中，有一块需要随机取样， 比如当前到达v节点，那么下一次随机会到达哪个节点。这种问题其实就是离散分布的随机变量的取样问题。 查了一些资料， 发现&lt;a href=&quot;https://en.wikipedia.org/wiki/Alias_method&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;Alias Method&lt;/a&gt; 是一种很高效的方式。
    
    </summary>
    
      <category term="读书笔记" scheme="http://shomy.top/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="alias method" scheme="http://shomy.top/tags/alias-method/"/>
    
  </entry>
  
</feed>
